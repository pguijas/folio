/** Constants module. */

/** The application version. */
export const VERSION = "1.0.0";

/** Maximum retry count. */
export let MAX_RETRIES = 3;

/** Default name. */
export var DEFAULT_NAME = "world";
