// reader-data.js - pre-rendered bodies for the artifacts that can open on
// the canvas. An artifact whose target has no entry here renders as the
// path it is, visibly not a link. Markdown ships as rendered HTML because
// these prototypes open over file:// where fetch() is blocked; html
// entries carry a relative src for an iframe (resolves in the repo and on
// the served site, where both card bundles sit under /_folio/kanban/).
window.READER = {
  "board/cards/the-board-reads-as-a-tree/prototypes-compared.md": {
    "type": "markdown",
    "title": "Five layouts compared",
    "html": "<h1>Decomposition views for the board — five prototypes compared</h1>\n<p>Built 2026-08-20 to answer one question: the cards carry a <code>parent</code> field that\nnothing renders, so what should render it. Five layouts were built in full,\nagainst the real 35-card board, and compared side by side rather than argued on\npaper. Each was then verified adversarially in a headless browser.</p>\n<p>The prototypes are here, beside this document and beside the card both belong\nto. None of it is product code and none of it will become product code; it is\nthe evidence a decision was made on, kept where the decision is recorded. Open\nany <code>.html</code> in this directory to see the layout it argues for.</p>\n<h2>The question</h2>\n<p><code>parent</code> has been in the cardfile format since it shipped: one card id,\nvalidated against the board, settable from the CLI, already a filter field. The\nguide states the gap plainly — \"<code>parent</code> is a validated pointer, not a\nworkflow\". On a board of 35 cards, none used it. The pointer existed and the\nshape it pointed at was invisible, so nobody reached for it.</p>\n<p>Because no real hierarchy existed, the prototypes ran on an invented one,\nchosen to force the cases that break naive implementations: three levels deep,\nand one child sitting in a different column than its parent.</p>\n<h2>The five</h2>\n<p><strong>Tree table.</strong> One table for the whole board, the first column carrying the\ntree by indentation, every other column a field read straight down. The literal\nreading of the request and the baseline for the rest.</p>\n<p><strong>Epic swimlanes.</strong> The kanban columns kept; rows become lanes, one per epic,\nwith children placed in the column their own status names. Decomposition and\nstatus as two axes of one grid.</p>\n<p><strong>Board with inline expansion.</strong> The incumbent column board, minimally changed:\na card with children grows a disclosure and lists them inside itself.</p>\n<p><strong>Tree rail and detail.</strong> A persistent outline rail on the left, the selected\nnode's children as a table on the right. Master-detail, the shape a product\ntool with deep decomposition converges on.</p>\n<p><strong>Document outline.</strong> The board as one nested document: no rules, no cells,\nindentation and typography carrying depth. The argument is that these cards are\nmarkdown files and a plan should read like a plan.</p>\n<h2>What was chosen</h2>\n<p><strong>Tree table</strong>, with the column board kept untouched beside it.</p>\n<p>Columns are the right reading for what is in flight and the wrong reading for\nwhat something breaks into. The two readings want different shapes, and the\nboard already works — so the answer is a second view over the same files, not a\nreplacement.</p>\n<p>Taken from the losing variants: the <code>DIVERGED</code> marker and its toolbar count\nfrom the tree table's own answer to the cross-column child, and the subtree\nrollup header from tree rail and detail.</p>\n<p>Rejected, with the reason worth keeping:</p>\n<ul>\n<li><strong>Swimlanes</strong> resolve the cross-column child for free, which is the strongest\n  single argument any variant made. They also encode exactly one level of\n  decomposition — a grandchild cannot get a lane without shredding the grid —\n  and they are mostly air: six lanes by four columns is twenty-four cells, of\n  which this board fills nine, measuring 4,457px tall at 1440px.</li>\n<li><strong>Inline expansion</strong> costs nothing to learn, and a deep tree stretches a\n  column without end.</li>\n<li><strong>Tree rail and detail</strong> scales furthest and shows the least at once.</li>\n<li><strong>Document outline</strong> reads best and scans worst. Comparing one field across\n  35 rows is a saccade per line where a table is one glance down a column.</li>\n</ul>\n<h2>What five independent implementations all got wrong</h2>\n<p>This is the part that justifies building five instead of one. A defect that\nevery implementation reaches independently is a property of the problem, not of\nthe implementation, and the real one will reach it too.</p>\n<h3>\"Done\" has no meaning on this board</h3>\n<p>All five defined a parent's progress as how many descendants reached the last\ncolumn. <code>Released</code> is empty, and <code>In progress</code> is empty. Every parent therefore\nprinted <code>0 of N</code> while its distribution bar rendered full — truthful, and it\nreads as a contradiction.</p>\n<p>Three separate verification passes reached the same conclusion independently:\nthis is not an execution defect and cannot be fixed by adjusting the widget.\nRedefining done to include <code>In review</code> would be a lie about the board.</p>\n<p><strong>The open question, which is a product decision:</strong> what counts as done for a\nparent — the last column, the ratio of checked acceptance criteria across the\nsubtree, or nothing at all. Until it is answered, any rollup shipped will read\nas a constant zero on this board.</p>\n<h3>Collapsing or filtering throws the focus out of the tree</h3>\n<p>Three of the five lost keyboard focus when the tree re-rendered, each for a\ndifferent reason: reading <code>document.activeElement</code> after <code>innerHTML</code> had\nalready been replaced; leaving the only <code>tabindex=\"0\"</code> on a node that\ncollapsing had set to <code>display: none</code>; and dropping the roving tab stop when a\nfilter removed the active row.</p>\n<p>Three independent implementations hitting the same class of bug means a\nre-rendering tree must decide explicitly where focus lands when the focused row\nstops existing. The rule that worked: move to the nearest still-rendered\nancestor, and fall back to the first row.</p>\n<h3>A roving tab stop leaks</h3>\n<p>One implementation promoted rows to <code>tabindex=\"0\"</code> without demoting the\nprevious holder; after four arrow presses a single column had five tab stops\nwhile its own comment claimed one.</p>\n<h3>Filter highlighting falls back to the browser default</h3>\n<p>Two variants styled <code>&lt;mark&gt;</code> in some contexts and not others, so matching text\nin an unstyled context painted <code>#ff0</code> into an oklch palette, most visible in\ndark mode.</p>\n<h3>A match can hide inside a collapsed branch</h3>\n<p>One filter marked ancestors as context only when the ancestor was not itself a\nmatch, so a match nested under another match never rendered while the toolbar\ncounted it. A filtered tree must open every ancestor of every match, and must\nrestore the hand-set collapse state when the filter clears.</p>\n<h2>Cycles are not validated</h2>\n<p>Found while designing, not while prototyping. <code>_validate_relations</code> checks that\n<code>parent</code> names an existing card and is not the card itself. It does not check\nthat following the chain terminates, so <code>a → b → a</code> passes the build today.</p>\n<p>This is harmless only because nothing walks the chain. Every prototype had to\ncarry its own visited set, and each was tested by injecting a two-cycle, a\nthree-cycle, a self-parent and a dangling parent. The fix belongs in the\nloader, before anything renders a tree.</p>\n<h2>A note on the NUL byte</h2>\n<p><code>template/components/kanban-board.tsx</code> wrote a cache-key separator as a\nliteral NUL byte inside a template string. It worked, and it made the file\nbinary to <code>grep</code> and <code>diff</code>.</p>\n<p>The brief warned the prototype agents about it. One of them, while fixing an\nunrelated markdown bug, wrote four raw NUL bytes into its own source — and\ncaught it. The failure mode is easy to reach by accident, which is the argument\nfor fixing the original rather than documenting it. The branch now writes an\nescaped separator, so the component is ordinary UTF-8 text again.</p>\n<h2>Where this went</h2>\n<ul>\n<li><code>the-board-reads-as-a-tree</code> — the epic, and the choice recorded above</li>\n<li><code>a-parent-cycle-fails-the-build</code> — the loader gap</li>\n<li><code>the-board-component-splits-into-modules</code> — the 4312-line file, and the NUL byte</li>\n<li><code>the-table-is-a-second-view</code> — the view switch and the URL</li>\n<li><code>the-table-draws-one-row-per-card</code> — the row grammar</li>\n<li><code>the-tree-filters-without-re-rooting</code> — filter semantics</li>\n<li><code>a-parent-says-what-it-breaks-into</code> — the rollup and the divergence marker</li>\n<li><code>the-cli-prints-the-tree</code> — the agent-facing surface</li>\n</ul>"
  },
  "board/cards/the-board-reads-as-a-tree/tree-table.html": {
    "type": "html",
    "title": "Tree table (chosen)",
    "src": "../the-board-reads-as-a-tree/tree-table.html"
  },
  "board/cards/the-board-reads-as-a-tree/epic-swimlanes.html": {
    "type": "html",
    "title": "Epic swimlanes (rejected)",
    "src": "../the-board-reads-as-a-tree/epic-swimlanes.html"
  },
  "board/cards/the-board-reads-as-a-tree/board-inline-expansion.html": {
    "type": "html",
    "title": "Inline expansion (rejected)",
    "src": "../the-board-reads-as-a-tree/board-inline-expansion.html"
  },
  "board/cards/the-board-reads-as-a-tree/document-outline.html": {
    "type": "html",
    "title": "Document outline (rejected)",
    "src": "../the-board-reads-as-a-tree/document-outline.html"
  },
  "board/cards/the-board-reads-as-a-tree/tree-rail-detail.html": {
    "type": "html",
    "title": "Tree rail and detail (rejected)",
    "src": "../the-board-reads-as-a-tree/tree-rail-detail.html"
  }
}
