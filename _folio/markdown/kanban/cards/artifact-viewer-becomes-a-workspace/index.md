{/* This page is generated by the official `folio_agents.integrations.kanban` plugin when a card's directory publishes documents but no index of its own. */}

# Artifact viewer becomes a workspace

*Released · feature · L*

Opening a card should leave floating layers behind and turn the kanban into one continuous, resizable page. Each selection progresses after its parent: filters lead to the canvas, the card follows the canvas, and the artifact follows the card to the right when space allows or below when it does…

- **[Progressive canvas direction](/docs/kanban/cards/artifact-viewer-becomes-a-workspace/progressive-canvas-direction/)**: Replace the current dialog and artifact overlay with one continuous kanban workspace. Each selection adds its next context without obscuring or…

- **[Progressive canvas (revised direction)](/_folio/kanban/artifact-viewer-becomes-a-workspace/progressive-canvas.html)**: progressive-canvas.html

The card belongs to [the board](/docs/kanban/).
