(function(){
const documents=[{"url":"docs/components/theme-configurator/index.html","title":"ThemeConfigurator","content":"ThemeConfigurator. A drawer popover widget that replaces the default light/dark control when documentation presets are available. A preset owns color tokens, document rhythm, code block treatment, borders, radius defaults, typography defaults, and its own controls. Changes are persisted in localStorage under a project-scoped key derived from the configured default preset and applied immediately. The default theme CSS is also rendered into the page so the generated site does not flash or fall back to Folio’s bundled typography before hydration. API. ThemeConfigurator. This component takes no props. It is rendered in the documentation sidebar drawer beside the existing appearance controls. Theme Flow. The drawer palette uses a compact two-step flow: StepPurpose Grouped library. Choose a visual direction from Expressive, Workspace, Product Docs, or Reference, after the Project group when the project defines a preset. Customize. Tune the selected preset’s controls, surface color, accent color, shell spacing, content width, reading rhythm, borders, code blocks, typography, and corner radius. The Back button returns readers from Customize to the grouped library. Light and dark selection lives in the same drawer palette as preset selection, while the keyboard shortcut still toggles between light and dark. With theme.dark_mode: false the theme provider forces the light theme: the drawer shows no mode controls and the shortcut does nothing. Preset Library. All built-in visual directions are presets. The drawer picker shows a current theme summary before the grouped library. Each group is rendered as a compact carousel row so the panel stays short as the preset library grows. GroupPresets Project. The project preset, when docs.yaml or a theme package defines one. Expressive. Organic Editorial, Carbon. Workspace. Workshop, Canopy. Product Docs. Beacon, Aperture, Ledger. Reference. Atlas, Stacks, Draftline, Proof. Other. Presets registered with registerPreset but listed in no group. PresetPurpose Workshop. Warm generated-site workspace based on the saved inspiration example, with light paper contrast and botanical accents. Canopy. Compact green workspace for simple examples, source snippets, and generated guides. Beacon. Main product-docs preset with endpoint cards, compact API workflows, and high-contrast examples. Atlas. Classic reference documentation with paper rhythm and sharp source examples. Ledger. Dense API tables and register pages for fast scanning. Proof. Printed-manual hierarchy with strong rules and editorial emphasis. Stacks. Calm catalog reading for long guides and stable navigation. Draftline. Working-document warmth for docs before release. Aperture. Neutral developer-docs style with compact spacing and rounded code panels. Organic Editorial. Poster-scale typography with cobalt organic image language for launches, programs, and editorial docs. Carbon. Stark monochrome technical mode. Organic Editorial is the default preset because it makes generated sites feel distinctive on first load while still keeping docs controls, code blocks, and API reference pages available through the same preset system. Each preset can be selected directly. Selecting a preset applies its default controls, typography, accent, radius, and layout defaults. Customize keeps the selected preset active while changing its options. theme.preset names the preset readers see first. Its ids are the lowercase names above (organic-editorial, beacon, draftline, …), an id a theme package or template overlay declares, or a new id the project defines in docs.yaml (see Project Theme Contract). Ids from earlier releases, such as folio or openai, still select their successor. Any other value stops the build with the list of valid ids and the nearest one. Workshop and Canopy include a Borders control for switching between fine, structured, and ruled outlines. This keeps the saved inspiration’s framed workspace feel available without forcing every generated page into the same border weight. Shared Controls. Every preset can expose its own controls. The global controls are always available and override the selected preset when changed: ControlPurpose Surface color. Override the page, sidebar, card, muted, and border palette. Shell spacing. Expose the outer page padding used by framed workspace themes. Content width. Adjust the prose and component measure without changing presets. Reading rhythm. Override base type size, line height, section gaps, and card padding. Borders. Tune card and shell rule strength globally. Code blocks. Switch source examples between soft, framed, plate, and terminal treatments. Typography. Switch heading, body, and code font treatment. Accent color. Override the preset’s primary accent. Corner radius. Adjust shared UI and card radius. Radius Options. OptionValue None. 0. Sm. 0.3rem. Md. 0.5rem. Lg. 0.75rem. Full. 1rem. The “Reset appearance” button restores the configured default preset and tuning. Project Theme Contract. Projects can define their own ThemeConfigurator preset in docs.yaml without forking the bundled template. During template preparation, Folio writes theme/project-theme.ts with a typed ThemePreset that merges Folio’s base docs tokens with the project’s overrides. tokens.light and tokens.dark accept CSS custom properties such as shadcn tokens (--background, --card, --border, --chart-1) and project tokens (--brand-accent). style accepts ThemeConfigurator layout variables such as --folio-content-max-width, --folio-section-gap, --folio-card-padding, --folio-code-bg, --folio-workspace-shell-topbar, --folio-workspace-shell-topbar-blur, and --folio-workspace-shell-topbar-border. Un-prefixed legacy names such as --content-max-width are still accepted for compatibility but are deprecated; use the --folio-* names. header.brand and header.badge replace the default docs navbar wordmark. header.repo, header.theme_toggle, header.action_label, and header.action_href replace the default docs navbar actions with project-owned actions; header.search: false hides the navbar search field while leaving the generated search index controlled by search.enabled. variants defines project-owned preset controls; each option can set a swatch for the control UI, override preview, style, and light/dark tokens while inheriting the base project theme. If an option has swatch but no full light/dark preview, Folio uses the swatch for both preview modes. For safety, token and style keys must be CSS custom properties beginning with --, values must be strings without CSS statement or block delimiters, and header and variant labels cannot include markup. Unknown tune keys are ignored with a warning that names the key they most likely meant, and a tune value the control does not offer stops the build; Tune Defaults lists the values. theme.radius must be one of the fixed radius scale values \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\", or a named alias (\"none\", \"sm\", \"md\", \"lg\", \"full\") that maps onto the same scale; any other value fails config validation. theme.variants is capped at 256 option combinations across all controls (the product of each control’s option count) because every combination is resolved and embedded into each generated page; a larger product fails config validation. Tune aliases map to the shared controls: YAML keyRuntime control font. fontId. accent / color. colorId. surface. surfaceColorId. shell. shellPaddingId. width / content_width. contentWidthId. rhythm / reading. rhythmId. borders / border. borderId. code / code_blocks. codeTreatmentId. font: \"geist\" selects the bundled Geist/Geist Mono pair and maps the public --font-sans / --font-mono tokens used by Tailwind utility classes. If theme.preset matches a built-in preset and the project only provides tune, Folio keeps the built-in preset and applies the configured defaults. If the project supplies name, description, scene, preview, style, tokens, or variants, Folio adds a Project group to the drawer and places the project preset before the built-in library. Theme Packages. Theme packages can replace the bundled configurator, project header actions, or theme/project-theme.ts while Folio still supplies generated content and metadata. See Theme Packages for the ownership model, file overlay rules, and validation checklist. Create a Custom Preset. The bundled presets live in the template’s theme/presets.ts and use the interfaces from theme/preset-types.ts. A project adds one without forking the template: a theme package calls registerPreset from its theme/project-theme.ts, or a template overlay ships its own copy of theme/presets.ts. Folio reads the id from either, so theme.preset can select it. Create a new object that satisfies ThemePreset. Give it stable defaultOptions, optional defaultRadiusIndex, optional defaultCustomization, and matching controls. Implement resolve(options) so every option combination returns light, dark, style, radius, and preview. Register it: call registerPreset(preset, groupId) from a theme package, or, in an overlay copy of presets.ts, add it to builtinPresets and list its id in a registerGroup call. A preset in no group shows under Other. The style object must use the namespaced --folio-* keys defined by ThemeStyle in template/theme/theme-contract.generated.ts (its header line names the generator, crates/folio-site/src/theme.rs). Un-prefixed keys such as --card-shadow fail the TypeScript check against ThemeStyle and are not read by the generated CSS. Example: Then register it. In an overlay copy of theme/presets.ts: and select it with theme.preset: \"notebook\". Generate a Preset with ChatGPT. Paste this prompt into ChatGPT, then register the returned object as described in Create a Custom Preset. After ChatGPT returns a preset: Paste it into your theme package’s theme/project-theme.ts or your overlay’s theme/presets.ts. Register it as in Create a Custom Preset. Set theme.preset to its id and run folio build. Open the drawer palette button. Test each generated control in light and dark mode. Example. Layout Component. ThemeConfigurator is rendered in the documentation drawer of every site built on the bundled template. A theme package or template overlay can replace it with components/theme-configurator.tsx."},{"url":"docs/deployment/ci-cd/index.html","title":"CI/CD","content":"CI/CD. CI/CD is a deployment strategy: the pipeline builds _site/, checks it, and publishes the same static artifact that local builds produce. This page focuses on GitHub Actions because folio init can generate ready-to-use Pages workflows. GitHub Actions Deployment. folio init creates a GitHub Pages workflow at .github/workflows/pages.yml. It builds docs on every push to main, uploads _site/, and deploys with GitHub Pages: If your docs build needs local project dependencies, edit the build steps in this same file. The folio github-pages commands are internal deployment helpers shipped in the folio binary. They keep the generated workflow readable while still preserving branch previews, pruning the previews whose pull request has closed, updating the preview index, saving Pages state, and verifying the deployed URLs. Pruning is why the build job asks for pull-requests: read: without it the previews of closed pull requests are published forever. Branch Preview Deploys. folio init also creates .github/workflows/branch-previews.yml for GitHub Pages branch previews. It runs from pull_request_target so the workflow definition and deploy steps come from the trusted base branch, while PR branch code is built in a separate unprivileged job. GitHub Pages serves one artifact per site, so Folio does not deploy each branch as an independent Pages site. Instead, every deploy publishes a complete static artifact with this layout: The production workflow and preview workflow cooperate through an internal folio-pages-state branch: A main deploy builds production docs into _site/. It copies any existing _site/previews/ folders from folio-pages-state. It regenerates /previews/index.html. It saves the complete artifact back to folio-pages-state. It deploys the combined artifact to GitHub Pages. A branch preview deploy follows the same rule: publish the complete site, not just the branch folder. The preview workflow resolves the open same-repository PR for the branch. An unprivileged build job checks out the PR head with persisted credentials disabled and builds docs with FOLIO_BASE_PATH=/repo/previews/pr-<number>-<branch>/. That build job uploads only the static _site/ preview artifact. A privileged deploy job revalidates that the PR head is still current before doing any write-capable work. The deploy job checks out the trusted base branch, starts the Pages artifact from the saved folio-pages-state root, and uses a trusted production build only as the first-deploy fallback. It downloads the static preview artifact and replaces only /previews/pr-<number>-<branch>/. It regenerates /previews/index.html, saves state, deploys, and verifies the preview URL. It creates or updates a sticky PR comment with the preview URL, preview index, branch, and commit. Production therefore remains at the normal Pages URL, while each open PR gets a stable preview URL such as: The preview folder name includes the PR number plus a URL-safe version of the branch name, which keeps similarly named branches from overwriting each other. If a PR is stale, closed, still a draft, or comes from a fork, the preview deploy exits early and leaves the current Pages site unchanged. The PR comment uses a hidden marker so the workflow updates the same comment on each deploy. Reviewers get the latest preview link directly in the conversation without a new comment on every push. The workflow still writes the same links to the Actions summary for debugging failed runs. Pull Request Checks. Run the docs build and, for a Python project, the coverage gate on every PR. Drop the coverage step for a JavaScript or Rust project: folio coverage reads Python only in this release. Coverage Gates. Use folio coverage to enforce minimum docstring coverage in CI. It reads Python only in this release. The gate applies to the total, not to each module: when the total falls below the threshold, the command prints Coverage 60.0% is below minimum 80.0% (with your numbers) and exits with code 1, failing the pipeline. Adjust the threshold to match your project’s standards. The report counts each module, its public classes, functions and methods, and shows per-module statistics. This run passes --min 80 even though one module sits at 50%: Pre-commit Hook. In a Python project, run a quick coverage check before every commit:"},{"url":"docs/plugins/roadmap/index.html","title":"Roadmap","content":"Roadmap. The built-in roadmap integration renders source-defined phases as a release timeline. The view below reads the same data as the standalone /roadmap/ route, which is optional: routes.public: true turns it on. The roadmap integration is compiled into folio: nothing to install, nothing to list. It stays inert until a roadmap: section appears in docs.yaml — the config key is the activation switch. Activate it in docs.yaml: The timeline is rendered from source: change phases in docs.yaml, commit the file, and rebuild the site to update this view. Give phases a project to render independent release sequences from one roadmap. Routes are opt-out: routes.docs (default true) publishes a generated /docs/roadmap/ page when your sources do not already contain one, and routes.public (default false) adds the standalone /roadmap/ page. The generated page is a # Roadmap heading over the timeline; write your own roadmap.md to replace it. The folio roadmap CLI command prints the configured phases as a table. phases must be a list of mappings. A phase takes id, version, project, title, status, layer, summary, command and features, and no other key. id, version, title, status, layer and summary are required strings: quote a version (\"0.1\"), or YAML reads it as a number. status is one of shipped, active, next or later, project and command are optional strings, and features, when present, must be a list. Any other shape stops the build with an error that names the entry, for example roadmap.phases[0].features must be a list (got string). A phase without features renders with an empty feature list. The optional top-level description is the page’s own copy: the /roadmap/ page shows it under the title, above the releases. Releases are ordered by version, never by status. Each feature can be a string or an object with text and an optional done boolean. Strings inherit completion from the phase: features in a shipped phase are complete. Use done to mark individual work within an active phase, or to override that inherited state: Naming the projects. Give a phase a project and the plugin groups the roadmap by it. On the standalone /roadmap/ page each project becomes a card, and every card holds that project’s whole plan: the release list on the left, the release you pick opened beside it. Projects are numbered independently, so two projects that ship separately each start at their own 0.1. An optional projects: block names them. A label titles the card and a description sits under it; without either, the project key is shown as written. The plugin also writes the projects: block to lib/roadmap-projects.ts as roadmapProjects, keyed by project, whether or not routes.public is on. A page that draws a project card outside the /roadmap/ view (a landing, or a theme page) reads the label and description from there, so they stay in docs.yaml. Every card is expanded by default, and each one can be collapsed to its heading. ?product=<key> opens one card and collapses the rest, which is the link a project’s own landing page should use: /roadmap/?product=cli. A release is addressable too, scoped by project: /roadmap/#cli-0.2. Collapsing hides a card’s releases; it does not drop them. Every release is in the served HTML whatever the page is showing, so a crawler or a reader with JavaScript off still sees the whole roadmap. The page’s Markdown mirror and llms-full.txt do not carry the timeline yet. Internally, the integration uses the same extension primitives as the other built-in plugins: it registers a Roadmap component for embedding and a RoadmapPage component for the standalone route, writes two typed data modules (lib/roadmap-data.ts and lib/roadmap-projects.ts), and, when routes.public is on, creates the /roadmap/ view with the required folio.public layout. Live demo. This is Folio’s own roadmap — the code tab shows the shape of the roadmap: section in this repository’s docs.yaml, the preview tab is the component rendering the real data: Folio's release line. v0.1 Foundation. Python first: source in, documented site out. Source analysis One command sets the project up. Reads your source, never runs it. API reference and your guides in one site. Types in signatures link to their pages. Edit a docstring, see the page. A component library and full-text search. llms.txt from day one. v0.2 Theming. Own the look at any depth, then ship it anywhere static files go. Visual ownership Presets, shareable packages, or your own template. Readers retheme the site from the site. A preview site per pull request. Social preview images. v0.3 One Binary. One native binary reads Python, JavaScript and Rust; the built-in plugins share one contract, and this site runs on it. One platform The core in Rust: one native binary, no Python to install. Three readers in the binary: Python, JavaScript, and Rust. Built-in plugins ship pages, components, and data. Your own React components in any page. Every project ships its authoring contract. Every page gets a portable Markdown mirror. Roadmap, landing, and OpenAPI integrations. v0.4 Every Reader. Up next. One site for every reader: the language the code is in, the version they are on, and the language they read. Everything the alpha marks not available. Reach TypeScript next to JavaScript. Versioning — docs for every version of your library. Internationalization — sites in your readers' languages. Cross-language references. One search, one llms.txt, every language. Inline type information in examples. Folio's own API reference, read by its Rust reader. v0.5 Open Platform. Later. Extensibility Project plugins return as sidecars over JSON-RPC. Language contract — a public way to add a language. A plugin catalog outsiders can publish into. Starter templates. Live plugin demos with their source. folio score — how agent-ready any docs site is. Agent discovery by the specs: .well-known and text/markdown negotiation. v0.6 More Languages. Later. Reach Go, C#, and Java next to Python, JavaScript, TypeScript, and Rust. v0.7 Migrations. Later. Adoption One-command import from MkDocs, Mintlify, and Read the Docs. Sphinx cross-links carry over. A theme preset that feels familiar. Redirects for every moved page. One site across products and repos. v0.8 API Portal. Later. API workflow Field-level API components. A page per endpoint. Builds fail on breaking changes. Try requests on the page. SDK examples verified against the code. Event APIs too."},{"url":"docs/deployment/index.html","title":"Deployment","content":"Deployment. Folio builds a static site into _site/. Deploy that folder to any static host. The internal .build/ workspace is only a cache and should not be committed or used as the public artifact. Deployment has one artifact and several delivery paths: PathUse when Static hosts. You want Vercel, Netlify, or a static web server to build or serve _site/. GitHub Pages. You want production docs and optional PR previews hosted by GitHub Pages. CI/CD. You want GitHub Actions or another pipeline to build, check, and publish docs automatically. Static Hosts. Deploy _site/ to Vercel, Netlify, or a small self-hosted static server. GitHub Pages. Configure base paths, Pages artifacts, production deploys, and branch previews. CI/CD. Automate deployment, branch previews, pull request checks, and coverage gates. Build Once. With Folio installed, run the build from your project root: The _site/ directory contains HTML, assets, Pagefind search data, llms.txt, llms-full.txt, and the Markdown mirrors and authoring contract under _folio/. flowchart LR Source[\"Source code and Markdown\"] --> Build[\"folio build --clean\"] Build --> Site[\"_site/ static artifact\"] Site --> Host[\"Static host\"] Site --> Pages[\"GitHub Pages\"] Site --> Pipeline[\"CI/CD publish job\"] Base Paths. Most hosts serve docs at the root. Use a base path when the generated site is published under a subpath such as /my-repo. Base path priority is: FOLIO_BASE_PATH environment variable. deploy.base_path in docs.yaml. GitHub Pages inference when deploy.provider: \"github-pages\" or FOLIO_DEPLOY_PROVIDER=github-pages is active in GitHub Actions. No base path. project.url is metadata only. It feeds sitemap and canonical URLs, but it does not control local routing or static asset paths. Choose a Strategy. Use Static Hosts for Vercel, Netlify, Docker, Caddy, Nginx, or any static file server. Use GitHub Pages when the public artifact is a GitHub Pages site and you need Pages-specific base path behavior. Use CI/CD when deployment should happen automatically on pushes, releases, pull requests, or scheduled workflows."},{"url":"docs/components/swot/index.html","title":"Swot","content":"Swot. A four-quadrant SWOT analysis rendered as a color-coded grid: strengths, weaknesses, opportunities, and threats, each with its own accent and marker. Use it for honest product or design assessments that a bulleted list would flatten. API. Swot. PropTypeDefaultDescription strengths. string[] — Internal, helpful. weaknesses. string[] — Internal, harmful. opportunities. string[] — External, helpful. threats. string[] — External, harmful. title. string. \"SWOT\" Header label. Example. Product SWOT. Espresso cart — Q3. internal ↑ · external ↓ S Strengths. Best beans in the district. Regulars know us by name. W Weaknesses. One grinder. Cash only. O Opportunities. Office park opening next door. T Threats. Chain opening across the street."},{"url":"docs/plugins/landing/index.html","title":"Landing Page","content":"Landing Page. Configure the optional homepage that appears before the documentation app. Folio starts as a docs-first site. The landing page is a built-in integration: it is compiled into folio and stays inert until a landing: section appears in docs.yaml. When you want a public entry point, add the section and keep the docs under /docs/. Use the landing page for project positioning, install commands, and links to the most important documentation routes. Keep detailed tutorials, configuration reference, and API content in the docs section. Recommended Structure. Hero: project name, one-sentence value proposition, and primary docs link. Install: the shortest command sequence that gets users to a local preview. Proof: links to generated API pages, guides, examples, or release notes. CTA: a final link to /docs or the quickstart guide. The generated landing page uses the same theme tokens, navigation, search metadata, and static export path as the rest of the site. hero.tagline is the kicker above the headline. Omit it and the headline opens the hero; there is no default kicker. landing.install lists the commands the install block shows. Omit it and the page renders no install block; there is no default command. Without landing.features and landing.sections, the page renders six default cards: “Automatic API Reference”, “One Config File”, “Built-in Integrations”, “Dark Mode, Search, Responsive”, “LLM-Friendly Output”, and “Markdown + API in One Site”. hero.variant picks the hero layout: docs-map (the default), source-pipeline, build-pipeline, or heartbeat. Any other value renders docs-map and the build warns, naming the valid variants. Section Catalog. landing.sections composes the homepage below the hero from reusable, config-driven sections. Each entry needs a type; everything else is optional and degrades to sensible defaults. Available types: features, comparison, output, routes, pipeline, funnel, install, stats, use-cases, cta, link-grid, cells, mechanism, and statement. A section with any other type, or with none, is passed through to the template unchanged and the build warns, listing the valid types: the bundled template renders nothing for it, and a theme package that overrides components/landing/sections.tsx may render it. Most sections share the same heading fields (eyebrow, title, description). Any section may also set stage: \"The mechanism\" — sections with a stage get a numbered rail above their heading: the zero-padded stage number, then the label as written (02 The mechanism), numbered in page order across all staged sections. Sections without a stage render unchanged. The showcase sections below are documented in detail because they render live plugin data or bespoke layouts. funnel — the build funnel plate. A technical-diagram centerpiece: source tiles on the left converge through a single build node, and every output tile fans out on the right. Each side is a two-column block of tiles with alternating margins, and a tile is a mark and a plain label — \"Markdown\", \"API reference\" — never a path or a glob. Inputs marked ghost: true render dimmed and dashed, for roadmap items that are not shipped yet, and an optional chip labels them (for example \"roadmap\"). Each tile may carry an icon, one of: config, python, javascript, rust, markdown, language, guides, api, pages, folder, search, agents (the llms.txt mark), hash. An unknown value is dropped and the tile renders without a mark. Setting description: \"\" suppresses the heading paragraph. When inputs or outputs are omitted the template renders Folio’s own defaults, so a bare - type: \"funnel\" works. The earlier plate’s guarantees, caption and command_notes keys are no longer rendered; a config that still carries one gets a build warning. features — rows or bento. The default features layout is the editorial row list. Setting variant: \"bento\" switches to a two-column bento grid where each card carries a small token-only vignette above its copy via visual, one of: components, llms, receipt, deploy, plugins, theming. An unknown visual is dropped and the card renders without a vignette. cells — small feature cells. A bento-style row of compact cards: a mono micro-label, a short claim, a one-line description, and an optional footer link (the whole cell becomes the link). The heading is optional — omit eyebrow/title to render the grid alone. Cells lay out four per row (three per row when the count is divisible by three). mechanism — YAML diff to live UI. Shows the edit-commit-rebuild loop: a code window with an optional git log strip, followed by a pipeline pill rail. Inside code, lines starting with + are tinted as additions and lines starting with - as removals — indented YAML list items are never mistaken for diff markers. statement — typographic closer. A huge centered statement with an optional accent-highlighted substring and optional CTA links (two work best). accent must be an exact substring of text; the first action defaults to the primary button style unless a primary flag says otherwise. size: \"md\" steps the headline and padding down for a mid-page thesis block (the default suits closers), and description renders a reading-size lead paragraph under the headline. All hrefs in these sections pass through the same scheme policy as other configured links; an unsafe value (for example javascript:) degrades to the section default with a warning instead of failing the build. Comparison Section. landing.comparison adds a feature matrix to the landing page. The table is yours: you name the tools across the columns and write every row, so the section says what your project wants to say about its own field. Each row needs a feature name and exactly one entry in values per name in tools. A cell is true (yes), false (no), or \"~\" (partial); the strings yes, no, true, and false are accepted too, and anything else reads as partial so a typo never turns into a claim about a named tool. Quote the tilde: a bare ~ is YAML’s null, which also reads as partial. Malformed config degrades instead of failing the build. A row whose value count disagrees with tools is dropped with a warning, because its cells would otherwise slide under the wrong column. Rows without a feature and rows whose values is not a list are dropped quietly. A comparison: mapping left with no usable row renders nothing and warns. The same keys work on a comparison entry in landing.sections, next to the usual eyebrow, title, and description heading fields: The two forms place the table differently. A comparison entry in landing.sections renders where you put it. The top-level landing.comparison key feeds the default section list instead, which a site uses only when it sets no landing.sections and its hero variant is source-pipeline. Deprecated: comparison: true. landing.comparison: true renders Folio’s own built-in matrix, which names the documentation tools Folio compares itself against. It still works and warns on every build; it will be removed. A comparison section with no tools and rows falls back to the same built-in matrix and warns the same way. Replace both with your own tools/rows table. Live demo. This site uses the plugin itself: the landing page at the site root is rendered from the landing: section of this repository’s docs.yaml."},{"url":"docs/components/tabs/index.html","title":"Tabs","content":"Tabs. Generic tabbed content panels for organizing alternative content views. Unlike CodeGroup (which is code-only), Tabs supports any content including text, tables, images, and nested components. API. Tabs. PropTypeDefaultDescription children. ReactNode. — One or more <TabItem> elements. TabItem. PropTypeDefaultDescription label. string. — The tab label shown in the tab bar. children. ReactNode. — Content displayed when this tab is active. Example. Python Node.js Go. Install with pip: pip install my-library. Install with npm: npm install my-library. Install with go get: go get github.com/example/my-library."},{"url":"docs/api-reference/index.html","title":"API Reference","content":"API Reference. Not available in this release. Folio reads Rust source, but this site does not publish a reference for Folio’s own code yet: it ships its landing and guides only. What the API reference is in your project. The API reference is the part of the site Folio generates from source. List your source roots under source.python.paths, source.javascript.paths or source.rust.paths in docs.yaml; every module becomes one page, and each page shows: the module’s doc comment as prose; every public class, function and type with its signature, including type annotations, defaults, decorators and Rust attributes (a Python signature leaves out self and cls and takes an unannotated parameter’s type from its docstring); a parameter table and return type per Python and JavaScript function, read from the signature and from Google or NumPy docstrings (with auto also reading reStructuredText and epydoc fields) or JSDoc tags; a Rust function shows its signature and doc comment, as rustdoc does; the raises, examples and notes of a docstring, and its deprecated, warning, see also, references and todo sections as labelled paragraphs; module constants, class attributes, struct fields and enum variants in tables, and JavaScript and Rust re-exports listed as written, without resolving what they point at. Languages lists what each reader takes from its source and where it stops. Folio reads the source and never executes it. Each generated page also gets an agent-readable Markdown mirror, so agents read the same reference people do. Quick Start walks the first build end to end, and Configuration documents the source block."},{"url":"docs/plugins/catalog/index.html","title":"Plugin Catalog","content":"Plugin Catalog. Every integration in the Folio binary, in one place. Every integration below is built in: compiled into folio, present in every build, and inert until its section appears in docs.yaml. Add the section and the integration builds its pages. Remove it and the integration goes inert again. A clean build clears any route it had already written. Built-in integrations. Roadmap. Product phases from docs.yaml rendered as a release timeline, with an optional standalone /roadmap page and a CLI table. Activated by the roadmap: key. Landing Page. A public homepage in front of your docs: hero variants, CTAs, install commands, and a section catalog. Activated by the landing: key. OpenAPI. Point openapi.sources at a spec file and get an endpoint index page (method, path, summary, operationId, tags) wired into the sidebar and search. Activated by the openapi: key. Activation. Static files. Files the site serves from its root without a plugin, such as an installer script, go under the public: key in docs.yaml. Folio’s own site serves install.sh that way. See public. Project plugins. Not available in this release. Plugins loaded from your own repository return with the sidecar protocol; the hooks they will implement are documented in Writing Plugins, and the built-ins above are their reference implementations."},{"url":"docs/installation/index.html","title":"Installation","content":"Installation. Install Folio and start the CLI. Install Folio. Folio is one native binary. Install it with the standalone installer: The script detects your operating system and architecture, downloads the matching release archive from GitHub Releases, installs the folio binary into ~/.local/bin, and prints folio --version. It warns when that directory is not on your PATH and when Node.js is missing. Your project does not need Python or any package manager. Inspect the script before running it: The installer reads four optional environment variables: VariableDefaultPurpose FOLIO_VERSION. latest. Release to install, for example 0.3.0-a1. FOLIO_INSTALL_DIR. ~/.local/bin. Directory that receives the folio binary. FOLIO_REPO. pguijas/folio. GitHub repository the release archive is fetched from. FOLIO_DOWNLOAD_URL. unset. Full URL of the release archive, for mirrors and air-gapped installs. Set them on the sh side of the pipe: Verify. Update. The binary updates itself. It resolves the latest release of pguijas/folio and, when that release is newer than the installed version, downloads the archive for your platform and the release’s SHA256SUMS, verifies the archive, unpacks it with tar beside the binary, swaps it into place and prints the new version. A binary that is up to date, or ahead of the latest release, says so and changes nothing; a checksum mismatch, a missing checksum file, an archive without the binary or a binary that does not start abort before the installed one is touched. SHA256SUMS comes from the same release as the archive, so it guards against a corrupted or truncated download, not against a compromised release or mirror. Like the installer it needs curl and tar, plus write access to the directory that holds folio. On Windows the release is a .zip, which tar also unpacks, and the previous binary stays beside the new one as folio.exe.old. --update reads two optional environment variables: VariableDefaultPurpose FOLIO_REPO. pguijas/folio. GitHub repository whose releases are checked; the installer reads the same variable. FOLIO_RELEASES_URL. https://github.com/<FOLIO_REPO>/releases. Base URL of the releases, for mirrors: <base>/latest must redirect to <base>/tag/<version>, and <base>/download/<tag>/ must serve the archive and SHA256SUMS. Re-running the installer installs the latest release as well. Node.js and pnpm. folio build and folio serve render the site through the bundled Next.js template and need Node.js 20.19+ and pnpm 10. Enable pnpm through Corepack and activate pnpm 10, or install it with npm: Bundled Template. The Next.js template ships inside the binary. The first folio build or folio serve materialises it once under the user cache directory, $XDG_CACHE_HOME/folio/template/<version>-<hash> or the platform equivalent (%LOCALAPPDATA% on Windows, ~/.cache when neither variable is set), and every build copies it from there into .build/. Set FOLIO_TEMPLATE_DIR to a template checkout to build from it instead. Manual Install. Every release on the Releases page ships one archive per target: TargetArchive x86_64-unknown-linux-gnu. folio-x86_64-unknown-linux-gnu.tar.gz. aarch64-unknown-linux-gnu. folio-aarch64-unknown-linux-gnu.tar.gz. x86_64-apple-darwin. folio-x86_64-apple-darwin.tar.gz. aarch64-apple-darwin. folio-aarch64-apple-darwin.tar.gz. x86_64-pc-windows-msvc. folio-x86_64-pc-windows-msvc.zip. Extract the archive and put the folio binary on your PATH. The installer script does not run on Windows; download the .zip from the Releases page. Build from Source. With a Rust toolchain installed, build the release binary from a checkout: The binary is written to target/release/folio. Contributing to Folio. Repository setup and the checks are covered in the Developer Guide. Next Steps. Once Folio is installed, head to the Quick Start guide to build your first documentation site."},{"url":"docs/components/feature-cards/index.html","title":"FeatureCard and CardGrid","content":"FeatureCard and CardGrid. Cards for feature overviews, landing pages, and link grids. FeatureCard renders a single card with a named icon, title, description, and optional link. CardGrid arranges multiple cards in a responsive grid layout. API. FeatureCard. PropTypeDefaultDescription title. string. — The card heading. description. string. — Short description text displayed below the title. icon. string. — Optional named icon displayed above the title. Common tokens include install, run, quickstart, workflow, network, topology, analytics, settings, monitor, api, code, server, warning, and community. Unknown values fall back to text rendering for backwards compatibility. href. string. — Optional URL. When set, the card becomes a clickable link with a hover effect. CardGrid. PropTypeDefaultDescription columns. 2 | 3 | 4. 3. Number of columns on large screens. The grid is always 1 column on mobile and 2 on tablet. children. ReactNode. — One or more <FeatureCard> elements (or any content). Example. Three-column grid with named icons: Auto API Reference. Extracts documentation from your source code automatically. Modern UI. Dark mode, search, and responsive layout out of the box. LLM Output. Generate llms.txt and llms-full.txt for AI assistants. Two-column grid without icons: Getting Started. Install Folio and build your first documentation site. Configuration. Customize your site with docs.yaml options."},{"url":"docs/components/doc-preview/index.html","title":"DocPreview","content":"DocPreview. Use DocPreview when a guide needs to show a generated documentation route or a small generated example instead of describing the result with another code block. It renders a responsive iframe with a short caption, a source view with a collapsible file drawer for the focused source file, a line-numbered code preview, and direct links to the page and source file. PreviewSource. Guide page preview. Show a built page directly in the guide so readers can inspect the output before building their own project. Open page. For tutorial examples, place a complete Folio project under docs/examples/<name>/ with its own docs.yaml, docs/, and optional src/ files. The docs build runs Folio for that project, serves the generated static output at /_folio/examples/<name>/, and shows the exact source files that produced it. folio build always builds the examples; folio serve builds them only with --previews, because each one is a full nested site, and a DocPreview whose example is not built says so in its place. PreviewSource. Generated docs site. A tiny docs site generated from a real example project. Open page."},{"url":"docs/plugins/index.html","title":"Plugins","content":"Plugins. One extension point for everything Folio builds: components, data, pages, and views. Folio’s built-in integrations are compiled into the folio binary and share one set of hooks — if you want to know whether the surface can do something, the answer is usually “one of the built-ins already does it.” What ships where. Landing Page, Roadmap, and OpenAPI are built-in integrations: present in every build, activated by their own section in docs.yaml, and inert without it. The complete set is listed in the Plugin Catalog. How activation works. The config key is the switch: add it and the integration builds its pages, remove it and the integration goes inert. There is no plugins: list. A file the site serves verbatim from its root needs no integration at all: list it under public:. Folio’s own docs.yaml serves install.sh that way. Project plugins. Not available in this release. Plugins loaded from your own repository return with the sidecar protocol: a plugin is a separate process, written in any language, that folio starts for the build and talks to over stdio. The hooks it will implement are documented in Writing Plugins, and what running one means for your machine in Trust & Safety."},{"url":"docs/theming/theme-packages/index.html","title":"Theme Packages","content":"Theme Packages. [!WARNING] Security A theme package is executed as trusted code during every build. The package files are overlaid onto the template that Folio then builds with pnpm install --frozen-lockfile (which executes dependency lifecycle scripts) and next build (which runs next.config.mjs in Node), so pointing theme.package at a directory is equivalent to running that code on your machine. Only point theme.package at frontend code you have reviewed and trust. Building an untrusted repository or branch in CI inherits the same remote-code-execution surface — treat a docs build like any other untrusted-code execution. Never put secrets, API tokens, or credentials in theme code or docs.yaml: static docs are published. Theme packages are the middle ownership level. They let a project ship a theme overlay, local or fetched from git, that is copied over the bundled Folio template before generated content and metadata are injected. Use a theme package when YAML personalization is too limited, but a full custom template would duplicate too much of Folio’s default docs runtime. Configure a Package. theme.package is resolved relative to the project directory. The directory is trusted frontend code: it can override template files, import dependencies already available to the template, and replace Folio’s default theme modules. Install a Package Someone Else Published. theme.package also takes a mapping, which is how a theme you did not write reaches your project without copying a directory into it: KeyRequiredPurpose git. yes. Repository to fetch: https://, ssh://, git@host:owner/repo or file://. There is no plain http. rev. yes. Tag, branch or commit to read it at. digest. yes. sha256: and 64 hex characters: the digest of the package tree. path. no. Subdirectory of the repository that holds the package; the root by default. Folio fetches the revision into the theme cache, one directory per digest under <cache>/folio/themes/, hashes the tree it got, and refuses to use it unless the hash is the one you pinned. FOLIO_THEME_CACHE_DIR points that cache somewhere else, which is how a CI job pre-seeds it and how an air-gapped machine fills it by hand. The build then overlays it exactly as it overlays a local package. The second build reads the cache and makes no network call. The digest is the pin, not the revision. A tag can be moved and a branch always moves. The digest cannot: it is the SHA-256 of every file in the package, so the same four lines of docs.yaml produce the same theme on every machine and in CI. When the hash does not match, the build fails and names both: Take a new digest by reviewing the change and reading the value out of that message. Folio never updates the pin for you. Pinning a package for the first time works the same way: put any 64 hex characters in digest, run the build, and copy the found value once you have read what you are installing. What a fetched package may not own. A theme package is executed as trusted code, and a fetched one was written by someone else. Two rules follow. Every package, local or fetched, is refused if it ships package.json, pnpm-lock.yaml or pnpm-workspace.yaml, because the frontend build installs against Folio’s own pinned lockfile. A fetched package is refused if it ships next.config.mjs: owning the Next config is the shortest path from a one-line change in docs.yaml to arbitrary code in your build, and it stays a decision a project makes about itself. Everything else the digest covers: you are trusting a tree you pinned, not a repository that can change under you. Fetching needs git on the machine. Ownership Model. OwnerResponsibilities Folio. Copy the bundled template, apply the theme package overlay, generate content, write metadata, inject fallback theme config, run the frontend build, and export static files. Theme package. Own selected frontend files such as layouts, global CSS, the configurator UI, project header actions, or theme/project-theme.ts. Folio still writes generated docs content and reserved build files into .build/. Do not edit .build/ directly; change the package source instead. Package Anatomy. A package can be small and only override the files it owns: Common override points: FilePurpose app/layout.tsx. Own font loading, metadata shell, providers, and global layout behavior. app/docs/layout.tsx. Own the docs navbar, sidebar placement, search slot, and ThemeConfigurator mount. components/theme-configurator.tsx. Replace the bundled configurator UI with a project-specific implementation. components/project-header-actions.tsx. Replace the default generated header action component. theme/project-theme.ts. Own all presets, controls, variants, defaults, and resolved tokens in TypeScript. app/globals.css. Override or replace template-level CSS when a project needs exact visual parity. A package that replaces components/theme-provider.tsx has to keep the const darkModeEnabled: boolean = true // __FOLIO_DARK_MODE__ line for theme.dark_mode: false to take effect. Without it the build warns and dark mode stays available. YAML and TypeScript Together. If the package supplies theme/project-theme.ts, Folio does not overwrite it with a YAML-generated module. The package owns the preset implementation. If the package omits theme/project-theme.ts, Folio still emits the safe docs.yaml-driven project preset described in Personalization. This lets a project start with YAML and graduate individual surfaces to TypeScript only when needed. Register a Custom Preset. A theme package can register a custom preset without forking presets.ts. From theme/project-theme.ts, import registerPreset and call it before exporting the projectThemePreset. The registerPreset function is the supported extension point. The optional second argument (\"project\") adds the preset to that display group in the configurator UI. If a preset with the same id already exists, it will be replaced with a console warning. theme.preset may name a preset the package declares. Folio reads the id: \"…\" values in the .ts and .tsx files under the package’s theme/ directory and accepts those ids next to the bundled ones, so preset: \"acme\" above builds. Any other id stops the build with the list of valid ids and the nearest one. A package that ships its own components/theme-configurator.tsx owns preset selection, and Folio does not check theme.preset then. See ThemeConfigurator for the full ThemePreset contract, including controls, resolve, and all theme fields. What Packages Should Not Do. Do not assume files written under .build/ are stable source files. Do not put secrets in theme code or docs.yaml; static docs are public. Do not use a package when the project needs a different app structure, dependency graph, route model, or product shell. Use custom templates for that level. Validation Checklist. Folio validates theme packages at build time before any overlay. If validation fails, the build halts with one error listing all violations. Reserved Paths. A theme package must NOT contain any of these paths. Folio generates them at build time, and including them will cause a validation error: content/ — Generated docs pages and metadata live here. lib/folio-template.ts — Folio internal contract file. lib/folio-mdx-contract.ts — Folio internal contract file. theme/theme-contract.generated.ts — Folio internal contract file. .next/ — Next.js build cache. node_modules/ — Dependency install directory. package.json — The frontend installs against Folio’s own manifest. pnpm-lock.yaml — The install is --frozen-lockfile against Folio’s lockfile. pnpm-workspace.yaml — Same reason. next.config.mjs — Fetched packages only; a local package may own it. Required Exports. If a theme package includes theme/project-theme.ts, that file MUST export both: projectThemePreset — The preset definition (colors, spacing, variants). projectThemeDefaultConfig — The default configuration object. Folio checks for export const <name> or export { <name> } forms. Missing exports fail validation. Example Validation Error. Why This Fails Early. Validation runs before any file overlay. You get one clear error with actionable messages instead of a cryptic Next.js build failure or runtime import breakage."},{"url":"docs/index.html","title":"Folio Docs","content":"Folio Docs. Folio Docs is one native Rust binary that turns source code and Markdown guides into a searchable static site, API reference, and agent-readable Markdown mirrors. It reads your source without running it, and never imports the package it documents. Python, JavaScript, and Rust read today; see Languages for what each reader covers and what arrives next. Install Folio, then run, from your project root: folio build and folio serve are the commands you run most; the CLI Reference covers the rest. Get started. Folio reads your project metadata, scans your source code, and generates a full documentation site with source pages, search, and dark mode. Quick Start walks the first build end to end; Installation covers the prerequisites and the standalone installer. How it works. Folio follows a four-stage pipeline: flowchart LR Source[\"▣ Source + Markdown\"] --> Parse[\"⚙ Parse syntax + doc comments\"] Parse --> IRNode[\"◇ IR objects\"] IRNode --> Generate[\"▤ Generate MDX pages\"] Generate --> BuildSite[\"▥ Build Nextra static site\"] Parse — Reads your source files with a native parser in the Rust core; no interpreter and no language toolchain runs. Extracts modules, functions, classes, signatures, type annotations, and doc comments. Nothing is executed. IR — Converts parsed data into a clean intermediate representation (ModuleIR, ClassIR, FunctionIR, TypeIR) that carries the language identity and everything needed for docs. Generate — Transforms the IR into MDX pages with function signatures, parameter tables, class overviews, and formatted docstring content. Build — Places MDX files into a Nextra site with shadcn/ui components. Produces a static site with search, dark mode, and responsive layout. Inside the binary. The folio binary carries the whole pipeline: The site builder owns the generated content workspace and search index. The MDX writer, sidebar, template workspace, theme support, and Next runtime live beside it. Parsing, IR, configuration, the built-in plugins, and orchestration ship in the same binary. Architecture walks the crates and the runtime flow. Agent-readable output. One build writes the HTML site, the Markdown mirror of every generated page, the LLM indexes, and the published authoring contract, from the same pass. Features. Automatic API reference. Point Folio at your source directories and get complete API docs — modules, classes, functions, parameters, and return types, all extracted and rendered automatically. One config file. A single docs.yaml of about thirty lines replaces Sphinx's conf.py, Makefile, and requirements setup. Modern UI. Dark mode, full-text search, responsive layout, and shadcn/ui components out of the box. No theme hunting. Markdown + API in one site. Write tutorials and guides in Markdown alongside the generated API reference — everything lives in one cohesive site. Static deployment. Export plain files for GitHub Pages, Vercel, Netlify, or Docker — no custom server, no vendor in the serving path. LLM-friendly output. Generates llms.txt and llms-full.txt following the llmstxt.org spec, so AI coding assistants understand your library. Plugins included. Roadmap, the landing page and OpenAPI ship in the binary; each switches on with its own section in docs.yaml. Project plugins: Not available in this release. Migrating from Sphinx. Move a Sphinx project's guides and API documentation to Folio, configure sources, and verify the generated site. Project status. Folio is under active development. The roadmap is rendered from this repository’s docs.yaml and shows what has shipped and what is in progress. Next steps. Why Folio — The comparison, the honest SWOT, and the LLM-era questions. Installation — Prerequisites and setup. Quick Start — Build your first docs site step by step. Architecture — How the CLI, parser, generator, template, and export pipeline fit together. Developer Guide — Build and test the Rust workspace from a checkout. Configuration — Full docs.yaml reference. CLI Reference — Every command, flag, and option. Languages — The source languages Folio reads, and the ones that arrive next. Writing Doc Comments — How Folio reads Python docstrings, JSDoc and Rust doc comments. API Reference — What the generated reference contains, and why this site ships without one for Folio’s own code. Components — UI components available in your docs. Theming — Customize presets, theme packages, and custom templates from one theming model. Deployment — Static hosts, GitHub Pages, CI/CD, and branch previews. Plugins — The built-in integrations and how each one activates. Roadmap — Where Folio is headed, rendered live from docs.yaml by its own plugin. Migrating from Sphinx — Move a Sphinx project to Folio."},{"url":"docs/components/example-tabs/index.html","title":"ExampleTabs","content":"ExampleTabs. Tabbed panels for showing multiple code examples for the same concept, such as basic vs. advanced usage or different input formats. Each tab displays its code as a plain, unhighlighted block. For highlighted tabs, use CodeGroup with fenced code blocks. API. ExampleTabs. PropTypeDefaultDescription examples. Example[] — Array of example objects. Example object. FieldTypeDefaultDescription label. string. — Tab label text. code. string. — The code string to display. language. string. \"python\" Sets the language-* class on the code element; the code is not highlighted. Example. Basic With Config Async. node = Node() node.start()."},{"url":"docs/components/method-accordion/index.html","title":"MethodAccordion","content":"MethodAccordion. Expandable accordion panels for documenting class methods. Each method shows its name and signature in the header, with the full description revealed on click. Async methods display an “async” badge. Generated API pages do not write this component, since they give each method a heading of its own; use it by hand in the pages you write. API. MethodAccordion. PropTypeDefaultDescription methods. Method[] — Array of method objects. Method object. FieldTypeDefaultDescription name. string. — Method name. signature. string. — Parameter signature (e.g. (self, x: int) -> bool). description. string. — Description of what the method does. isAsync. boolean. false. If true, displays an “async” badge next to the name. children. ReactNode. — Additional content (parameter tables, examples, etc.) rendered inside the accordion. Example. asyncconnect(self, address: str, timeout: float = 30.0) -> None. disconnect(self) -> None. asyncsend(self, data: bytes, compress: bool = True) -> int."},{"url":"docs/components/stat-strip/index.html","title":"StatStrip","content":"StatStrip. A row of big numbers with labels — the fastest way to make scale legible. Values are strings, so units and symbols render exactly as written. API. StatStrip. PropTypeDefaultDescription stats. { value, label, detail? }[] — One entry per number. Example. Project stats. 94. pages. generated per build. 40. components. 867. tests. green before every merge."},{"url":"docs/plugins/authoring/index.html","title":"Writing Plugins","content":"Writing Plugins. Extend the build pipeline with custom components, data, views, and hooks. Not available in this release. Project plugins are not in Folio 0.3. They return with the sidecar protocol: a plugin is a separate process, written in any language, that folio starts for the build and talks to over NDJSON JSON-RPC on stdio. This page documents the hooks and primitives that surface ships with; the built-in integrations already run on them. How Plugins Work. The plugin system is built on three concepts: Hook specifications define the extension points (what plugins can do). Hook implementations are the handlers a plugin provides for the hooks it needs. The plugin host inside folio calls those implementations at the right time during the build. During folio build, the host calls config_keys, configure, register_extensions, collect_docs, emit_assets, and post_build, in that order. During folio serve it also watches the directories a plugin names in watch_paths and calls on_watched_change for a change under one of them. Extension lifecycle. 1 Config keys. config_keys. Declare plugin-owned top-level docs.yaml keys before validation. 2 Configure. configure. Normalize raw plugin config and store data on config.extra. 3 Register UI. register_extensions. Register components, typed data modules, layouts, and generated views. 4 Collect docs. collect_docs. Add Markdown sources to Folio's normal page pipeline. 5 Emit assets. emit_assets. Write generated files into the prepared site before the frontend build. 6 Watch. watch_paths / on_watched_change. Name extra directories for folio serve to watch, and react to a change under them. 7 Post-build. post_build. Run after the static output directory is written. Available Hooks. These are the hooks called by the build pipeline. New plugins should use register_extensions for UI work and data-backed views. Every hook that reads configuration receives the resolved config: project (name, version, repo, url), output_dir, project_dir, and the extra map. configure gets it mutably, together with the raw docs.yaml mapping, and stores the plugin’s normalized section in extra under its own key; later hooks read it back from there. An error from configure, register_extensions, or collect_docs stops the build as Plugin '<name>' failed in hook '<hook>': <message>. An error from emit_assets, on_watched_change, or post_build becomes a warning with the same text plus (skipped), and the build goes on. Extension Model. Folio keeps customization simple by using four primitives: Component — a named React export that can be exposed to MDX pages or plugin views. Data — JSON-serializable values emitted as typed frontend modules. Layout — a route shell with named slots. Views must choose a layout. View — a route composed from a layout, slot blocks, components, and data. Plugins should prefer the extension registry over writing arbitrary files. Folio owns import generation, route placement, and build safety. config_keys. Build-wired. Declare top-level docs.yaml keys that belong to your plugin. Folio uses this before validation so plugin-owned configuration does not produce unknown-key warnings. Return the key names as a list; a plugin that owns no key returns an empty one. configure. Build-wired. Read plugin-owned configuration from the raw docs.yaml mapping and store normalized data on extra. This keeps plugin data out of the core config model while making it available to later build hooks. project_dir is set before this hook runs, so resolve relative paths from your plugin’s config against it rather than the process working directory. register_extensions. Build-wired. Register components, layouts, data modules, and views. This is the preferred hook for custom UI, plugin-owned routes, and typed frontend data. collect_docs. Build-wired. Return Markdown or MDX files that should become ordinary Folio documentation pages. Each document carries a source path and a clean relative route, for example reports/quality.md under the project directory published at reports/quality. Folio parses the source and puts it through the same page generation, sidebar, local-image copying, link validation, search, sitemap, Markdown mirror, llms.txt, and incremental manifest as files under source.docs. A route collision with a project document or another plugin fails before either page is written. Use emit_assets instead for files that should be served verbatim. An unlisted document keeps every surface except the sidebar: the page still compiles at its route and enters search, the sitemap, the Markdown mirror, and llms.txt, but never appears in the docs navigation — a folder holding only unlisted pages is hidden with it. Use it for plugin output that answers to a URL without belonging to the documentation’s table of contents. watch_paths and on_watched_change. Serve-wired. watch_paths returns directories folio serve should watch besides the sources; a path that is not an existing directory is ignored. on_watched_change receives the asset builder, the changed path, and whether it was added, modified, or deleted, and returns true when it handled the change. The plugin registry is built once when folio serve starts, so this hook can rewrite pages and assets, but data modules written by register_extensions refresh only on the next start. post_build. Build-wired. Run actions after the build completes; the hook receives the path of the generated output directory. Common uses include copying additional assets, generating sitemaps, or running link checkers. emit_assets. Build-wired. Write generated files into the prepared site before search, dependency checks, and the static build run. Use this for generated MDX pages or public assets. Prefer register_extensions for typed data modules and layout-backed views. The hook receives the asset builder, which offers these operations: write_page(route, content) — write an MDX page into the content directory. The route is recorded as a live page automatically, so a successful write_page needs no separate register_route call. page_exists(route) — report whether a page for the route is already on disk (for example, persisted from a prior warm build). read_page(route) / remove_page(route) — read or delete a page in the content directory. Together with a marker string embedded in your generated content, these carry the warm-build contract: refresh a page only when it still carries your marker, and never touch a user-authored page at the same route. list_pages(prefix) — routes of the pages currently on disk under a content-directory prefix. Use it when your plugin generates a variable set of pages: on a warm build, list your namespace, and remove the marker-tagged pages whose source of truth no longer exists. register_route(route) — record a route as a live page so the link checker treats internal links to it as valid. Call this for every page your plugin owns even when you skip write_page because the page persists from a prior build; otherwise links to your page are flagged as broken on warm builds. Only register a route once the page is guaranteed to exist on disk — write the page first, or confirm with page_exists — because a registered route with no page passes link checking and then 404s on the deployed site. read_meta(directory) / write_meta(directory, meta_json) — read and write the _meta.ts sidebar file for a content directory. read_meta returns the raw file text (an empty string when the file does not exist). Always merge your entry into the existing content instead of rewriting the file: _meta.ts files written by the sidebar generator contain nested object entries (folder titles, collapse state) that must be preserved. copy_static_asset(relative, source) — copy one source file into the workspace public/ directory at a containment-checked relative path so the static site serves it verbatim. remove_static_tree(relative) — remove an owned subtree below public/ before republishing it on a warm build. It refuses the public root and escaping paths. emitted_routes() — the set of routes recorded via write_page/register_route in this build. write_llm_files(llms_txt, llms_full_txt) — override the generated llms.txt / llms-full.txt files. Dividing Work Between the Build Hooks. Three hooks split a plugin’s output surface, and a plugin may use more than one: register_extensions owns everything typed and registry-managed: components, typed data modules, layouts, and views. Folio generates the imports, the TypeScript modules, and the view routes for you, and validates names and references at registration time. collect_docs owns Markdown and MDX source files: Folio turns them into normal documentation pages and includes every standard generated surface. emit_assets owns raw or programmatically generated files in the prepared site: verbatim public assets and pages that do not exist as Markdown source files. A typical shape: register_extensions registers a component and writes the typed data it renders; collect_docs contributes authored Markdown that uses it; emit_assets publishes any raw bundle the page links to. The roadmap integration pairs the first with a layout-backed view; the OpenAPI example below pairs it with programmatically generated pages. Official Example: Roadmap. The built-in roadmap integration shows the intended shape for plugin-owned data and generated routes. It is compiled into folio, and the roadmap: config key is what activates it — without the key the integration emits nothing: The integration declares roadmap as a config key, stores normalized data under extra[\"roadmap\"], emits lib/roadmap-data.ts and lib/roadmap-projects.ts, and can generate a standalone /roadmap/ route. The docs route (/docs/roadmap/) is for explanation; the public route (/roadmap/) is for displaying the same real data outside the docs shell. The implementation follows the four-primitives model: it registers the Roadmap component, writes typed roadmap data, and declares /roadmap/ as a folio.public layout-backed view. The dedicated-page contract. Plugins that need a standalone page should follow the grammar the roadmap view uses, so every plugin page feels like one product: Declare the view on the folio.public layout. Its band renders the Home link, optional sibling cross-links (a links prop), the title, and an optional description — and a narrow prop switches the page from full width to a centered document column. Open the content with the ViewHeaderRule component (mono micro-label, hairline, metadata or controls on the right) — the rule the roadmap’s “Releases” row uses. Cross-link a sibling view only after checking its route is actually enabled in extra, so a disabled integration never produces a dead link. Official Example: OpenAPI. The OpenAPI integration is the reference for the register_extensions / emit_assets division of labor. It is compiled into folio; its openapi: section activates it: Walking through its hooks in build order: config_keys declares openapi as a plugin-owned top-level key, so it does not trigger unknown-key warnings. configure loads and normalizes each spec and stores the result under extra[\"openapi\"]. Relative path values are resolved against the project directory, so builds work from any working directory, and a path that resolves outside the project directory fails the build. register_extensions registers the OpenApiReference component and writes a typed data module (lib/openapi-data.ts) whose hand-authored TypeScript interfaces type the exported constant. emit_assets writes one MDX page per source, merges a _meta.ts sidebar entry for it without disturbing the entries the sidebar generator wrote, and registers each route — even when the page already exists from a prior warm build — so internal links to the page stay valid. Registering components. For a single component file (or a directory of them), the components: key in docs.yaml is how a site adds its own React components — a plugin is for a component that needs data modules, layouts, views, or other hooks alongside it. A plugin registers a component from register_extensions with: name — the JSX name pages use. Must be a valid JavaScript identifier. import_path — the module path the generated import uses (for example @/components/__folio_components/interactive-demo). export_name — the export to import when it differs from name. expose_mdx — expose the component to MDX pages without imports (on by default). source_path — a component source file copied into the build. Relative paths are resolved against the project directory, not the process working directory. props — a mapping of prop names to TypeScript type strings. This is documentation metadata; it does not by itself validate props or publish the component anywhere. required — mark the component as part of the template contract: custom templates must wire it in their mdx-components.tsx, and template validation fails when it is missing. category — a free-form taxonomy label. It plays no role in the MDX component contract. contract / source_label — mark the component as part of the published MDX component contract (folioMdxComponents); source_label carries the contract source string. Contract membership is explicit: declaring props alone does not add a component to the contract. A flagged plugin or config component joins the builtins in both places the contract is published: the lib/folio-mdx-contract.ts module written for the template, and the /_folio/contract.json file in the exported site. After registering, a component is available in any MDX page without import statements: Name collisions and shadowing builtins. Registering a component whose name matches a Folio builtin (for example Callout or Timeline) is allowed: the config or plugin component shadows the builtin and the build emits a warning noting the override. Registering the same name twice from config or plugins is an error that names both origins. The published authoring contract. Every build writes /_folio/contract.json into the exported site. It answers, for this project, the three questions an agent otherwise has to guess at: which components a page may use, which top-level docs.yaml keys the project accepts, and which docs pages the build emitted. Source batches refresh this contract during folio serve. An unchanged payload keeps its file bytes and modification time; generatedAt records the last semantic contract change, rather than every save or build invocation. Abridged — a real build lists every component, every accepted key, and every route: components — the MDX component contract for this build: the Folio builtins plus every config or plugin component flagged as contract. The same set Folio writes into lib/folio-mdx-contract.ts for the template. configKeys — the core Folio keys unioned with the keys the active integrations claim through their config_keys hook. Any other top-level key warns when docs.yaml is read: plugins, i18n and versions as ignored in this release, anything else as an unknown key. routes — the docs URLs this build emitted, integration-owned pages included. instructions — one line, addressed to the reader: tolerate fields you do not know. Later Folio releases add fields, and a reader that rejects unknown ones breaks on the next upgrade. Read the payload defensively. mdxContractVersion versions the components list, and folioVersion moves with every release, so neither signals a change to the envelope itself. Treat unknown fields as additions rather than errors. The file is written during the build, from that build’s configuration and active integrations, and it describes the site published next to it. Read it as a snapshot of that build, not as a view of the repository as it stands now. Folio publishes it through the workspace public/ directory, which the Next static export carries through unchanged, so the path is the same on every deployment: https://<site>/_folio/contract.json. A plugin can publish a file the same way from emit_assets with copy_static_asset. Publishing a plugin. Add the topic folio-plugin to the public repository. A plugin catalog is on the roadmap, and that topic is the only signal it will use: no listing request, no review queue, no account. Nothing indexes the topic today, so adding it now costs one click and is the whole publishing step when the catalog arrives. Before you publish, read Trust & Safety. A plugin runs inside its users’ builds, and this publishing convention carries no vetting by design."},{"url":"docs/components/code-blocks/index.html","title":"Code Blocks","content":"Code Blocks. Fenced code blocks with syntax highlighting, line highlighting, line numbers, filenames, and word highlighting — powered by Shiki and rehype-pretty-code . Code blocks use standard Markdown fenced code block syntax with meta strings for additional features. Copy controls appear when readers hover over or focus a code block. API. All features are controlled via the meta string after the language identifier: FeatureSyntaxDescription Line highlighting. {2,4-6} Highlight specific lines. Commas separate individual lines, dashes define ranges. Line numbers. showLineNumbers. Display line numbers in the gutter. Filename. filename=\"app.py\" Display a filename header above the code block. Word highlighting. \"print\" Highlight all occurrences of a specific word or string. Disable copy. copy=false. Hide the hover and focus copy control for a specific block. Features can be combined in any order on the same code block. Example. Line highlighting. Highlight specific lines with {lines} after the language: Line numbers. Add showLineNumbers to display line numbers: Filename. Add filename=\"...\" to show a file header: Word highlighting. Highlight specific words with quoted strings: Combined features. All meta features work together:"},{"url":"docs/configuration/index.html","title":"Configuration","content":"Configuration. Complete reference for every option in your docs.yaml file. Quick Start. Start with the project name and at least one source input. Folio needs source paths or Markdown docs to generate pages: Minimum docs.yaml. This is enough for Folio to scan source, write _site/, and enable the default docs theme. project.name The product name shown in navigation, metadata, and generated pages. string. source.python.paths Python packages or modules to scan. list[string] source.docs Markdown guide directories to include. list[string] This scans your Python package and Markdown guides, outputs to _site/, and enables dark mode. Full Reference. Here is every available section and field. project. Project metadata shown in the navbar, page titles, and generated output. FieldTypeDefaultDescription name. string. \"Untitled\" Project name displayed in the navbar and page titles. If missing or empty, a warning is emitted and \"Untitled\" is used. version. string. \"0.0.0\" Version string shown in the docs header. Quote it (version: \"1.0\"): an unquoted 1.0 is a YAML number, not a string, and fails the load. repo. string. \"\" URL to the source repository (e.g. GitHub). Used for source links, the navbar repository link, the “Question? Give us feedback” link on docs pages (it opens an issue there) and project metadata. Without it, none of these links is shown. repo_ref. string. \"main\" Branch, tag, or commit used for generated source links. url. string. \"\" Public site URL for sitemap, canonical metadata, and social previews. It does not control local routing or static asset paths. source. Controls where folio looks for source code and documentation files. source.python. The python key can be either a mapping (recommended) or a simple list of paths. Mapping form (recommended): FieldTypeDefaultDescription paths. list[string] [] Python package directories, or a conventional src/ import root. Each path is resolved relative to the project directory. exclude. list[string] [] Glob patterns or directory paths to exclude from documentation. Useful for skipping tests, vendored code, or internal modules. docstring_style. string. \"auto\" Docstring format to parse. Options: \"auto\", \"google\", \"numpy\". An unknown value warns and falls back to Google. source.python. Use the mapping form when you need exclusions or a specific docstring parser. paths Python source directories to scan. list[string] exclude Glob patterns or directories to skip. list[string]default: [] docstring_style Docstring parser: auto, google, or numpy. stringdefault: auto. When src/ itself is listed and is not a Python package, Folio treats it as an import root. A file such as src/tools.py publishes as tools, and src/my_library/client.py publishes as my_library.client; Folio does not add a synthetic src. prefix. A path that names a package directly, such as src/my_library, continues to use that directory as the package name. List form (shorthand): You can also pass a plain list of paths. In this case, no exclude patterns are applied: source.javascript. FieldTypeDefaultDescription paths. list[string] required. Directories with .js, .mjs or .cjs sources. Each path is resolved relative to the project directory. exclude. list[string] [] Glob patterns or file/directory paths to skip. Folio names a module after its path (utils/format.js is utils.format, and an index.js takes its directory’s name), reads the JSDoc comment above each export, and publishes what the file exports under api-reference/javascript/. node_modules is never read, and a .jsx file is skipped with a warning. source.rust. FieldTypeDefaultDescription paths. list[string] required. Crate directories, a workspace holding them, or a crate’s src/. Each path is resolved relative to the project directory. exclude. list[string] [] Glob patterns or file/directory paths to skip. Folio reads each crate’s Cargo.toml for its name, follows the pub mod declarations from lib.rs or main.rs, and publishes the public items under api-reference/rust/. source.docs. FieldTypeDefaultDescription docs. list[string] [] Directories containing Markdown (.md) documentation pages. These are converted to MDX and included in the site alongside the API reference. .rst files are migration inputs, not build inputs. Convert them to Markdown before placing them in source.docs; Folio warns when .rst files are present in a docs source directory. deploy. Controls deployment-specific path handling. Most sites can omit this section. Use it when the static site is published under a subpath such as /my-repo. Base path priority is: FOLIO_BASE_PATH environment variable. deploy.base_path in docs.yaml. GitHub Pages inference when deploy.provider: \"github-pages\" or FOLIO_DEPLOY_PROVIDER=github-pages is active in GitHub Actions. No base path. folio serve stays rooted at / unless FOLIO_BASE_PATH is explicitly set. FieldTypeDefaultDescription provider. \"github-pages\" | string. \"\" Enables provider-specific inference. For GitHub Pages project sites, Folio infers /{repo} from GITHUB_REPOSITORY; user or organization pages like owner.github.io stay at /. base_path. string. \"\" Explicit static asset base path such as \"/docs\" or \"/my-repo\". Use \"/\" for a root deployment. For custom domains or reverse proxies, set the base path explicitly: versions and i18n. Not available in this release; ignored with a warning. A project can keep either key in docs.yaml: the build warns and carries on without it. output. FieldTypeDefaultDescription output. string. \"_site\" Directory where the generated documentation site is written. Resolved relative to the project directory. Removed by folio clean. Folio keeps an incremental manifest in .build/. The manifest includes source hashes plus config, template, and generator fingerprints, so changing docs.yaml or the generator invalidates stale generated pages automatically. public. Repository files served verbatim from the site root, such as an installer script or a file that needs a stable URL. FieldTypeDefaultDescription public. list[string] [] Project-relative files copied to the root of the built site under their own file name. folio serve serves them from / too. A file that does not exist fails the build with public file not found: <absolute path>. A path outside the project directory fails with public must stay within the project directory; a path under .build/ or the output directory fails with public cannot point inside the .build directory or public cannot point inside the output directory. Folio’s own site serves install.sh this way. theme. Controls the visual appearance of the generated documentation site. FieldTypeDefaultDescription preset. string. \"organic-editorial\" Default visual preset for the generated site. folio init offers organic-editorial, beacon, atlas, and workshop; it may also be any built-in id, one a theme package or overlay declares, or a new id that theme.name, theme.tokens or theme.style defines. Anything else stops the build and names the nearest id. dark_mode. bool. true. Enable the dark mode toggle. When enabled, users can press d to switch between light and dark themes; false keeps the site light, with no mode controls, toggles or d shortcut, and theme.header.theme_toggle then warns. Must be a YAML boolean; an explicit null is an error. logo. string. \"\" Path to a logo image shown in the docs navbar beside the project name, under deploy.base_path. Resolved relative to the project directory; a missing file stops the build. favicon. string. \"\" Path to a favicon file. Resolved relative to the project directory. name. string. \"\" Optional project preset display name. When set with project theme data, Folio adds a Project group to the configurator. description. string. \"\" Optional project preset description. preview. mapping. {} Optional light and dark swatch colors for the project preset preview. radius. string. \"\" Default radius choice for the theme configurator. Must be one of \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\", or a named alias (\"none\", \"sm\", \"md\", \"lg\", \"full\"); any other value fails config validation. tune. mapping. {} Default configurator choices such as font, accent, surface, width, rhythm, borders, code blocks, and radius. style. mapping. {} Safe layout and typography CSS custom property overrides. tokens. mapping. {} Safe light/dark CSS variable overrides for shadcn tokens and project tokens. header. mapping. {} Docs header brand, badge, repository link, search visibility, theme toggle, and project action. variants. mapping. {} Project-owned configurator controls with options, swatches, previews, style overrides, and token overrides. Capped at 256 option combinations across all controls (the product of each control’s option count); larger products fail config validation. package. string | mapping. \"\" Theme package copied over the bundled template before generated content and metadata are injected. A string is a directory in the project. A mapping (git, rev, digest, optional path) fetches a published package and verifies its tree against the digest. See Theme Packages. See Theming for the ownership model, safe personalization options, theme packages, and custom templates. template. Expert escape hatch for replacing the bundled Folio frontend with a project-owned template. Omit this section to use the default Folio template. FieldTypeDefaultDescription path. string. \"\" Local Next/Nextra-compatible template directory. Resolved relative to the project directory. Must stay inside the project and cannot point at .build/ or the output directory. overlay_path. string. \"\" Directory of files layered on top of the bundled template (overlay files win; missing files fall back to the bundled template). Mutually exclusive with path: if both are set, path wins and the overlay is ignored with a warning. Held to the same location guards as path. docs_route_base. string. \"/docs\" Public route where generated documentation pages are served. Folio rewrites generated links, search URLs, sitemap entries, canonical metadata, LLM output, and the copied Next.js docs route to this path. params. mapping. {} Arbitrary JSON-serializable values exposed to the template as build-time data. Folio does not interpret these values. Custom templates own layout, CSS, JavaScript, package dependencies, routing chrome, search UI, and the meaning of template.params. Folio still owns the generated MDX content, _meta.ts files, search index, Markdown exports, and static export pipeline. See Custom Templates for the full contract and required file structure. search. Controls the built-in full-text search powered by Pagefind . Search is enabled by default, appears in the docs navbar, and opens with Cmd+K on macOS or Ctrl+K on Windows/Linux. FieldTypeDefaultDescription enabled. bool. true. Enable or disable the navbar search. Set to false to hide search entirely. Must be a YAML boolean; an explicit null is an error. placeholder. string. \"\" Custom placeholder text for the search input. When empty, the default “Search documentation…” is used. To disable search completely: nav. An ordered list of strings defining the top-level sidebar sections. Guide keeps all authored pages together in their normal order. API Reference and Source Code both place the generated source tree, which keeps the stable /api-reference/ route. Other labels order a matching top-level page or folder; unknown labels are ignored rather than creating dead routes. FieldTypeDefaultDescription nav. list[string] [] Top-level navigation sections. sidebar. Controls generated sidebar section behavior. Generated groups start collapsed by default (sidebar.default_collapsed: true). Set default_collapsed: false if you want generated groups to start expanded. FieldTypeDefaultDescription default_collapsed. bool. true. Render generated folder entries with Nextra’s open: false state so sidebar sections start collapsed. Leaf pages are unchanged. Set to false to expand generated groups. Must be a YAML boolean; an explicit null is an error. By default, Folio emits object entries in generated _meta.ts files for folders: This applies to generated guide folders and API reference folder groups. It does not change the route for folder index pages, and it does not collapse individual leaf pages. llm. Controls generation of llms.txt files for AI consumption. FieldTypeDefaultDescription generate_llms_txt. bool. true. Generate an llms.txt summary file in the output directory. This is a condensed index of your documentation for LLMs. generate_llms_full_txt. bool. true. Generate an llms-full.txt file containing the full text of all documentation pages. Both keys must be YAML booleans; an explicit null is an error. Every page is also written as plain Markdown under _folio/markdown/, linked from the page head as a text/markdown alternate and listed in the sitemap. These mirrors are lossy on purpose: complex component props, such as an entire data table, do not survive. Prose, headings, lists, code blocks, Mermaid source, component children, and useful labels from simple cards do. Treat a mirror as the page’s portable text, not as a pixel-equivalent copy of the page. components. Every entry resolves relative to the project directory and must stay inside it, directory or spec from/path alike: a component is trusted frontend code, held to the rule theme.package already obeys. Custom React components to include in the generated site. Registered components are copied into Folio’s generated component namespace and exposed to MDX without requiring manual imports in Markdown pages. This is how a site adds its own components. Each list entry is either a directory path or a named component spec: Entry formTypeDescription Directory. string. Path to a directory, relative to the project directory. Every top-level .tsx/.jsx file in it is registered as a component named after its PascalCased file stem (hero.tsx → Hero, my-chart.tsx → MyChart). Each file must have a named export matching the derived name. Named spec. mapping. A single component file with an explicit name, source path, and options. Named spec fields: FieldTypeDescription name. string. JSX name exposed to MDX. from. string. Path to the .tsx or .jsx component file, relative to the project directory. export. string. Export name in the source file. Defaults to name. expose.mdx. bool. Make the component available in docs MDX. Defaults to true. Validation is loud: a directory that does not exist fails the build, a directory without any .tsx/.jsx files produces a warning, and a from: file that does not exist fails the build when components are copied into the workspace. If two source files share the same filename stem, Folio generates distinct component import paths automatically. A malformed value — a non-list value (components: \"docs/components\" must be components: [\"docs/components\"]), or a list entry that is neither a path string nor a {name, from} mapping — fails config loading for every command (folio build, folio serve, …). A component whose name matches a Folio builtin (for example Callout) replaces the builtin and emits a warning; two components: entries (or a config entry and a component of a built-in plugin) with the same name raise an error. Built-in integrations. The landing page, the roadmap and OpenAPI ship in the folio binary; Plugins covers each. Each one activates through its own top-level section in docs.yaml and stays inert without it; there is no plugins: list. Project plugins loaded from your own repository: Not available in this release. Path Resolution. In CLI commands, relative paths in docs.yaml are resolved from the active project directory. When you run folio build /path/to/project or folio build --project-dir /path/to/project, that directory becomes the base for config paths. For example, given this structure: The config source.python.paths: [\"src/my_lib\"] resolves to /path/to/my-project/src/my_lib. Config Validation. folio validates your config file. A few problems stop the load; everything else is a warning, and the build proceeds with defaults where possible: Duplicate keys anywhere in the file fail the load. Typed keys are checked. String keys such as project.version must be quoted YAML strings (version: \"1.0\"); boolean keys (theme.dark_mode, llm.*, search.enabled, sidebar.default_collapsed) must be YAML booleans. An explicit null on a typed key is an error. Missing config file stops the command with an error naming the path that was expected. Unknown top-level keys produce a warning listing the unrecognized keys. Core top-level keys are: project, source, output, public, theme, template, nav, sidebar, llm, search, components, and deploy, plus the sections of the built-in integrations: landing, roadmap, and openapi. versions and i18n are recognised and ignored with a warning. Unknown keys inside a core section (project, source, theme, template, llm, deploy, sidebar, search, components) produce a warning too, naming the nearest valid key when one is close: Unknown project keys in docs.yaml: vesion (did you mean 'version'?). The landing, roadmap and openapi sections are not checked in this release: a typo there is ignored without a warning. An unknown source.python.docstring_style warns and falls back to Google. The plugins: key warns with exactly project plugins are not available in this release; the plugins key is ignored. The configKeys list in the authoring contract (/_folio/contract.json) never includes plugins. Missing or empty project.name produces a warning and defaults to \"Untitled\". Common Patterns. Monorepo Setup. When your Python package lives in a subdirectory of a larger repository: Excluding Test Files. Keep test files out of your API reference: Custom Output Directory. Write the built site to a specific location for CI/CD deployment: Multiple Python Source Directories. Document code spread across multiple directories: Minimal Docs-Only Site. If you just want to serve Markdown documentation without Python API reference:"},{"url":"docs/components/hook-map/index.html","title":"HookMap","content":"HookMap. Visualize an extension lifecycle with stage names, hook names, and short descriptions. API. PropTypeDefaultDescription title. string. \"Extension lifecycle\" Panel title. hooks. HookMapItem[] — Ordered lifecycle rows. HookMapItem supports stage, hook, and description. Example. Extension lifecycle. 1 Lint. run_linters. Check formatting and static analysis before tests. 2 Test. run_tests. Execute the project test suite with coverage enabled. 3 Build. build_artifacts. Build distributable artifacts from the verified sources. 4 Deploy. publish_site. Upload the built output to the hosting provider."},{"url":"docs/architecture/index.html","title":"Architecture","content":"Architecture. Folio Docs has one local build pipeline. The CLI reads configuration and parses project sources once, then emits the human site and its agent-readable mirrors. The pipeline runs inside the folio binary. Its implementation lives in the Rust crates under crates/. Runtime Flow. flowchart LR CLI[\"▸ CLI: build / serve\"] --> Config[\"▣ docs.yaml\"] Config --> Sources[\"▤ Source + Markdown\"] Sources --> Parse[\"⚙ Parse\"] Parse --> IRNode[\"◇ IR objects\"] IRNode --> Docs[\"▥ folio-docs\"] Extensions[\"⊕ Built-in plugins\"] --> Docs Template[\"▧ Nextra template\"] --> Docs Docs --> Site[\"✓ HTML site + search\"] Docs --> Context[\"✓ Markdown + llms.txt + contract\"] folio-cli is the CLI host: it handles arguments and runs the build pipeline. The folio binary is crates/folio-cli/src/bin/folio.rs, a main that calls folio_cli::run and nothing else. folio-config loads docs.yaml and resolves project paths. One language crate per language profile parses the configured sources into folio-ir objects (ModuleIR, ClassIR, FunctionIR, TypeIR); folio-lang-python, folio-lang-javascript and folio-lang-rust are the profiles that ship today. The Python reader parses docstrings with the parsers in folio-ir: auto, the default, detects reStructuredText, Google, NumPy and epydoc per docstring, and source.python.docstring_style pins Google or NumPy. folio-docs turns the IR into MDX pages and the API reference overview, and computes routes, cross-references, coverage and the llms.txt text. folio-mdx reads the Markdown guides and turns them into MDX, and turns MDX back into the Markdown mirrors. folio-site owns the site workspace, sidebar, metadata, themes, search data, link checker and Next runtime, and writes the pages, mirrors and LLM files. folio-watch watches sources and invalidates the retained build for folio serve. folio-plugins holds the component manifest, the authoring contract and the built-in plugins (landing, roadmap, OpenAPI). The Nextra template ships inside the binary and is materialised once under the user cache directory ($XDG_CACHE_HOME/folio/template/<version>-<hash> or the platform equivalent); FOLIO_TEMPLATE_DIR overrides it. Crate boundaries. crates/* owns the engine and the CLI host: eleven crates, one per boundary. folio-cli carries the host as a library and builds the folio binary from its own src/bin/folio.rs. The root Cargo workspace coordinates the packages without owning an engine. Folio Docs internals. folio-site splits build-environment work into smaller steps: copies the bundled template from the cache directory (or FOLIO_TEMPLATE_DIR) into .build/ and removes bundled demo content. replaces template placeholders with docs.yaml values. writes plugin components, typed data modules, and generated views. installs dependencies, runs Next.js, serves development mode, and copies static output. rewrites exported links so the static site works from file://. Reading the site as an agent. The same build emits the pages a person reads and the text an agent fetches, from one pass over the sources: llms.txt at the site root indexes every published page and llms-full.txt carries all of them as Markdown, both following the llmstxt.org spec. The llm: section switches them off (Configuration). _folio/markdown/<route>.md mirrors each page, and every HTML page declares its mirror as a text/markdown alternate and lists it in the sitemap. Fetch the mirror instead of scraping the HTML. _folio/contract.json lists the core MDX components every template wires, with their props, the top-level docs.yaml keys the binary recognises, and every route the build emitted: the authoring surface, as the binary that wrote the site sees it. Two invariants hold for anything reading or writing a Folio project. Folio parses source and never executes it, so a package needs no install, no runtime dependency and no import stub for its reference to build. And folio --help is the authority on commands and flags, Configuration on docs.yaml keys, and the component catalogue on components; a flag or key that appears in neither folio --help nor Configuration does not exist. Extension boundary. The only plugins in this release are the built-in ones. Project plugins: Not available in this release. Plugin internals stay isolated from source parsing and theme implementation details. A plugin contributes documents and components to the site while inheriting every generated surface: a contributed page also receives a Markdown mirror and appears in the configured LLM indexes."},{"url":"docs/components/preview-code/index.html","title":"PreviewCode","content":"PreviewCode. Pair a rendered component example with its MDX source. The Code tab shows the fenced block you write beside the preview, not source read back from the rendered part, so keep the two in step by hand: nothing checks that they match. Use PreviewCode for component catalog examples so readers can inspect the result first and switch to the code without scrolling through a duplicated section. API. PreviewCode. PropTypeDefaultDescription title. string. — Optional heading shown above the preview/code switcher. description. string. — Optional short context for the example. defaultMode. \"preview\" | \"code\" \"preview\" Initial tab. children. ReactNode. — Include one fenced code block plus the rendered preview content. Example. Callout example. The first fenced block becomes the Code tab. The remaining children become the Preview tab. Use real output. Prefer generated pages, command output, and source snippets over abstract placeholders."},{"url":"docs/components/compare-matrix/index.html","title":"CompareMatrix","content":"CompareMatrix. A comparison matrix with check / dash / partial cells and a highlighted “us” column. Use it where the honest answer is a grid, not three paragraphs of prose. API. CompareMatrix. PropTypeDefaultDescription tools. string[] — Column headers. rows. { feature, values, note? }[] — One row per capability; values are true (check), false (dash), \"~\" (partial), or free text. caption. string. — Label above the feature column. highlight. number. 0. Index of the emphasized column. Example. Tool comparison. CapabilityEspresso cartChain caféVending machine Knows your order. ~ — Open at 6am. — Latte artthe swan is negotiable. ~ —"},{"url":"docs/components/steps/index.html","title":"Steps","content":"Steps. Numbered step-by-step instructions with a vertical timeline. Steps are numbered automatically using CSS counters — you do not provide numbers manually. Use Steps for tutorials, setup guides, and any sequential process. API. Steps. PropTypeDefaultDescription children. ReactNode. — One or more <Step> elements. Step. PropTypeDefaultDescription title. string. — The step heading. children. ReactNode. — The step content (text, code, components, etc.). Example. Install the package. Run uv add my-lib to install. Create a config file. Run my-lib init to generate a default configuration. Start the server. Run my-lib serve to start the development server."},{"url":"docs/deployment/github-pages/index.html","title":"GitHub Pages","content":"GitHub Pages. GitHub Pages serves static files. Folio’s Pages integration publishes the same _site/ artifact used by other hosts, with extra handling for project-site base paths and branch previews. Base Path Setup. Set project.url in docs.yaml to the final Pages URL before deploying so sitemap and metadata use the public URL. For GitHub Pages builds, set FOLIO_DEPLOY_PROVIDER=github-pages in the build step or add deploy.provider: \"github-pages\" to docs.yaml. Folio then infers /repo-name for project pages from GITHUB_REPOSITORY, while user and organization pages such as owner.github.io stay at /. Use FOLIO_BASE_PATH or deploy.base_path only when you need an explicit override. Production Deploys. Build your docs. Publish the static site. Upload _site/ as the GitHub Pages artifact. Automate with GitHub Actions. Use the workflow in CI/CD when Pages should deploy on push. Branch Previews. folio init creates a second GitHub Actions workflow for branch previews on the same GitHub Pages site. It runs from pull_request_target: PR branch code is built in an unprivileged job, and the privileged deploy job only consumes the static _site/ artifact. The preview appears under /previews/pr-<number>-<branch>/, for example https://owner.github.io/project/previews/pr-17-docs-redesign/. GitHub Pages publishes one artifact for the whole site. Folio keeps production and branch previews together in that single artifact: A main deploy builds production docs at the site root. It restores existing preview folders from the internal folio-pages-state branch. A branch deploy starts from the saved folio-pages-state root, downloads the static preview artifact, replaces only /previews/pr-<number>-<branch>/, and deploys the complete artifact again. That means the production URL stays stable while PR previews are updated independently. The preview index is available at /previews/, and each deploy prints the preview URL and index URL in the Actions summary before verifying both return HTTP 200. Successful preview deploys also create or update one sticky PR comment with the latest preview link, so reviewers do not need to open the Actions run to find it. State Branch. The production workflow and preview workflow cooperate through an internal folio-pages-state branch. This branch stores the last complete Pages artifact so a preview deploy can replace one preview folder without dropping production docs or other previews. Do not edit the state branch manually unless you are intentionally repairing the published Pages artifact."},{"url":"docs/quickstart/index.html","title":"Quick Start","content":"Quick Start. Go from an existing repository to searchable documentation in a few minutes. Folio Docs reads your source and guides without running any of your code. It is one native binary, so your project does not need Python; folio build and folio serve need Node.js 20.19+ and pnpm 10. See Installation for manual installs and troubleshooting. Install Folio. Install the folio binary once with the installer script. Initialize your repository. Let Folio detect the package and create a small docs.yaml. Preview the result. Open the generated site and inspect its HTML and agent-readable outputs. Build the static site. Export plain files that can be deployed anywhere. 1. Install Folio. 2. Initialize your repository. Run the wizard from the project you want to document: folio init detects Python, JavaScript, and Rust projects, reads their metadata and source directories, then writes docs.yaml; the CLI Reference lists what it detects. A minimal configuration for a Python package looks like this: docs.yaml. Folio reads these paths; it never imports or executes the package. project.name Name shown in the generated site. string. source.python.paths Python packages scanned for public symbols. list[string] source.docs Markdown directories published as guides. list[string] Only keep source paths that exist. A JavaScript or Rust project uses source.javascript or source.rust instead; Languages covers each reader. The configuration reference covers exclusions, docstring formats, the built-in integrations, and theming. 3. Preview the result. Open http://localhost:4321. The development server watches the source files of every configured language and the Markdown guides, and rebuilds changed pages; restart it after editing docs.yaml. Its public root also exposes: /llms.txt, the compact project index; /llms-full.txt, the expanded text export; /_folio/markdown/, the per-page Markdown mirrors. Use folio serve --verbose when a module or page is missing. 4. Build the static site. Generated output. The workspace is disposable; the static output is the artifact you deploy. .build/workspace Generated frontend workspace and development cache. _site/static Deployable HTML, assets, search, and agent outputs. _site/llms.txtllm Compact project context for agents. _site/_folio/markdown/llm One discoverable Markdown mirror per page. Deploy _site/ with the deployment guide. Next steps. Writing doc comments. Components. Plugins."},{"url":"docs/components/class-overview/index.html","title":"ClassOverview","content":"ClassOverview. A card displaying a class with its name, base classes, decorators, and description. Every Python and JavaScript class on a generated API page opens with one, and the page writes the class docstring below the card rather than in description. You can also use it by hand for class documentation you write yourself. API. ClassOverview. PropTypeDefaultDescription name. string. — The class name. bases. (string | { name: string, href?: string })[] [] List of base classes. Each entry can be a plain string or an object with name and optional href for linking to the base class docs. decorators. string[] [] List of decorator names (displayed with @ prefix). description. string. — A short description of the class. Example. Basic usage with string base classes: @dataclass. class FederatedLearner(BaseNode, Serializable). A node that participates in federated learning rounds. With linked base classes: @dataclass@deprecated. class FederatedLearner(BaseNode, Serializable). A node that participates in federated learning rounds."},{"url":"docs/theming/index.html","title":"Theming","content":"Theming. Folio has one theming model with three ownership levels. Start with the bundled template, move to a theme package when a project needs exact visual control, and use a custom template only when the documentation must live inside a fully project-owned frontend. Choose an Ownership Level. LevelConfigure withUse whenWho owns the frontend Theme personalization. theme.preset, theme.tune, theme.tokens, theme.header, theme.variants. The bundled Folio docs shell is right, but the brand, colors, typography, spacing, or header need tuning. Folio owns the template; your project owns safe theme data. Theme package. theme.package. The docs should still use Folio’s bundled template as a base, but a project needs to override files such as layouts, CSS, the configurator, or project theme code. Folio owns generated content; the package overlays selected template files. Custom template. template.path. The docs must run inside a product-specific Next/Nextra frontend with its own routes, dependencies, chrome, search UI, or application layout. The custom template owns the frontend workspace. Personalize a Theme. Set presets, defaults, tokens, variants, header actions, logo, favicon, and dark mode from docs.yaml. Build a Theme Package. Overlay files on the bundled template while Folio still injects generated content and metadata. Use a Custom Template. Bring a full Next/Nextra frontend and consume Folio's generated MDX, metadata, and component contract. How Folio Applies Theme Configuration. flowchart TD Config[\"docs.yaml\"] --> Theme[\"theme.*\"] Config --> Template[\"template.*\"] Theme --> Preset[\"Bundled preset and safe project preset\"] Theme --> Package{\"theme.package?\"} Package -->|No| Bundled[\"Bundled Folio template\"] Package -->|Yes| Overlay[\"Copy package over bundled template\"] Preset --> Bundled Preset --> Overlay Template --> Custom{\"template.path?\"} Custom -->|No| Build[\"Prepare generated docs workspace\"] Custom -->|Yes| TemplateWorkspace[\"Copy project-owned template\"] Bundled --> Build Overlay --> Build TemplateWorkspace --> Build Build --> Site[\"Static documentation site\"] Folio always owns the generated documentation data: parsed API pages, converted Markdown, _meta.ts, search metadata, LLM outputs, and static export. The theming choice controls how much of the presentation layer your project owns. Recommended Path. Start with theme.preset, theme.tune, logo, and favicon. Add project tokens, header configuration, and variants if the bundled shell is still the right product experience. Move to theme.package when the project needs exact control over selected template files but still wants Folio’s bundled template as a base. Move to template.path when the documentation frontend is a product workspace, not just a styled Folio docs site. Related Reference. Configuration documents the complete docs.yaml shape. ThemeConfigurator documents the runtime drawer, built-in preset controls, and the underlying preset TypeScript contract. Components lists the MDX components that generated docs and custom templates may need to expose."},{"url":"docs/troubleshooting/index.html","title":"Troubleshooting","content":"Troubleshooting. Match the message, then read the cause. Everything below is something the folio binary prints. Match the message first: the build names the step it failed on and the file it was reading, so the message usually is the diagnosis. Environment check failed. The message names Node.js, pnpm, or both. folio build and folio serve render through the bundled Next.js template, so both are checked before any work starts and a stale toolchain fails in the first seconds instead of minutes in: See Installation for the prerequisites and the bundled template. Config file not found. The message ends with the path Folio looked for. The command ran outside the project directory, or the config has a different name. Any of these resolves it: A module, class, or function is missing from the API reference. Run the build with --verbose: it prints the source roots it scanned and every page it wrote. The usual cause is source.<language>.paths not covering the package, or source.<language>.exclude matching more than intended. Check both against Configuration, and Languages for what each reader reads. A symbol that is present but empty is a missing docstring, which folio coverage --verbose lists one by one. Unknown config keys in docs.yaml. Loading warns about every key a core section does not have (the top level, project, source, theme, template, llm, deploy, sidebar, search and components), names the nearest valid key when one is close, and continues with defaults: Unknown project keys in docs.yaml: vesion (did you mean 'version'?). The landing, roadmap and openapi sections are not checked in this release, so a typo there is ignored without a warning. Check the key and its nesting against Configuration before reporting a bug. Pages look stale after editing the config or the template. Builds are incremental against a hash manifest in .build/. The manifest covers source hashes plus config, template, and generator fingerprints, so most edits invalidate what they should. Force the full rebuild when one slips through: Port 4321 is already in use. The message names the port and the way out. Pick another one, or stop the process holding it: Broken internal links. The build reports them as warnings on the Links step, one line per link: <page>:<line> → <target>. The page is the one holding the link, the target is the link as it appears in the generated page (a relative link shows as its resolved /docs/ route), and the line counts in the generated .build/content/<page>.mdx, not in the source file. They do not stop the build. Relative links between guide pages (./configuration) survive the static export; absolute paths into the site root do not always. The static export fails at the end of a build. The complete export log is printed once in the build output panel and saved to .build/.folio-build.log. Read that file before guessing: the underlying error comes from Next.js and the log has the stack it printed. Something else. The CLI Reference is the authority on commands, flags, and exit codes, and Configuration on what docs.yaml accepts. When neither explains it, open an issue with the command you ran and the output."},{"url":"docs/why-folio/index.html","title":"Why Folio","content":"Why Folio. The questions that decide whether Folio is for you — answered honestly. “ Why Folio exists. Documentation rots because it lives apart from the code. Folio generates your docs from your source — the reference rebuilds from the current code, and the site it ships in is one you’re proud to put your project’s name on. That’s the whole pitch: you keep writing docstrings and markdown, Folio turns them into a modern site with a generated API reference, instant search, and theming down to the last token. One command against your codebase produces: 1. docs.yaml. the entire configuration. 0. hand-written API pages. the reference is parsed from source. 30+ components. typed vocabulary for rich pages. 0. servers. static output, deploy anywhere. Folio against the field. CapabilityFoliopdocSphinxMkDocsMintlifyGitBook Python API. ~ — — Guides. ~ Static export. — — LLM friendly. — — — ExtensibilityFolio: your own components and templates; project plugins: Not available in this release. ~ ~ ~ ~ Open source. — — Git + CI. ~ ~ ~ And the capabilities the standard matrix doesn’t capture: CapabilityFolioSphinx / MkDocsDocusaurus / NextraMintlify Agent artifacts (llms.txt + machine output)llms.txt, llms-full.txt, Markdown mirrors, the authoring contract. — ~ Theming down to full template ownership. ~ — Versioned docsFolio: Not available in this release. — Twenty years of ecosystem. — ~ — The last row is the honest one: Sphinx’s extension depth is real, and if your docs depend on exotic extensions, evaluate Folio on a branch first. Folio’s structural differentiator is one source model feeding both the human site and the machine artifacts. Choosing a docs tool. Why Folio instead of Sphinx or MkDocs Material? Same job — parse Python source into reference docs — with a different output ceiling: a modern Next.js/shadcn site, instant search, theming to full template ownership, and agent-readable artifacts, from one docs.yaml. The trade: Sphinx’s extension ecosystem is twenty years deep, and Folio’s static analysis doesn’t cover everything (see the SWOT below — we put it in writing). Why not Docusaurus, Nextra, or Fumadocs? Excellent site frameworks for hand-written pages. Python API reference needs a separate integration; Folio reads it from source and supplies the site as one workflow (built on Nextra internally). Why not Mintlify or GitBook? Hosted products, per-seat pricing, and they ingest prose, not source. Folio is a build tool: static output you deploy anywhere (GitHub Pages workflow included), no vendor in the serving path, and the reference is generated from code rather than scraped from markdown you maintain by hand. I'm migrating from Sphinx. What doesn't convert? Being honest: Folio’s parser is static AST analysis, and it never imports your package. Inherited members and runtime-generated APIs are not covered. Intersphinx, Sphinx cross-links, and a one-command importer from MkDocs, Mintlify, or Read the Docs with a report of what didn’t convert: Not available in this release; the roadmap’s 0.7 Migrations phase lists them. Migrating from Sphinx walks the manual move. Why not just have an LLM emit the HTML? Because docs have exactly one non-negotiable property — being true — and that’s a provenance problem, not a fluency problem. Six concrete reasons, each one structural: 1 · Ground truth beats plausible text An LLM writing HTML documents what it believes your API is. Folio reads signatures, types, and defaults from the source AST instead of asking a model to invent them. Static analysis has limits, but provenance stays inspectable. 2 · A snapshot vs a function Emitted HTML rots on the first commit that changes your code. Folio is f(source, markdown) → site: re-run on every push, the reference updates itself, and every internal link is checked, with the broken ones named in the build output. Regenerating HTML re-rolls the hallucination dice on content that didn’t change — and bills you tokens for markup boilerplate. 3 · Reviewable diffs are the safety rail When an agent updates the docs here, the diff is a few lines of Markdown or one key in docs.yaml, and a human reviews it in seconds. If agents regenerated HTML, the diff would be megabytes of unreviewable markup. Small semantic sources of truth are the only real safety rail in human–agent collaboration. 4 · A site needs a linker A whole site needs shared navigation, one search index, cross-references that resolve, social cards. LLMs emit locally plausible pages that don’t cohere globally; Folio provides the global invariants: xref resolution, a link checker that names every broken internal link, generated sidebars. 5 · Constrained generation appreciates over time In raw HTML every div soup is “valid” — infinite ways to be wrong. Writing Folio markdown, an agent picks from a typed component vocabulary and the build rejects what doesn’t exist. And the same markdown renders better over time as the template improves: HTML artifacts depreciate, Folio artifacts appreciate. 6 · The next reader is an agent Emitted HTML is the worst format for other agents — it forces lossy scraping. Folio keeps the semantic source as the artifact and emits every output from one pass: HTML for people, markdown mirrors and machine-readable output for agents. “ That question, compressed. LLMs write better assembly every month — that’s exactly why you want a build step you can trust. Markdown plus typed components is the high-level language; Folio does the generating, the linking, and the checking. The honest SWOT. Folio, mid-2026. internal ↑ · external ↓ S Strengths. Typed source IR — signatures come from source. One pass emits the human site AND the agent artifacts. Built-in integrations in the binary — landing, roadmap, OpenAPI. Theming from tokens to full template ownership. W Weaknesses. Young ecosystem — project plugins: Not available in this release. Node + pnpm required at build time. Static parsing: no inherited members, no runtime-generated APIs. Three languages today — Python, JavaScript and Rust; TypeScript, Go, C#, and Java on the roadmap, same funnel. O Opportunities. Material-for-MkDocs maintenance mode: thousands of projects re-deciding. Agent-readable docs becoming table stakes — priced at $100+/mo elsewhere. Skills in agent frameworks are markdown: Folio is their natural renderer. T Threats. Zensical consolidating the MkDocs succession first. Hosted platforms moving down-market with free tiers. “Good enough” LLM-generated one-off sites for small projects. Practical. Do I need to know React or Next.js? No. You write markdown and a docs.yaml. Components are tags in markdown (<Callout>, <Swot>, <Roadmap>…) with documented props. React only becomes relevant if you opt into deep customization. Why does Folio need Node? Folio itself is one native binary, but the output is a Next.js static site — that’s where search, theming, and interactivity come from. Folio manages the frontend workspace (.build/ is disposable) and checks your toolchain up front, but Node ≥ 20.19 and pnpm must exist on the build machine. The deployed site needs neither. Other languages? Versioned docs? i18n? Python, JavaScript and Rust read today; Languages covers each reader. TypeScript is on the roadmap for 0.4 Every Reader, and Go, C#, and Java follow later. Versioned docs and i18n: Not available in this release; the roadmap places them in 0.4. A question this page doesn’t answer? Open an issue — unanswered questions are documentation bugs."},{"url":"docs/plugins/trust/index.html","title":"Trust & Safety","content":"Trust & Safety. What a plugin can do on your machine, what Folio guarantees, and where those guarantees stop. Not available in this release. Project plugins are not in Folio 0.3. They return with the sidecar protocol, and this page describes the trust decision that surface asks of you. The built-in integrations are part of the folio binary and need no such decision. A plugin is code you run. A Folio plugin is a program. folio starts it for the build and calls its hooks over stdio, so it runs with your environment variables, your filesystem permissions, and your network access. Anything your user account can read, write, or send, a configured plugin can read, write, or send. The risk class is not new. Adding a plugin is the same trust decision you already make when you install a package and import it: you are choosing to execute someone else’s code with your own privileges. Judge a plugin the way you judge a library, by its author, its source, and its release history, and not the way you judge a config file. This is deliberate. A sidecar is its own process, but a sandbox that could stop a hostile plugin would also stop the useful ones from reading your source tree, writing pages, and adding CLI commands. Before you run folio in a repository you did not write. Read its docs.yaml first. Whatever it configures a build to start is the code you are about to execute. Two situations where this matters most: Cloning an unfamiliar repository. Inspecting a project’s docs config costs one cat; running a build inside it does not ask first. CI. A pull request that adds a plugin, or edits one already configured, runs that code in the job that builds the docs. Treat a docs build of an untrusted branch exactly like running its test suite. What you can do. Read docs.yaml before running folio in an unfamiliar repository. Prefer plugins you can read. A plugin in the repository is in front of you. A published one has a source repository, a release history, and an author. Pin what you install. Version-pin a plugin like any other dependency and review the diff when you raise the pin. Build untrusted branches without secrets. Folio’s own pull-request preview workflow splits the build in two: the job that runs pull-request code has read-only permissions and produces a static artifact, and only that artifact reaches the privileged job that deploys it. Any docs build of untrusted code wants that shape, or a container, or a throwaway environment. Keep credentials out of docs.yaml and out of plugin code. Static docs are published; anything a build embeds ships to readers. Report what looks wrong. A plugin nobody configured getting started is a bug in Folio. Report it privately through GitHub Security Advisories rather than a public issue. The boundary. Folio does not review, sandbox, or vet plugins. There is no signing, no permission model, no allowlist, and no restriction on what plugin code may do once you have configured it. The folio-plugin GitHub topic described in Publishing a plugin is a discovery convention, not an endorsement. A catalog built on it is on the roadmap, and a listing there would mean exactly one thing: a public repository carries a topic. It would not mean the code was read by anyone. The trust decision stays with you. It is the same one you make for every other dependency in your environment, and Folio’s job is to make sure you can see what you are deciding about. The equivalent decision for frontend code is documented in Theme Packages and Custom Templates."},{"url":"docs/components/pull-quote/index.html","title":"PullQuote","content":"PullQuote. A high-emphasis statement block for the one sentence the page exists to deliver — verdicts, theses, the line you want quoted back. API. PullQuote. PropTypeDefaultDescription children. ReactNode. — The statement. kicker. string. — Small uppercase label above it. attribution. string. — Source line below it. Example. Verdict. “ The short version. Nobody remembers the third-best espresso in the district."},{"url":"docs/plugins/openapi/index.html","title":"OpenAPI","content":"OpenAPI. Turn an OpenAPI spec into an endpoint index page in your docs. The OpenAPI integration is compiled into folio: nothing to install, nothing to list. It stays inert until an openapi: section appears in docs.yaml. What it builds. For each source the integration writes one docs page, /docs/<route>/, with an OpenApiReference block and an entry in the sidebar. The page shows the spec’s title, description and version, the number of operations and schemas, the schema names, and one row per operation with its: method and path, summary and description, operationId, tags. Operations are listed path by path, in the order the spec declares the paths, and in get, post, put, patch, delete, head, options, trace order within a path. The same data is written to lib/openapi-data.ts as openApiSources, so a page or a theme can read it too. Not available in this release. The page is an index of the endpoints, not a full reference. Parameters, request bodies, responses, schema fields and “try it” requests are not rendered. Sources. openapi.sources is a list of sources, or a single mapping for one source. Each source takes: KeyDescription path. The spec file, YAML or JSON. A relative path resolves against the project directory. The path must stay inside the project directory; one that resolves outside it stops the build. content. The spec inline, as a mapping or as YAML/JSON text, instead of path. spec is accepted as another name for it. title. The page title. Defaults to the spec’s info.title, then to OpenAPI. description. The text under the title. Defaults to the spec’s info.description. route. Where the page goes under /docs/. Defaults to api-reference/<slug of the title>. A configured route is cleaned up one segment at a time: each segment is lowercased and slugged (HTTP v1.2 becomes http-v1-2) and empty segments are dropped; a route with no segment left (/) takes the default. A . or .. segment stops the build, so a route cannot climb out of the docs. Errors. A path that does not exist, or a spec that is not valid YAML or JSON, stops the build with an error that names the source. A spec that parses to something other than a mapping is skipped with a warning. Editing a spec during folio serve. folio serve reads the specs when it starts. After you edit a spec, restart folio serve to see the change."},{"url":"docs/languages/index.html","title":"Languages","content":"Languages. Folio documents Python, JavaScript, and Rust, reading source without importing your package, installing your dependencies, building your crate, or running your code. The native parsers extract modules, declarations, signatures, types, attributes, and doc comments. Not available in this release. TypeScript is a separate parser, planned for 0.4 Every Reader. .ts, .tsx, .mts and .cts files are not read; the build reports them in a warning instead. Every supported language feeds the same API reference, search index, Markdown mirrors, and LLM files. Reading source does not require that language’s interpreter, compiler, or build toolchain. Source coverage. LanguageDeclarationsDocumentation Python. Modules, functions and async functions, classes and nested classes, methods, properties, class attributes (enum members included), module constants, signatures, annotations, and decorators. Google-style and NumPy-style docstrings, detected per docstring; detection also reads reStructuredText :param: fields and epydoc @param fields, and Sphinx roles such as :class:`Foo` read as code. JavaScript. .js, .mjs, and .cjs; named and default ESM exports, including local declarations exported by export { name }; CommonJS module.exports (a function, a class, or an object of names) and exports.*; functions and generators, arrow functions bound to an exported name, classes with constructors, static methods, getters and setters, class fields, and constants. JSDoc descriptions, @param (with [name=default], and options.name properties as rows of their own), @returns, @throws, @deprecated, @example, and {@link} read as its text. Rust. Public functions, structs and fields, enums and variants, traits and associated items, inherent and trait implementations, type aliases, constants, and statics. ///, //!, and #[doc = \"...\"] comments read as rustdoc reads them: untagged code blocks are Rust, # hidden lines are dropped, and intra-doc links keep their text; signatures and attributes as written. See Writing Doc Comments for Python examples, method kinds, annotations, and control over what gets documented, and for what the JSDoc and Rust readers take from a comment. Configuration. Point source.python.paths at your Python source roots and source.docs at your Markdown guides: exclude accepts exact files, directories and glob patterns. Docstring style can be auto, google, or numpy; auto also reads reStructuredText and epydoc fields. LanguageSource rootsConfiguration key Python. Directories containing Python packages or modules. source.python.paths. JavaScript. Directories containing .js, .mjs, or .cjs files. source.javascript.paths. Rust. A crate directory containing Cargo.toml, a workspace holding several of them, or a crate’s src/ directory. source.rust.paths. Each language block accepts its own exclude list, and every parser ships in the binary: configuring a language is all it takes to read it. See Configuration for the complete source shape. Module names and reference pages. LanguageModule discoveryExample reference route Python. Package and module names relative to the source root. api-reference/mypackage/utils. JavaScript. File paths relative to the source root, with / as .; utils/index.js becomes utils. When two files publish one name (utils/index.js and utils.js, or index.mjs and index.cjs), the first in path order is read and the other is named in a warning. api-reference/javascript/utils. Rust. Crate names from Cargo.toml, with hyphens converted to underscores; mod declarations followed from lib.rs or main.rs. api-reference/rust/demo_crate/models. Limits. Source is inspected statically. Folio does not execute imports, evaluate application code, or infer runtime behavior. LanguageLimits Python. Documentation comes from source declarations, annotations, and docstrings; the package does not need to be installed or importable. A module’s __all__ decides what it publishes; without one, a name with a leading underscore is skipped. A dunder method other than __init__ appears only when it has a docstring, @overload stubs give way to the implementation, and a property’s setter and deleter fold into its getter. reStructuredText directives such as .. note:: are shown as written. JavaScript. Only what a file exports is documented, and a #private class member is not. node_modules, dist, build, and dot directories are never read, .jsx is skipped with a warning, and TypeScript is reported in a warning rather than read. A setter beside its getter is shown once, as the getter. Types and defaults come from the source and from JSDoc, never from inference; re-exports are listed by name without resolving the module they come from. Rust. Only plain pub items are documented: pub(crate), pub(super), #[doc(hidden)], and impls of private types are skipped, and so are macro definitions and invocations. A module is read only if some lib.rs, main.rs, or parent module declares it pub. pub use is listed as written under Re-exports without resolving its target, so an item re-exported from a private module is not documented. cfg attributes are displayed without evaluation, and #[doc = include_str!(...)] is not read. Inline-module items appear on the page of the file that declares them."},{"url":"docs/components/index.html","title":"Components","content":"Components. Reusable MDX components for Folio documentation pages, including callouts, tabs, previews, diagrams, and API reference helpers. Component catalog. Pick the right primitive before writing another docs section. Folio ships 33 documentation components across 5 families. Use this page as a routing layer: find the job, open the component, then copy the pattern into a real guide or generated API page. 33 entries5 familiesMDX ready. Fast paths. Start from the shape of the page. Write a setup guide TerminalSession + Steps + Checklist. Explain generated output FileTree + BuildArtifact + ConfigPanel. Document Python APIs ClassOverview + ParamTable + TypeBadge. Workflow Components. Build setup guides, CLI references, migration notes, and plugin docs that stay easy to scan. CommandsTerminalSessionCommand and output blocks with prompt, status, and working directory context.OpenConfigConfigPanelAnnotated configuration snippets for docs.yaml examples.OpenOutputBuildArtifactCompact summaries of generated files, folders, and routes.OpenCLICommandGridScannable CLI command cards with common flags.OpenCompareBeforeAfterSide-by-side migration, config, and output comparisons.OpenStateChecklistReadiness and deployment checklists with explicit states.OpenPluginsHookMapPlugin lifecycle stages with hook names and descriptions.Open. Interactive Components. Add controlled disclosure, alternate examples, and sequence structure without custom page logic. RevealAccordionCollapsible content sections for FAQs and grouped information.OpenViewsTabsTabbed panels for organizing alternative content views.OpenSequenceStepsNumbered step-by-step instructions with a vertical timeline.OpenHistoryTimelineVertical timeline for changelogs and version history.OpenCodeCodeGroupTabbed code blocks for multi-language or multi-variant examples.OpenExamplesPreviewCodePaired rendered previews and source snippets for component examples.OpenExamplesExampleTabsTabbed code panels for basic vs. advanced usage patterns.Open. Display Components. Present diagrams, file structures, notes, feature summaries, comparisons, key numbers, and rendered technical notation. MessageCalloutHighlighted message blocks for notes, warnings, tips, and alerts.OpenSummaryFeatureCardsCards for feature overviews, landing pages, and link grids.OpenFilesFileTreeVisual file and folder tree for project structures.OpenPreviewDocPreviewResponsive iframe frames for showing generated docs pages inside a guide.OpenFrameBrowserFrameBrowser window chrome with a mono URL bar for framing embedded examples.OpenDiagramMermaidDiagrams rendered as SVG for flows and sequences.OpenFormulaMathLaTeX math rendering with KaTeX.OpenCodeCode BlocksSyntax highlighting, line numbers, filenames, and word highlighting.OpenNumbersStatStripA row of big numbers with labels that makes scale legible.OpenEmphasisPullQuoteA high-emphasis block for the one sentence the page exists to deliver.OpenCompareCompareMatrixComparison grid with check, dash, and partial cells and a highlighted column.OpenAssessSwotFour-quadrant grid of strengths, weaknesses, opportunities, and threats.Open. API Reference Components. These appear automatically when Folio turns Python source into reference pages. ClassClassOverviewCard displaying a Python class with bases, decorators, and description.OpenMethodMethodAccordionExpandable panels for documenting class methods and signatures.OpenParamsParamTableStructured table for function or method parameters.OpenTypesTypeBadgeInline badge for displaying type annotations with optional links.OpenLifecycleDeprecationNoticeBanner indicating deprecated classes, functions, or modules.Open. Theme Components. Small pieces of site chrome used by the generated Nextra theme. ThemeThemeConfiguratorPopover widget for customizing accent color and border radius.OpenActionsPage ActionsHeader controls for copying a page or opening it with AI context.Open."},{"url":"docs/migration/index.html","title":"Migrating from Sphinx","content":"Migrating from Sphinx. Step-by-step guide to moving a Sphinx project to Folio by hand. One-command importers, from MkDocs, Mintlify or Read the Docs: Not available in this release. Why Folio covers the reasons to switch; this page covers the move. Step-by-step migration. 1. Install Folio. Follow Installation. 2. Initialize configuration. Run the init command in your project root. It reads the project name and version from pyproject.toml and finds the source directory on disk: This creates a docs.yaml file. Edit it to point to your Python source directories: 3. Convert existing documentation. Keep existing Markdown pages and review any directives or components specific to your previous documentation tool. If you have hand-written documentation in .rst format, convert it to Markdown before adding it to source.docs. Folio does not ship or invoke an RST converter; use your migration tool of choice, review the generated Markdown, and then let Folio build the converted pages. Folio warns about .rst files only when those files are still present inside a directory listed in source.docs. It does not emit a warning if the .rst files were already removed before running folio build or folio serve. Name guide files and directories with hyphens, not underscores: write common-errors/index.md, published at /docs/common-errors/. In this release a guide page whose path contains an underscore, such as common_errors/index.md, renders a not-found page at both /docs/common_errors/ and /docs/common-errors/. Old Sphinx URLs are not redirected. API reference routes are the exception: Python package and module names keep their underscores. 4. Leave your docstrings alone. Your docstrings need no conversion. With docstring_style: \"auto\", the default, Folio detects the style per docstring and reads reStructuredText (:param name:), Google, NumPy and epydoc. Set source.python.docstring_style only to pin one style for a codebase that mixes them by accident. 5. Remove the previous documentation setup. Once migration is verified, remove configuration and dependencies used only by your previous documentation tool. For a Sphinx project, these may include: conf.py. Makefile (if only used for docs). make.bat. _build/ directory. _static/, _templates/ directories. Sphinx from your dependencies. 6. Build and verify. This starts a dev server at http://localhost:4321 where you can verify your documentation looks correct. Mapping Sphinx concepts to Folio. conf.py to docs.yaml. Sphinx conf.pyFolio docs.yaml project = \"Name\" project.name: \"Name\" version = \"1.0\" project.version: \"1.0\" extensions = [\"autodoc\"] Not needed (automatic). html_theme = \"furo\" Built-in theme presets (see Theming). html_static_path. public: for files served from the site root; images a page uses go beside the page. html_logo. theme.logo. html_favicon. theme.favicon. exclude_patterns. source.python.exclude. autodoc_member_order. Not configurable (source order). autodoc to automatic generation. In Sphinx, you write explicit directives to pull in API documentation: In Folio, you list your source directories and everything is documented automatically: No per-module directives needed. Use __all__ in your Python modules to control which symbols appear in the documentation. RST directives. Folio does not translate RST directives during a build. Convert them before adding the page to source.docs, then review common constructs against this mapping: Common manual rewrites: RST DirectiveConverted To .. code-block:: python. Fenced code block (```python). .. note:: Callout (note). .. warning:: Callout (warning). .. tip:: Callout (tip). .. hint:: Callout (tip). .. danger:: Callout (danger). .. error:: Callout (danger). .. important:: Callout (warning). .. caution:: Callout (warning). .. attention:: Callout (warning). .. seealso:: Callout (info). .. deprecated:: Callout (danger) with version. .. versionadded:: Callout (note) with version. .. versionchanged:: Callout (note) with version. .. image:: path. ![path](path). RST headings (underlines). Markdown headings (#, ##, etc.). inline code `inline code` :role:`text` `text` Features that need manual redesign: RST FeatureStatus .. toctree:: Not needed (auto-generated navigation). .. include:: Not supported. .. math:: $$ block, rendered with KaTeX (inline: $…$). .. table:: Use Markdown tables instead. .. raw:: Not supported. .. tab-set:: / .. tab-item:: Convert manually to <Tabs> and <TabItem> .. only:: Not supported. Cross-references (:ref:, :doc:). Not available in this release; use Markdown links. Substitutions (|name|). Not supported. Field lists (:field:). Not supported. Footnotes. Markdown footnotes ([^1]). For Sphinx tab sets, rewrite each block with Folio’s MDX tabs components: For callouts that were written as Markdown blockquotes during migration, use Folio’s Callout component directly. Patterns such as > **Warning:** should become <Callout type=\"warning\">. For example, convert > **Warning:** This changes state. to: Sphinx extensions to Folio features. Many common Sphinx extension use cases are built into Folio. Custom extensions: Not available in this release. Sphinx ExtensionFolio Equivalent autodoc. Built-in (automatic). napoleon. Built-in (Google and NumPy styles). viewcode. Built-in: each definition links to its source line when project.repo is set. intersphinx. Not available in this release. todo. Not supported. coverage. Built-in (folio coverage). doctest. Not supported. sphinx-copybutton. Built-in (code blocks have copy buttons). Custom extensions. Not available in this release. make html to folio build. Sphinx CommandFolio Command make html. folio build. make clean. folio clean. sphinx-autobuild. folio serve. sphinx-quickstart. folio init. What works differently. Sphinx roles do not link. Sphinx has an extensive cross-reference system (:class:, :func:, :meth:, etc.) that creates links between documented symbols. Folio generates API links for parsed type annotations; Sphinx role syntax is Not available in this release. In a docstring a role reads as code, unlinked: :class:`MyClass` renders as MyClass. In a Markdown page a role stays as written. Replace roles with Markdown links where you need the link. No intersphinx. Sphinx’s intersphinx extension lets you link to other projects’ documentation. Intersphinx is Not available in this release. If your docs link heavily to external API documentation (for example the Python standard library), replace those links with plain URLs. Source order, not alphabetical. Folio documents members in the order they appear in the source file. Sphinx’s autodoc_member_order option with alphabetical sorting has no equivalent. Only the field lists of reStructuredText docstrings. Folio reads the Sphinx field lists in docstrings (:param x:, :type x:, :returns:, :rtype:, :raises:). Other reStructuredText markup in a docstring, such as substitutions, directives or grid tables, is not converted and shows as written. Common gotchas. Docstring parsing errors. If no style parses a docstring, Folio falls back to the first line as the summary and the rest as the description. The parameter table still lists the signature’s parameters, without descriptions. Indentation matters: in a Google-style section, a line at column zero ends the section and the lines from there to the next section header are dropped. Missing __init__ docs. Folio documents __init__ like any other method. If you were relying on Sphinx’s autoclass_content = \"both\" to merge class and __init__ docstrings, you may need to adjust your docstrings. Private members. Without __all__, Folio documents every top-level class, function and annotated variable, and every unannotated assignment to an upper-case name, when the name does not start with _. Define __all__ in a module to choose exactly what appears: it can list a class, function or annotated variable whose name starts with _, while an unannotated assignment still needs an upper-case name without a leading _. MyST directives disappear silently. A fenced block whose info string is in braces, such as MyST’s {note} or {eval-rst}, is removed with its content, and the build does not warn. Rewrite each one in Markdown or as a component before adding the page to source.docs. An importer that rewrites these blocks, tab sets and blockquote-style callouts: Not available in this release. Raw embeds disappear silently. The Markdown-to-MDX step removes raw <iframe>, <script>, <style>, <video>, <audio>, <object>, and <embed> elements, inside code blocks too, and the build does not warn. Use a normal Markdown link or a documented component instead of a raw YouTube or HTML embed. Images stay beside the page. An image path relative to the page is copied with it, as long as the file sits in the page’s directory or below it. A path with .. is removed from the page and the build warns image not found: ../img.png (escapes the docs directory). Curly braces are escaped for you. Folio outputs MDX, where a bare { starts a JSX expression, so it escapes bare braces in prose; code blocks, inline code and $…$ math pass through untouched. A line that starts with < is treated as JSX and left alone, so a literal brace on such a line needs a backslash (\\{). Markdown details that change. Links to .md files drop the extension and point at the published page; class= on an HTML tag becomes className=; a mermaid fence renders as a diagram. Feature comparison. FeatureSphinxFolio Python API docs. Via autodoc extension. Built-in, automatic. Docstring styles. Google, NumPy, reStructuredText. Google, NumPy, reStructuredText, epydoc, auto-detected per docstring. Configuration. Python (conf.py). YAML (docs.yaml). Output format. HTML, PDF, ePub, etc. HTML (Next.js). Theme system. Jinja2 templates. React + shadcn/ui. Dark mode. Theme-dependent. Built-in. Hot reload dev server. Via sphinx-autobuild. Built-in. Search. Built-in. Built-in (Pagefind). Cross-references. Full support. Generated API type links; Sphinx roles: Not available in this release. Intersphinx. Full support. Not available in this release. RST support. Native. Convert to Markdown before build. Markdown support. Via MyST. Native. Custom extensions. Extensive plugin ecosystem. Not available in this release. PDF output. Built-in. Not supported. i18n. Built-in. Not available in this release. LLM-friendly output. Not built-in. llms.txt + llms-full.txt. Custom components. Jinja2 macros. React/MDX components. Setup complexity. High. Low. Build speed. Moderate. Fast (Turbopack). Migration checklist. TODO Install Folio and run folio init. TODO Edit docs.yaml with project details and source paths. WARN Convert .rst files to .md. Folio ships no converter: use the tool you prefer, then review the Markdown by hand. TODO Replace :ref: and :doc: cross-references. Sphinx roles do not become links: use Markdown links. TODO Rewrite {eval-rst} and other MyST directive blocks. TODO Move html_static_path files to public: or beside the pages that use them. TODO Run folio serve and verify every page. TODO Remove Sphinx configuration files and dependencies. TODO Update CI/CD scripts to use folio build."},{"url":"docs/components/deprecation-notice/index.html","title":"DeprecationNotice","content":"DeprecationNotice. A styled banner indicating that a class, function, or module is deprecated. Displays a red “Deprecated” badge with version information and guidance on what to use instead. Generated API pages do not write this component: a docstring’s Deprecated: section or a JSDoc @deprecated tag renders as a Deprecated: paragraph. Use it by hand in the pages you write. API. DeprecationNotice. PropTypeDefaultDescription since. string. — Version when the deprecation was introduced (e.g. \"1.5.0\"). alternative. string. — Name of the replacement to use. Rendered in a <code> element with “Use … instead.” phrasing. message. string. — Custom deprecation message. The rendered card shows: A red “Deprecated” badge. “since X.X.X” in muted text (if since is provided). The custom message (if provided). “Use alternative instead.” (if alternative is provided). Example. Deprecatedsince 2.0.0. This class will be removed in v3.0. Use FederatedNode instead. Minimal usage with just a version: Deprecatedsince 1.5.0."},{"url":"docs/components/command-grid/index.html","title":"CommandGrid","content":"CommandGrid. A compact command overview for CLI-heavy pages. Use CommandGrid with one or more CommandCard children. API. CommandGrid. PropTypeDefaultDescription children. ReactNode. — CommandCard elements. CommandCard. PropTypeDefaultDescription command. string. — CLI command shown in monospace. title. string. — Human-readable command name. description. string. — Short task description. href. string. — Optional link target. flags. string[] [] Common flags shown as badges. Example. Initialize. folio init. Create docs.yaml from project metadata. Build. folio build. Generate MDX, search, and static output. --clean--verbose."},{"url":"docs/components/checklist/index.html","title":"Checklist","content":"Checklist. A readiness list with explicit states. Use it for prerequisites, deployment checks, and release gates. API. PropTypeDefaultDescription title. string. — Optional checklist title. items. ChecklistItem[] — Rows to render. ChecklistItem supports label, description, and state. State can be done, warn, or todo. Example. Before you build. OK Node.js 20.19+ and pnpm 10. Required by folio build and folio serve. TODO docs.yaml exists. Run folio init if it is missing. WARN No stale output committed. Keep .build/ and _site/ out of source control unless your deployment needs them."},{"url":"docs/components/copy-page-button/index.html","title":"Page Actions","content":"Page Actions. Folio renders a compact Ask AI page-actions button above every documentation page. It is meant for source-heavy docs: copy the current page, or send the current page context to ChatGPT without adding extra setup. PageActionsButton. A single outline menu button that includes Copy page, View Markdown, a ChatGPT-only assistant action, and MCP JSON actions. The ChatGPT row uses the bundled brand icon and opens ChatGPT with a short Read from ... prompt using the current page’s Markdown URL, including configured deploy base paths. MCP copies a JSON page-context payload for local tooling. It takes no author-facing props, and it is not an MDX component: the docs page layout renders it and passes it the page’s Markdown mirror, so a page cannot place, move or remove it. Layout Component. PageActionsButton is rendered by the docs page layout above every documentation page. The button at the top of this page is the live example."},{"url":"docs/components/callout/index.html","title":"Callout","content":"Callout. Highlighted message blocks for notes, warnings, tips, and other important information. Each type has its own icon; the colors come from the theme tokens below. API. Callout. PropTypeDefaultDescription type. \"note\" | \"warning\" | \"info\" | \"tip\" | \"check\" | \"danger\" \"info\" The visual style of the callout. title. string. — Optional bold title displayed above the content. children. ReactNode. — The callout body content. Available Types. TypeColor tokenUse for note. --primary. General notes and remarks. warning. --warning. Important warnings the reader should not miss. info. --muted. Neutral supplementary information. tip. --primary. Helpful tips and best practices. check. --primary. Success states or confirmation of correct behavior. danger. --destructive. Critical warnings about destructive or breaking behavior. note, tip and check share the --primary color and differ by icon. Example. Python 3.10+ This library requires Python 3.10 or later. This function modifies the input array in place. Performance. Use batch mode for datasets larger than 10,000 rows. All tests passing. The test suite completed successfully with 100% coverage. Breaking Change. The legacy_mode parameter was removed in v2.0. This is a neutral informational message with no title."},{"url":"docs/components/mermaid/index.html","title":"Mermaid","content":"Mermaid. Renders Mermaid diagrams as SVG directly in the browser. Supports flowcharts, sequence diagrams, class diagrams, state diagrams, ER diagrams, Gantt charts, and all other Mermaid diagram types. Automatically switches between light and dark themes to match the site. API. Mermaid (JSX component). PropTypeDefaultDescription chart. string. — The Mermaid diagram definition string. Fenced code block (recommended). You can also use standard Mermaid fenced code blocks. Folio automatically converts these into <Mermaid> components during the build: Example. Fenced code block syntax. graph TD A[Start] --> B{Decision} B -->|Yes| C[OK] B -->|No| D[Cancel] JSX component syntax. graph TD A[Start] --> B{Decision} B -->|Yes| C[OK] B -->|No| D[Cancel] Sequence diagram. sequenceDiagram Client->>Server: POST /train Server->>Worker: dispatch(job) Worker-->>Server: result Server-->>Client: 200 OK. Class diagram. classDiagram class Node { +str address +connect() +disconnect() } class FederatedNode { +train() +aggregate() } Node <|-- FederatedNode."},{"url":"docs/theming/personalization/index.html","title":"Personalization","content":"Personalization. Personalization keeps the bundled Folio template and changes the theme data it receives. This is the right level when the docs should still feel like Folio, but with your project’s brand, typography, accent, spacing, and header defaults. Basic Theme Options. FieldPurpose preset. Default visual preset for the generated site. dark_mode. true (the default) offers light, dark, and system modes through next-themes; false keeps the site light. logo. Copies a project logo into the generated site and shows it in the docs navbar beside the project name. favicon. Copies a project favicon into the generated site. preset must be a preset the configurator shows: a built-in id, one a theme package declares, or a new id that theme.name, theme.tokens or theme.style defines (see Project Presets). Any other value stops the build with the list of valid ids and the nearest one, so preset: \"beakon\" asks whether you meant beacon. Dark mode is enabled by default. When enabled, readers can use the theme control or press d outside form fields to switch between light and dark modes. With dark_mode: false the site stays light whatever the reader’s system or saved preference: the mode controls leave the configurator, the header and landing theme toggles are not rendered, and d does nothing. theme.header.theme_toggle: true is then ignored with a warning. The logo path is resolved from the project directory and must exist; a missing file stops the build. The image takes the monogram’s place in the docs navbar and is served under deploy.base_path when one is set. The landing navbar and the previews page keep the monogram. Theme Configurator. Every generated site includes the Folio ThemeConfigurator drawer when theme presets are available. The drawer exposes the configured default preset, the built-in preset library, and tuning controls for typography, accent, surfaces, content width, reading rhythm, borders, code blocks, shell spacing, and radius. It also offers light, dark and system mode unless theme.dark_mode is false. Reader preferences are persisted in a project-scoped localStorage key derived from the configured default preset. Folio also renders the default theme CSS into the page before hydration so the site does not flash through unconfigured typography or colors. See ThemeConfigurator for the built-in preset catalog and the TypeScript preset contract. Tune Defaults. Use theme.tune to set the default configurator choices without creating a new preset: Common aliases map to the internal configurator ids: YAML keyInternal control font. fontId. accent or color. colorId. surface. surfaceColorId. shell. shellPaddingId. width or content_width. contentWidthId. rhythm or reading. rhythmId. borders or border. borderId. code or code_blocks. codeTreatmentId. Each value must be one the configurator offers for that control: ControlValues fontId. folio, sans, geist, serif, mono. colorId. ink, laurel, indigo, copper. surfaceColorId. preset, paper, moss, mist. shellPaddingId. preset, flush, frame, gallery. contentWidthId. preset, focus, docs, wide. rhythmId. preset, compact, balanced, roomy. borderId. preset, fine, structured, ruled. codeTreatmentId. preset, soft, framed, plate, terminal. Any other value stops the build with the list and the nearest value, for example theme.tune.rhythm must be one of 'preset', 'compact', 'balanced', 'roomy'; got 'dense'. A key that is none of the above warns, names the key it most likely meant, and is ignored. font: \"geist\" selects the bundled Geist and Geist Mono font pair and maps the public --font-sans and --font-mono tokens used by Tailwind utilities. theme.radius (and its theme.tune.radius alias) is validated against the fixed radius scale: \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\". The named aliases \"none\", \"sm\", \"md\", \"lg\", and \"full\" map onto the same scale (matching the configurator’s radius labels). Any other value fails config validation, because the configurator maps the configured radius onto this fixed scale. Project Presets. When a project supplies name, description, scene, preview, style, tokens, or variants, Folio emits theme/project-theme.ts during template preparation and adds the project preset before the built-in preset library. docs.yaml. A project-owned theme preset generated from safe YAML data. theme.name Label shown in the configurator Project group. string. theme.preview Light and dark swatches for the preset preview. object. theme.style Layout and typography CSS custom properties. object. theme.tokens Light and dark shadcn/project CSS variable overrides. object. theme.header Docs header brand, badge, repo, search, and action controls. object. theme.variants Project-owned preset controls with options, swatches, and token overrides. object. tokens.light and tokens.dark accept CSS custom properties such as shadcn tokens (--background, --card, --border, --chart-1) and project tokens (--brand-accent). style accepts layout variables such as --folio-content-max-width, --folio-section-gap, --folio-card-padding, --folio-code-bg, --folio-workspace-shell-topbar, --folio-workspace-shell-topbar-blur, and --folio-workspace-shell-topbar-border. The un-prefixed legacy spellings (for example --content-max-width) are still accepted for compatibility but are deprecated; new configs should use the canonical --folio-* names. Header URL Validation. The header can carry links: theme.header.repo and theme.header.action_href. Folio validates these URLs at config-load time against an allowlist of safe schemes. Permitted values are: http and https URLs (for example https://github.com/acme/sdk); mailto: links; and. relative URLs (for example /dashboard or docs/index). Unsafe schemes — notably javascript: and data: — are rejected. A header link using a rejected scheme raises a config error and fails the build, naming the offending field, rather than being emitted into the generated site. This keeps script-injection payloads out of the docs header even when the docs.yaml comes from an untrusted source. Use an http(s), mailto:, or relative URL for any header link. The top-level project.repo is validated less strictly because repository URLs legitimately use non-web schemes: ssh://, git://, git+https://, and scp-style git@host:path forms are accepted, and only schemes that could execute script or read local files (javascript:, data:, vbscript:, file:) are rejected. Variants. Variants let a project expose its own configurator controls. Each option can set a swatch, preview colors, style overrides, and light/dark token overrides while inheriting the base project preset. If an option has swatch but no full light/dark preview, Folio uses the swatch for both preview modes. Every option combination across all variant controls is resolved and embedded into each generated page, so theme.variants is capped at 256 combinations (the product of each control’s option count). Exceeding the cap fails config validation; reduce the number of controls or options. CSS Variables. Folio themes use oklch colors through CSS custom properties: Sidebar-specific variables control the left navigation independently from the main content area: VariablePurpose --sidebar. Sidebar background. --sidebar-foreground. Sidebar text color. --sidebar-primary. Sidebar active item color. --sidebar-accent. Sidebar hover/focus background. --sidebar-border. Sidebar border color. What Plugin Surfaces Rely On. The landing and the roadmap are drawn from the tokens on this page and nothing else: no colour of their own, no per-page override. That is the contract a plugin surface keeps, and it is what makes a preset switch or a theme package restyle every page at once. FamilyTokensSet by Surfaces and ink. --background, --foreground, --card, --card-foreground, --popover, --popover-foreground, --muted, --muted-foreground, --accent, --accent-foreground, --secondary, --secondary-foreground, --border, --input, --ring, --radius. the preset; theme.tokens overrides. Emphasis. --primary, --primary-foreground, --destructive, --warning. the preset. Data. --chart-1 to --chart-5; the index pages, the preview cards and Mermaid draw from these. the preset. Type. --font-sans, --font-mono, --folio-heading-font-family, --folio-body-font-family. the preset; theme.tune. Layout. the --folio-* variables listed under Project Presets. the preset; theme.style. Mermaid diagrams read the computed values of these tokens when they render and fall back to a neutral palette only when none resolve. A plugin surface that needs a colour these families do not carry adds a named token in the same shape rather than a literal, so a theme can reach it. When Personalization Is Not Enough. Use theme packages when the project needs to override files inside the bundled template while keeping Folio’s template as a base. Use custom templates when the project needs to own the entire frontend workspace."},{"url":"docs/components/build-artifact/index.html","title":"BuildArtifact","content":"BuildArtifact. Summarize the files, directories, and generated routes produced by a Folio command. API. PropTypeDefaultDescription title. string. \"Build artifacts\" Panel title. description. string. — Optional summary. items. BuildArtifactItem[] — Artifact rows. BuildArtifactItem supports path, kind, and description. Example. Generated output. The build creates an intermediate app and final static files. .build/workspace Intermediate Nextra project with generated MDX. _site/static Deployable static site. _site/llms.txtllm Summary file for AI coding assistants."},{"url":"docs/deployment/static-hosts/index.html","title":"Static Hosts","content":"Static Hosts. Static hosts only need the generated _site/ directory. The host can either run folio build --clean itself or serve a prebuilt artifact uploaded by your CI pipeline. Vercel. Use Vercel as a static-site host: Configure the project. In Vercel project settings, use: Framework Preset: Other. Install Command: npm install -g pnpm@10 && curl -LsSf https://pguijas.github.io/folio/install.sh | sh. Build Command: ~/.local/bin/folio build --clean. Output Directory: _site. The install command runs the standalone installer from Installation during each build, so there is nothing to add to your repository. The binary lands in ~/.local/bin, which is why the build command calls it by path. Deploy. Push to your production branch. Vercel runs the build command and publishes _site/. Vercel already builds automatically on push. If you need required documentation checks before deploy, add a CI workflow that runs folio build --clean as a pull request check, plus folio coverage --min 80 for Python projects (coverage reads Python only in this release; see CI/CD). Netlify. Create a netlify.toml that publishes the static artifact: Netlify will rebuild the docs on each deploy and serve _site/ directly. Self-hosted. Any static file server can host the generated site: For containerized deployments, copy _site/ into a small static web server image such as Nginx, Caddy, or any equivalent platform image. Do not copy .build/; it is a generated workspace cache, not the deployable artifact."},{"url":"docs/components/math/index.html","title":"Math (LaTeX)","content":"Math (LaTeX). Inline and display math rendering via KaTeX . Use $ for inline expressions and $$ for centered display equations. Math support is enabled by default — no configuration needed. API. Math uses Markdown syntax, not a JSX component: SyntaxBehaviorDescription $...$. Inline. Renders the expression inline with surrounding text. $$...$$. Display (block). Renders the expression as a centered block equation. Math expressions inside code blocks (` or ```) are not rendered — they display as literal text, so you can safely document LaTeX syntax. Example. Inline math. The Pythagorean theorem states that a2+b2=c2a^2 + b^2 = c^2a2+b2=c2 for a right triangle. Einstein’s famous equation E=mc2E = mc^2E=mc2 relates energy and mass. Display equations. x=−b±b2−4ac2ax = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}x=2a−b±b2−4ac Matrices. [a11a12a21a22][x1x2]=[b1b2]\\begin{bmatrix} a_{11} & a_{12} \\\\ a_{21} & a_{22} \\end{bmatrix} \\begin{bmatrix} x_1 \\\\ x_2 \\end{bmatrix} = \\begin{bmatrix} b_1 \\\\ b_2 \\end{bmatrix}[a11a21a12a22][x1x2]=[b1b2] Summations. The binomial theorem: (x+y)n=∑k=0n(nk)xn−kyk(x + y)^n = \\sum_{k=0}^{n} \\binom{n}{k} x^{n-k} y^k(x+y)n=k=0∑n(kn)xn−kyk."},{"url":"docs/components/code-group/index.html","title":"CodeGroup","content":"CodeGroup. Tabbed code blocks for showing the same concept in multiple languages or multiple variants of a command. Tabs are automatically labeled from the language of each code block, or you can provide explicit labels. API. CodeGroup. PropTypeDefaultDescription labels. string[] — Optional explicit tab labels. If omitted, labels are auto-detected from each code block’s language (e.g. python, bash). children. ReactNode. — Two or more fenced code blocks to display as tabs. Label auto-detection tries, in order: a title prop, a data-language attribute, a language-* CSS class, and falls back to Tab N. Example. Auto-detected labels from language: python javascript. With explicit labels: pip uv conda."},{"url":"docs/components/param-table/index.html","title":"ParamTable","content":"ParamTable. A structured table for displaying function or method parameters with name, type, default value, and description columns. Every Python and JavaScript function with parameters on a generated API page gets one, filled from its signature and its docstring or JSDoc; Rust functions show their signature instead. You can also use it by hand for documentation you write yourself. API. ParamTable. PropTypeDefaultDescription args. Param[] — Array of parameter objects. Param object. FieldTypeDefaultDescription name. string. — Parameter name. type. string. — Type annotation string. default. string. \"-\" Default value. Shown as - if omitted. description. string. — Description of the parameter. href. string. — Optional URL to link the type badge to its documentation. Example. NameTypeDefaultDescription host. str. \"localhost\" Server hostname. port. int. 8080. Server port number. debug. bool. False. Enable debug mode. workers. int. - Number of worker processes. Required."},{"url":"docs/components/browser-frame/index.html","title":"BrowserFrame","content":"BrowserFrame. Browser window chrome around any content: three dots, a mono URL bar, and an optional right-aligned status label. Use it to frame live embeds — roadmap miniatures, rendered components, page excerpts — as the page they ship on, instead of pasting a screenshot. API. PropTypeDefaultDescription url. string. — Address shown in the mono URL bar. label. string. — Optional right-aligned mono label, e.g. \"● LIVE\". footer. ReactNode. — Optional status-bar strip inside the window, under the content: the place for links or actions that belong to the framed content. children. ReactNode. — Content rendered inside the window body. Example. pguijas.github.io/folio/docs/plugins/roadmap. ● LIVE. Releases. 3 / 8. v0.1 Foundation. v0.2 Theming. v0.3 One Binary. v0.4 Every Reader. Up next. + 4 more. Notes. Server-renderable: no client state, safe in any MDX page or generated view. not-prose-armored: margins are reset so it sits cleanly inside prose content. Pairs with <Roadmap compact /> for landing-page roadmap miniatures."},{"url":"docs/components/config-panel/index.html","title":"ConfigPanel","content":"ConfigPanel. Pair a YAML example with short field notes. Use it when a configuration snippet needs explanation but a full reference table would interrupt the guide. API. PropTypeDefaultDescription title. string. — Section title. description. string. — Short explanation below the title. fields. ConfigField[] [] Field notes shown below the example. children. ReactNode. — Usually a fenced YAML block. ConfigField supports name, type, default, and description. Example. source.python. Tell Folio where your package code lives. paths Python source directories to scan. list[string] docstring_style Parser for Google or NumPy style docstrings. stringdefault: auto."},{"url":"docs/components/timeline/index.html","title":"Timeline","content":"Timeline. Vertical timeline for changelogs, version history, or sequential events. Each item shows a date, title, optional badge, and description. Badges like \"new\" and \"breaking\" have special styling. API. Timeline. PropTypeDefaultDescription children. ReactNode. — One or more <TimelineItem> elements. TimelineItem. PropTypeDefaultDescription date. string. — Date or version label. title. string. — Event or release title. badge. string. — Optional badge text. \"new\" and \"breaking\" have special styling; any other string displays as a neutral badge. children. ReactNode. — Description or details. Example. v2.0.0breaking. Major release. Dropped Python 3.8 support. Migrated to Pydantic v2. New plugin API. v1.5.0new. Mermaid diagrams. Added support for Mermaid diagram rendering in documentation pages. v1.4.2. Bug fixes. Fixed sidebar ordering issue with nested modules. Improved build performance. v1.4.0new. Search customization. Added configurable search placeholder and the ability to disable search."},{"url":"docs/theming/custom-templates/index.html","title":"Custom Templates","content":"Custom Templates. [!WARNING] Security A custom template is executed as trusted code during every build. Folio runs pnpm install --frozen-lockfile (which executes dependency lifecycle scripts) and next build (which runs the template’s next.config.mjs in Node), so pointing template.path at a directory is equivalent to running that code on your machine. Only point template.path at frontend code you have reviewed and trust. Building an untrusted repository or branch in CI inherits the same remote-code-execution surface — treat a docs build like any other untrusted-code execution. Never put secrets, API tokens, or credentials in template.params: they are baked into the static export and published. Custom templates are the expert escape hatch for teams that need complete control over their documentation site. A template is a full frontend workspace. It is not a color-token override system: it owns layout, routes, CSS, JavaScript, components, package dependencies, search UI, navigation chrome, and brand behavior. Use personalization when the bundled Folio template is close enough. Use theme packages when you only need to overlay selected files on the bundled template. Use template.overlay_path when you want to override just one or a few template files while inheriting the rest of the bundled template. Use template.path when the documentation must live inside a product-specific frontend shell. Configure a Template. Point Folio at a local template directory from docs.yaml: template.path is resolved relative to the project directory. It must point to a trusted local directory inside the project, and it cannot point at .build/ or the configured output directory. template.params is an arbitrary JSON-serializable mapping. Folio does not interpret these values; the selected template owns their meaning. See the template.params contract below for the full rules and a worked example. template.docs_route_base controls where documentation pages are served. It defaults to /docs. Set it when the generated docs need to live at a product route such as /reference/docs. Folio uses this path for generated links, search URLs, sitemap entries, canonical metadata, LLM output, and the copied Next.js route folder. Overlay (partial override). template.path is all-or-nothing: you must fork the entire bundled template to change a single file. When you only need to override one or a few files, use the opt-in template.overlay_path instead. In overlay mode Folio starts from the bundled template, then copies your overlay files on top — your files win, and any file you do not provide falls back to the bundled template. Only place the files you want to change inside the overlay directory, mirroring their path in the template tree: After the overlay is merged, Folio runs the normal injection pipeline on the result, so project metadata, the docs route base, search, and theme configuration are injected exactly as they would be for the bundled template. [!IMPORTANT] template.overlay_path and template.path are mutually exclusive. template.path is a full replacement; template.overlay_path is a partial override. If both are set, template.path wins and the overlay is ignored with a warning. The overlay directory is held to the same guards as template.path: it must live inside the project, may not point at .build/ or the output directory, and may not contain symlinks. Because the merged template still has to satisfy the injection-marker contract, any marker-bearing file you override must keep the required markers verbatim — the safest approach is to copy the file from the bundled template and edit around the markers. Limitations. The overlay is a file-level merge, not a deep merge. Overriding a file replaces it entirely; Folio does not merge JSON (for example package.json) or splice individual functions inside a file. Deleting a bundled file via the overlay is not supported — an overlay can only add or replace files. For deeper changes than a handful of file replacements, fork the whole template with template.path. Ownership Model. Folio owns generated documentation data. Your template owns presentation. OwnerResponsibilities Folio. Parse Python, JavaScript/TypeScript, Rust and Markdown sources, generate MDX pages, write _meta.ts, emit API reference pages, write Markdown exports, build the search index, link-check generated routes, and export static files. Template. Render the configured docs route, provide compatible MDX components, style the whole site, implement navigation/search/version UI, ship dependencies, and decide how template.params affect the frontend. Do not edit .build/. Folio copies the template there, removes template demo content, injects generated content and metadata, runs the frontend build, and may overwrite reserved generated files on every build. Template Anatomy. Folio currently supports local Next/Nextra-compatible templates. A custom template must include a source docs route at app/docs; Folio relocates that folder to template.docs_route_base inside .build/ when the route base is not /docs. The build runtime expects pnpm install --frozen-lockfile, pnpm run build, and a static export in out/. Keep app/docs as the template source route even when the public route is configured differently. Injection markers. Folio does not template these files with a templating engine. It performs string substitutions and regex block edits against specific source files after copying the template into .build/. The marker strings below are a load-bearing contract: a custom template must contain them verbatim, in the expected files, or Folio cannot inject project metadata, the docs route base, the repository link, the header, search, the theme preset, dark mode, the base path, or Folio-managed MDX component wiring. [!IMPORTANT] Folio validates the load-bearing markers before building. If a required marker (or the file that owns it) is missing, the build fails fast with template.path is missing required Folio injection markers: <marker> in <file>, ..., one <marker> in <file> pair per missing entry (an overlay reports as template.overlay_path), alongside the existing missing-files and MDX-contract checks. The load-bearing set is the always-applied project metadata in app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, and the base-path marker in next.config.mjs. Optional targets — paired blocks that are stripped when a feature is off, the mdx-components.tsx component markers (which fall back to a ...components, spread), and markers that only live in optional files such as the landing page or theme configurator — are not hard failures: when one is absent, that injection does not happen and the build goes on. The safest way to author a custom template is to copy the marker-bearing files verbatim from the bundled template and edit around the markers, rather than recreating them from scratch. Text replacements. Each of these is a string → value replacement. The marker may appear more than once in a file; all occurrences are replaced. The project name, description, site URL, repository URL, monogram and the landing text markers are escaped for where the marker sits, so a name such as Acme \"Docs\" <v2> reaches the page as text and never as code: A marker that is a whole \"…\" or '…' string literal becomes a JSON string. Inside a longer \"…\" literal it is escaped for that string (HTML-escaped when the literal is a JSX attribute value, as in title=\"… __PROJECT_NAME__\"). Inside a `…` template literal, backslashes, backticks and ${ are escaped. Anywhere else it is JSX text and becomes an expression such as {\"Acme SDK\"}, so Built with __PROJECT_NAME__ renders as text. The _JSON markers are pasted as JSON and belong where a JavaScript value is expected. Marker stringExpected file(s)What Folio substitutes __PROJECT_NAME__. app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx, app/previews/layout.tsx. Project name from docs.yaml. __PROJECT_DESCRIPTION__. app/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/opengraph-image.tsx. Documentation for <name>. __SITE_URL__. app/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/sitemap.ts, app/robots.ts. Configured site URL (trailing slash trimmed). __PROJECT_MONOGRAM__. app/icon.svg, app/docs/layout.tsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx, app/previews/layout.tsx. Two-letter lowercase monogram derived from the name. __PROJECT_REPO__. app/docs/layout.tsx, app/previews/layout.tsx. Repository URL (emptied when no repo is set). __DOCS_INDEX_CANONICAL_PATH__. app/docs/[[...mdxPath]]/page.jsx. Canonical docs index path (/ when a landing page is enabled, otherwise the docs route base). __DOCS_ROUTE_BASE__. app/sitemap.ts, app/robots.ts. Configured template.docs_route_base. __INCLUDE_DOCS_INDEX__. app/sitemap.ts, app/robots.ts. true/false depending on whether the landing page is enabled. __FOLIO_DOCS_ROUTE_BASE__. next.config.mjs. Configured template.docs_route_base (the bundled template does not use it). __LANDING_*__, __LANDING_*_JSON__. app/page.tsx, components/landing-navbar.tsx. Landing hero/CTA/feature/section data (raw and JSON-encoded forms). __PROJECT_NAME_JSON__, __PROJECT_MONOGRAM_JSON__. app/page.tsx, components/landing-navbar.tsx. JSON-encoded name/monogram for the landing page. Paired block markers. These are START/END comment pairs that wrap a region. Folio either replaces the region’s contents (preserving indentation) or strips the whole block — and its marker lines — when the corresponding feature is not configured. Include both the start and end marker, and the placeholder content in between, exactly as in the bundled template. Marker pairExpected fileWhat Folio substitutes // __PROJECT_REPO_IMPORTS_START__ … // __PROJECT_REPO_IMPORTS_END__. app/docs/layout.tsx. Repository-link imports (block removed when no repo is set). {/* __PROJECT_REPO_LINK_START__ */} … {/* __PROJECT_REPO_LINK_END__ */} app/docs/layout.tsx. Repository link markup (block removed when no repo is set). {/* __PROJECT_HEADER_LOGO_START__ */} … {/* __PROJECT_HEADER_LOGO_END__ */} app/docs/layout.tsx. The theme.logo image and the project header brand/badge markup (the monogram is dropped when a logo is set; with neither configured only the marker lines are removed and the placeholder between them stays). // __PROJECT_HEADER_ACTION_IMPORTS_START__ … // __PROJECT_HEADER_ACTION_IMPORTS_END__. app/docs/layout.tsx. ProjectHeaderActions import (removed when no header actions are configured). {/* __PROJECT_HEADER_ACTIONS_START__ */} … {/* __PROJECT_HEADER_ACTIONS_END__ */} app/docs/layout.tsx. Header action markup (removed when no header actions are configured). // __FOLIO_COMPONENT_IMPORTS__. mdx-components.tsx. Reserved for internal Folio use: import lines Folio may inject for additional MDX components. // __FOLIO_COMPONENT_ENTRIES__. mdx-components.tsx. Reserved for internal Folio use: entries Folio may add to the MDX components map. The two mdx-components.tsx markers are single comment lines (not pairs). They are reserved for internal Folio use. If they are absent but Folio needs to inject components, it falls back to prepending imports and inserting entries next to an existing ...components, spread — so keeping the markers gives you deterministic placement. Troubleshooting. Custom-template failures surface as one of a few recognizable symptoms. Use this table to map the symptom to its cause and fix. SymptomCauseFix Build fails listing missing required files. The template directory is missing an anatomy file Folio expects (for example next.config.mjs, mdx-components.tsx, or the app/docs route). Add the missing files. Copy the anatomy skeleton from the bundled template rather than recreating it. Build fails listing missing MDX components. mdx-components.tsx does not export a required Folio component name, so generated MDX would import a component that does not exist. Export the named components from mdx-components.tsx. See the MDX Component Contract. Build fails with template.path is missing required Folio injection markers: X in Y, ... (one marker in file pair per missing entry). A load-bearing injection marker — or the file that owns it — is absent, so Folio cannot inject project metadata, the docs route base, or the base path. Restore the marker verbatim in the named file. The load-bearing set lives in app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, and next.config.mjs; copy those files from the bundled template and edit around the markers. Warning that the overlay is ignored. Both template.path and template.overlay_path are set. They are mutually exclusive; template.path wins. Remove whichever key you do not want. Use template.path for a full fork, template.overlay_path for a partial override. Config error mentioning an unsafe URL scheme. A header URL uses a rejected scheme (javascript:, data:), or a repository URL uses a scheme that could execute script or read local files. Use an http(s), mailto:, or relative URL for header links; repository URLs may also use ssh://, git://, git+https://, or scp-style forms. See header URL validation. Optional markers (paired feature blocks, the mdx-components.tsx markers, and markers that only live in optional files such as the landing page or theme configurator) do not fail the build. When one is absent Folio skips that injection without a message, with one exception: theme.dark_mode: false warns when components/theme-provider.tsx lacks its marker. If a template change did not take effect and the build still succeeded, compare the file in .build/ with the bundled template: a marker that is still there verbatim, or missing from both, was never injected. Config markers. These are inline // __MARKER__ comments appended to a real source line. Folio replaces the entire line (the value plus the marker comment) with the configured value, so both the placeholder assignment and the trailing marker must be present verbatim. Marker stringExpected fileWhat Folio substitutes const configuredDefaultPresetId = \"organic-editorial\" // __FOLIO_THEME_PRESET__. components/theme-configurator.tsx. Rewrites the line to the configured theme.preset. const configuredBasePath = '' // __FOLIO_BASE_PATH__. next.config.mjs. Rewrites the line to the resolved deploy base path. const darkModeEnabled: boolean = true // __FOLIO_DARK_MODE__. components/theme-provider.tsx. Rewrites the line to false when theme.dark_mode is false, so the provider forces the light theme. next.config.mjs is also edited by other substitutions: contentDirBasePath is rewritten to the docs route base, NEXT_PUBLIC_FOLIO_DOCS_ROUTE_BASE is appended next to NEXT_PUBLIC_FOLIO_BASE_PATH, and the __I18N_CONFIG__ line is removed. Localized docs are not available in this release. Generated Files. After Folio copies the template into .build/, it writes generated data into reserved paths: Path in .build/Purpose content/ Generated MDX pages and Nextra _meta.ts files. public/_folio/markdown/ Plain Markdown versions used by page actions and LLM workflows. lib/search-index.ts. Static search document metadata for the navbar search component. lib/folio-template.ts. Build-time project metadata and template params for custom templates. lib/folio-mdx-contract.ts. Versioned list of Folio MDX component names and prop contracts. .folio-manifest.json. Incremental rebuild cache. .folio-build.log. Last frontend build log. template.params contract. template.params is the supported channel for passing project-specific configuration from docs.yaml into a custom template. The contract is small and deliberately strict so that what reaches the frontend is predictable: JSON-serializable. template.params must be a mapping whose values are JSON-serializable (strings, numbers, booleans, null, lists, and nested mappings). Folio validates this at config-load time. A value JSON cannot carry (for example a mapping that uses a list as a key) fails the build with a clear template.params must be JSON-serializable error. Normalized at load time. An absent params key and an explicit null both become {}. A params value that is not a mapping (a list, string, or number) is ignored with a warning and also becomes {}, so the template still builds with empty params instead of crashing. Emitted frozen as as const. Folio writes the validated mapping verbatim into lib/folio-template.ts as folioTemplateParams, serialized with JSON.stringify and suffixed with as const. The as const makes the object and its string/number/boolean literals read-only at the type level, so editors infer literal types (\"dense\", not string). NOT type-checked. Folio does not generate or validate a TypeScript type for your params. folioTemplateParams is typed only by the literal values that happened to be present in the last build. The template owns the meaning of each field and is responsible for any narrowing, defaulting, or runtime validation it needs. Treat unknown fields as unknown and guard before use. Worked example. Pass a navbar variant and product name through docs.yaml: Folio emits the params into lib/folio-template.ts (other exports omitted): A template component then branches on a field, defaulting when it is absent: template.params are build-time/static-site visible. Do not put secrets, API tokens, private keys, or environment-specific credentials in them. MDX Component Contract. Generated API and guide pages can reference Folio component names. A custom template must expose compatible components from mdx-components.tsx, or the Next.js build will fail when generated MDX imports a missing component. Folio writes the current contract to lib/folio-mdx-contract.ts with folioMdxContractVersion. The build also checks custom templates for required component names before running the frontend build, so missing required components fail early. At minimum, custom templates should provide these names when they render Folio’s own generated API and guide output: ComponentUsed for ParamTable. Function and method parameter tables. ClassOverview. Class summaries and inheritance information. SourceLink. Links back to repository source files. ApiReferenceIndex. Generated API reference landing page. Callout, Tabs, TabItem, Mermaid. Converted Markdown/MDX guide content. You can re-export Folio-compatible components, wrap them in product styling, or replace them entirely. The important contract is the component name and props that generated MDX expects. Sidebar Metadata Contract. Folio regenerates Nextra _meta.ts files on every build. Custom templates may rely on these stability guarantees: Root entries preserve Folio’s documented page order first, then append remaining discovered pages. Nested directory index pages are emitted as { \"display\": \"hidden\" } so the folder opens to its index page without duplicating an Index item. Folders start collapsed: while sidebar.default_collapsed is true (the default) a folder entry is emitted as { \"title\": \"…\", \"theme\": { \"collapsed\": true } }. Set it to false and the entry is the plain title string. Folio emits no other collapse state. What Default Options Mean. When template.path is omitted, Folio uses the bundled template and applies Folio-owned options such as theme.preset, theme.tune, theme.dark_mode, logo, favicon, Pagefind search, and the default docs layout. A theme.tune value the configurator does not offer always stops the build and names the nearest valid value. theme.preset is checked the same way against the bundled presets (and a theme package’s) unless template.path is set. When template.path is set, those options are not a guarantee that Folio can style your template. A custom template may choose to read the generated project metadata, use template.params, or ignore Folio’s bundled theme system entirely. That separation is intentional: expert templates get full control instead of a partial override layer. Compatibility Checklist. Before committing a custom template, verify that it: Builds with pnpm install --frozen-lockfile and pnpm run build. Uses Next static export and writes out/. Renders generated content from content/ under the configured docs route. Supports Nextra _meta.ts files if it uses Nextra navigation. Exposes Folio-compatible MDX components from mdx-components.tsx. Handles deploy.base_path or FOLIO_BASE_PATH if the site is deployed under a subpath. Implements search, dark mode, landing pages, or version and language switching itself when those experiences are needed. Keeps template source files under the configured template directory. Treats .build/, content/, public/_folio/, lib/search-index.ts, lib/folio-template.ts, and lib/folio-mdx-contract.ts as generated build output. Custom templates execute frontend install and build scripts. Use templates you own or trust, review dependency changes, and keep the lockfile checked in with the template."},{"url":"docs/components/file-tree/index.html","title":"FileTree","content":"FileTree. A visual file and folder tree for showing project structures, directory layouts, and file hierarchies. Parses an indented text string into a tree with folder/file icons, connectors, and proper nesting. API. FileTree. PropTypeDefaultDescription tree. string. — An indented text string representing the file tree. Lines ending with / are rendered as folders; all others are rendered as files. Nesting is determined by indentation (spaces). Formatting rules: Lines ending with / are rendered as folders (accent-colored icon). All other lines are rendered as files (muted icon). Nesting is determined by indentation level. Multiple root entries are supported at the top level. Example. my-project. pyproject.toml. README.md. src. └── my_lib. ├── __init__.py. └── core.py. tests. └── test_core.py. docs. ├── index.md. └── guide. └── installation.md."},{"url":"docs/developing/index.html","title":"Developer Guide","content":"Developer Guide. How to build, test, and run everything in the repository from a checkout. Folio ships as one native Rust binary: folio. The repository is one Cargo workspace plus the site template the binary renders through. This guide covers building the binary, running the tests, and working on the template. Prerequisites. ToolNeeded forNotes Rust toolchain. The workspace. Install via rustup . rust-toolchain.toml pins the stable channel with clippy and rustfmt; rustup picks it up automatically. Node.js 20.19+ and pnpm 10. folio build, folio serve, and template work. The generated site renders through the bundled Nextra/Next.js template. Not needed to build the binary or run cargo test. Get pnpm with corepack enable pnpm && corepack prepare pnpm@10 --activate, or npm install -g pnpm@10. Get the source. Repository layout. The root Cargo.toml coordinates the packages in one virtual workspace. crates/* contains the eleven engine crates: the CLI host (folio-cli), configuration, parsers, page generation, plugin interfaces, site builder and watcher. folio-cli is both the library and the package that builds the released folio binary, from src/bin/folio.rs. Beside the crates: docs/examples/ — the small example projects the guides build and the tests read. What a build must produce from them is checked in as a golden beside the crate that produces it, under crates/<crate>/tests/fixtures/. template/ — the Nextra/Next.js site template, built with pnpm. docs/ — the guides on this site. The root docs.yaml is the config of Folio’s own site and theme/folio-site its theme. install.sh — the installer behind the install one-liner. Build and verify. Run these from the repository root. The first three are the Rust checks CI runs (CI adds --locked); What CI runs lists the rest. cargo fmt --all --check verifies formatting without rewriting files; drop --check to apply it. cargo clippy --workspace --all-targets -- -D warnings lints every crate and target. CI treats warnings as errors with -D warnings; match it locally to avoid surprises. cargo test --workspace runs the tests across all crates. cargo build --release produces target/release/folio using the thin-LTO, stripped release profile. Installing the binary you built. The release build is a single self-contained executable. Copy target/release/folio to a directory on your PATH (for example ~/.local/bin/) and run folio --version to confirm the shell picks up your build rather than an installed release. To run a command without installing, cargo run --release -p folio-cli -- build --clean builds and runs the same binary. Tests. cargo test --workspace runs every crate’s tests. Unit tests live in a separate file beside the module they test: src/sidebar.rs declares #[cfg(test)] #[path = \"sidebar_tests.rs\"] mod tests; and loads src/sidebar_tests.rs. Tests for lib.rs, main.rs, or mod.rs use a tests.rs file in the same directory. This keeps access to private helpers without mixing the test bodies into implementation files, and a directory under src/ only ever holds submodules. Shared test-only helpers use separate child modules the same way. Crate integration tests live under each crate’s tests/ directory. The CLI suites live in crates/folio-cli/tests/, including the documentation checks in docs_surface.rs: that is the package that builds the binary, so they keep testing the actual executable. The same directory holds the release surface checks (release_surface.rs), workspace layout rules (workspace_boundaries.rs), and the shared support the CLI suites load with mod common; (common/). cargo test -p folio-cli runs all of these targets. Narrow the run while iterating: A new behaviour lands with its test: a unit test when it is local to one function, an integration test under the crate’s tests/ when it crosses crates or touches the filesystem, and a black-box run of the binary when it is what a user sees. The CLI tests in crates/folio-cli/tests/ run the built binary through env!(\"CARGO_BIN_EXE_folio\") against a project from docs/examples/ and check the result against the checked-in golden. The fixtures and the tests are the definition of correct output. Each crate keeps the fixtures its own tests read under crates/<crate>/tests/fixtures/: the config that must load without a warning (folio-config), the sources each reader must parse with the IR they must parse to (golden_ir.json in folio-lang-javascript and folio-lang-rust; rich_package_ir.json and example_package_ir.json in folio-lang-python, the second for the example package under docs/examples/generated-site/), the MDX contract baseline (folio-plugins) and the pages, llms.txt and llms-full.txt a build must write (folio-docs, compared again through the CLI by folio-cli). FOLIO_UPDATE_GOLDEN=1 rewrites the goldens of these tests: The diff is the point: a golden regenerated without reading it turns a regression into the contract. Benchmarks. crates/folio-lang-python/benches/parse.rs is a criterion parse-throughput benchmark over the *.py files under $FOLIO_BENCH_CORPUS. The crate declares it as a [[bench]] target with harness = false and keeps criterion as a dev-dependency, so cargo bench -p folio-lang-python runs it. The template. Everything folio build renders comes from template/, a Next.js/Nextra workspace. Work on it directly with pnpm: pnpm run lint and pnpm run typecheck are the template’s own checks. What CI runs. One workflow gates changes: rust (.github/workflows/rust.yml). It runs on pushes and pull requests to main and develop and on v* tags, and the release workflow calls it before building. Its jobs install the stable toolchain and cache the build: lint (Linux) runs cargo fmt --all --check and cargo clippy --workspace --all-targets --locked -- -D warnings, then the template’s pnpm install --frozen-lockfile, pnpm lint and pnpm typecheck on Node 22 and pnpm 10. test (Linux and macOS) runs cargo test --workspace --locked on Node 22. windows runs cargo test -p folio-cli --test cli_surface --locked. real-export (Linux) runs the two suites ignored by default because they run a real pnpm install and Next export: cargo test -p folio-site --test e2e_export --locked -- --ignored and cargo test -p folio-cli --test build_pipeline --locked -- --ignored. CI does not run cargo build --release; the release workflow does. Releasing. A release is a tag. Bump version in the root Cargo.toml and project.version in docs.yaml to the same string, spelled the Cargo way (0.3.0-a1; the tests check they agree), record the changes in CHANGELOG.md under a ## <version> — <date> heading, merge, then tag v<version> and push the tag: .github/workflows/release.yml fails before building if the tag does not match the workspace version. It then builds one archive per target on five targets (Linux and macOS on x86_64 and aarch64 as .tar.gz with the folio binary at the archive root, which install.sh installs, and Windows x86_64 as a .zip to download by hand), runs install.sh against the two archives the runners execute natively (x86_64 Linux, arm64 macOS), and publishes a GitHub Release with the archives, a SHA256SUMS file and the version’s CHANGELOG.md entry as its notes; a version with no entry fails the release. install.sh downloads from releases/latest, so releases are published as regular releases, not prereleases, while the version string carries the alpha. Pushing a tag is the publication decision; only the repository owner tags. Pull requests that touch the workflow or the installer run the build matrix without publishing, so the release path is proven before a tag exists. Next steps. Architecture — How the CLI, parser, generator, and export pipeline fit together. CLI Reference — Every command, flag, and option. Writing Doc Comments — How Folio reads Python docstrings, JSDoc and Rust doc comments."},{"url":"docs/components/before-after/index.html","title":"BeforeAfter","content":"BeforeAfter. Show two related code snippets side by side. It works well for migration notes, config changes, and generated output comparisons. API. PropTypeDefaultDescription before. string. — Left code snippet. after. string. — Right code snippet. beforeLabel. string. \"Before\" Left label. afterLabel. string. \"After\" Right label. language. string. — Accepted and ignored. Example. RST. .. note:: Install Folio first. MDX. <Callout type=\"note\"> Install Folio first. </Callout>"},{"url":"docs/docstrings/index.html","title":"Writing Doc Comments","content":"Writing Doc Comments. How to write doc comments that produce clear, complete API documentation. Most of this page is about Python docstrings; JSDoc and Rust doc comments close it. For Python, Folio supports Google-style and NumPy-style docstrings. By default, Folio auto-detects the style for each docstring, and the detection also reads reStructuredText and epydoc docstrings. You can force a parser with source.python.docstring_style in docs.yaml, which takes \"auto\", \"google\" or \"numpy\" (see Configuration). Google-style format. Google-style parsing is built in. Set source.python.docstring_style to \"google\" when you want to force this parser for every docstring: A basic Google-style docstring looks like this: NumPy-style format. To use NumPy-style docstrings, set source.python.docstring_style to \"numpy\" in your docs.yaml: A NumPy-style docstring uses section headers with underlines instead of trailing colons: The default source.python.docstring_style value is \"auto\", which detects the style on a per-docstring basis: it parses each docstring as reStructuredText, Google, NumPy and epydoc, and keeps whichever reading finds the most parameters, returns, raises and other fields. This is useful for projects that mix conventions or are migrating from one style to the other. Docstring sections. Folio recognizes the following sections in a docstring. A Google-style heading is written as shown, capitalized and followed by a colon; a NumPy-style heading is underlined. Google-style also accepts Arguments:, Parameters: and Params: for Args:, Exceptions: and Except: for Raises:, Example: for Examples:, and Note: for Notes:. Args. Document function parameters under the Args: section. Each parameter is listed on its own line with an indented description: Returns. Describe the return value under Returns:: Raises. List exceptions the function may raise under Raises:: Examples. Provide usage examples under Examples:. These are rendered as code blocks in the generated docs: Notes. Add extra context under Notes:. This section is useful for implementation details, caveats, or related information: Other sections. Attributes: describes a class’s attributes or a module’s constants (see Class variables). Deprecated:, Warning:, See Also:, References: and Todo: each render as a labelled paragraph after the description, such as Deprecated: followed by the section’s text. Sphinx roles such as :class:`Config` and :meth:`load` read as code. Type annotations. folio extracts type information from two places, in order of priority: Function signatures (preferred) — type annotations on parameters and return types. Docstring type fields — the (type) syntax in Google-style docstrings. If a parameter has a type annotation in the function signature, that takes precedence. If there is no signature annotation, folio falls back to the type specified in the docstring. Both styles produce the same output. When both are present, the signature annotation wins. Decorators and method kinds. folio detects decorators on functions and methods and renders them differently based on their kind. @property. A property is marked @property above its heading, its signature is its name, and its return annotation is shown as its Type. The getter docstring is the description, and no parameters are listed. A setter or deleter written as @full_name.setter or @full_name.deleter folds into the property instead of getting an entry of its own: @staticmethod. Static methods are marked @staticmethod above their heading. Since they don’t receive self or cls, all parameters are documented: @classmethod. Class methods are marked @classmethod above their heading. The cls parameter is left out of the signature and the parameter table, just like self: Custom decorators. Every other decorator is shown as written, above the signature: *args and **kwargs. Variable positional and keyword arguments are fully supported. They appear in the documentation with their * or ** prefix implied by their argument kind: The parser tracks argument kinds internally: var_positional for *args and var_keyword for **kwargs. Keyword-only arguments (those after * in the signature) are tracked as keyword_only. Controlling what gets documented with __all__. Without __all__, Folio documents every top-level class, function and constant whose name does not start with an underscore. Defining __all__ restricts the page to the names it lists, and a listed name is documented even with a leading underscore: Inside a class, a member whose name starts with an underscore is skipped, with two exceptions: __init__ is always documented, and any other dunder method, such as __call__ or __eq__, is documented when it has a docstring. When a function has @overload stubs, only its implementation is shown. __all__ must be a simple list or tuple of string literals, annotated or not. Folio parses it statically from the AST, so a dynamic construction like __all__ = get_exports() is not read, and the module falls back to the underscore rule. Nested classes. folio fully supports nested (inner) classes. They are parsed recursively and appear as sub-sections within their parent class documentation: Inner classes have their own methods, class variables, and can even contain further nested classes. Module-level constants. folio documents module-level constants in two ways: With type annotations — any top-level annotated assignment is captured: Without type annotations — only UPPER_CASE names (without a leading underscore) are captured: The constant’s name, type (if annotated), and assigned value are all shown in the generated documentation. An Attributes: section in the module docstring gives a constant its description. Class variables. Assignments inside a class body, annotated or not, are documented as class attributes, and so are an enum’s members: They appear in a table in the class section with each attribute’s name, type, value and description. The description comes from the class docstring’s Attributes: section, whose type fills in when the assignment has no annotation; an attribute the section names but the class body does not assign, such as one set in __init__, gets a row without a value. Async functions. Async functions and methods are fully supported. Their signature keeps the async keyword, as in async def fetch(url: str, timeout: float = 10.0): Tips for writing good docstrings. Start with a one-line summary. The first line of your docstring becomes the short description: a module’s is its description in the API index, the page metadata and llms.txt. Keep it concise and action-oriented (e.g., “Connect to the database” not “This function connects to the database”). Separate the summary from the body. Leave a blank line between the summary and the longer description. The parser treats the first line as short_description and everything after the blank line as long_description. Document all public parameters. Every parameter that appears in the function signature should have a corresponding entry in the Args: (Google) or Parameters (NumPy) section. Missing descriptions will render as empty in the docs. Use type annotations in signatures. Prefer def foo(x: int) over documenting types in the docstring. Signature annotations are more reliable and are checked by type checkers like mypy. Keep examples runnable. Examples in the Examples: section should be valid Python that a reader could copy-paste. Use >>> prefix for interactive-style examples. Document exceptions explicitly. If your function raises exceptions, list them in the Raises: section. This helps users write proper error handling code. Complete example. Here is a well-documented module that brings the Google-style features of this guide together: JSDoc. The JavaScript reader takes the /** ... */ comment written directly above an exported declaration, with no blank line between them. A file’s first comment documents the module when a blank line separates it from the code below. The description comes first, then one parameter row per @param: [name] is read as the parameter’s name (the row does not mark it optional), [name=default] gives its default, and options.port becomes a row of its own after options. @returns (or @return) and @throws fill the return and raise lines, @deprecated renders as a Deprecated: paragraph, and each @example becomes a code block. {@link Target} reads as Target in code, {@link Target text} or {@link Target|text} as its text, and a URL target as a link. A parameter without a JSDoc type shows Any. Other tags are not read. Rust doc comments. The Rust reader takes /// and #[doc = \"...\"] comments on an item and //! comments for the module, and reads their Markdown as rustdoc does: a code block without a language, or tagged only with rustdoc attributes such as no_run or should_panic, is Rust; lines starting with # inside a Rust block are hidden, as in rustdoc output, and ## at the start of a line stands for a literal #; intra-doc links such as [`Config`], [the config][Config] and [text](crate::Config) keep their text, and URL links stay links; headings such as # Examples and # Panics sit below the item’s own heading. A Rust function shows its signature as written, with no linked types, and its doc comment; there is no parameter table. #[doc = include_str!(\"...\")] is not read in this release."},{"url":"docs/components/accordion/index.html","title":"Accordion","content":"Accordion. Collapsible content sections for organizing information into expandable panels. Use Accordion for FAQs, grouped content, or any situation where you want to reduce page clutter while keeping information accessible. Unlike MethodAccordion, which is structured for API methods, Accordion supports any content. API. Accordion. PropTypeDefaultDescription children. ReactNode. — One or more <AccordionItem> elements. AccordionItem. PropTypeDefaultDescription title. string. — The heading shown in the collapsed state. defaultOpen. boolean. false. Whether the item starts expanded. children. ReactNode. — Content revealed when expanded. Example. What is Folio? Folio is a documentation engine with a native Rust core that uses Nextra and shadcn/ui to produce structured, interactive documentation sites. How does it compare to Sphinx? Folio generates a modern React-based site with live search, dark mode, and responsive design out of the box. It parses the same Google/NumPy docstrings as Sphinx. Can I migrate from Sphinx? Yes. Folio includes a migration guide for moving existing pages and Sphinx conventions to Markdown."},{"url":"docs/cli/index.html","title":"CLI Reference","content":"CLI Reference. Every command, flag, and option of the folio CLI. Running folio with no arguments prints the help text and exits with code 2, like any usage error: an unknown command or flag, a missing value, or a flag that needs another one (build --port without --open). Initialize. folio init. Create docs.yaml for an existing project. --yes. Build. folio build. Generate MDX, search data, LLM files, and static output. --clean--open--verbose. Serve. folio serve. Start a local authoring server with source watching. --port--open--kill-existing. Coverage. folio coverage. Measure Python docstring coverage and enforce a minimum threshold. --min--verbose. Clean. folio clean. Remove the build cache and the generated output directory. --config. Roadmap. folio roadmap. Preview the roadmap phases defined in docs.yaml. --config. Global Options. FlagShortDescription --version. -V. Print the installed Folio version and exit. --update. Replace this binary with the latest GitHub release and exit. --help. -h. Show the help message and exit. Every command accepts it too, as in folio build -h. It prints folio <version>, for example folio 0.3.0-a1. In the folio init example below, X.Y.Z stands for the installed version; it is compiled into the binary. --update follows releases/latest of pguijas/folio to the newest release tag. When that release is newer than the installed version, the archive for this platform is downloaded together with the release’s SHA256SUMS, checked against it, unpacked with tar and swapped into the binary’s own path; the Installed row then shows the new folio --version. A binary that is up to date, or ahead of the latest release, reports so and changes nothing. The flag runs alone: folio --update build is a usage error. It needs curl and tar, the tools the installer uses, and write access to the directory that holds folio. See Update for the environment variables it reads. Commands. folio init. Generate a docs.yaml configuration file for an existing project. This is the recommended way to get started. Argument / OptionRequiredDefaultDescription DIRECTORY. No. Current working directory. Path to the project directory where docs.yaml will be created. --yes / -y. No. false. Skip all prompts and use detected defaults (useful for CI/scripts). Interactive wizard: When run without --yes, folio init shows what it detected, pre-filled from the project scan, and asks two questions: the docstring style and the visual preset. In a terminal each question is an inline menu. The arrow keys or j/k move, Return submits, a digit submits that option, and Esc, q or Ctrl+C abort. The open menu reads ? Docstring style › - Use arrow-keys. Return to submit. above its options; once submitted, it is replaced by one line with the chosen value. While a menu is open, the tips line under the banner (· tip ·) shows the next Folio tip every second. When stdin or stdout is not a terminal, or CI is set, the questions become numbered lists that read one line each; an empty line keeps the default. The screen at 80 columns after Return on both defaults: With --yes there are no questions: the created files and the next commands are listed in a Ready panel, which suggests folio coverage only when a Python source was detected. What it does: Runs node --version and pnpm --version for the tools found on PATH. A missing or too old Node.js or pnpm prints Environment check failed: with one line per problem, as a warning: init goes on, since the build is what needs them. Checks if docs.yaml already exists. If it does, prints docs.yaml already exists, skipping. and exits without overwriting. Reads the project name and version from pyproject.toml, Cargo.toml ([package], or [workspace.package] for the version) and package.json, in that order of precedence. Without any, the directory name and 0.1.0 are used. A manifest that cannot be parsed is reported in a warning and skipped. Folio never runs Python or Cargo: the Python row shows the declared requires-python, or not detected. Detects the GitHub repo URL from git remote origin (if available). Detects one source path per language: Python: the first existing directory of src/<package_name>/ (src layout), src/, <package_name>/ (flat layout) and <project_name>/. Next to a Cargo.toml or package.json, the directory must also hold a .py file. Rust: with a Cargo.toml, the root (./) of a [workspace]; for a single crate, src/ when it holds lib.rs or main.rs, else ./. JavaScript: with a package.json, the first of src/ and lib/ that holds .js, .mjs or .cjs files, else ./ when such files sit at the root. TypeScript is not read in this release. The Source row lists each path with its language, or no sources detected. The Framework row names what was found: a Python framework such as Typer package, Rust crate, Rust workspace or JavaScript package, joined with + in a mixed project, or not detected. Displays the detected project settings, then prompts for docstring style and visual preset (or uses defaults with --yes). Generates docs.yaml with the chosen theme preset, a source entry for each detected language, and an API Reference navigation entry when a source was found. With no source detected, source.python is left as a commented hint. Creates docs/index.md when docs/ is missing or holds no Markdown file. A docs/ with Markdown in it is left untouched. Creates a GitHub Pages workflow at .github/workflows/pages.yml if it does not already exist. Creates a GitHub Pages PR preview workflow at .github/workflows/branch-previews.yml if it does not already exist. Prints created file paths relative to the current shell directory, so folio init tmp/project reports tmp/project/docs.yaml. Examples: Exit codes: CodeMeaning 0. Success, or docs.yaml already exists (skipped). 1. The directory does not exist, or the wizard was aborted. Nothing is written. Esc, q or Ctrl+C print ^C Init aborted. No files changed.; stdin closing before an answer prints Init aborted: stdin ended before an answer. No files changed; rerun with --yes to take the defaults. 2. Usage error, such as an unknown flag. folio build. Parse your source code and documentation files, then generate a static documentation site. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --verbose. -v. flag. false. Show detailed output during the build process. --config. -c. TEXT. \"docs.yaml\" Path to the config file, relative to the project directory. --clean. flag. false. Force full rebuild, clearing the .build/ cache directory. --open. -o. flag. false. Serve the built static site locally, open it in your browser, and keep the preview server running until interrupted. --port. -p. PORT. 8787. Port for the --open preview. When it is taken, the next free port is used and its URL printed. Only valid with --open: folio build --port 9000 alone is a usage error (exit 2). What it does: Loads the config file from the project directory. Parses the configured source files and extracts doc comments, class hierarchies, and module structure. Converts Markdown documentation files to MDX. Generates the Nextra site in a .build/ directory. Runs the Next.js build to produce the final output. Build output uses the same compact terminal rhythm as folio init: a centered Folio banner followed by short status rows for sources, template prep, page generation, link checks, dependencies, export, completion, and the ready output path. Warnings stay attached to their step instead of appearing as raw warning output. During static export, Folio prints the full export log once in a bordered build output panel and also saves it to .build/.folio-build.log. The incremental cache tracks source file hashes plus config, template, and generator inputs. Changing docs.yaml, Folio’s generator code, or the site template invalidates generated pages even when Python and Markdown sources are unchanged. Generator inputs include the Markdown-mirror and LLM/contract generators, so changes to those converters also refresh cached source pages. Examples: folio build --open is intentionally blocking after the build: it starts a static preview server and waits until you press Ctrl+C, which ends the process with the interrupt status (130 in most shells). Exit codes: CodeMeaning 0. Build succeeded. 1. Build failed. Error details are printed to the console. Common causes: missing config file, parse errors, no source modules or documentation found, or Next.js build failure. 2. Usage error, such as --port without --open. folio serve. Build the documentation and start a local development server with hot reloading. This is the primary command during development. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --verbose. -v. flag. false. Show detailed output. --config. -c. TEXT. \"docs.yaml\" Path to the config file. --port. -p. INT. 4321. Port for the development server. A port already in use is refused before the build starts: Error: Port 4321 is already in use. Stop the existing process or rerun with --kill-existing. --open. -o. flag. false. Open the documentation in your default browser after the server starts. --clean. flag. false. Force full rebuild, clearing the .build/ cache directory. --previews. flag. false. Build the example projects under docs/examples/ before serving. Each is a full nested site, so a cold serve skips them and says so; a page that embeds an unbuilt example says so too. --kill-existing. flag. false. Stop the process listening on the selected port before serving. Processes that are only connected to that port, such as a browser tab, are left alone. What it does: Performs the same build steps as folio build, except the example projects under docs/examples/, which it builds only with --previews. Prints Starting dev server... and starts a Next.js development server on the specified port. File watching: after the dev server starts, Folio prints Watching for file changes... and watches the configured source paths of every language and the documentation directories for changes. Edits are automatically detected, the affected pages and local images are regenerated, and the browser updates via hot reload. Incremental builds: subsequent serve runs reuse cached dependencies (.build/node_modules/) and skip pnpm install when deps haven’t changed. Keeps the server running until interrupted with Ctrl+C. In a terminal, Ctrl+C reaches both Folio and the Next.js dev server, so both stop. Folio installs no signal handler in this release: a kill sent to the folio process alone ends Folio but leaves the dev server running on the port, and the next folio serve --kill-existing stops it. The --clean flag forces a full rebuild from scratch, clearing all caches. Markdown routes follow the same rule during builds and watched edits: README.md publishes its folder’s index page, including nested folders and custom documentation URL bases. Editing or deleting the README updates or removes that page and its Markdown mirror. Watched saves are grouped into one source batch. Folio retains parsed Python modules, reparses changed modules, and uses the build generator to refresh pages, navigation, search, Markdown mirrors, the authoring contract, and LLM indexes. Changing the symbol index also regenerates API consumers so renamed or deleted types do not leave stale links. Guides and plugin-collected documents are reparsed on each batch; plugin callbacks remain serial. Local Markdown images refresh when their files change, disappear, or return. The source manifest records copied assets and removes obsolete copies once no page owns them. Identical images keep their modification time. Generated output directories and common editor temporary files are ignored by source watching. If different images map to the same output path, generation reports both source pages and stops before writing; shared images with identical bytes are allowed. Search reuses extracted text for unchanged generated pages within the running builder; page replacement, deletion, and documentation URL changes invalidate it. Unchanged core generated text keeps its bytes and modification time. Changed files are replaced atomically, one file at a time. The contract’s generatedAt records its last semantic change. This is not a transaction across the whole site: a later write or plugin failure can leave some outputs refreshed. The source manifest is saved only after source outputs finish successfully. A parse or render failure occurs before core source pages are written, and pending Python edits are retried on the next source save. Preview examples rebuild when an example changes. Changes to configuration, component implementations, themes, or undeclared plugin inputs still require restarting folio serve. Plugins own the refresh policy for their static bundles. Examples: Exit codes: CodeMeaning 1. Failed to start: a missing config file, a port already in use, or a build error; or the dev server exited on its own with a failure. 2. Usage error, such as an unknown flag. Stopping the server with Ctrl+C ends the process with the interrupt status (130 in most shells), and a kill with 143; there is no 0 exit for an interrupted server. Batch tracing: Set FOLIO_TRACE to record a JSONL event stream for watch batches. See the environment variables section for the full schema. folio coverage. Analyze docstring coverage for configured Python source files. Coverage reads Python only in this release: a config without source.python paths is refused with Error: folio coverage reads Python sources only in this release, and docs.yaml lists no source.python paths. JavaScript and Rust sources still build; they are just not counted. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --config. -c. TEXT. \"docs.yaml\" Path to the config file. --verbose. -v. flag. false. List each undocumented symbol. --min. FLOAT. 0. Minimum coverage percentage. Exits with code 1 if coverage is below this value. Examples: Exit codes: CodeMeaning 0. Coverage check completed and met the configured threshold. 1. Config was missing, it lists no Python source paths, no modules were found, or coverage was below --min. folio clean. Remove generated build artifacts and output directories. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --config. -c. TEXT. \"docs.yaml\" Config file to read output from, relative to the project directory. What it does: Removes two directories: .build/ — the intermediate Nextra project used during the build process. The output directory — by default _site/, or whatever output is set to in the config file. The clean command reads this value directly from YAML, so cleanup still works when the rest of the config is broken; a config it cannot read is reported in a warning and _site/ is used. The output directory is checked the same way folio build checks it: an output that is absolute, outside the project, the project itself, or that contains .git or a configured source directory (such as docs/ or src/) is ignored with a Warning: Ignoring unsafe output in docs.yaml: ... line, and _site/ is cleaned instead. If either path exists but is not a directory, nothing is removed and the command fails. If neither directory exists, it prints “Nothing to clean.” Examples: Exit codes: CodeMeaning 0. The directories were removed, or there was nothing to clean. 1. A path to remove is not a directory (Error: docs.yaml is not a directory; folio clean only removes directories. Check output in docs.yaml.), the project directory does not exist, or a removal failed. 2. Usage error, such as an unknown flag. folio roadmap. Preview the source-defined roadmap phases configured under the roadmap: key in docs.yaml as a table. Provided by the built-in roadmap integration (see the Roadmap page). ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --config. -c. TEXT. \"docs.yaml\" Path to the config file. Examples: Exit codes: CodeMeaning 0. Phases were listed, or no phases are configured. 1. Config file was missing, or conflicting directory arguments were passed. Environment Variables. Folio reads these from the environment. Command-line options win over all of them. FOLIO_TRACE. Path to a JSONL file. When set, folio serve appends one JSON object per watch-batch event: the event name, a monotonic timestamp, and the event’s own fields. Unset, nothing is recorded. FOLIO_BASE_PATH. The static asset base path, such as /my-repo. It overrides deploy.base_path and provider inference. folio serve stays rooted at / unless this is set. FOLIO_DEPLOY_PROVIDER. Set to github-pages to turn on GitHub Pages base-path inference without writing deploy.provider into docs.yaml. NO_COLOR. Set to anything to drop ANSI colour from every command. Error Messages. Here are common errors you might encounter and what they mean: Error: Config file not found: <path>/docs.yaml No docs.yaml was found in the project directory; the message ends with the absolute path Folio looked for. Run folio init to create one, or specify the correct path with --config. Build failed: ... The build process encountered an error. Run with --verbose for detailed output. Common causes include syntax errors in Python source files, invalid YAML in the config, or missing dependencies. docs.yaml already exists, skipping. The init command found an existing config file and did not overwrite it. This is informational, not an error. Typical Workflow. A typical documentation workflow looks like this:"},{"url":"docs/components/terminal-session/index.html","title":"TerminalSession","content":"TerminalSession. Render a command with prompt context, optional output, and a compact status marker. Most guide command snippets should use plain fenced bash blocks. Reserve TerminalSession for cases where prompt context, exact output, or status state matters to the reader. Avoid invented success logs in guide pages. Use output only for exact text the reader should recognize, such as a version check, error message, or URL printed by a command. For setup commands, omit output so the command stays easy to copy. The header copy button copies only the command value, not the prompt, output, or notes. API. PropTypeDefaultDescription command. string. — Command shown after the prompt. output. string | string[] — Terminal output shown below the command. cwd. string. — Optional working directory label. title. string. \"Terminal\" Header label. prompt. string. \"$\" Prompt marker before the command. status. \"neutral\" | \"success\" | \"error\" \"neutral\" Header status color. children. ReactNode. — Optional explanatory note below the terminal block. Example. X.Y.Z is a placeholder for the installed version in this static output example. Check version. $ folio --version folio X.Y.Z."},{"url":"docs/components/type-badge/index.html","title":"TypeBadge","content":"TypeBadge. A small inline badge for displaying a type annotation. Renders as a styled badge with monospace font. When an href is provided, the badge becomes a link to the type’s documentation page. Every type in a generated parameter table is one. API. TypeBadge. PropTypeDefaultDescription type. string. — The type annotation text (e.g. \"str\", \"list[int]\", \"Optional[Config]\"). href. string. — Optional URL to link the badge to a type’s documentation page. Example. Returns a Config object. The timeout parameter accepts float or None."}];
let options={};
const script=document.currentScript;
const rootHref=new URL('./',script&&script.src||document.baseURI).href;
function normalize(value){return String(value||'').normalize('NFD').replace(/[̀-ͯ]/g,'').toLowerCase();}
function escapeHtml(value){return String(value).replace(/[&<>"']/g,function(ch){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch];});}
function escapeRegex(value){return String(value).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');}
function splitPath(path){return path.split('/').filter(Boolean);}
function currentDir(){const current=new URL(location.href);const root=new URL(rootHref);let rel=decodeURIComponent(current.pathname).slice(decodeURIComponent(root.pathname).length);if(!rel||rel.endsWith('/'))return rel;return rel.split('/').slice(0,-1).join('/')+'/';}
function relativePath(target){if(/^(https?:)?\/\//.test(target))return target;const hashIndex=target.indexOf('#');const hash=hashIndex>=0?target.slice(hashIndex):'';const noHash=hashIndex>=0?target.slice(0,hashIndex):target;const queryIndex=noHash.indexOf('?');const query=queryIndex>=0?noHash.slice(queryIndex):'';const path=queryIndex>=0?noHash.slice(0,queryIndex):noHash;const from=splitPath(currentDir());const to=splitPath(path);while(from.length&&to.length&&from[0]===to[0]){from.shift();to.shift();}let rel=[...from.map(function(){return '..';}),...to].join('/')||'index.html';if(location.protocol==='file:'&&/\.html$/.test(rel))rel+='?folio-search=1';return rel+query+hash;}
function excerpt(content,terms){const normalized=normalize(content);let index=-1;for(const term of terms){index=normalized.indexOf(term);if(index!==-1)break;}const start=Math.max(0,index-80);const raw=String(content||'').slice(start,start+220).trim();let html=escapeHtml((start>0?'... ':'')+raw+(start+220<String(content||'').length?' ...':''));for(const term of terms){if(!term)continue;html=html.replace(new RegExp('('+escapeRegex(escapeHtml(term))+')','ig'),'<mark>$1</mark>');}return html;}
function search(term){const terms=normalize(term).split(/\s+/).filter(Boolean);if(!terms.length)return [];return documents.map(function(doc){const title=normalize(doc.title);const content=normalize(doc.content);const haystack=title+' '+content;if(!terms.every(function(token){return haystack.includes(token);})){return null;}const titleHits=terms.filter(function(token){return title.includes(token);}).length;const score=titleHits*10+terms.reduce(function(total,token){return total+(content.includes(token)?1:0);},0);const href=relativePath(doc.url);return {id:doc.url,score:score,words:[],data:async function(){return {url:href,meta:{title:doc.title},sub_results:[{title:doc.title,url:href,excerpt:excerpt(doc.content,terms)}]};}};}).filter(Boolean).sort(function(a,b){return b.score-a.score;});}
const api={options:async function(nextOptions){options=nextOptions||options;},preload:async function(){return null;},search:async function(term){const results=search(term);return {results:results,unfilteredResultCount:results.length,filters:{},totalFilters:{},timings:{preload:0,search:0,total:0}};},debouncedSearch:async function(term){return api.search(term,options);}};
window.__folioStaticSearch=api;
if(location.protocol==='file:'&&!window.pagefind){window.pagefind=api;}
})();
