(function(){
const documents=[{"url":"docs/components/example-tabs/index.html","title":"ExampleTabs","content":"Ask AI. ExampleTabs. Tabbed panels for showing multiple code examples for the same concept, such as basic vs. advanced usage or different input formats. Each tab displays a syntax-highlighted code block. API. ExampleTabs. PropTypeDefaultDescription examples. Example[] \u2014 Array of example objects. Example object. FieldTypeDefaultDescription label. string. \u2014 Tab label text. code. string. \u2014 The code string to display. language. string. \"python\" Syntax highlighting language. Example. Basic With Config Async. node = Node() node.start()."},{"url":"docs/api-reference/folio_docs/docs/site_builder/index.html","title":"folio_docs.docs.site_builder source","content":"Ask AI. folio_docs.docs.site_builder source. Classes. class SiteBuilder. source __init__ source. NameTypeDefaultDescription config. Config. - template_dir. str. - build_dir. str. - verbose. bool. False. Returns: None - @property. manifest_path source. Type: Path - load_manifest source. Returns: dict - save_manifest source. NameTypeDefaultDescription manifest. dict. - Returns: None - prepare source. NameTypeDefaultDescription clean. bool. False. Returns: None - view_routes source. Site-absolute routes of registry views (e.g. {\u201c/roadmap\u201d}). Recorded by apply_extensions; valid link targets for check_links. Returns: set[str] - apply_extensions source. NameTypeDefaultDescription registry. object. - Returns: None - _write_mdx_contract_module source. Rewrite lib/folio-mdx-contract.ts from the live registry. Template preparation writes the module before any plugin has run, so the version it writes can only describe the builtin manifest. Once the registry exists, the config and plugin components flagged contract=True belong in it too \u2014 and the same set is what write_authoring_contract publishes. NameTypeDefaultDescription components. Iterable[ComponentDefinition] - Returns: None - write_authoring_contract source. Publish the authoring contract as a static file in the export. Written under the workspace public/ directory, which the Next static export carries through unchanged \u2014 the same route the per-page Markdown mirrors take. Call it after the extensions are applied, so the component list and the emitted routes are complete. NameTypeDefaultDescription config_keys. Iterable[str] - generated_at. str. - Returns: Path - _runtime source. Returns: NextRuntime - write_page source. NameTypeDefaultDescription route. str. - content. str. - Returns: None - copy_page_asset source. Copy a file a page references to sit beside the generated page. MDX compiles ![alt](shot.png) into import __img0 from \"shot.png\", resolved relative to the generated .mdx. Without the file beside it the build does not merely lose the image, it fails: \u201cModule not found: Can\u2019t resolve \u2018shot.png\u2019\u201d. So a documentation page that shows a screenshot has to carry the screenshot into the content tree, at the same relative path the author wrote. NameTypeDefaultDescription route. str. - relative. str. - source. Path. - Returns: None - copy_static_asset source. Copy a file into public/, where the site serves it verbatim. copy_page_asset puts a file beside a generated page so MDX can import it; this puts a file on the site so a reader can open it. It is how a plugin publishes something it does not own a page for, in the same tree as /_folio/markdown/<route>.md and the preview examples. The file is served exactly as it is on disk, so nothing here renders, rewrites, or interprets it. NameTypeDefaultDescription relative. str. - source. Path. - Returns: None - remove_static_tree source. Drop a subtree of public/ before republishing it. Warm builds keep the workspace, so a file that stopped existing in the project would otherwise stay on the site forever \u2014 deleted from the repository and still served. Emitters that publish a whole directory clear it first and write what exists now. NameTypeDefaultDescription relative. str. - Returns: None - register_route source. Record a route as a live page so link-checking treats it as valid. Plugins should call this for every page they own \u2014 even when they skip write_page because the page already exists from a prior build \u2014 so internal links to plugin pages are not flagged as broken. NameTypeDefaultDescription route. str. - Returns: None - emitted_routes source. Routes recorded via write_page/register_route since the last prepare(). Returns: set[str] - restore_emitted_routes source. Reset the emitted-routes set to a snapshot taken via emitted_routes(). Used by the build core to roll back routes half-registered by a failed emit_assets hookimpl: a plugin that crashed between register_route and write_page must not leave a missing page whitelisted for the link checker. NameTypeDefaultDescription routes. set[str] - Returns: None - page_exists source. NameTypeDefaultDescription route. str. - Returns: bool - read_page source. Return the current on-disk content of a page in the content dir. Plugins use this on warm builds for write-if-changed refreshes of pages they generated on a prior build (e.g. the openapi and kanban plugins compare the existing page against the regenerated content before rewriting it). NameTypeDefaultDescription route. str. - Returns: str - page_markdown_exists source. NameTypeDefaultDescription route. str. - Returns: bool - list_pages source. Routes of the pages currently on disk under a content-dir prefix. The counterpart of remove_page for plugins that generate a variable set of pages: a warm build keeps the workspace, so a page generated for something that no longer exists has to be found before it can be dropped, and only its owner knows which marker to look for. NameTypeDefaultDescription prefix. str. - Returns: list[str] - remove_page source. NameTypeDefaultDescription route. str. - Returns: None - write_meta source. NameTypeDefaultDescription directory. str. - meta_json. str. - Returns: None - read_meta source. NameTypeDefaultDescription directory. str. - Returns: str - remove_meta_tree source. NameTypeDefaultDescription directory. str. - Returns: None - write_llm_files source. NameTypeDefaultDescription llms_txt. str | None. None. llms_full_txt. str | None. None. serve. bool. False. Returns: None - write_preview_examples source. Build named documentation preview examples into public static assets. NameTypeDefaultDescription examples_dir. str | Path. - Returns: None - _build_preview_example_project source. NameTypeDefaultDescription example_dir. Path. - target_dir. Path. - Returns: None - @staticmethod. _reset_preview_example_workspace source. NameTypeDefaultDescription example_build_dir. Path. - Returns: None - _preview_example_base_path source. NameTypeDefaultDescription example_name. str. - Returns: str - _write_preview_example_manifest source. NameTypeDefaultDescription example_dir. Path. - target_dir. Path. - Returns: None - @staticmethod. _preview_example_source_paths source. NameTypeDefaultDescription example_dir. Path. - Returns: list[Path] - _page_path source. NameTypeDefaultDescription route. str. - Returns: Path - _page_markdown_dir source. Returns: Path - _page_markdown_path source. NameTypeDefaultDescription route. str. - Returns: Path - _write_page_markdown source. NameTypeDefaultDescription route. str. - content. str. - Returns: None - @staticmethod. _preview_example_language source. NameTypeDefaultDescription path. Path. - Returns: str - @staticmethod. _mdx_to_markdown source. NameTypeDefaultDescription content. str. - Returns: str - write_search_index source. Write a lightweight search index for folio serve development mode. Returns: None - _content_route_to_docs_url source. NameTypeDefaultDescription route. str. - Returns: str - @classmethod. _mdx_search_title source. NameTypeDefaultDescription raw. str. - route. str. - Returns: str - @classmethod. _mdx_search_content source. NameTypeDefaultDescription raw. str. - Returns: str - @staticmethod. _clean_search_text source. NameTypeDefaultDescription value. str. - Returns: str - install_deps source. Returns: bool - build source. NameTypeDefaultDescription **kwargs. Any. - Returns: None - @staticmethod. kill_port source. NameTypeDefaultDescription port. int. - Returns: bool - serve source. NameTypeDefaultDescription port. int. 4321. kill_existing. bool. False."},{"url":"docs/api-reference/folio_docs/coverage/index.html","title":"folio_docs.coverage source","content":"Ask AI. folio_docs.coverage source. Coverage analysis for Python documentation. Analyzes parsed ModuleIR objects and reports which symbols have docstrings vs which are undocumented. Classes. @dataclass. class CoverageResult. source Coverage statistics for a set of modules. @property. percentage source. Return coverage as a percentage (0-100). Type: float - Functions. _is_private source. Return True if the name is private (starts with _) but not init. NameTypeDefaultDescription name. str. - Returns: bool - _has_docstring source. Return True if the short_description is non-empty. NameTypeDefaultDescription docstring_short. str. - Returns: bool - _analyze_function source. Check a single function/method and classify it. NameTypeDefaultDescription func. FunctionIR. - prefix. str. - documented. list[str] - undocumented. list[str] - Returns: None - _analyze_class source. Check a class and all its public methods. NameTypeDefaultDescription prefix. str. - documented. list[str] - undocumented. list[str] - Returns: None - analyze_module source. Analyze a single module and return coverage statistics. NameTypeDefaultDescription module. ModuleIR. - Returns: CoverageResult - analyze_modules source. Analyze multiple modules. Returns a dict mapping module name to CoverageResult. NameTypeDefaultDescription modules. list[ModuleIR] - Returns: dict[str, CoverageResult] - aggregate source. Aggregate multiple CoverageResults into a single total. NameTypeDefaultDescription results. dict[str, CoverageResult] - Returns: CoverageResult -"},{"url":"docs/components/preview_code/index.html","title":"PreviewCode","content":"Ask AI. PreviewCode. Pair a rendered component example with the exact MDX source that produced it. Use PreviewCode for component catalog examples so readers can inspect the result first and switch to the code without scrolling through a duplicated section. API. PreviewCode. PropTypeDefaultDescription title. string. \u2014 Optional heading shown above the preview/code switcher. description. string. \u2014 Optional short context for the example. defaultMode. \"preview\" | \"code\" \"preview\" Initial tab. children. ReactNode. \u2014 Include one fenced code block plus the rendered preview content. Example. Callout example. The first fenced block becomes the Code tab. The remaining children become the Preview tab. Use real output. Prefer generated pages, command output, and source snippets over abstract placeholders."},{"url":"docs/components/swot/index.html","title":"Swot","content":"Ask AI. Swot. A four-quadrant SWOT analysis rendered as a color-coded grid: strengths, weaknesses, opportunities, and threats, each with its own accent and marker. Use it for honest product or design assessments that a bulleted list would flatten. API. Swot. PropTypeDefaultDescription strengths. string[] \u2014 Internal, helpful. weaknesses. string[] \u2014 Internal, harmful. opportunities. string[] \u2014 External, helpful. threats. string[] \u2014 External, harmful. title. string. \"SWOT\" Header label. Example. Product SWOT. Espresso cart \u2014 Q3. internal \u2191 \u00b7 external \u2193 S Strengths. Best beans in the district. Regulars know us by name. W Weaknesses. One grinder. Cash only. O Opportunities. Office park opening next door. T Threats. Chain opening across the street."},{"url":"docs/components/compare-matrix/index.html","title":"CompareMatrix","content":"Ask AI. CompareMatrix. A comparison matrix with check / dash / partial cells and a highlighted \u201cus\u201d column. Use it where the honest answer is a grid, not three paragraphs of prose. API. CompareMatrix. PropTypeDefaultDescription tools. string[] \u2014 Column headers. rows. { feature, values, note? }[] \u2014 One row per capability; values are true (check), false (dash), \"~\" (partial), or free text. caption. string. \u2014 Label above the feature column. highlight. number. 0. Index of the emphasized column. Example. Tool comparison. CapabilityEspresso cartChain caf\u00e9Vending machine Knows your order. ~ \u2014 Open at 6am. \u2014 Latte artthe swan is negotiable. ~ \u2014"},{"url":"docs/deployment/static-hosts/index.html","title":"Static Hosts","content":"Ask AI. Static Hosts. Static hosts only need the generated _site/ directory. The host can either run uv run folio build --clean itself or serve a prebuilt artifact uploaded by your CI pipeline. Vercel. Use Vercel as a static-site host: Install Folio. Add Folio to your project so Vercel can run the same build command as your local machine: Configure the project. In Vercel project settings, use: Framework Preset: Other. Install Command: corepack prepare pnpm@10 --activate && uv sync --locked. Build Command: uv run folio build --clean. Output Directory: _site. Deploy. Push to your production branch. Vercel runs the build command and publishes _site/. Vercel already builds automatically on push. If you need required documentation checks before deploy, add a CI workflow that runs folio coverage --min 80 and folio build --clean as pull request checks. Netlify. Create a netlify.toml that publishes the static artifact: Netlify will rebuild the docs on each deploy and serve _site/ directly. Self-hosted. Any static file server can host the generated site: For containerized deployments, copy _site/ into a small static web server image such as Nginx, Caddy, or any equivalent platform image. Do not copy .build/; it is a generated workspace cache, not the deployable artifact."},{"url":"docs/kanban/index.html","title":"Index - Folio","content":"Ask AI. Redirecting to the board..."},{"url":"docs/api-reference/folio_docs/workflows/index.html","title":"folio_docs.workflows source","content":"Ask AI. folio_docs.workflows source. Functions. github_pages_workflows source. Returns: dict[Path, str] -"},{"url":"docs/api-reference/folio_docs/docs/integrations/index.html","title":"folio_docs.docs.integrations source","content":"Ask AI. folio_docs.docs.integrations source. First-party integrations published through Folio Docs."},{"url":"docs/kanban/cards/index.html","title":"Folio boards","content":"Ask AI. Folio boards. Independent Docs and Agents work cycles, read directly from the board branch. How a published board accepts a change. A published board is read-only until you give it a write path. The dialog exports git commands, but those land nowhere: a GitHub Pages site has no repository\u2026 The board reads as a tree. parent has been a real field since the cardfile format shipped: one card id, validated against the board, settable from the CLI, and already a filter field.\u2026 Landing for the two-product family. Compare two ways to introduce the Folio product family while Folio Docs is available and Folio for Agents is not: a compact family pre-landing, or a shared\u2026 Artifacts read from the canvas. The build publishes what a card produced; reading it still means leaving the board. A doc: artifact opens a compiled page at /docs/kanban/<id>/<stem>/ wearing\u2026 Artifact viewer becomes a workspace. Opening a card should leave floating layers behind and turn the kanban into one continuous, resizable page. Each selection progresses after its parent: filters\u2026 Kanban is one board with flexible filters. The public board renders every card in one instance, but the only filter is a mount-time URL parameter with no visible control: a visitor who lands on a\u2026 The board takes the name and absorbs the roadmap. Decision card. The kanban plugin renames to board and the roadmap plugin folds into it: one git-backed store, several views. Milestones move out of docs.yaml\u2026"},{"url":"docs/kanban/cards/artifact_viewer_becomes_a_workspace/index.html","title":"Artifact viewer becomes a workspace","content":"Ask AI. Artifact viewer becomes a workspace. Released \u00b7 feature \u00b7 L. Opening a card should leave floating layers behind and turn the kanban into one continuous, resizable page. Each selection progresses after its parent: filters lead to the canvas, the card follows the canvas, and the artifact follows the card to the right when space allows or below when it does\u2026 Progressive canvas direction. Replace the current dialog and artifact overlay with one continuous kanban workspace. Each selection adds its next context without obscuring or\u2026 Progressive canvas (revised direction). progressive-canvas.html. The card belongs to the board."},{"url":"docs/kanban/cards/the_board_reads_as_a_tree/index.html","title":"The board reads as a tree","content":"Ask AI. The board reads as a tree. Backlog \u00b7 0.2 \u00b7 feature \u00b7 XL. parent has been a real field since the cardfile format shipped: one card id, validated against the board, settable from the CLI, and already a filter field. Nothing renders it. The docs admit the gap in as many words \u2014 \u201cparent is a validated pointer, not a workflow\u201d \u2014 and when this card was\u2026 Prototype brief \u2014 nested/decomposition views for the Folio kanban board. These are throwaway prototypes. Nothing here ships. They exist so the owner can look at several answers side by side and pick one. Do not modify\u2026 Five layouts compared. Built 2026-08-20 to answer one question: the cards carry a parent field that nothing renders, so what should render it. Five layouts were built in\u2026 board-data.js. board-data.js. board-inline-expansion.css. board-inline-expansion.css. Inline expansion (rejected). board/cards/the-board-reads-as-a-tree/board-inline-expansion.html. board-inline-expansion.js. board-inline-expansion.js. document-outline.css. document-outline.css. Document outline (rejected). board/cards/the-board-reads-as-a-tree/document-outline.html. document-outline.js. document-outline.js. epic-swimlanes.css. epic-swimlanes.css. Epic swimlanes (rejected). board/cards/the-board-reads-as-a-tree/epic-swimlanes.html. epic-swimlanes.js. epic-swimlanes.js. tree-rail-detail.css. tree-rail-detail.css. Tree rail and detail (rejected). board/cards/the-board-reads-as-a-tree/tree-rail-detail.html. tree-rail-detail.js. tree-rail-detail.js. tree-table.css. tree-table.css. Tree table (chosen). board/cards/the-board-reads-as-a-tree/tree-table.html. tree-table.js. tree-table.js. The card belongs to the board."},{"url":"docs/kanban/cards/artifacts-read-from-the-canvas/brief/index.html","title":"Prototype brief \u2014 reading a card\u2019s artifacts from the canvas","content":"Ask AI. Prototype brief \u2014 reading a card\u2019s artifacts from the canvas. These are decision prototypes. Nothing here ships as written. They exist so the owner can look at several answers side by side and pick one. Everything lives in board/cards/artifacts-read-from-the-canvas/; do not modify anything outside it. The problem. A card can attach what it produced, and the build publishes it. But reading an artifact means leaving the board. A doc: artifact opens a compiled page at /docs/kanban/<id>/<stem>/ \u2014 full documentation chrome, sidebar, breadcrumbs, a place in the docs tree that has nothing to do with the documentation. A file: artifact opens the raw published file in a new tab. Either way the canvas is gone: the column you were scanning, the dialog you were reading, the filter you had typed. The owner\u2019s direction, verbatim in spirit: artifacts must be readable from the canvas, and card pages indexing into /docs is weird. These prototypes answer the first half. A sibling note (where-card-pages-publish.md) answers the second. What already exists (do not redesign it). The board at /kanban renders status columns; clicking a card opens a dialog styled as a mail: header, description, criteria, comments, trail, and an artifact band at the foot (tinted kind icon, label, mono target path). artifacts: entries are typed: doc, file, pr, api, url. A target inside the card\u2019s own directory gets an href at build time; everything else keeps href: \"\" and renders as a plain path, deliberately not a link. The board already writes its filter to the URL (/kanban/?q=...); state that survives a reload and can be pasted into a report is an established habit, not a novelty. The site is static. Nothing writes; nothing needs a server. Your data. board-data.js sets window.BOARD \u2014 the real board, 44 cards, converted from the build\u2019s own generated data module. Card shape: reader-data.js sets window.READER: a map from artifact target to what a reader can show. type: \"markdown\" entries carry pre-rendered html; type: \"html\" entries carry a relative iframe src. An artifact whose target has no READER entry cannot open \u2014 render it as the path it is, visibly not a link. That rule is real: the board holds a doc: into design/research/ and one into docs/guide/ that nothing publishes, and a bare pr: 23. Load both with plain <script src> tags \u2014 no fetch(), these files open over file:// where fetch is blocked. The card that matters for the demo is the-board-reads-as-a-tree: one markdown artifact (a long comparison document with tables and code) and five HTML prototypes that embed live. Make sure the whole demo path works on it. Non-negotiable output shape. Three files, named for your variant slug: <slug>.html, <slug>.css (linked stylesheet), <slug>.js (deferred script). Opens by double-clicking the HTML. No build step, no CDN, no external fonts, no network. Plain HTML + CSS + vanilla JS. Works at 1440px and at 900px. Write files in pieces \u2014 never attempt a single write longer than about 400 lines; build long files up with follow-up edits. Visual language \u2014 match Folio, do not invent a look. Folio reads as a technical publishing system: quiet, exact, source-driven. Thin rules, flat surfaces, --radius: 0 \u2014 square corners. No gradients, no decorative shadows, no icon packs, no emoji. Body in a system sans stack, labels and paths in mono, small uppercase mono labels with wide tracking (11px, letter-spacing .14em) for metadata. Copy these tokens verbatim: Dark is first-class (toggle Auto/Light/Dark like the sibling prototypes): Read ../the-board-reads-as-a-tree/tree-table.html and its CSS for the register in practice (page head, notes panel, theme toggle), and ../the-board-reads-as-a-tree/tree-rail-detail.js for a detail-pane precedent. Do not copy their layout; copy their manners. Required behaviour, whatever the layout. The canvas is real. Render the actual board (columns, all 44 cards) and open a card the way the board does. A minimal dialog is fine (title, metadata line, description, artifact band); the artifact band is the subject, so it is not minimal. A markdown artifact reads well. Comfortable measure, working headings, tables, code blocks, in both themes. The comparison document is ~8k of rendered HTML; pagination is not required, good typography is. An HTML artifact embeds live (iframe), with a visible way to open it full, in its own tab, for when embedding is not enough. A closed door looks closed. No READER entry: the tile renders kind, label, and mono path, and is not clickable. Never a dead link. Walk the band. Previous/next moves through the artifacts of the card being read without going back to the dialog. Esc unwinds one level at a time: reader to card, card to board. Focus returns to the element that opened what you just closed. The reading position is a URL. Opening an artifact writes it to the query or hash; loading that URL reopens board, card, and artifact. The board already does this for filters; reading joins it. Keyboard end to end, visible focus throughout. Both themes, via the same Auto/Light/Dark toggle as the sibling prototypes. Also required: a notes panel in the page itself. A collapsible section titled \u201cWhat this variant argues\u201d with: the one-sentence thesis; what happens to the compiled /docs/kanban/... page if this variant wins (does a deep link still need it, and as what); and what this design is bad at \u2014 be honest, the owner is comparing, and a variant that hides its weakness wastes the comparison. Rules of engagement. Do not touch anything outside board/cards/artifacts-read-from-the-canvas/. Do not run folio build, start a server, or git commit. Build the whole thing: all cards render, every artifact case handled \u2014 a prototype with three fake cards is useless."},{"url":"docs/components/class_overview/index.html","title":"ClassOverview","content":"Ask AI. ClassOverview. A card displaying a Python class with its name, base classes, decorators, and description. This component is typically auto-generated from source code during the build, but you can also use it manually to create hand-crafted class documentation. API. ClassOverview. PropTypeDefaultDescription name. string. \u2014 The class name. bases. (string | { name: string, href?: string })[] [] List of base classes. Each entry can be a plain string or an object with name and optional href for linking to the base class docs. decorators. string[] [] List of decorator names (displayed with @ prefix). description. string. \u2014 A short description of the class. Example. Basic usage with string base classes: @dataclass. class FederatedLearner(BaseNode, Serializable). A node that participates in federated learning rounds. With linked base classes: @dataclass@deprecated. class FederatedLearner(BaseNode, Serializable). A node that participates in federated learning rounds."},{"url":"docs/components/deprecation-notice/index.html","title":"DeprecationNotice","content":"Ask AI. DeprecationNotice. A styled banner indicating that a class, function, or module is deprecated. Displays a red \u201cDeprecated\u201d badge with version information and guidance on what to use instead. This component is typically auto-generated when a docstring contains a Deprecated: section. API. DeprecationNotice. PropTypeDefaultDescription since. string. \u2014 Version when the deprecation was introduced (e.g. \"1.5.0\"). alternative. string. \u2014 Name of the replacement to use. Rendered in a <code> element with \u201cUse \u2026 instead.\u201d phrasing. message. string. \u2014 Custom deprecation message. The rendered card shows: A red \u201cDeprecated\u201d badge. \u201csince X.X.X\u201d in muted text (if since is provided). The custom message (if provided). \u201cUse alternative instead.\u201d (if alternative is provided). Example. Deprecatedsince 2.0.0. This class will be removed in v3.0. Use FederatedNode instead. Minimal usage with just a version: Deprecatedsince 1.5.0."},{"url":"docs/configuration/index.html","title":"Configuration","content":"Ask AI. Configuration. Complete reference for every option in your docs.yaml file. Quick Start. Start with the project name and at least one source input. Folio needs Python source paths or Markdown docs to generate pages: Minimum docs.yaml. This is enough for Folio to scan source, write _site/, and enable the default docs theme. project.name The product name shown in navigation, metadata, and generated pages. string. source.python.paths Python packages or modules to scan. list[string] source.docs Markdown guide directories to include. list[string] This scans your Python package and Markdown guides, outputs to _site/, and enables dark mode. Full Reference. Here is every available section and field. project. Project metadata shown in the navbar, page titles, and generated output. FieldTypeDefaultDescription name. string. \"Untitled\" Project name displayed in the navbar and page titles. If missing or empty, a warning is emitted and \"Untitled\" is used. version. string. \"0.0.0\" Version string shown in the docs header. repo. string. \"\" URL to the source repository (e.g. GitHub). Used for source links and project metadata. repo_ref. string. \"main\" Branch, tag, or commit used for generated source links. url. string. \"\" Public site URL for sitemap, canonical metadata, and social previews. It does not control local routing or static asset paths. source. Controls where folio looks for source code and documentation files. deploy. Controls deployment-specific path handling. Most sites can omit this section. Use it when the static site is published under a subpath such as /my-repo. Base path priority is: FOLIO_BASE_PATH environment variable. deploy.base_path in docs.yaml. GitHub Pages inference when deploy.provider: \"github-pages\" or FOLIO_DEPLOY_PROVIDER=github-pages is active in GitHub Actions. No base path. folio serve stays rooted at / unless FOLIO_BASE_PATH is explicitly set. FieldTypeDefaultDescription provider. \"github-pages\" | string. \"\" Enables provider-specific inference. For GitHub Pages project sites, Folio infers /{repo} from GITHUB_REPOSITORY; user or organization pages like owner.github.io stay at /. base_path. string. \"\" Explicit static asset base path such as \"/docs\" or \"/my-repo\". Use \"/\" for a root deployment. For custom domains or reverse proxies, set the base path explicitly: source.python. The python key can be either a mapping (recommended) or a simple list of paths. Mapping form (recommended): FieldTypeDefaultDescription paths. list[string] [] Python package directories, or a conventional src/ import root. Each path is resolved relative to the project directory. exclude. list[string] [] Glob patterns or directory paths to exclude from documentation. Useful for skipping tests, vendored code, or internal modules. docstring_style. string. \"auto\" Docstring format to parse. Options: \"auto\", \"google\", \"numpy\". source.python. Use the mapping form when you need exclusions or a specific docstring parser. paths Python source directories to scan. list[string] exclude Glob patterns or directories to skip. list[string]default: [] docstring_style Docstring parser: auto, google, or numpy. stringdefault: auto. When src/ itself is listed and is not a Python package, Folio treats it as an import root. A file such as src/tools.py publishes as tools, and src/my_library/client.py publishes as my_library.client; Folio does not add a synthetic src. prefix. A path that names a package directly, such as src/my_library, continues to use that directory as the package name. List form (shorthand): You can also pass a plain list of paths. In this case, no exclude patterns are applied: source.docs. FieldTypeDefaultDescription docs. list[string] [] Directories containing Markdown (.md) documentation pages. These are converted to MDX and included in the site alongside the API reference. .rst files are migration inputs, not build inputs. Convert them to Markdown before placing them in source.docs; Folio warns when .rst files are present in a docs source directory. output. FieldTypeDefaultDescription output. string. \"_site\" Directory where the generated documentation site is written. Resolved relative to the project directory. Removed by folio clean. Folio keeps an incremental manifest in .build/. The manifest includes source hashes plus config, template, and generator fingerprints, so changing docs.yaml or the generator invalidates stale generated pages automatically. theme. Controls the visual appearance of the generated documentation site. FieldTypeDefaultDescription preset. string. \"organic-editorial\" Default visual preset for the generated site. folio init offers organic-editorial, beacon, atlas, and workshop; advanced users can use any preset id available in the bundled theme library. dark_mode. bool. true. Enable the dark mode toggle. When enabled, users can press d to switch between light and dark themes. logo. string. \"\" Path to a logo image file displayed in the navbar. Resolved relative to the project directory. favicon. string. \"\" Path to a favicon file. Resolved relative to the project directory. name. string. \"\" Optional project preset display name. When set with project theme data, Folio adds a Project group to the configurator. description. string. \"\" Optional project preset description. preview. mapping. {} Optional light and dark swatch colors for the project preset preview. radius. string. \"\" Default radius choice for the theme configurator. Must be one of \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\", or a named alias (\"none\", \"sm\", \"md\", \"lg\", \"full\"); any other value fails config validation. tune. mapping. {} Default configurator choices such as font, accent, surface, width, rhythm, borders, code blocks, and radius. style. mapping. {} Safe layout and typography CSS custom property overrides. tokens. mapping. {} Safe light/dark CSS variable overrides for shadcn tokens and project tokens. header. mapping. {} Docs header brand, badge, repository link, search visibility, theme toggle, and project action. variants. mapping. {} Project-owned configurator controls with options, swatches, previews, style overrides, and token overrides. Capped at 256 option combinations across all controls (the product of each control\u2019s option count); larger products fail config validation. package. string. \"\" Local theme package directory copied over the bundled template before generated content and metadata are injected. See Theming for the ownership model, safe personalization options, theme packages, and custom templates. template. Expert escape hatch for replacing the bundled Folio frontend with a project-owned template. Omit this section to use the default Folio template. FieldTypeDefaultDescription path. string. \"\" Local Next/Nextra-compatible template directory. Resolved relative to the project directory. Must stay inside the project and cannot point at .build/ or the output directory. overlay_path. string. \"\" Directory of files layered on top of the bundled template (overlay files win; missing files fall back to the bundled template). Mutually exclusive with path: if both are set, path wins and the overlay is ignored with a warning. Held to the same location guards as path. docs_route_base. string. \"/docs\" Public route where generated documentation pages are served. Folio rewrites generated links, search URLs, sitemap entries, canonical metadata, LLM output, and the copied Next.js docs route to this path. params. mapping. {} Arbitrary JSON-serializable values exposed to the template as build-time data. Folio does not interpret these values. Custom templates own layout, CSS, JavaScript, package dependencies, routing chrome, search UI, and the meaning of template.params. Folio still owns the generated MDX content, _meta.ts files, search index, Markdown exports, and static export pipeline. See Custom Templates for the full contract and required file structure. search. Controls the built-in full-text search powered by Pagefind . Search is enabled by default, appears in the docs navbar, and opens with Cmd+K on macOS or Ctrl+K on Windows/Linux. FieldTypeDefaultDescription enabled. bool. true. Enable or disable the navbar search. Set to false to hide search entirely. placeholder. string. \"\" Custom placeholder text for the search input. When empty, the default \u201cSearch documentation\u2026\u201d is used. To disable search completely: nav. An ordered list of strings defining the top-level sidebar sections. Guide keeps all authored pages together in their normal order. API Reference and Source Code both place the generated source tree, which keeps the stable /api-reference/ route. Other labels order a matching top-level page or folder; unknown labels are ignored rather than creating dead routes. FieldTypeDefaultDescription nav. list[string] [] Top-level navigation sections. sidebar. Controls generated sidebar section behavior. Generated groups start collapsed by default (sidebar.default_collapsed: true). Set default_collapsed: false if you want generated groups to start expanded. FieldTypeDefaultDescription default_collapsed. bool. true. Render generated folder entries with Nextra\u2019s open: false state so sidebar sections start collapsed. Leaf pages are unchanged. Set to false to expand generated groups. By default, Folio emits object entries in generated _meta.ts files for folders: This applies to generated guide folders and API reference folder groups. It does not change the route for folder index pages, and it does not collapse individual leaf pages. llm. Controls generation of llms.txt files for AI consumption. FieldTypeDefaultDescription generate_llms_txt. bool. true. Generate an llms.txt summary file in the output directory. This is a condensed index of your documentation for LLMs. generate_llms_full_txt. bool. true. Generate an llms-full.txt file containing the full text of all documentation pages. Every page is also written as plain Markdown under _folio/markdown/, linked from the page head as a text/markdown alternate and listed in the sitemap. These mirrors are lossy on purpose: complex component props, such as an entire data table, do not survive. Prose, headings, lists, code blocks, Mermaid source, component children, and useful labels from simple cards do. Treat a mirror as the page\u2019s portable text, not as a pixel-equivalent copy of the page. components. Custom React components to include in the generated site. Registered components are copied into Folio\u2019s generated component namespace and exposed to MDX without requiring manual imports in Markdown pages. For registering components from Python (together with data modules, layouts, and views), see the plugins guide \u2014 for a handful of component files, components: is the lighter option. Each list entry is either a directory path or a named component spec: Entry formTypeDescription Directory. string. Path to a directory, relative to the project directory. Every top-level .tsx/.jsx file in it is registered as a component named after its PascalCased file stem (hero.tsx \u2192 Hero, my-chart.tsx \u2192 MyChart). Each file must have a named export matching the derived name. Named spec. mapping. A single component file with an explicit name, source path, and options. Named spec fields: FieldTypeDescription name. string. JSX name exposed to MDX and plugin views. from. string. Path to the .tsx or .jsx component file, relative to the project directory. export. string. Export name in the source file. Defaults to name. expose.mdx. bool. Make the component available in docs MDX. Defaults to true. Validation is loud: a directory that does not exist fails the build, a directory without any .tsx/.jsx files produces a warning, and a from: file that does not exist fails the build when components are copied into the workspace. If two source files share the same filename stem, Folio generates distinct component import paths automatically. Upgrade note: earlier releases gated components: behind an experimental feature flag and silently ignored the key when the flag was off. Now that the key is always active, a malformed value \u2014 a non-list value (components: \"docs/components\" must be components: [\"docs/components\"]), or a list entry that is neither a path string nor a {name, from} mapping \u2014 fails config loading for every command (folio build, folio serve, \u2026). If an upgrade suddenly reports a components error, fix the key\u2019s shape or remove it. A component whose name matches a Folio builtin (for example Callout) replaces the builtin and emits a warning; two components: entries (or a config entry and a plugin component) with the same name raise an error. plugins. Python plugins that extend the build pipeline. See the plugins guide for the hook reference and registration forms. FieldTypeDefaultDescription plugins. list[string] [] Plugin identifiers: a Python module path (e.g. \"my_plugin\") or a relative file path starting with ./ (e.g. \"./plugins/custom.py\"). Path Resolution. In CLI commands, relative paths in docs.yaml are resolved from the active project directory. When you run folio build /path/to/project or folio build --project-dir /path/to/project, that directory becomes the base for config paths. If you call load_config() directly, file plugin paths are resolved from the directory containing the config file. For example, given this structure: The config source.python.paths: [\"src/my_lib\"] resolves to /path/to/my-project/src/my_lib. Config Validation. folio validates your config file and warns about issues: Unknown top-level keys produce a warning listing the unrecognized keys. Core top-level keys are: project, source, output, theme, template, nav, sidebar, llm, search, plugins, components, and deploy. Missing or empty project.name produces a warning and defaults to \"Untitled\". Missing config file raises a FileNotFoundError with the path that was expected. MVP-disabled features are excluded from public docs, API reference, search, sitemap, and LLM output until the feature is ready to publish as beta. These are warnings, not errors \u2014 the build will still proceed with defaults where possible. Common Patterns. Monorepo Setup. When your Python package lives in a subdirectory of a larger repository: Excluding Test Files. Keep test files out of your API reference: Custom Output Directory. Write the built site to a specific location for CI/CD deployment: Multiple Python Source Directories. Document code spread across multiple directories: Minimal Docs-Only Site. If you just want to serve Markdown documentation without Python API reference:"},{"url":"docs/kanban/cards/the-board-reads-as-a-tree/brief/index.html","title":"Prototype brief \u2014 nested/decomposition views for the Folio kanban board","content":"Ask AI. Prototype brief \u2014 nested/decomposition views for the Folio kanban board. These are throwaway prototypes. Nothing here ships. They exist so the owner can look at several answers side by side and pick one. Do not modify anything outside .artifacts/kanban-tree/. The problem. The board (/kanban) renders cards in status columns. Cards can decompose into subtasks, but nothing renders that. The owner wants a nested table view so a task breaks into smaller tasks, and calls it central to how the board gets used. What already exists (do not redesign it). parent: is a real, shipped field on every card: one card id, or empty. validated at build: must be an existing id on the board, and not the card itself. settable via folio kanban add --parent <id> and folio kanban update --set parent=<id> already a filter field, so parent:some-id filters today. no cycle detection beyond self-parent \u2014 a\u2192b\u2192a passes the build, so every tree walk you write must be cycle-safe or it hangs the page. The real board has 36 cards and none of them sets parent:. The data file ships an invented hierarchy so there is something to nest. Decided already (do not re-litigate). The view is read + move: you can see the tree, collapse and expand it, and move a card to another column. Moves stage locally and are then applied by copying folio kanban move commands \u2014 the site is static, it never writes. Reparenting from the UI is out of scope. Changing a card\u2019s parent stays a cardfile edit or a CLI call. Do not build drag-to-reparent. Your data. board-data.js sets window.BOARD. Load it with a plain \u201c \u2014 do not fetch() it, these files are opened over file:// and fetch is blocked there. The hierarchy deliberately contains the cases that break naive implementations: Depth 3: kanban-single-board-with-filters \u2192 the-card-dialog-reads-like-a-mail \u2192 the-dialog-renders-markdown. A child in a different column than its parent: theme-contract-for-plugin-surfaces is in Backlog, its parent plugin-system-unification is in In review. This is the case that makes \u201cnested\u201d and \u201ckanban\u201d disagree \u2014 your design has to answer it explicitly, not accidentally. 15 root cards with no children at all, which must not look broken or second-class. Cards with empty milestone / type / size / assignee. Do not render \u201d\u2014\u201d noise across a whole column of blanks; decide what absent looks like. Non-negotiable output shape. Three files, all in .artifacts/kanban-tree/, named for your variant slug: <slug>.html \u2014 markup and the notes section. <slug>.css \u2014 all styling, linked with <link rel=\"stylesheet\" href=\"<slug>.css\"> <slug>.js \u2014 all behaviour, linked with \u201c Relative <link> and <script> work fine over file://; only fetch() does not. Write them one at a time, in separate tool calls, and never attempt a single write longer than about 400 lines. A previous run of this task died because agents tried to emit one enormous file in one call and the run was killed for inactivity. Build up longer files with follow-up edits. Small, frequent writes. Opens correctly by double-clicking the .html (file://). No build step, no bundler. No CDN, no external fonts, no network of any kind. Use system font stacks. The real site uses Sora (body) and JetBrains Mono (code/labels); approximate with ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", sans-serif and ui-monospace, \"SF Mono\", Menlo, monospace. Plain HTML + CSS + vanilla JS. No React, no framework. Works at 1440px and at 900px wide. It does not need to work on a phone. Visual language \u2014 match Folio, do not invent a look. Folio reads as a technical publishing system: quiet, exact, source-driven. Thin rules, flat surfaces, --radius: 0 \u2014 square corners, this is not a rounded UI. No gradients, no shadows used as decoration, no icon packs, no emoji. Copy these tokens verbatim and use oklch() directly: Dark mode is first-class \u2014 support it with prefers-color-scheme or a toggle: Existing register worth carrying: small uppercase mono labels with wide tracking (font-mono, 11px, letter-spacing .14em) for metadata like FEATURE \u00b7 PLUGIN PLATFORM \u00b7 0.3; the card title in semibold sans at normal size; counts as 13/3 when a WIP limit is exceeded (and that overflow reads in --destructive). Read template/components/kanban-board.tsx for the incumbent card face before you design yours. The raw NUL byte discovered during this comparison has since been replaced, so ordinary text tools read the file normally. Required behaviour, whatever the layout. Collapse / expand a parent, and a way to collapse or expand everything. Filter by a text query over title and id, live. Filtering a tree has a real decision in it: when a child matches and its parent does not, does the parent stay as context, or does the child rise to the root? Pick one, make it look deliberate, and say which you picked in your notes. Move a card to another column, staged locally, with a visible count of staged moves and a way to see the resulting folio kanban move <id> <column> commands. Staging must survive collapse/expand and filtering. Progress: a parent\u2019s children carry status. Show how much of a parent is done, in whatever form your design argues for. Keyboard: arrow keys or tab move between rows, and collapse/expand is reachable without a mouse. Visible focus. Also required: a notes panel in the page itself. At the top or in a collapsible footer, a short section titled \u201cWhat this variant argues\u201d with: the one-sentence thesis of this layout; how you answered the cross-column child; how you answered tree filtering; what this design is bad at (be honest \u2014 the owner is comparing, and a variant that hides its weakness wastes the comparison). Rules of engagement. Do not touch template/, folio/, board/, docs/, or tests/. Do not run folio build or start a server. Do not git commit. Build the whole thing. A prototype with three fake rows is useless; render all 35 cards from the real data."},{"url":"docs/kanban/cards/the_board_takes_the_name_and_absorbs_the_roadmap/index.html","title":"The board takes the name and absorbs the roadmap","content":"Ask AI. The board takes the name and absorbs the roadmap. Released \u00b7 0.2 \u00b7 plan \u00b7 S. Decision card. The kanban plugin renames to board and the roadmap plugin folds into it: one git-backed store, several views. Milestones move out of docs.yaml into board data (a phase moving next \u2192 active becomes a commit with history), /roadmap stays as a view of that registry with its own\u2026 Panel verdict. Five agents, 2026-08-28: one merge assessor, three name generators (plain-literal, market, prior-art lenses), one adversary with kill authority. The\u2026 The card belongs to the board."},{"url":"docs/api-reference/folio_docs/docs/integrations/landing/index.html","title":"folio_docs.docs.integrations.landing source","content":"Ask AI. folio_docs.docs.integrations.landing source. First-party Folio Docs landing integration. The landing feature ships as a default plugin (see DEFAULT_PLUGINS in folio/plugin.py): it is loaded for every build and stays inert until a landing: key appears in docs.yaml. configure() is the single owner of the key \u2014 core folio_docs/config.py no longer parses it \u2014 and populates the Config.landing_* fields consumed downstream by the core template injector. Ownership seam (why there is no register_extensions/emit_assets here): the rendered page intentionally stays in the bundled template (template/app/page.tsx + template/components/landing*.tsx), specialized by marker replacement in TemplateConfigInjector._inject_landing_page during SiteBuilder.prepare(). That injection runs before any plugin emission hook, and the disabled-path fallback (docs index at /) plus the coupled __DOCS_INDEX_CANONICAL_PATH__ / __INCLUDE_DOCS_INDEX__ markers must be written even when this plugin is inert, so no public hook can take that emission over without breaking the documented template contract (docs/guide/theming/custom-templates.md). The plugin therefore owns the config surface; the injector reads the fields this hook sets at load_config time, which happens before prepare(). Functions. config_keys source. Returns: list[str] - configure source. NameTypeDefaultDescription config. Any. - raw_config. dict[str, Any] - Returns: None - landing_enabled source. landing: <bool> shorthand, enabled: subkey (default true), else true. NameTypeDefaultDescription raw_landing. Any. - Returns: bool - landing_hero_variant source. NameTypeDefaultDescription raw_variant. Any. - Returns: str - landing_sections source. NameTypeDefaultDescription raw_sections. Any. - Returns: list[dict[str, Any]] - _string source. NameTypeDefaultDescription value. Any. - default. str. '' Returns: str - _safe_section_href source. A config href validated by the shared scheme policy, or default. Landing sections are presentational, so unlike kanban card links an unsafe or malformed href degrades to the default with a warning instead of failing the build. NameTypeDefaultDescription raw_value. Any. - path. str. - default. str. - Returns: str - _normalize_heading_fields source. Coerce shared heading fields in place: non-strings degrade to absent. NameTypeDefaultDescription section. dict[str, Any] - Returns: None - _normalize_stage source. Coerce the optional stage label in place: blank degrades to absent. Any section (and the hero) may carry a short stage label (\u201cThe mechanism\u201d); the template numbers staged blocks at render time, so the plugin only guarantees a non-empty stripped string. Non-string or whitespace-only values delete the key, and the section renders without a stage rail exactly as before. NameTypeDefaultDescription section. dict[str, Any] - Returns: None - _normalize_actions source. Coerce the optional actions list in place. actions is shared across section types rather than owned by one, so normalizing it per type left cta unguarded: an entry without href reached the template and crashed the prerender on action.href.startsWith. An action needs a title and a usable href; anything else drops with a warning, like every other malformed row. NameTypeDefaultDescription section. dict[str, Any] - Returns: None - _normalize_hero_notice source. The hero\u2019s announcement chip: one plain message plus a link. Kept deliberately simple \u2014 no lists, no rotation, no derived data. A notice without usable text degrades to absent; the link passes the same href scheme policy as every other configured link. NameTypeDefaultDescription raw_notice. Any. - Returns: dict[str, str] - _normalize_boards_section source. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _normalize_mechanism_section source. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _normalize_statement_section source. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _labeled_entries source. Clean a list of {key, detail} mappings; entries without key drop. NameTypeDefaultDescription raw_entries. Any. - key. str. - allow_icon. bool. False. Returns: list[dict[str, str]] - _normalize_harness_section source. The two product surfaces plus the meta-harness relationship. The bundled template owns useful defaults, so omitted lists stay empty and trigger that copy there. Config can replace every label without introducing a new vocabulary or implying that Folio controls the harnesses it wraps. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _normalize_funnel_section source. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _normalize_features_section source. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _normalize_cells_section source. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _warn_builtin_comparison source. NameTypeDefaultDescription source. str. - Returns: None - _comparison_value source. One matrix cell: yes (True), no (False) or partial (\"~\"). An unrecognized value reads as partial rather than as a yes or a no, so a malformed cell never invents a claim about a named tool. YAML parses a bare ~ as null, so values: [true, ~] arrives here as None and lands on partial, which is what the tilde means in the table anyway. NameTypeDefaultDescription raw_value. Any. - Returns: bool | str - _comparison_tools source. The column names; blanks and non-strings drop. NameTypeDefaultDescription raw_tools. Any. - Returns: list[str] - _comparison_rows source. Rows carrying exactly one value per tool; anything else drops. A row whose value count disagrees with tools would slide every cell under the wrong column, so it is dropped with a warning instead of being padded into a claim nobody wrote. NameTypeDefaultDescription raw_rows. Any. - tool_count. int. - Returns: list[dict[str, Any]] - _comparison_table source. {caption, tools, rows} from a config mapping, or {} if unusable. NameTypeDefaultDescription raw_table. Any. - Returns: dict[str, Any] - landing_comparison source. Normalize landing.comparison: the project\u2019s own table, or the bool. Returns {caption, tools, rows} for a configured table, True for the deprecated bool that renders Folio\u2019s bundled matrix, and False when the key is off, absent, or leaves nothing to render. NameTypeDefaultDescription raw_comparison. Any. - Returns: bool | dict[str, Any] - _normalize_comparison_section source. A comparison section carrying the project\u2019s own table. The table sits on the section under the same keys as landing.comparison (caption, tools, rows). A section without a usable table keeps rendering the deprecated built-in matrix, so the keys are dropped rather than handed to the template as an empty shell. NameTypeDefaultDescription section. dict[str, Any] - Returns: dict[str, Any] - _mapping source. NameTypeDefaultDescription value. Any. - Returns: dict[str, Any] - normalize_landing source. Normalize a raw landing: section into its canonical mapping. Accepts the bool shorthand (landing: false) and tolerates non-mapping values anywhere in the tree (they degrade to defaults). Defaults mirror the pre-plugin core parser exactly: hero variant docs-map, primary CTA Get Started//docs, empty secondary CTA, empty install/feature/ section lists, and the comparison section off unless the project fills in its own table. NameTypeDefaultDescription raw_landing. Any. - Returns: dict[str, Any] -"},{"url":"docs/components/feature-cards/index.html","title":"FeatureCard and CardGrid","content":"Ask AI. FeatureCard and CardGrid. Cards for feature overviews, landing pages, and link grids. FeatureCard renders a single card with a named icon, title, description, and optional link. CardGrid arranges multiple cards in a responsive grid layout. API. FeatureCard. PropTypeDefaultDescription title. string. \u2014 The card heading. description. string. \u2014 Short description text displayed below the title. icon. string. \u2014 Optional named icon displayed above the title. Common tokens include install, run, quickstart, workflow, network, topology, analytics, settings, monitor, api, code, server, warning, and community. Unknown values fall back to text rendering for backwards compatibility. href. string. \u2014 Optional URL. When set, the card becomes a clickable link with a hover effect. CardGrid. PropTypeDefaultDescription columns. 2 | 3 | 4. 3. Number of columns on large screens. The grid is always 1 column on mobile and 2 on tablet. children. ReactNode. \u2014 One or more <FeatureCard> elements (or any content). Example. Three-column grid with named icons: Auto API Reference. Extracts documentation from your Python source code automatically. Modern UI. Dark mode, search, and responsive layout out of the box. LLM Output. Generate llms.txt and llms-full.txt for AI assistants. Two-column grid without icons: Getting Started. Install Folio and build your first documentation site. Configuration. Customize your site with docs.yaml options."},{"url":"docs/api-reference/folio_docs/docs/sidebar/index.html","title":"folio_docs.docs.sidebar source","content":"Ask AI. folio_docs.docs.sidebar source. Functions. _meta_value_to_ts source. NameTypeDefaultDescription value. Any. - indent. int. 2. Returns: list[str] - _meta_to_ts source. Convert a meta dict to a TypeScript export default { ... } string. NameTypeDefaultDescription meta. dict[str, Any] - Returns: str - meta_to_ts source. Public wrapper for rendering a Nextra _meta.ts from a meta dict. Exposed for plugins that emit their own _meta.ts entries (e.g. the openapi plugin) so they share Folio\u2019s exact serialization. NameTypeDefaultDescription meta. dict[str, Any] - Returns: str - _build_module_tree source. Build a nested tree from dotted module names. Returns a dict where keys are path segments and values are either nested dicts (intermediate nodes) or None (leaf modules). NameTypeDefaultDescription modules. list[ModuleIR] - Returns: dict - _title_case source. Convert a slug to a human-readable title. NameTypeDefaultDescription slug. str. - Returns: str - _sidebar_title source. Normalize a page title for Nextra sidebar metadata. NameTypeDefaultDescription title. Any. - fallback. str. - Returns: str - _folder_meta source. NameTypeDefaultDescription title. str. - default_collapsed. bool. - Returns: str | dict[str, Any] - _generate_meta_from_tree source. Recursively generate _meta.ts files from a module tree. NameTypeDefaultDescription tree. dict. - path_prefix. str. - result. dict[str, str] - include_index. bool. False. default_collapsed. bool. False. Returns: None - _order_for_dir source. Declared page order for a directory, as (slug, title) pairs. Walks _DOC_PAGE_ORDER one path segment at a time, so a directory at any depth orders its pages. Returns [] for a path nothing declares. NameTypeDefaultDescription path. tuple[str, ...] - Returns: list[tuple[str, str]] - _generate_doc_meta source. Generate ordered doc page entries for the root and subdirectory _meta.ts. Returns (root_meta, extra_meta_files) where: root_meta: dict of slug -> title for the root _meta.ts. extra_meta_files: dict of path -> ts_content for subdirectory _meta.ts files. NameTypeDefaultDescription docs. list[MarkdownResult] - default_collapsed. bool. False. Returns: tuple[dict[str, Any], dict[str, str]] - _move_entries_to_end source. NameTypeDefaultDescription meta. dict[str, Any] - slugs. tuple[str, ...] - Returns: None - _apply_nav_order source. Apply the documented top-level nav order to entries that exist. Guide represents all authored documentation in its existing order; API Reference and Source Code both name the generated source tree. Other labels match an authored page or folder by slug. Unknown labels are ignored instead of creating dead sidebar routes. NameTypeDefaultDescription meta. dict[str, Any] - nav. list[str] - Returns: dict[str, Any] - generate_meta_files source. Generate all _meta.ts files needed (root + docs + source code). Returns a dict mapping file paths (relative) to their TS content. NameTypeDefaultDescription nav. list[str] - modules. list[ModuleIR] - docs. list[MarkdownResult] | None. None. default_collapsed. bool. False. Returns: dict[str, str] -"},{"url":"docs/components/hook_map/index.html","title":"HookMap","content":"Ask AI. HookMap. Visualize an extension lifecycle with stage names, hook names, and short descriptions. API. PropTypeDefaultDescription title. string. \"Extension lifecycle\" Panel title. hooks. HookMapItem[] \u2014 Ordered lifecycle rows. HookMapItem supports stage, hook, and description. Example. Extension lifecycle. 1 Lint. run_linters. Check formatting and static analysis before tests. 2 Test. run_tests. Execute the project test suite with coverage enabled. 3 Build. build_artifacts. Build distributable artifacts from the verified sources. 4 Deploy. publish_site. Upload the built output to the hosting provider."},{"url":"docs/components/file-tree/index.html","title":"FileTree","content":"Ask AI. FileTree. A visual file and folder tree for showing project structures, directory layouts, and file hierarchies. Parses an indented text string into a tree with folder/file icons, connectors, and proper nesting. API. FileTree. PropTypeDefaultDescription tree. string. \u2014 An indented text string representing the file tree. Lines ending with / are rendered as folders; all others are rendered as files. Nesting is determined by indentation (spaces). Formatting rules: Lines ending with / are rendered as folders (accent-colored icon). All other lines are rendered as files (muted icon). Nesting is determined by indentation level. Multiple root entries are supported at the top level. Example. my-project. pyproject.toml. README.md. src. \u2514\u2500\u2500 my_lib. \u251c\u2500\u2500 __init__.py. \u2514\u2500\u2500 core.py. tests. \u2514\u2500\u2500 test_core.py. docs. \u251c\u2500\u2500 index.md. \u2514\u2500\u2500 guide. \u2514\u2500\u2500 installation.md."},{"url":"docs/components/method-accordion/index.html","title":"MethodAccordion","content":"Ask AI. MethodAccordion. Expandable accordion panels for documenting class methods. Each method shows its name and signature in the header, with the full description revealed on click. Async methods display an \u201casync\u201d badge. This component is typically auto-generated for classes with public methods. API. MethodAccordion. PropTypeDefaultDescription methods. Method[] \u2014 Array of method objects. Method object. FieldTypeDefaultDescription name. string. \u2014 Method name. signature. string. \u2014 Parameter signature (e.g. (self, x: int) -> bool). description. string. \u2014 Description of what the method does. isAsync. boolean. false. If true, displays an \u201casync\u201d badge next to the name. children. ReactNode. \u2014 Additional content (parameter tables, examples, etc.) rendered inside the accordion. Example. asyncconnect(self, address: str, timeout: float = 30.0) -> None. disconnect(self) -> None. asyncsend(self, data: bytes, compress: bool = True) -> int."},{"url":"docs/kanban/cards/kanban-single-board-with-filters/index.html","title":"Kanban is one board with flexible filters","content":"Ask AI. Kanban is one board with flexible filters. Released \u00b7 0.1. The public board renders every card in one instance, but the only filter is a mount-time URL parameter with no visible control: a visitor who lands on a milestone deep link cannot see the filter, change it, or clear it. Cards also carry their full body on the board, which buries the column story.\u2026 Reference teardown: SVAR and ReUI. Researched 2026-08-04 for the board redesign. Both libraries were read at the source, not the marketing page. The question was narrow: what should\u2026 The card belongs to the board."},{"url":"docs/plugins/index.html","title":"Plugins","content":"Ask AI. Plugins. One extension point for everything Folio builds: components, data, pages, views, and CLI commands. Folio\u2019s plugin system is pluggy-based and released. First-party plugins ship inside Folio itself and go through the exact same hooks a third-party plugin would use \u2014 if you want to know whether the API can do something, the answer is usually \u201cone of the bundled plugins already does it.\u201d What ships where. Roadmap and Landing Page are default plugins: loaded on every build, activated by one key in docs.yaml, and inert without it. OpenAPI ships in the same package but loads only when folio_docs.docs.integrations.openapi is listed under plugins:. The complete set is listed in the Plugin Catalog. How activation works. For a default plugin the config key is the switch: add it and the plugin builds its pages. Every other plugin is listed under plugins:. Project plugins publish files too. A plugin does not have to render pages. The emit_assets hook writes into the workspace public/ directory, and everything there passes through the static export untouched, so a small project plugin can publish a plain file at the site root. This site does exactly that with docs/plugins/agent_guide.py, which writes agent-guide.md : a briefing you point a coding agent at so it explains, installs, and troubleshoots Folio from this repository\u2019s facts instead of improvising. It covers the concept model, the real install commands and toolchain minimums, a set of symptom-to-command recipes, and the rule that the configuration reference is the authority on what docs.yaml accepts. The board and its repository protocol belong to the independently installed Folio for Agents product. Write your own. The whole surface is documented in Writing Plugins. The bundled plugins double as reference implementations: folio_docs/docs/integrations/roadmap.py for components, data, routes, and CLI, plus folio_docs/docs/integrations/landing.py for config-key ownership."},{"url":"docs/components/index.html","title":"Components","content":"Ask AI. Components. Reusable MDX components for Folio documentation pages, including callouts, tabs, previews, diagrams, and API reference helpers. Component catalog. Pick the right primitive before writing another docs section. Folio ships 30 documentation components across 5 families. Use this page as a routing layer: find the job, open the component, then copy the pattern into a real guide or generated API page. 30 entries5 familiesMDX ready. Fast paths. Start from the shape of the page. Write a setup guide TerminalSession + Steps + Checklist. Explain generated output FileTree + BuildArtifact + ConfigPanel. Document Python APIs ClassOverview + ParamTable + TypeBadge. Workflow Components. Build setup guides, CLI references, migration notes, and plugin docs that stay easy to scan. CommandsTerminalSessionCommand and output blocks with prompt, status, and working directory context.OpenConfigConfigPanelAnnotated configuration snippets for docs.yaml examples.OpenOutputBuildArtifactCompact summaries of generated files, folders, and routes.OpenCLICommandGridScannable CLI command cards with common flags.OpenCompareBeforeAfterSide-by-side migration, config, and output comparisons.OpenStateChecklistReadiness and deployment checklists with explicit states.OpenPluginsHookMapPlugin lifecycle stages with hook names and descriptions.Open. Interactive Components. Add controlled disclosure, alternate examples, and sequence structure without custom page logic. RevealAccordionCollapsible content sections for FAQs and grouped information.OpenViewsTabsTabbed panels for organizing alternative content views.OpenSequenceStepsNumbered step-by-step instructions with a vertical timeline.OpenHistoryTimelineVertical timeline for changelogs and version history.OpenCodeCodeGroupTabbed code blocks for multi-language or multi-variant examples.OpenExamplesPreviewCodePaired rendered previews and source snippets for component examples.OpenExamplesExampleTabsTabbed code panels for basic vs. advanced usage patterns.Open. Display Components. Present diagrams, file structures, notes, feature summaries, and rendered technical notation. MessageCalloutHighlighted message blocks for notes, warnings, tips, and alerts.OpenSummaryFeatureCardsCards for feature overviews, landing pages, and link grids.OpenFilesFileTreeVisual file and folder tree for project structures.OpenPreviewDocPreviewResponsive iframe frames for showing generated docs pages inside a guide.OpenFrameBrowserFrameBrowser window chrome with a mono URL bar for framing embedded examples.OpenDiagramMermaidDiagrams rendered as SVG for flows and sequences.OpenFormulaMathLaTeX math rendering with KaTeX.OpenCodeCode BlocksSyntax highlighting, line numbers, filenames, and word highlighting.Open. API Reference Components. These appear automatically when Folio turns Python source into reference pages. ClassClassOverviewCard displaying a Python class with bases, decorators, and description.OpenMethodMethodAccordionExpandable panels for documenting class methods and signatures.OpenParamsParamTableStructured table for function or method parameters.OpenTypesTypeBadgeInline badge for displaying type annotations with optional links.OpenLifecycleDeprecationNoticeBanner indicating deprecated classes, functions, or modules.Open. Theme Components. Small pieces of site chrome used by the generated Nextra theme. ThemeThemeConfiguratorPopover widget for customizing accent color and border radius.OpenActionsPage ActionsHeader controls for copying a page or opening it with AI context.OpenFeedbackPageFeedbackWas this page helpful? widget with thumbs up/down.Open."},{"url":"docs/components/browser-frame/index.html","title":"BrowserFrame","content":"Ask AI. BrowserFrame. Browser window chrome around any content: three dots, a mono URL bar, and an optional right-aligned status label. Use it to frame live embeds \u2014 board miniatures, rendered components, page excerpts \u2014 as the page they ship on, instead of pasting a screenshot. API. PropTypeDefaultDescription url. string. \u2014 Address shown in the mono URL bar. label. string. \u2014 Optional right-aligned mono label, e.g. \"\u25cf LIVE\". children. ReactNode. \u2014 Content rendered inside the window body. Example. folio-docs.dev/roadmap. \u25cf LIVE. docs releases. 2 / 7. v0.1 Foundation. v0.2 Theming. v0.3 Plugin Platform. In progress. v0.4 More Languages. Up next. + 3 more \u00b7 full roadmap \u2192 agents releases. 0 / 4. v0.1 Repository Board. In progress. v0.2 Session Memory. Up next. v0.3 Project Coordination. Up next. v0.4 Harness Network. Later. Notes. Server-renderable: no client state, safe in any MDX page or generated view. not-prose-armored: margins are reset so it sits cleanly inside prose content. Pairs with <Roadmap compact /> for Docs landing-page miniatures. Optional products may register their own framed components."},{"url":"docs/components/terminal-session/index.html","title":"TerminalSession","content":"Ask AI. TerminalSession. Render a command with prompt context, optional output, and a compact status marker. Most guide command snippets should use plain fenced bash blocks. Reserve TerminalSession for cases where prompt context, exact output, or status state matters to the reader. Avoid invented success logs in guide pages. Use output only for exact text the reader should recognize, such as a version check, error message, or URL printed by a command. For setup commands, omit output so the command stays easy to copy. The header copy button copies only the command value, not the prompt, output, or notes. API. PropTypeDefaultDescription command. string. \u2014 Command shown after the prompt. output. string | string[] \u2014 Terminal output shown below the command. cwd. string. \u2014 Optional working directory label. title. string. \"Terminal\" Header label. prompt. string. \"$\" Prompt marker before the command. status. \"neutral\" | \"success\" | \"error\" \"neutral\" Header status color. children. ReactNode. \u2014 Optional explanatory note below the terminal block. Example. Check version. $ folio --version folio 0.3.0."},{"url":"docs/agents/board/cli/index.html","title":"CLI reference","content":"Ask AI. CLI reference. folio board is a command group. Bare invocation is a read-only table; the write subcommands (add, move, update, trail, comment, attach) operate boards: one logical operation, one targeted file edit, optionally one conventional commit. Shared behavior. OptionMeaning --project-dir PATH. Project directory. Defaults to the current directory. --config, -c. Config file path. Defaults to agents.yaml. --commit. On any write command, wraps the operation in one conventional commit scoped to the board directory. What happenedOutputExit code Validation or write error. Error: <message> 1. Usage error (missing argument, bad option). usage panel. 2. Warning only. warning: <message> 0. Failure upstream of the validator. Python traceback. nonzero. Scripts should treat any nonzero exit as failure rather than parsing for the Error: prefix. Write commands and check require a board directory. When the config has no board.source, when the source names a file or missing path, or when a columns: key is present, the commands exit 1 and nothing is written. Edits are targeted line surgery, never a YAML round-trip, so hand formatting and comments survive. Structurally unusual files (block scalars, multiline values, flow-style artifacts:) get a loud refusal (\u201cedit the file manually\u201d) with the file left untouched. Each write is checked before it is allowed to stand: CommandAfter the edit move, update, attach. The whole board is silently reloaded. If the edit made it invalid, the original file bytes are restored \u2014 a file attach also deletes the copy, or moves it back \u2014 and the command errors with \u201cthe edit was rolled back\u201d. add. The whole board is reloaded, and a rejected card has its new file deleted: a rejected card must never survive to break the next build. trail, comment. Only the written line is validated, with no full reload. A prose line can warn, but it cannot break board topology. init. Creates a board and wires it into the config: board. board.yaml. cards. \u251c\u2500\u2500 read-me-first.md. \u2514\u2500\u2500 _TEMPLATE.md. SKILL.md. agents.yaml. board.yaml declares three columns, cards/ holds one starter card and the copy-me template, SKILL.md is the in-repo operating protocol, and a board: section is appended to agents.yaml so the standalone CLI can find it. FlagMeaning --path. Board directory to create (default board). --branch. Branch to create the board on (default board). --no-branch. Scaffold on the current branch instead. --commit. Commit the new board and the config change together. Refuses rather than overwrites; the four refusal conditions are listed in Start a board. The config is appended to as text, never round-tripped, so comments and hand formatting survive. The generated board is loaded and validated before the command reports success. This is the only command that writes agents.yaml, and it runs before a board exists; see the rules in Operating a board. show. The board as a table: column (with n/limit counts), card id, title, type, size, assignee, tags. Blocked cards are marked with the blocking ids. show accepts the project directory as a positional argument as an alternative to --project-dir; passing both (with different paths) is an error. A board straight out of init, with three cards added and one moved: check. Validates the cardfile board \u2014 the pre-commit and CI gate. Runs the exact normalization the build runs, prints every warning, and exits 1 on the first topology error (the fail list in Board formats). On success: add. Creates cards/<slug-of-title>.md. FlagMeaning --status, -s. Target column id; defaults to the first column. --description, -d. Initial body text. --tags. Comma-separated tags. --assignee. Comma-separated assignees. One name is written scalar, several as a list. --priority. low, normal, or high. --track. Optional workstream inside this product. --milestone. Release id for this product, such as 0.2. --parent. Parent card id; must exist on the board. The card id is the slugified title. An existing cards/<id>.md is an error (\u201calready exists\u201d), as is an unknown --status or --parent. created is set to today. This is the one place the CLI uses a YAML dumper: a brand-new file has no hand formatting to preserve, and the dumper safely quotes titles and tags containing YAML metacharacters. move. A one-line status edit. An unknown card or target column errors before anything is written. FlagMeaning --after CARD. Place the card after an anchor by writing an order rank. --commit. Commit the move. Two conditions warn instead of refusing, at exit 0 with the move applied: WarningCondition WIP limit. The destination column is at or over its limit. Open blockers. The card has blocked_by entries whose card is not in the terminal (last-listed) column. The --after anchor must be in the destination column and must itself carry a numeric order; ranks are the explicit-ordering exception, so the CLI refuses to invent one for the anchor. The new rank is the midpoint between the anchor and the next-higher rank, or anchor + 100 if the anchor is the highest-ranked: Anchor at order: 200, next rank 300 \u2192 the moved card gets 250. Anchor highest at 200 \u2192 it gets 300. All --after validation and math run before any write, so a bad anchor leaves the file untouched. See Ordering for the sort rules. update. FlagMeaning --set field=value. Repeatable. Only the eleven fields below are accepted. --commit. Commit the update. The priority, order, and created warnings below print when the board is next loaded, by check, build, or the next CLI command, rather than at update time: the update\u2019s own revalidation runs with warnings suppressed. FieldRule title. Any string. assignee. --set assignee=ana,bo writes the list form assignee: [ana, bo]; a single name stays scalar. priority. low, normal, or high. Any other value warns and sorts as normal. size. Validated against the scale S, M, L, XL: any case in, uppercase written. order. The explicit rank. A non-numeric value warns and is ignored. parent. Must name an existing card and not the card itself; anything else rolls the edit back. created. YYYY-MM-DD. Any other shape warns, and the card stops ordering and filtering by date. type. Any string. track. Any string naming a workstream inside this product. milestone. Any string. source. Any string. Status changes go through move; tags, blocked_by, and artifacts stay one-line hand edits by design. A multi---set is atomic: if any pair fails, the whole file is restored. trail. FlagMeaning --note, -n. Required. Whitespace is collapsed and the result must be non-empty. --ref. Commit sha or PR #n. Must not contain parentheses or newlines. --actor. Defaults to the slugified git user.name, falling back to local. --commit. Commit the trail line. Appends one session-trail line at the end of the ## Trail section, creating the section at the end of the file if missing. The date is always today. comment. FlagMeaning --by. Author name. Defaults to the slugified git user.name. --commit. Commit the comment. Appends one comment at the end of the ## Comments section, creating the section at the end of the file if missing. The date is always today; the text has its whitespace collapsed and must be non-empty. attach. Two forms, one at a time: a file path, or exactly one artifact flag. A file path copies the file into cards/<id>/, creating the directory if needed \u2014 that alone is the attach, since artifacts: derives from the directory. The card file stays untouched unless --label is given, which also appends one line naming the sibling by its bare name (doc for .md/.mdx, file otherwise) so the label lands on the derived entry. --move moves the file instead of copying it. FlagMeaning --move. Move PATH instead of copying it. --label. Also write the bare-name label carrier line. --commit. Commit the file, and the card when --label was given. A moved tracked source\u2019s deletion rides the same commit. Refused: a PATH that is missing or not a regular file; a filename already present in the card\u2019s directory (remove or rename first \u2014 labelling an existing sibling is the --file form\u2019s job); a name starting with . or _, which derivation skips, so nothing would publish. After the write the whole board is revalidated; on failure the copy is deleted \u2014 or moved back to its source \u2014 and the card file restored. An artifact flag appends one line to the artifacts: block, creating the block if missing \u2014 the form for what is not a file, and for labels. FlagTarget --doc. Project-relative Markdown path. --api. API symbol; any non-empty string. --file. Project-relative path, optionally with an #L12 fragment. --pr. Pull request number; must be positive. --url. URL, subject to the href scheme policy. --label. Optional display label, valid on any kind. --commit. Commit the artifact line. Shape is validated before the write; target validation runs through the full-board revalidation, so a javascript: URL or an escaping path is rolled back. A --doc/--file path that resolves to no file lands and warns at check and build time instead \u2014 a stale path is prose, not topology. For a file already in the card\u2019s directory, attaching by its bare name adds a label to the derived artifact rather than a second entry. A flow-style artifacts: [...] block is refused with \u201cedit the file manually\u201d. --commit. One operation, one commit, scoped to the board directory: git add -- <board-dir> If the board directory has no changes: prints \u201cnothing to commit (the board was already in this state)\u201d and exits 0 without committing. git commit -m \"<message>\" -- <board-dir> Only files under the board directory are ever staged or committed, so a board commit never carries unrelated project changes. Two exceptions stage what the operation itself touched outside the board: folio board init stages the board: section it appended to agents.yaml, because a board without its config entry is not yet loadable; and attach --move of a git-tracked source stages the source\u2019s deletion, because the deletion is the other half of the move. A git failure is an error (exit 1). CommandCommit message init. board: init. add. board: add <id> move. board: <id> <from> -> <to> update. board: update <id> trail. board: trail <id> comment. board: comment on <id> attach (typed flag). board: attach <kind> to <id> attach (file). board: attach <id> <filename>"},{"url":"docs/api-reference/folio_agents/index.html","title":"folio_agents source","content":"Ask AI. folio_agents source. Git-backed board domain for Folio for Agents."},{"url":"docs/api-reference/folio_docs/branding/index.html","title":"folio_docs.branding source","content":"Ask AI. folio_docs.branding source. Functions. _center_text source. NameTypeDefaultDescription line. str. - width. int | None. - Returns: str - folio_news_item source. NameTypeDefaultDescription elapsed_seconds. float. 0. interval. float. 1.0. Returns: str - current_folio_news_item source. Returns: str - folio_news_line source. NameTypeDefaultDescription width. int | None. None. news_item. str | None. None. Returns: str - folio_banner source. NameTypeDefaultDescription version. str. '' width. int | None. None. news_item. str | None. None. include_news. bool. True. Returns: str -"},{"url":"docs/plugins/landing/index.html","title":"Landing Page","content":"Ask AI. Landing Page. Configure the optional homepage that appears before the documentation app. Folio starts as a docs-first site. The landing page ships as a first-party default plugin (folio_docs.docs.integrations.landing): it is loaded on every build and stays inert until a landing: section appears in docs.yaml. When you want a public entry point, add the section and keep the docs under /docs/. Use the landing page for project positioning, install commands, and links to the most important documentation routes. Keep detailed tutorials, configuration reference, and API content in the docs section. Recommended Structure. Hero: project name, one-sentence value proposition, and primary docs link. Install: the shortest command sequence that gets users to a local preview. Proof: links to generated API pages, guides, examples, or release notes. CTA: a final link to /docs or the quickstart guide. The generated landing page uses the same theme tokens, navigation, search metadata, and static export path as the rest of the site. Omit hero.tagline to use the project-based default. Set it to an explicit empty string when the headline should open the hero without a kicker. Section Catalog. landing.sections composes the homepage below the hero from reusable, config-driven sections. Each entry needs a type; everything else is optional and degrades to sensible defaults. Available types: features, comparison, output, routes, pipeline, funnel, install, stats, use-cases, cta, link-grid, cells, boards, harness, mechanism, and statement. Most sections share the same heading fields (eyebrow, title, description). Any section may also set stage: \"The mechanism\" \u2014 sections with a stage get a numbered narrative rail above their heading (STAGE 02 \u00b7 THE MECHANISM \u2026 02 / 04), numbered in page order across all staged sections. Sections without a stage render unchanged. The showcase sections below are documented in detail because they render live plugin data or bespoke layouts. funnel \u2014 the build funnel plate. A technical-diagram centerpiece: source inputs on the left narrow into a single build node, and every output surface fans out on the right. Inputs marked ghost: true render dimmed and dashed \u2014 for roadmap items that are not shipped yet \u2014 and an optional chip labels them (for example \"roadmap\"). An optional guarantees list renders as a mono apparatus strip inside the plate, above the caption. Each input and output may carry an icon node mark, one of: config, python, markdown, language, folder, search, agents, hash, board. An unknown value is dropped and the card renders without a mark. Setting description: \"\" suppresses the heading paragraph, which is how a plate that speaks for itself drops its prose lead. When inputs or outputs are omitted the template renders Folio\u2019s own defaults, so a bare - type: \"funnel\" works. features \u2014 rows or bento. The default features layout is the editorial row list. Setting variant: \"bento\" switches to a two-column bento grid where each card carries a small token-only vignette above its copy via visual, one of: components, llms, receipt, deploy, plugins, theming. An unknown visual is dropped and the card renders without a vignette. cells \u2014 small feature cells. A bento-style row of compact cards: a mono micro-label, a short claim, a one-line description, and an optional footer link (the whole cell becomes the link). The heading is optional \u2014 omit eyebrow/title to render the grid alone. Cells lay out four per row (three per row when the count is divisible by three). harness \u2014 independent products around repository truth. Pairs Folio Docs, the docs generator, with Folio for Agents, the meta-harness. The code-native diagram places the coding harnesses a team already uses inside the Folio for Agents frame. It describes a harness over harnesses without implying that Folio replaces or orchestrates those tools. Folio Docs and Folio for Agents keep separate packages and release cycles. Every label is configurable. Omit harnesses or unifies to use the bundled generic nodes. boards \u2014 live roadmap miniature. Renders the real compact Roadmap component inside browser-window chrome, reading the data generated by the Docs roadmap plugin. The section renders nothing when the roadmap has no data. mechanism \u2014 YAML diff to live UI. Shows the edit-commit-rebuild loop: a code window with an optional git log strip, followed by a pipeline pill rail. Inside code, lines starting with + are tinted as additions and lines starting with - as removals \u2014 indented YAML list items are never mistaken for diff markers. statement \u2014 typographic closer. A huge centered statement with an optional accent-highlighted substring and optional CTA links (two work best). accent must be an exact substring of text; the first action defaults to the primary button style unless a primary flag says otherwise. size: \"md\" steps the headline and padding down for a mid-page thesis block (the default suits closers), and description renders a reading-size lead paragraph under the headline. All hrefs in these sections pass through the same scheme policy as other configured links; an unsafe value (for example javascript:) degrades to the section default with a warning instead of failing the build. Comparison Section. landing.comparison adds a feature matrix to the landing page. The table is yours: you name the tools across the columns and write every row, so the section says what your project wants to say about its own field. Each row needs a feature name and exactly one entry in values per name in tools. A cell is true (yes), false (no), or \"~\" (partial); the strings yes, no, true, and false are accepted too, and anything else reads as partial so a typo never turns into a claim about a named tool. Quote the tilde: a bare ~ is YAML\u2019s null, which also reads as partial. Malformed config degrades instead of failing the build. A row whose value count disagrees with tools is dropped with a warning, because its cells would otherwise slide under the wrong column. Rows without a feature and rows whose values is not a list are dropped quietly. A comparison: mapping left with no usable row renders nothing and warns. The same keys work on a comparison entry in landing.sections, next to the usual eyebrow, title, and description heading fields: The two forms place the table differently. A comparison entry in landing.sections renders where you put it. The top-level landing.comparison key feeds the default section list instead, which a site uses only when it sets no landing.sections and its hero variant is source-pipeline. Deprecated: comparison: true. landing.comparison: true renders Folio\u2019s own built-in matrix, which names the documentation tools Folio compares itself against. It still works and warns on every build; it will be removed. A comparison section with no tools and rows falls back to the same built-in matrix and warns the same way. Replace both with your own tools/rows table. Live demo. This site uses the plugin itself: the landing page at the site root is rendered from the landing: section of this repository\u2019s docs.yaml."},{"url":"docs/components/theme-configurator/index.html","title":"ThemeConfigurator","content":"Ask AI. ThemeConfigurator. A drawer popover widget that replaces the default light/dark control when documentation presets are available. A preset owns color tokens, document rhythm, code block treatment, borders, radius defaults, typography defaults, and its own controls. Changes are persisted in localStorage under a project-scoped key derived from the configured default preset and applied immediately. The default theme CSS is also rendered into the page so the generated site does not flash or fall back to Folio\u2019s bundled typography before hydration. API. ThemeConfigurator. This component takes no props. It is rendered in the documentation sidebar drawer beside the existing appearance controls. Theme Flow. The drawer palette uses a compact two-step flow: StepPurpose Grouped library. Choose a visual direction from Workspace, Product Docs, Reference, or Expressive. Customize. Tune the selected preset\u2019s controls, surface color, accent color, shell spacing, content width, reading rhythm, borders, code blocks, typography, and corner radius. The Back button returns readers from Customize to the grouped library. Light and dark selection lives in the same drawer palette as preset selection, while the keyboard shortcut still toggles between light and dark. Preset Library. All built-in visual directions are presets. The drawer picker shows a current theme summary before the grouped library. Each group is rendered as a compact carousel row so the panel stays short as the preset library grows. GroupPresets Workspace. Workshop, Canopy. Product Docs. Beacon, Aperture, Ledger. Reference. Atlas, Stacks, Draftline, Proof. Expressive. Organic Editorial, Carbon. PresetPurpose Workshop. Warm generated-site workspace based on the saved inspiration example, with light paper contrast and botanical accents. Canopy. Compact green workspace for simple examples, source snippets, and generated guides. Beacon. Main product-docs preset with endpoint cards, compact API workflows, and high-contrast examples. Atlas. Classic reference documentation with paper rhythm and sharp source examples. Ledger. Dense API tables and register pages for fast scanning. Proof. Printed-manual hierarchy with strong rules and editorial emphasis. Stacks. Calm catalog reading for long guides and stable navigation. Draftline. Working-document warmth for docs before release. Aperture. Neutral developer-docs style with compact spacing and rounded code panels. Organic Editorial. Poster-scale typography with cobalt organic image language for launches, programs, and editorial docs. Carbon. Stark monochrome technical mode. Organic Editorial is the default preset because it makes generated sites feel distinctive on first load while still keeping docs controls, code blocks, and API reference pages available through the same preset system. Each preset can be selected directly. Selecting a preset applies its default controls, typography, accent, radius, and layout defaults. Customize keeps the selected preset active while changing its options. Workshop and Canopy include a Borders control for switching between fine, structured, and ruled outlines. This keeps the saved inspiration\u2019s framed workspace feel available without forcing every generated page into the same border weight. Shared Controls. Every preset can expose its own controls. The global controls are always available and override the selected preset when changed: ControlPurpose Surface color. Override the page, sidebar, card, muted, and border palette. Shell spacing. Expose the outer page padding used by framed workspace themes. Content width. Adjust the prose and component measure without changing presets. Reading rhythm. Override base type size, line height, section gaps, and card padding. Borders. Tune card and shell rule strength globally. Code blocks. Switch source examples between soft, framed, plate, and terminal treatments. Typography. Switch heading, body, and code font treatment. Accent color. Override the preset\u2019s primary accent. Corner radius. Adjust shared UI and card radius. Radius Options. OptionValue None. 0. Sm. 0.3rem. Md. 0.5rem. Lg. 0.75rem. Full. 1rem. The \u201cReset appearance\u201d button restores the configured default preset and tuning. Project Theme Contract. Projects can define their own ThemeConfigurator preset in docs.yaml without forking the bundled template. During template preparation, Folio writes theme/project-theme.ts with a typed ThemePreset that merges Folio\u2019s base docs tokens with the project\u2019s overrides. tokens.light and tokens.dark accept CSS custom properties such as shadcn tokens (--background, --card, --border, --chart-1) and project tokens (--status-running). style accepts ThemeConfigurator layout variables such as --folio-content-max-width, --folio-section-gap, --folio-card-padding, --folio-code-bg, --folio-workspace-shell-topbar, --folio-workspace-shell-topbar-blur, and --folio-workspace-shell-topbar-border. Un-prefixed legacy names such as --content-max-width are still accepted for compatibility but are deprecated; use the --folio-* names. header.brand and header.badge replace the default docs navbar wordmark. header.repo, header.theme_toggle, header.action_label, and header.action_href replace the default docs navbar actions with project-owned actions; header.search: false hides the navbar search field while leaving the generated search index controlled by search.enabled. variants defines project-owned preset controls; each option can set a swatch for the control UI, override preview, style, and light/dark tokens while inheriting the base project theme. If an option has swatch but no full light/dark preview, Folio uses the swatch for both preview modes. For safety, token and style keys must be CSS custom properties beginning with --, values must be strings without CSS statement or block delimiters, and header and variant labels cannot include markup. Unknown tune keys are ignored with a warning. theme.radius must be one of the fixed radius scale values \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\", or a named alias (\"none\", \"sm\", \"md\", \"lg\", \"full\") that maps onto the same scale; any other value fails config validation. theme.variants is capped at 256 option combinations across all controls (the product of each control\u2019s option count) because every combination is resolved and embedded into each generated page; a larger product fails config validation. Tune aliases map to the shared controls: YAML keyRuntime control font. fontId. accent / color. colorId. surface. surfaceColorId. shell. shellPaddingId. width / content_width. contentWidthId. rhythm / reading. rhythmId. borders / border. borderId. code / code_blocks. codeTreatmentId. font: \"geist\" selects the bundled Geist/Geist Mono pair and maps the public --font-sans / --font-mono tokens used by Tailwind utility classes. If theme.preset matches a built-in preset and the project only provides tune, Folio keeps the built-in preset and applies the configured defaults. If the project supplies name, description, preview, style, tokens, or variants, Folio adds a Project group to the drawer and places the project preset before the built-in library. Theme Packages. Theme packages can replace the bundled configurator, project header actions, or theme/project-theme.ts while Folio still supplies generated content and metadata. See Theme Packages for the ownership model, file overlay rules, and validation checklist. Create a Custom Preset. Custom presets live in template/theme/presets.ts and use the interfaces from template/theme/preset-types.ts. Create a new object that satisfies ThemePreset. Give it stable defaultOptions, optional defaultRadiusIndex, optional defaultCustomization, and matching controls. Implement resolve(options) so every option combination returns light, dark, style, radius, and preview. Add the exported preset to presets. The style object must use the namespaced --folio-* keys defined by ThemeStyle in template/theme/theme-contract.generated.ts (generated from folio/schemas/theme_contract.py). Un-prefixed keys such as --card-shadow fail the TypeScript check against ThemeStyle and are not read by the generated CSS. Example: Then register it: Generate a Preset with ChatGPT. Paste this prompt into ChatGPT, then add the returned object to template/theme/presets.ts. After ChatGPT returns a preset: Paste it into template/theme/presets.ts. Add the exported preset to presets. Run pnpm run lint from template. Build the docs and open the drawer palette button. Test each generated control in light and dark mode. Example. Layout Component. ThemeConfigurator is rendered in the documentation drawer when theme presets are available. If presets are disabled, Nextra\u2019s native light/dark control remains in place."},{"url":"docs/agents/board/agents/index.html","title":"Operating a board","content":"Ask AI. Operating a board. The contract for moving a board without breaking it: the session protocol, and every rule with the reason it holds. This is the page an agent reads before it touches board/. folio board init writes board/SKILL.md into the project that owns the board, giving agents an in-repo entry point even when they have not opened the published docs. Any agent follows the same steps, whether Claude Code, another framework, or a human with an editor; git is the only backend. How an agent finds this. Nothing installs this protocol. An agent arrives at it by one of three paths, each of them an ordinary file. board/SKILL.md, in the checkout. folio board init writes it with a skill preamble: a name, and a description naming when it applies. The preamble is the shape skill-reading runtimes recognize, but those runtimes scan their own directories, not board/, so today an agent reaches this file because something points at it: a line in the repository\u2019s agent instructions, or whoever opened the session. Folio registers the file with nothing: it calls no runtime and knows of none. The published guide, when configured. The optional Folio Docs adapter can include these pages and the board in an agent-readable static site. What init scaffolds is a stub, not the contract: how to read the board, four commands, five rules, and a closing line sending the reader to these pages for the card schema and the validation rules. It is meant to grow with the project. The checked-in skill remains canonical even when a site publishes a copy. Session lifecycle. Start: move your card into the working column before the feature work begins. During the session: ordinary code commits. The board stays untouched. End of session: Append one trail line to every card you touched, with the commit sha or PR number as the ref. Move cards whose state changed. Commit work outputs (research, analysis, designs) into the card\u2019s own directory, board/cards/<id>/. The files at its top level are the card\u2019s artifacts, derived at build: Markdown compiles into site pages, other files publish verbatim, and each one opens on the served board. Attaching by hand is for labels and for what is not a file (pr:, url:, api:); a sibling\u2019s target is its bare name. A doc: elsewhere opens only when a docs source publishes that page; anything else validated-but-unpublished is shown as its path. Run folio board check before committing. It is the standalone board gate; the optional Docs adapter consumes the same loader. What a session leaves behind. The card\u2019s directory, board/cards/<id>/, is the session\u2019s workspace, and because the build publishes it, what a session produced is readable from the board rather than only from a clone. One file per output. A brief is a file, a comparison is a file, each prototype is a file. One notes.md accumulating a whole session is not something a later reader opens twice. The top level is the band on the card. Every file at the directory\u2019s top level is an artifact, derived \u2014 so curation is placement. What a later reader would open sits at the top level; supporting material (a prototype\u2019s stylesheet, its data) goes in a subdirectory, which still publishes but is not listed. Scratch stays out. Screenshots from a verification run, throwaway scripts, downloaded fixtures: those go outside the board entirely, or into a dot-directory inside the card\u2019s own (.verify/, .cache/), which the build leaves behind. A _-prefixed file stays in the bundle but off the band and out of compilation. When a decision needs looking at rather than arguing about, keep a brief stating the problem and the rules, one file per variant, and a comparison that says what each variant is bad at. Those files publish from the card\u2019s directory and open from the card\u2019s page. The build enforces little here: a doc: or file: target that resolves to no file warns at build and check, naming the card and the path, and a directory whose card is missing is reported. Whether the card owns the path decides only whether the artifact opens. Everything above is convention. No build fails because a card directory is one long notes.md. Two-stage artifact work. Artifact generation uses exactly two compact stages: Review creates standalone candidates in one owning card\u2019s directory and presents their links without changing product files. Integrate starts only after explicit owner confirmation, applies the selected direction to Folio, and validates it. Between stages, retain only the objective, confirmed decisions, open choice, canonical artifact links, and validation state. Generation transcripts, rejected detail, and repeated file contents are not durable project memory. Condense artifact context. An agent using the board skill recognizes this maintenance directive: It is not a shell command or Folio CLI surface. It replaces verbose artifact-related working context with one compact checkpoint containing the five fields above. The scope is exactly one permanent card id: the agent may read only cards/<id>.md and cards/<id>/. It must reject all, ., paths, globs, multiple ids, a missing card, or any attempt to widen the operation. Condensing context never edits, moves, or deletes files. A durable change found during maintenance is a separate operation and still requires explicit owner confirmation. Say where it lives. Every operation ends with a report the human can click. Never announce an outcome without linking what it produced. Before the handoff, open or request every rendered artifact URL and verify it responds successfully. A repository path or local filesystem path alone is not an artifact notification. The card file. board/cards/<id>.md is the permanent home of the plan; link it every time you create or change a card. The served board. The card is on /kanban/, and /kanban/?q=<filter> narrows it to what you are reporting on: /kanban/?q=status:in-review tag:spec is a link to exactly the cards you just described. The filter is one expression, and ?milestone=<v> still works on its own. Every artifact you attached from the card\u2019s directory. Markdown opens at <docs route>/kanban/cards/<id>/<stem>/; raw files open at /_folio/kanban/<id>/<file>. Link the rendered page, not the repository path. \u201cCard created\u201d is an incomplete report. The contract is: \u201cCard created: board/cards/<id>.md \u2014 on the board at /kanban/.\u201d One logical operation, one commit. Every board commit touches only board/ and carries a conventional message. The CLI\u2019s --commit flag produces these automatically, scoped to the board directory \u2014 unrelated staged changes are never swept in. The message for each operation is listed under --commit. Editing by hand (no CLI required). The format is plain files; any editor works, under the same rules the CLI enforces: Move a card: change the status line to another column id. That is the whole move: a one-line diff. Add a trail line: append it as the last line of the ## Trail section, never at the top (create the section at the end of the file if missing). Create a card: copy cards/_TEMPLATE.md to cards/<slug>.md. The filename must be a lowercase slug and is permanent \u2014 it is the card id. Lists are hand edits by design: tags, blocked_by, and removing an artifact or trail line are one-line edits in the file; the CLI does not do them. Rules, and why they hold. RuleWhy it holds Never edit agents.yaml during a session. It is the Agents control plane; only init writes it, and write commands read it only to resolve board.source. board.yaml is human-owned. Column changes are topology; a human asked for them or they don\u2019t happen. Never rewrite other cards\u2019 files in your commit. One card, one file is what makes conflicts structurally impossible. Omit order unless placement was requested. Ordering is computed; ranks are the exception, never bulk-renumbered. Appends go at the section end. Tail appends make concurrent-session conflicts mechanically resolvable. How the CLI edits files without destroying your formatting is covered in the Kanban CLI; why prose warns and topology fails, in Board formats. The browser never writes card files; it captures intent. A drag stays in the browser until you export it as a list of folio board move commands, so every mutation still funnels through the validating CLI and ordinary git review. Merge conflicts. Conflicts are rare by construction: one card is one file and there is no shared index, so two sessions editing different cards can never conflict. Only two shapes exist, both with mechanical resolutions. Both sides moved the same card: Resolution: read both sessions\u2019 trail lines and keep the column that reflects reality after both, done here if the other session\u2019s trail line records the work landing. Keep both trail lines. The second shape is both sides appending a trail line to the same card. Keep both lines, ordered by date. Never use a union merge driver: a silent both-sides merge of a status edit corrupts the frontmatter."},{"url":"docs/theming/personalization/index.html","title":"Personalization","content":"Ask AI. Personalization. Personalization keeps the bundled Folio template and changes the theme data it receives. This is the right level when the docs should still feel like Folio, but with your project\u2019s brand, typography, accent, spacing, and header defaults. Basic Theme Options. FieldPurpose preset. Default visual preset for the generated site. dark_mode. Enables light, dark, and system mode support through next-themes. logo. Copies a project logo into the generated site and renders it in the docs chrome. favicon. Copies a project favicon into the generated site. Dark mode is enabled by default. When enabled, readers can use the theme control or press d outside form fields to switch between light and dark modes. Theme Configurator. Every generated site includes the Folio ThemeConfigurator drawer when theme presets are available. The drawer exposes the configured default preset, the built-in preset library, and tuning controls for typography, accent, surfaces, content width, reading rhythm, borders, code blocks, shell spacing, and radius. Reader preferences are persisted in a project-scoped localStorage key derived from the configured default preset. Folio also renders the default theme CSS into the page before hydration so the site does not flash through unconfigured typography or colors. See ThemeConfigurator for the built-in preset catalog and the TypeScript preset contract. Tune Defaults. Use theme.tune to set the default configurator choices without creating a new preset: Common aliases map to the internal configurator ids: YAML keyInternal control font. fontId. accent or color. colorId. surface. surfaceColorId. shell. shellPaddingId. width or content_width. contentWidthId. rhythm or reading. rhythmId. borders or border. borderId. code or code_blocks. codeTreatmentId. font: \"geist\" selects the bundled Geist and Geist Mono font pair and maps the public --font-sans and --font-mono tokens used by Tailwind utilities. theme.radius (and its theme.tune.radius alias) is validated against the fixed radius scale: \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\". The named aliases \"none\", \"sm\", \"md\", \"lg\", and \"full\" map onto the same scale (matching the configurator\u2019s radius labels). Any other value fails config validation, because the configurator maps the configured radius onto this fixed scale. Project Presets. When a project supplies name, description, preview, style, tokens, or variants, Folio emits theme/project-theme.ts during template preparation and adds the project preset before the built-in preset library. docs.yaml. A project-owned theme preset generated from safe YAML data. theme.name Label shown in the configurator Project group. string. theme.preview Light and dark swatches for the preset preview. object. theme.style Layout and typography CSS custom properties. object. theme.tokens Light and dark shadcn/project CSS variable overrides. object. theme.header Docs header brand, badge, repo, search, and action controls. object. theme.variants Project-owned preset controls with options, swatches, and token overrides. object. tokens.light and tokens.dark accept CSS custom properties such as shadcn tokens (--background, --card, --border, --chart-1) and project tokens (--status-running). style accepts layout variables such as --folio-content-max-width, --folio-section-gap, --folio-card-padding, --folio-code-bg, --folio-workspace-shell-topbar, --folio-workspace-shell-topbar-blur, and --folio-workspace-shell-topbar-border. The un-prefixed legacy spellings (for example --content-max-width) are still accepted for compatibility but are deprecated; new configs should use the canonical --folio-* names. Header URL Validation. The header can carry links: theme.header.repo and theme.header.action_href. Folio validates these URLs at config-load time against an allowlist of safe schemes. Permitted values are: http and https URLs (for example https://github.com/acme/sdk); mailto: links; and. relative URLs (for example /dashboard or docs/index). Unsafe schemes \u2014 notably javascript: and data: \u2014 are rejected. A header link using a rejected scheme raises a config error and fails the build, naming the offending field, rather than being emitted into the generated site. This keeps script-injection payloads out of the docs header even when the docs.yaml comes from an untrusted source. Use an http(s), mailto:, or relative URL for any header link. The top-level project.repo is validated less strictly because repository URLs legitimately use non-web schemes: ssh://, git://, git+https://, and scp-style git@host:path forms are accepted, and only schemes that could execute script or read local files (javascript:, data:, vbscript:, file:) are rejected. Variants. Variants let a project expose its own configurator controls. Each option can set a swatch, preview colors, style overrides, and light/dark token overrides while inheriting the base project preset. If an option has swatch but no full light/dark preview, Folio uses the swatch for both preview modes. Every option combination across all variant controls is resolved and embedded into each generated page, so theme.variants is capped at 256 combinations (the product of each control\u2019s option count). Exceeding the cap fails config validation; reduce the number of controls or options. CSS Variables. Folio themes use oklch colors through CSS custom properties: Sidebar-specific variables control the left navigation independently from the main content area: VariablePurpose --sidebar. Sidebar background. --sidebar-foreground. Sidebar text color. --sidebar-primary. Sidebar active item color. --sidebar-accent. Sidebar hover/focus background. --sidebar-border. Sidebar border color. When Personalization Is Not Enough. Use theme packages when the project needs to override files inside the bundled template while keeping Folio\u2019s template as a base. Use custom templates when the project needs to own the entire frontend workspace."},{"url":"docs/kanban/cards/artifacts-read-from-the-canvas/index.html","title":"Artifacts read from the canvas","content":"Ask AI. Artifacts read from the canvas. Released \u00b7 0.1 \u00b7 feature \u00b7 L. The build publishes what a card produced; reading it still means leaving the board. A doc: artifact opens a compiled page at /docs/kanban/<id>/<stem>/ wearing full documentation chrome, and a \u201cKanban\u201d folder sits in the docs sidebar between \u201cMigrating from Sphinx\u201d and \u201cSource Code\u201d as if board\u2026 Prototype brief \u2014 reading a card's artifacts from the canvas. These are decision prototypes. Nothing here ships as written. They exist so the owner can look at several answers side by side and pick one.\u2026 Four readers compared. Built 2026-08-24 to answer one question: a card can attach what it produced, but reading it means leaving the board, either for a compiled page under\u2026 Where card pages publish. Written 2026-08-24, beside four prototypes of reading a card's artifacts from the board canvas. The prototypes answer how a human reads a card's\u2026 board-data.js. board-data.js. board-reader-page.css. board-reader-page.css. Board reader page. board/cards/artifacts-read-from-the-canvas/board-reader-page.html. board-reader-page.js. board-reader-page.js. dialog-reader.css. dialog-reader.css. Dialog reader. board/cards/artifacts-read-from-the-canvas/dialog-reader.html. dialog-reader.js. dialog-reader.js. reader-data.js. reader-data.js. reading-overlay.css. reading-overlay.css. Reading overlay (recommended). board/cards/artifacts-read-from-the-canvas/reading-overlay.html. reading-overlay.js. reading-overlay.js. reading-rail.css. reading-rail.css. Reading rail. board/cards/artifacts-read-from-the-canvas/reading-rail.html. reading-rail.js. reading-rail.js. The card belongs to the board."},{"url":"docs/components/command_grid/index.html","title":"CommandGrid","content":"Ask AI. CommandGrid. A compact command overview for CLI-heavy pages. Use CommandGrid with one or more CommandCard children. API. CommandGrid. PropTypeDefaultDescription children. ReactNode. \u2014 CommandCard elements. CommandCard. PropTypeDefaultDescription command. string. \u2014 CLI command shown in monospace. title. string. \u2014 Human-readable command name. description. string. \u2014 Short task description. href. string. \u2014 Optional link target. flags. string[] [] Common flags shown as badges. Example. Initialize. folio init. Create docs.yaml from project metadata. Build. folio build. Generate MDX, search, and static output. --clean--verbose."},{"url":"docs/components/compare_matrix/index.html","title":"CompareMatrix","content":"Ask AI. CompareMatrix. A comparison matrix with check / dash / partial cells and a highlighted \u201cus\u201d column. Use it where the honest answer is a grid, not three paragraphs of prose. API. CompareMatrix. PropTypeDefaultDescription tools. string[] \u2014 Column headers. rows. { feature, values, note? }[] \u2014 One row per capability; values are true (check), false (dash), \"~\" (partial), or free text. caption. string. \u2014 Label above the feature column. highlight. number. 0. Index of the emphasized column. Example. Tool comparison. CapabilityEspresso cartChain caf\u00e9Vending machine Knows your order. ~ \u2014 Open at 6am. \u2014 Latte artthe swan is negotiable. ~ \u2014"},{"url":"docs/components/callout/index.html","title":"Callout","content":"Ask AI. Callout. Highlighted message blocks for notes, warnings, tips, and other important information. Each type has a distinct color and icon to help readers quickly identify the nature of the message. API. Callout. PropTypeDefaultDescription type. \"note\" | \"warning\" | \"info\" | \"tip\" | \"check\" | \"danger\" \"info\" The visual style of the callout. title. string. \u2014 Optional bold title displayed above the content. children. ReactNode. \u2014 The callout body content. Available Types. TypeColorUse for note. Blue. General notes and remarks. warning. --warning (ochre). Important warnings the reader should not miss. info. Slate. Neutral supplementary information. tip. Emerald. Helpful tips and best practices. check. Green. Success states or confirmation of correct behavior. danger. Red. Critical warnings about destructive or breaking behavior. Example. Python 3.10+ This library requires Python 3.10 or later. This function modifies the input array in place. Performance. Use batch mode for datasets larger than 10,000 rows. All tests passing. The test suite completed successfully with 100% coverage. Breaking Change. The legacy_mode parameter was removed in v2.0. This is a neutral informational message with no title."},{"url":"docs/api-reference/folio_docs/parser/python_parser/index.html","title":"folio_docs.parser.python_parser source","content":"Ask AI. folio_docs.parser.python_parser source. Functions. _resolve_style source. NameTypeDefaultDescription style. str. - Returns: docstring_parser.Style - _parse_docstring source. NameTypeDefaultDescription raw. str | None. - style. docstring_parser.Style. docstring_parser.Style.GOOGLE. Returns: DocstringIR - _get_annotation source. NameTypeDefaultDescription node. ast.expr | None. - Returns: str - _parse_function source. NameTypeDefaultDescription node. ast.FunctionDef | ast.AsyncFunctionDef. - source_file. str. - is_in_class. bool. False. style. docstring_parser.Style. docstring_parser.Style.GOOGLE. Returns: FunctionIR - _parse_class source. NameTypeDefaultDescription node. ast.ClassDef. - source_file. str. - style. docstring_parser.Style. docstring_parser.Style.GOOGLE. Returns: ClassIR - parse_python_file source. NameTypeDefaultDescription path. Path. - module_name. str. - style. docstring_parser.Style. docstring_parser.Style.GOOGLE. Returns: ModuleIR - _is_python_excluded source. Match project-relative glob patterns and exact file/directory paths. NameTypeDefaultDescription path. Path. - excludes. list[str] - Returns: bool - _is_python_import_root source. Whether a configured path is the conventional src/ import root. NameTypeDefaultDescription path. Path. - Returns: bool - parse_python_directory source. NameTypeDefaultDescription directory. str. - package_name. str. - excludes. list[str] - docstring_style. str. 'google' Returns: list[ModuleIR] - parse_python_source_root source. Parse modules and packages beneath a conventional import root. NameTypeDefaultDescription directory. str. - excludes. list[str] - docstring_style. str. 'google' Returns: list[ModuleIR] -"},{"url":"docs/components/accordion/index.html","title":"Accordion","content":"Ask AI. Accordion. Collapsible content sections for organizing information into expandable panels. Use Accordion for FAQs, grouped content, or any situation where you want to reduce page clutter while keeping information accessible. Unlike MethodAccordion, which is structured for API methods, Accordion supports any content. API. Accordion. PropTypeDefaultDescription children. ReactNode. \u2014 One or more <AccordionItem> elements. AccordionItem. PropTypeDefaultDescription title. string. \u2014 The heading shown in the collapsed state. defaultOpen. boolean. false. Whether the item starts expanded. children. ReactNode. \u2014 Content revealed when expanded. Example. What is Folio? Folio is a modern Python documentation generator that uses Nextra and shadcn/ui to produce structured, interactive documentation sites. How does it compare to Sphinx? Folio generates a modern React-based site with live search, dark mode, and responsive design out of the box. It parses the same Google/NumPy docstrings as Sphinx. Can I migrate from Sphinx? Yes. Folio includes a migration guide for moving existing pages and Sphinx conventions to Markdown."},{"url":"docs/api-reference/folio_docs/docs/builtin_drift/index.html","title":"folio_docs.docs.builtin_drift source","content":"Ask AI. folio_docs.docs.builtin_drift source. Guard the builtin manifest against the bundled MDX components. Two guards, because the manifest can drift from the template in two ways. Functions. template_component_entry_names source. Extract shorthand component entry names from mdx-components.tsx. Comments and import statements are ignored; only whole-line shorthand mapping entries (Name, at any indentation) count. NameTypeDefaultDescription template_text. str. - Returns: list[str] - check_template_drift source. Compare the builtin manifest and a template\u2019s component entries. Bidirectional: reports builtins declared in :data:BUILTIN_COMPONENTS that have no entry in template_text, and component entries present in template_text that the manifest does not declare. Returns human-readable drift descriptions; empty when the two agree. NameTypeDefaultDescription template_text. str. - Returns: list[str] - _matching_brace source. Index just past the brace group opening at text[start]. NameTypeDefaultDescription text. str. - start. int. - Returns: int - _split_members source. Split an object type body into members, ignoring nested punctuation. NameTypeDefaultDescription body. str. - Returns: list[str] - _object_fields source. Map member name -> required, for one object type body. NameTypeDefaultDescription body. str. - Returns: dict[str, bool] - _resolve_alias source. The object body of a same-file interface/type declaration. NameTypeDefaultDescription source. str. - name. str. - Returns: str | None - _props_type_text source. The type expression annotating component\u2019s destructured props. NameTypeDefaultDescription source. str. - component. str. - Returns: str | None - component_prop_names source. Map component\u2019s prop names to whether they are required. Handles both an inline destructured annotation and a named Props interface or type alias declared in the same file. Returns None when the component is not declared in source. NameTypeDefaultDescription source. str. - component. str. - Returns: dict[str, bool] | None - component_object_field_names source. For each object-shaped prop, the field names it carries. A prop typed Array<{ ... }> or SomeAlias[] declares a shape the manifest also spells out, so the two shapes have to agree too - that is how ApiReferenceIndex came to name four fields its component never reads. NameTypeDefaultDescription source. str. - component. str. - Returns: dict[str, set[str]] - _type_expression_fields source. Field names of the first object shape a type expression resolves to. NameTypeDefaultDescription source. str. - type_text. str. - Returns: set[str] - _manifest_field_names source. NameTypeDefaultDescription type_text. str. - Returns: set[str] - check_component_prop_drift source. Compare each builtin\u2019s declared props against its component source. Reports manifest props the component does not accept, component props the manifest omits, optionality mismatches, and object-shape field mismatches. Components whose source cannot be found or parsed are skipped rather than reported, so a refactor to a new declaration style degrades to no guard instead of a false alarm. NameTypeDefaultDescription components_dir. Path. - Returns: list[str] -"},{"url":"docs/agents/board_component/index.html","title":"KanbanBoard","content":"Ask AI. KanbanBoard. Optional Folio Docs canvas for a Folio for Agents board, with filtering, staged drag-and-drop, and a progressive public workspace. API. PropTypeDefaultDescription compact. boolean. false. Hides the filter bar, card detail dialog, staging card, and URL parameter syncing. For embedding boards in documentation pages. maxCardsPerColumn. number. \u2014 Caps the number of cards shown per column in embeds. Example. Activate the kanban plugin with your own source to render this preview. Preview unavailable. The standalone CLI reads the board configured by board.source in agents.yaml. The optional Folio Docs adapter reads kanban.source in docs.yaml. Folio\u2019s own operational board lives on its independent board branch, so this release documentation shows the invocation without coupling the code release to that board\u2019s update cycle. Progressive workspace. The public board keeps one continuous surface order: filters, canvas, selected card, selected artifact. Selecting something adds its context after its parent; it never covers the canvas. A wide viewport continues the chain to the right. When the chain no longer fits, the card or artifact continues below without changing document or keyboard order. Each hairline between surfaces is a resizer. Drag it, use its arrow keys, or double-click it to restore the default. Closing the artifact returns to the card; closing the card also closes its artifact. Filters close independently. The URL carries ?card= and ?artifact=, and a readable artifact can enter and leave the browser\u2019s native full-screen mode. Open context is reading space, not a preview. The card starts at 760px in its active dimension and the artifact at 1040px wide or 900px high. Resizing cannot reduce the card below 440px wide or 420px high, nor the artifact below 560px wide or 480px high. The card continues below through 1535px, the artifact through 2559px, and only larger viewports use all four surfaces in one row. Boards embedded inside documentation keep the modal card and artifact drawer. Theming. Every structural node carries a data-slot attribute for styling. Additional attributes mark dynamic state. AttributeTarget data-slot=\"kanban\" Root container. data-slot=\"kanban-filters\" Filter bar. data-slot=\"kanban-filter-panel\" Filter composer rail or drawer. data-slot=\"kanban-workspace\" Responsive layout owner for the public canvas. data-slot=\"kanban-workspace-divider\" Keyboard- and pointer-resizable separator between workspace surfaces. data-slot=\"kanban-canvas-shell\" Toolbar and board canvas surface. data-slot=\"kanban-board\" Board canvas holding all columns. data-slot=\"kanban-column\" Single column container. data-slot=\"kanban-column-header\" Column title and count. data-slot=\"kanban-card-list\" List of cards within a column. data-slot=\"kanban-card\" Individual card tile. data-slot=\"kanban-card-dialog\" Card detail dialog. data-slot=\"kanban-card-panel\" Selected card in the public workspace. data-slot=\"kanban-artifact-drawer\" Reading drawer for a published artifact, inside the card dialog. data-slot=\"kanban-artifact-reader\" Selected artifact in the public workspace. data-slot=\"kanban-staging\" Move staging card shown when moves are pending. data-workspace-surface. Surface identity (canvas, card, or artifact) in the public workspace. data-column. Column identifier on column nodes. data-card. Card identifier on card nodes. data-dragging. Present on cards during drag operations. The staging banner and its slot describe the current browser-staged flow; the write-path direction is tracked on the project board. The board page renders on pure white in light mode. Dark mode and every preset keep their own background."},{"url":"docs/theming/theme-packages/index.html","title":"Theme Packages","content":"Ask AI. Theme Packages. [!WARNING] Security A theme package is executed as trusted code during every build. The package files are overlaid onto the template that Folio then builds with pnpm install --frozen-lockfile (which executes dependency lifecycle scripts) and next build (which runs next.config.mjs in Node), so pointing theme.package at a directory is equivalent to running that code on your machine. Only point theme.package at frontend code you have reviewed and trust. Building an untrusted repository or branch in CI inherits the same remote-code-execution surface \u2014 treat a docs build like any other untrusted-code execution. Never put secrets, API tokens, or credentials in theme code or docs.yaml: static docs are published. Theme packages are the middle ownership level. They let a project ship a local theme overlay that is copied over the bundled Folio template before generated content and metadata are injected. Use a theme package when YAML personalization is too limited, but a full custom template would duplicate too much of Folio\u2019s default docs runtime. Configure a Package. theme.package is resolved relative to the project directory. The directory is trusted frontend code: it can override template files, import dependencies already available to the template, and replace Folio\u2019s default theme modules. Ownership Model. OwnerResponsibilities Folio. Copy the bundled template, apply the theme package overlay, generate content, write metadata, inject fallback theme config, run the frontend build, and export static files. Theme package. Own selected frontend files such as layouts, global CSS, the configurator UI, project header actions, or theme/project-theme.ts. Folio still writes generated docs content and reserved build files into .build/. Do not edit .build/ directly; change the package source instead. Package Anatomy. A package can be small and only override the files it owns: Common override points: FilePurpose app/layout.tsx. Own font loading, metadata shell, providers, and global layout behavior. app/docs/layout.tsx. Own the docs navbar, sidebar placement, search slot, and ThemeConfigurator mount. components/theme-configurator.tsx. Replace the bundled configurator UI with a project-specific implementation. components/project-header-actions.tsx. Replace the default generated header action component. theme/project-theme.ts. Own all presets, controls, variants, defaults, and resolved tokens in TypeScript. app/globals.css. Override or replace template-level CSS when a project needs exact visual parity. YAML and TypeScript Together. If the package supplies theme/project-theme.ts, Folio does not overwrite it with a YAML-generated module. The package owns the preset implementation. If the package omits theme/project-theme.ts, Folio still emits the safe docs.yaml-driven project preset described in Personalization. This lets a project start with YAML and graduate individual surfaces to TypeScript only when needed. Register a Custom Preset. A theme package can register a custom preset without forking presets.ts. From theme/project-theme.ts, import registerPreset and call it before exporting the projectThemePreset. The registerPreset function is the supported extension point. The optional second argument (\"project\") adds the preset to that display group in the configurator UI. If a preset with the same id already exists, it will be replaced with a console warning. See ThemeConfigurator for the full ThemePreset contract, including controls, resolve, and all theme fields. What Packages Should Not Do. Do not assume files written under .build/ are stable source files. Do not put secrets in theme code or docs.yaml; static docs are public. Do not use a package when the project needs a different app structure, dependency graph, route model, or product shell. Use custom templates for that level. Validation Checklist. Folio validates theme packages at build time before any overlay. If validation fails, the build halts with a clear ValueError listing all violations. Reserved Paths. A theme package must NOT contain any of these paths. Folio generates them at build time, and including them will cause a validation error: content/ \u2014 Generated docs pages and metadata live here. lib/folio-template.ts \u2014 Folio internal contract file. lib/folio-mdx-contract.ts \u2014 Folio internal contract file. .next/ \u2014 Next.js build cache. node_modules/ \u2014 Dependency install directory. Required Exports. If a theme package includes theme/project-theme.ts, that file MUST export both: projectThemePreset \u2014 The preset definition (colors, spacing, variants). projectThemeDefaultConfig \u2014 The default configuration object. Folio checks for export const <name> or export { <name> } forms. Missing exports fail validation. Example Validation Error. Why This Fails Early. Validation runs before any file overlay. You get a clear Python error with actionable messages instead of a cryptic Next.js build failure or runtime import breakage."},{"url":"docs/api-reference/folio_agents/ops/index.html","title":"folio_agents.ops source","content":"Ask AI. folio_agents.ops source. Board operations as importable functions \u2014 the write path every surface shares. The CLI\u2019s subcommands, the serve write API, and any future mount all call these four operations; each one is a targeted file edit through the verified surgery in kanban_edit, a board revalidation with rollback, and optionally one conventional commit whose pathspec is limited to the touched card file. Refusals raise (OpError, or ExpectationError when the board moved under the caller); nothing here prints. Classes. class OpError(ValueError). source A refused operation: bad input, unknown ids, a failed commit. class ExpectationError(OpError). source The caller\u2019s picture of the board is stale; nothing was written. @dataclass. class OpResult. source class _CardDumper(yaml.SafeDumper). source Block mappings, inline sequences \u2014 the format the rest of the CLI reads. The mapping has to be block style: every other command edits card frontmatter by line surgery, so a card dumped as {title: ..., status: ...} could never be moved again \u2014 move looked for a status: line, found none, and refused the file as structurally unusual. The sequences have to stay inline: tags: [cli, core] is the one-line hand edit the format documents, and PyYAML\u2019s default_flow_style is all-or-nothing, so the two rules need a representer rather than a flag. Functions. _represent_flow_sequence source. NameTypeDefaultDescription dumper. yaml.SafeDumper. - data. list. - Returns: Any - resolve_actor source. Returns: str - _today source. Returns: str - _project_dir source. NameTypeDefaultDescription board_dir. Path. - project_dir. Optional[Path] - Returns: Path - _load_board source. NameTypeDefaultDescription board_dir. Path. - project_dir. Path. - Returns: dict[str, Any] - _card_path source. NameTypeDefaultDescription board_dir. Path. - card_id. str. - Returns: Path - _find_card source. NameTypeDefaultDescription board. dict[str, Any] - card_id. str. - Returns: tuple[dict, dict] - revalidate_or_rollback source. Board-level validation after an edit; a bad board never survives. The line-surgery editor verifies each file in isolation, but only a full reload catches board-topology damage (a dangling parent set via update, an artifact whose doc target is missing). On failure the card file gets its original bytes back before the error propagates. NameTypeDefaultDescription board_dir. Path. - project_dir. Path. - path. Path. - original. str. - Returns: None - commit_paths source. Stage and commit exactly paths; True when a commit was made. False means the working tree already held this state \u2014 not an error, the caller decides whether that is worth a word. NameTypeDefaultDescription project_dir. Path. - paths. list[Path] - message. str. - Returns: bool - compute_after_rank source. The midpoint rank for an after-anchor, computed from the pre-edit board. NameTypeDefaultDescription column. dict[str, Any] - after. str. - _rank source. NameTypeDefaultDescription card. dict[str, Any] - Returns: float | None - move_card source. Move a card to another column \u2014 a one-line status: edit. NameTypeDefaultDescription board_dir. Path. - card_id. str. - status. str. - expect_status. str | None. None. after. str | None. None. actor. str. '' commit. bool. True. project_dir. Optional[Path] None. Returns: OpResult - update_card source. Set one allowlisted frontmatter field (assignee takes a comma list). NameTypeDefaultDescription board_dir. Path. - card_id. str. - field_name. str. - value. str. - actor. str. '' commit. bool. True. project_dir. Optional[Path] None. Returns: OpResult - comment_card source. Append one comment to the card\u2019s thread (always at the end). NameTypeDefaultDescription board_dir. Path. - card_id. str. - text. str. - actor. str. '' commit. bool. True. project_dir. Optional[Path] None. Returns: OpResult - add_card source. Create a new card file; an empty status lands in the first column. NameTypeDefaultDescription board_dir. Path. - title. str. - status. str. '' description. str. '' tags. list[str] | None. None. priority. str. '' track. str. '' milestone. str. '' parent. str. '' assignee. list[str] | None. None. actor. str. '' commit. bool. True. project_dir. Optional[Path] None. Returns: OpResult -"},{"url":"docs/why-folio/index.html","title":"Why Folio","content":"Ask AI. Why Folio. The questions that decide whether Folio is for you \u2014 answered honestly. \u201c Why Folio exists. Documentation rots because it lives apart from the code. Folio generates your docs from your source \u2014 the reference rebuilds from the current code, and the site it ships in is one you\u2019re proud to put your project\u2019s name on. That\u2019s the whole pitch: you keep writing docstrings and markdown, Folio turns them into a modern site with a generated API reference, instant search, and theming down to the last token. One command against a Python codebase produces: 1. docs.yaml. the entire configuration. 0. hand-written API pages. the reference is parsed from source. 40. components. typed vocabulary for rich pages. 0. servers. static output, deploy anywhere. Folio against the field. Feature matrix comparing Folio with pdoc, Sphinx, MkDocs, Mintlify, and GitBook.ToolPython APIGuidesStatic exportLLM friendlyExtensibilityOpen sourceGit + CIFolio Yes. Yes. Yes. Yes. Yes. Yes. Yes. pdoc Yes. Some. Yes. No. Some. Yes. Some. Sphinx Yes. Yes. Yes. No. Yes. Yes. Yes. MkDocs Some. Yes. Yes. No. Yes. Yes. Yes. Mintlify No. Yes. No. Yes. Some. No. Some. GitBook No. Yes. No. Yes. Some. No. Some. And the capabilities the standard matrix doesn\u2019t capture: CapabilityFolioSphinx / MkDocsDocusaurus / NextraMintlify Agent artifacts (llms.txt + machine output)on the roadmap: ir.json, MCP. \u2014 ~ Theming down to full template ownership. ~ \u2014 Versioned docsimplemented, stabilizing behind a flag. ~ Twenty years of ecosystem. \u2014 ~ \u2014 The last row is the honest one: Sphinx\u2019s extension depth is real, and if your docs depend on exotic extensions, evaluate Folio on a branch first. Folio\u2019s structural differentiator is one source model feeding both the human site and the machine artifacts. Choosing a docs tool. Why Folio instead of Sphinx or MkDocs Material? Same job \u2014 parse Python source into reference docs \u2014 with a different output ceiling: a modern Next.js/shadcn site, instant search, theming to full template ownership, and agent-readable artifacts, from one docs.yaml. The trade: Sphinx\u2019s extension ecosystem is twenty years deep, and Folio\u2019s static analysis doesn\u2019t cover everything yet (see the SWOT below \u2014 we put it in writing). Why not Docusaurus, Nextra, or Fumadocs? Excellent site frameworks for hand-written pages. Python API reference needs a separate integration; Folio reads it from source and supplies the site as one workflow (built on Nextra internally). Why not Mintlify or GitBook? Hosted products, per-seat pricing, and they ingest prose, not source. Folio is a build tool: static output you deploy anywhere (GitHub Pages workflow included), no vendor in the serving path, and the reference is generated from code rather than scraped from markdown you maintain by hand. I'm migrating from Sphinx. What doesn't convert yet? Being honest: Folio\u2019s parser is static AST analysis. Inherited members, intersphinx inventories, and runtime-generated APIs are not covered today \u2014 they\u2019re tracked on the public roadmap\u2019s migration phase alongside a conf.py/mkdocs.yml importer with a what-didn\u2019t-convert report. Why not just have an LLM emit the HTML? Because docs have exactly one non-negotiable property \u2014 being true \u2014 and that\u2019s a provenance problem, not a fluency problem. Six concrete reasons, each one structural: 1 \u00b7 Ground truth beats plausible text An LLM writing HTML documents what it believes your API is. Folio reads signatures, types, and defaults from the source AST instead of asking a model to invent them. Static analysis has limits, but provenance stays inspectable. 2 \u00b7 A snapshot vs a function Emitted HTML rots on the first commit that changes your code. Folio is f(source, markdown) \u2192 site: re-run on every push, the reference updates itself, and every internal link is checked, with the broken ones named in the build output. Regenerating HTML re-rolls the hallucination dice on content that didn\u2019t change \u2014 and bills you tokens for markup boilerplate. 3 \u00b7 Reviewable diffs are the safety rail When an agent moves a kanban card here, the diff is a one-line status: frontmatter edit a human reviews in seconds. If agents regenerated HTML, the diff would be megabytes of unreviewable markup. Small semantic sources of truth are the only real safety rail in human\u2013agent collaboration. 4 \u00b7 A site needs a linker A whole site needs shared navigation, one search index, cross-references that resolve, versioning, social cards. LLMs emit locally plausible pages that don\u2019t cohere globally; Folio provides the global invariants: xref resolution, a link checker that names every broken internal link, generated sidebars. 5 \u00b7 Constrained generation appreciates over time In raw HTML every div soup is \u201cvalid\u201d \u2014 infinite ways to be wrong. Writing Folio markdown, an agent picks from a typed component vocabulary and the build rejects what doesn\u2019t exist. And the same markdown renders better over time as the template improves: HTML artifacts depreciate, Folio artifacts appreciate. 6 \u00b7 The next reader is an agent Emitted HTML is the worst format for other agents \u2014 it forces lossy scraping. Folio keeps the semantic source as the artifact and emits every output from one pass: HTML for people, markdown mirrors and machine-readable output for agents. \u201c That question, compressed. LLMs write better assembly every month \u2014 that\u2019s exactly why you want a build step you can trust. Markdown plus typed components is the high-level language; Folio does the generating, the linking, and the checking. The honest SWOT. Folio, mid-2026. internal \u2191 \u00b7 external \u2193 S Strengths. Typed source IR \u2014 signatures come from source. One pass emits the human site AND the agent artifacts. Released plugin platform; git-as-database patterns proven. Theming from tokens to full template ownership. W Weaknesses. Young ecosystem \u2014 no third-party plugins yet. Node + pnpm required at build time. Static parsing: inherited members and intersphinx pending. Python today \u2014 TypeScript, Rust, Go, C#, and Java on the roadmap, same funnel. O Opportunities. Material-for-MkDocs maintenance mode: thousands of projects re-deciding. Agent-readable docs becoming table stakes \u2014 priced at $100+/mo elsewhere. Skills in agent frameworks are markdown: Folio is their natural renderer. T Threats. Zensical consolidating the MkDocs succession first. Hosted platforms moving down-market with free tiers. \u201cGood enough\u201d LLM-generated one-off sites for small projects. Practical. Do I need to know React or Next.js? No. You write markdown and a docs.yaml. Components are tags in markdown (<Callout>, <Swot>, <Roadmap>\u2026) with documented props. React only becomes relevant if you opt into deep customization. Why does a Python tool need Node? Because the output is a Next.js static site \u2014 that\u2019s where search, theming, and interactivity come from. Folio manages the frontend workspace (.build/ is disposable) and checks your toolchain up front, but Node \u2265 20.19 and pnpm must exist on the build machine. The deployed site needs neither. If I drag cards on the kanban board, who sees it? Folio for Agents owns that workflow. Moves become folio board move commands and ordinary commits; Folio Docs only renders the board when its optional integration is installed. Other languages? Versioned docs? i18n? Python today; TypeScript, Rust, Go, C#, and Java are a roadmap phase, and the machine-readable IR schema is language-aware from day one. Versioned docs and i18n are implemented behind FOLIO_EXPERIMENTAL while they stabilize \u2014 graduating versions is part of the active phase. A question this page doesn\u2019t answer? Open an issue \u2014 unanswered questions are documentation bugs."},{"url":"docs/api-reference/folio_docs/cli/index.html","title":"folio_docs.cli source","content":"Ask AI. folio_docs.cli source. Classes. class _InitIntroTicker. source __init__ source. NameTypeDefaultDescription width. int. - cursor_rows_below_news. int. - initial_news_item. str. - interval. float. - Returns: None - start source. Returns: None - stop source. Returns: None - add_static_lines source. NameTypeDefaultDescription count. int. - Returns: None - set_active_prompt_lines source. NameTypeDefaultDescription count. int. - Returns: None - clear_active_prompt_lines source. Returns: None - _run source. Returns: None - refresh source. Returns: None - class _InitIntroPrinter. source __init__ source. NameTypeDefaultDescription info. dict. - target. Path. - repo. str. - compact. bool. - animate_news. bool. - Returns: None - __enter__ source. Returns: '_InitIntroPrinter' - __exit__ source. NameTypeDefaultDescription *exc_info. object. - Returns: None - add_static_lines source. NameTypeDefaultDescription count. int. - Returns: None - Functions. _load_project_cli_plugins source. Dispatch register_cli for plugins listed in ./docs.yaml. Typer finalizes the command table when this module is imported \u2014 before any command (and its project-directory argument) is parsed \u2014 so project plugins can only contribute CLI commands when the project config is resolvable from the current working directory. Running folio from outside the project still loads project plugins for build hooks; only their extra CLI commands require running inside the project. Every failure degrades to a warning: a broken plugin must never take down the whole CLI. NameTypeDefaultDescription cli_app. typer.Typer. - Returns: PluginManager | None - _resolve_project_target source. NameTypeDefaultDescription directory. Path | None. - project_dir. Path | None. - Returns: Path - _format_cli_path source. NameTypeDefaultDescription target. Path. - Returns: str - _command_target_suffix source. NameTypeDefaultDescription target. Path. - Returns: str - _exit_if_feature_disabled source. NameTypeDefaultDescription feature. str. - Returns: None - _detected_label_spacer source. NameTypeDefaultDescription label. str. - label_width. int. - Returns: str - _detected_summary_body source. NameTypeDefaultDescription info. dict. - target. Path. - repo. str. - Returns: Text - _print_init_intro source. NameTypeDefaultDescription info. dict. - target. Path. - repo. str. - compact. bool. False. news_item. str | None. None. Returns: tuple[int, str] - _init_intro_renderable source. NameTypeDefaultDescription info. dict. - target. Path. - repo. str. - news_item. str | None. None. Returns: Group - _renderable_plain_lines source. NameTypeDefaultDescription renderable. object. - Returns: list[str] - _cursor_rows_below_news source. NameTypeDefaultDescription renderable. object. - Returns: int - _active_init_intro_ticker source. Returns: _InitIntroTicker | None - _set_active_init_prompt_lines source. NameTypeDefaultDescription count. int. - Returns: None - _clear_active_init_prompt_lines source. Returns: None - _add_static_init_prompt_lines source. NameTypeDefaultDescription count. int. - Returns: None - _write_completed_line source. NameTypeDefaultDescription choice_set. dict[str, object] - value. str. - Returns: None - _ask_init_choice source. NameTypeDefaultDescription title. str. - options. tuple[tuple[str, str, str], ...] - default. str. - style. str. - Returns: str - prompt_line source. NameTypeDefaultDescription choice_set. dict[str, object] - Returns: str - completed_line source. NameTypeDefaultDescription choice_set. dict[str, object] - value. str. - Returns: str - _formatted_init_prompt_title source. NameTypeDefaultDescription title. str. - Returns: str - menu_line source. NameTypeDefaultDescription choice. dict[str, str] - index. int. - selected_index. int. - Returns: str - draw_menu source. NameTypeDefaultDescription choice_set. dict[str, object] - choices. list[dict[str, str]] - selected_index. int. - first_draw. bool. - Returns: None - clear_menu source. NameTypeDefaultDescription choices. list[dict[str, str]] - Returns: None - _init_can_use_arrow_select source. Returns: bool - _init_readchar source. _ask_init_arrow_choice source. NameTypeDefaultDescription title. str. - options. tuple[tuple[str, str, str], ...] - default. str. - style. str. - Returns: str | None - _print_init_ready source. NameTypeDefaultDescription target. Path. - created. list[str] - compact. bool. False. Returns: None - _created_cli_path source. NameTypeDefaultDescription target. Path. - relative_path. str | Path. - Returns: str - _dependency_name source. NameTypeDefaultDescription spec. str. - Returns: str - _project_dependency_names source. NameTypeDefaultDescription pyproject_data. dict. - Returns: set[str] - _detect_framework source. NameTypeDefaultDescription pyproject_data. dict. - Returns: str - _documentation_status source. NameTypeDefaultDescription target. Path. - Returns: str - _detect_project source. Detect project name, version, and Python source paths from pyproject.toml. NameTypeDefaultDescription target. Path. - Returns: dict - _detect_git_remote source. Try to detect the GitHub repo URL from git remote. NameTypeDefaultDescription target. Path. - Returns: str - _yaml_scalar source. Render value as a quoted YAML scalar. Detected project metadata (pyproject name/version, the git remote, source paths) is repository content, so interpolating it raw would let a clone close the quote and append top-level keys - plugins: among them - to the config Folio then trusts. JSON string syntax is valid YAML double-quoted style, so json.dumps is the escape we want. NameTypeDefaultDescription value. object. - Returns: str - _generate_docs_yaml source. NameTypeDefaultDescription info. dict. - Returns: str - _version_callback source. NameTypeDefaultDescription value. bool. - Returns: None - main source. NameTypeDefaultDescription version. bool. typer.Option(False, '--version', '-V', callback=_version_callback, is_eager=True, help='Show version'). Returns: None - init source. Initialize a new Folio documentation project. NameTypeDefaultDescription directory. Path. typer.Argument(default=None, help='Project directory (defaults to cwd)'). yes. bool. typer.Option(False, '--yes', '-y', help='Skip prompts, use detected defaults'). Returns: None - _serve_static_site source. Start a local HTTP server for a built static site. NameTypeDefaultDescription site_dir. Path. - port. int. 8787. open_browser. bool. False. kill_existing. bool. False. Returns: None - _serve_and_open source. Start a local HTTP server for the built site and open it in the browser. NameTypeDefaultDescription site_dir. Path. - port. int. 8787. Returns: None - _read_output_dir_best_effort source. NameTypeDefaultDescription config_path. Path. - target. Path. - Returns: Path - _resolve_version_output_dir source. NameTypeDefaultDescription output_base. Path. - version_path. object. - Returns: Path - _write_default_version_redirect source. Write a static root redirect to the default docs version. NameTypeDefaultDescription output_base. Path. - version_path. str. - Returns: None - _sync_version_matrix source. Inject current version/plugin config into a checked-out historical ref. NameTypeDefaultDescription config_path. Path. - versions. list[dict] - synced_config. dict | None. None. Returns: None - _version_sync_config source. NameTypeDefaultDescription config_path. Path. - pm. object. - Returns: dict - _stable_hash source. NameTypeDefaultDescription value. object. - Returns: str - _resolve_git_commit source. NameTypeDefaultDescription project_dir. Path. - ref. str. - Returns: str - _version_build_manifest source. NameTypeDefaultDescription version. dict. - commit. str. - versions. list[dict] - synced_config. dict. - Returns: dict - _read_version_build_manifest source. NameTypeDefaultDescription output_path. Path. - Returns: dict | None - _version_output_has_content source. NameTypeDefaultDescription output_path. Path. - Returns: bool - _version_build_manifest_matches source. NameTypeDefaultDescription output_path. Path. - expected. dict. - Returns: bool - _write_version_build_manifest source. NameTypeDefaultDescription output_path. Path. - manifest. dict. - Returns: None - _remove_version_worktree source. Remove a generated version worktree and prune stale Git metadata. NameTypeDefaultDescription project_dir. Path. - worktree_dir. Path. - Returns: None - _build_configured_versions source. Build docs for every version configured in docs.yaml. NameTypeDefaultDescription project_dir. Path. - verbose. bool. - config. str. - clean. bool. - Returns: None - build source. NameTypeDefaultDescription directory. Path. typer.Argument(default=None, help='Project directory (defaults to cwd)'). project_dir. Path. typer.Option(default=None, help='Compatibility option for scripts that prefer named arguments'). verbose. bool. typer.Option(False, '--verbose', '-v', help='Show detailed output'). config. str. typer.Option('docs.yaml', '--config', '-c', help='Config file path'). clean. bool. typer.Option(False, '--clean', help='Force full rebuild (clear cache)'). open_browser. bool. typer.Option(False, '--open', '-o', help='Starts a static preview in the browser and blocks until interrupted'). Returns: None - build_versions source. Build docs for all configured versions. NameTypeDefaultDescription directory. Path. typer.Argument(default=None, help='Project directory (defaults to cwd)'). project_dir. Path. typer.Option(default=None, help='Compatibility option for scripts that prefer named arguments'). verbose. bool. typer.Option(False, '--verbose', '-v', help='Show detailed output'). config. str. typer.Option('docs.yaml', '--config', '-c', help='Config file path'). clean. bool. typer.Option(False, '--clean', help='Force full rebuild (clear cache)'). Returns: None - serve source. NameTypeDefaultDescription directory. Path. typer.Argument(default=None, help='Project directory (defaults to cwd)'). project_dir. Path. typer.Option(default=None, help='Compatibility option for scripts that prefer named arguments'). verbose. bool. typer.Option(False, '--verbose', '-v', help='Show detailed output'). config. str. typer.Option('docs.yaml', '--config', '-c', help='Config file path'). port. int. typer.Option(4321, '--port', '-p', help='Dev server port'). open_browser. bool. typer.Option(False, '--open', '-o', help='Open browser automatically'). clean. bool. typer.Option(False, '--clean', help='Force full rebuild (clear cache)'). versions. bool. typer.Option(False, '--versions', help='Build and serve every configured version as a static preview', hidden=True). kill_existing. bool. typer.Option(False, '--kill-existing', help='Stop an existing process on the selected port before serving'). Returns: None - coverage source. Analyze documentation coverage of Python source files. NameTypeDefaultDescription directory. Path. typer.Argument(default=None, help='Project directory (defaults to cwd)'). project_dir. Path. typer.Option(default=None, help='Compatibility option for scripts that prefer named arguments'). config. str. typer.Option('docs.yaml', '--config', '-c', help='Config file path'). verbose. bool. typer.Option(False, '--verbose', '-v', help='List each undocumented symbol'). min_coverage. float. typer.Option(0, '--min', help='Minimum coverage percentage (exit 1 if below)'). Returns: None - clean source. NameTypeDefaultDescription directory. Path. typer.Argument(default=None, help='Project directory (defaults to cwd)'). project_dir. Path. typer.Option(default=None, help='Compatibility option for scripts that prefer named arguments'). Returns: None -"},{"url":"docs/installation/index.html","title":"Installation","content":"Ask AI. Installation. Install Folio and start the CLI. Install Folio. Dependency note: Folio uses Python 3.10+ for the CLI, and also depends on Node.js 20.19+ and pnpm 10 for folio build and folio serve. Use Corepack to activate pnpm 10: Then verify the CLI: Standalone Installer. The standalone installer requires uv, installs the folio-docs distribution as a uv tool, checks Node.js 20.19+, and tries to activate pnpm 10 through Corepack when it can. Inspect the script before running it: Install a specific Folio version: Skip automatic pnpm setup when you only want the Python CLI: Other Install Methods. uv project uv tool pip. Use uv add folio-docs inside a Python project. Use uv tool install folio-docs when you want the folio command available outside a project. Add Folio for Agents to the same tool environment when you also want folio board: Contributing to Folio. Repository setup, tests, and frontend development belong to the contributor workflow rather than installation. See the contributing guide . Next Steps. Once Folio is installed, head to the Quick Start guide to build your first documentation site."},{"url":"docs/components/terminal_session/index.html","title":"TerminalSession","content":"Ask AI. TerminalSession. Render a command with prompt context, optional output, and a compact status marker. Most guide command snippets should use plain fenced bash blocks. Reserve TerminalSession for cases where prompt context, exact output, or status state matters to the reader. Avoid invented success logs in guide pages. Use output only for exact text the reader should recognize, such as a version check, error message, or URL printed by a command. For setup commands, omit output so the command stays easy to copy. The header copy button copies only the command value, not the prompt, output, or notes. API. PropTypeDefaultDescription command. string. \u2014 Command shown after the prompt. output. string | string[] \u2014 Terminal output shown below the command. cwd. string. \u2014 Optional working directory label. title. string. \"Terminal\" Header label. prompt. string. \"$\" Prompt marker before the command. status. \"neutral\" | \"success\" | \"error\" \"neutral\" Header status color. children. ReactNode. \u2014 Optional explanatory note below the terminal block. Example. Check version. $ folio --version folio 0.3.0."},{"url":"docs/theming/custom-templates/index.html","title":"Custom Templates","content":"Ask AI. Custom Templates. [!WARNING] Security A custom template is executed as trusted code during every build. Folio runs pnpm install --frozen-lockfile (which executes dependency lifecycle scripts) and next build (which runs the template\u2019s next.config.mjs in Node), so pointing template.path at a directory is equivalent to running that code on your machine. Only point template.path at frontend code you have reviewed and trust. Building an untrusted repository or branch in CI inherits the same remote-code-execution surface \u2014 treat a docs build like any other untrusted-code execution. Never put secrets, API tokens, or credentials in template.params: they are baked into the static export and published. Custom templates are the expert escape hatch for teams that need complete control over their documentation site. A template is a full frontend workspace. It is not a color-token override system: it owns layout, routes, CSS, JavaScript, components, package dependencies, search UI, navigation chrome, and brand behavior. Use personalization when the bundled Folio template is close enough. Use theme packages when you only need to overlay selected files on the bundled template. Use template.overlay_path when you want to override just one or a few template files while inheriting the rest of the bundled template. Use template.path when the documentation must live inside a product-specific frontend shell. Configure a Template. Point Folio at a local template directory from docs.yaml: template.path is resolved relative to the project directory. It must point to a trusted local directory inside the project, and it cannot point at .build/ or the configured output directory. template.params is an arbitrary JSON-serializable mapping. Folio does not interpret these values; the selected template owns their meaning. See the template.params contract below for the full rules and a worked example. template.docs_route_base controls where documentation pages are served. It defaults to /docs. Set it when the generated docs need to live at a product route such as /reference/docs. Folio uses this path for generated links, search URLs, sitemap entries, canonical metadata, LLM output, and the copied Next.js route folder. Overlay (partial override). template.path is all-or-nothing: you must fork the entire bundled template to change a single file. When you only need to override one or a few files, use the opt-in template.overlay_path instead. In overlay mode Folio starts from the bundled template, then copies your overlay files on top \u2014 your files win, and any file you do not provide falls back to the bundled template. Only place the files you want to change inside the overlay directory, mirroring their path in the template tree: After the overlay is merged, Folio runs the normal injection pipeline on the result, so project metadata, the docs route base, search, versions, and theme configuration are injected exactly as they would be for the bundled template. [!IMPORTANT] template.overlay_path and template.path are mutually exclusive. template.path is a full replacement; template.overlay_path is a partial override. If both are set, template.path wins and the overlay is ignored with a warning. The overlay directory is held to the same guards as template.path: it must live inside the project, may not point at .build/ or the output directory, and may not contain symlinks. Because the merged template still has to satisfy the injection-marker contract, any marker-bearing file you override must keep the required markers verbatim \u2014 the safest approach is to copy the file from the bundled template and edit around the markers. Limitations. The overlay is a file-level merge, not a deep merge. Overriding a file replaces it entirely; Folio does not merge JSON (for example package.json) or splice individual functions inside a file. Deleting a bundled file via the overlay is not supported \u2014 an overlay can only add or replace files. For deeper changes than a handful of file replacements, fork the whole template with template.path. Ownership Model. Folio owns generated documentation data. Your template owns presentation. OwnerResponsibilities Folio. Parse Python and Markdown sources, generate MDX pages, write _meta.ts, emit API reference pages, write Markdown exports, build the search index, link-check generated routes, and export static files. Template. Render the configured docs route, provide compatible MDX components, style the whole site, implement navigation/search/version UI, ship dependencies, and decide how template.params affect the frontend. Do not edit .build/. Folio copies the template there, removes template demo content, injects generated content and metadata, runs the frontend build, and may overwrite reserved generated files on every build. Template Anatomy. Folio currently supports local Next/Nextra-compatible templates. A custom template must include a source docs route at app/docs; Folio relocates that folder to template.docs_route_base inside .build/ when the route base is not /docs. The build runtime expects pnpm install --frozen-lockfile, pnpm run build, and a static export in out/. Keep app/docs as the template source route even when the public route is configured differently. Injection markers. Folio does not template these files with a templating engine. It performs literal string substitutions and regex block edits against specific source files after copying the template into .build/. The marker strings below are a load-bearing contract: a custom template must contain them verbatim, in the expected files, or Folio cannot inject project metadata, the docs route base, the repository link, the header, search, versions, the theme preset, the base path, or Folio-managed MDX component wiring. [!IMPORTANT] Folio validates the load-bearing markers before building. If a required marker (or the file that owns it) is missing, the build fails fast with a ValueError that names every missing (file, marker) pair, alongside the existing missing-files and MDX-contract checks. The load-bearing set is the always-applied project metadata in app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, and the base-path marker in next.config.mjs. Optional targets \u2014 paired blocks that are stripped when a feature is off, the mdx-components.tsx component markers (which fall back to a ...components, spread), and markers that only live in optional files such as the landing page or theme configurator \u2014 are not hard failures. When one is absent Folio logs the skip at debug level and emits an info-level summary of the files it injected, instead of failing or silently no-op\u2019ing. The safest way to author a custom template is still to copy the marker-bearing files verbatim from the bundled template and edit around the markers, rather than recreating them from scratch. Text replacements. Each of these is a literal string \u2192 value replacement. The marker may appear more than once in a file; all occurrences are replaced. Marker stringExpected file(s)What Folio substitutes __PROJECT_NAME__. app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx, app/page.tsx. Project name from docs.yaml. __PROJECT_DESCRIPTION__. app/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx. Documentation for <name>. __SITE_URL__. app/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/sitemap.ts, app/robots.ts. Configured site URL (trailing slash trimmed). __PROJECT_MONOGRAM__. app/icon.svg, app/docs/layout.tsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx, app/page.tsx. Two-letter lowercase monogram derived from the name. __PROJECT_REPO__. app/docs/layout.tsx. HTML-escaped repository URL (emptied when no repo is set). __DOCS_INDEX_CANONICAL_PATH__. app/docs/[[...mdxPath]]/page.jsx. Canonical docs index path (/ when a landing page is enabled, otherwise the docs route base). __DOCS_ROUTE_BASE__. app/sitemap.ts, app/robots.ts. Configured template.docs_route_base. __INCLUDE_DOCS_INDEX__. app/sitemap.ts, app/robots.ts. true/false depending on whether the landing page is enabled. __FOLIO_DOCS_ROUTE_BASE__. next.config.mjs. Configured template.docs_route_base. __VERSIONS__, __CURRENT_VERSION_PATH__. components/version-selector.tsx. JSON version list and the current version path. __LANDING_*__, __LANDING_*_JSON__. app/page.tsx, components/landing-navbar.tsx. Landing hero/CTA/feature/section data (raw and JSON-encoded forms). __PROJECT_NAME_JSON__, __PROJECT_MONOGRAM_JSON__. app/page.tsx, components/landing-navbar.tsx. JSON-encoded name/monogram for the landing page. Paired block markers. These are START/END comment pairs that wrap a region. Folio either replaces the region\u2019s contents (preserving indentation) or strips the whole block \u2014 and its marker lines \u2014 when the corresponding feature is not configured. Include both the start and end marker, and the placeholder content in between, exactly as in the bundled template. Marker pairExpected fileWhat Folio substitutes // __PROJECT_REPO_IMPORTS_START__ \u2026 // __PROJECT_REPO_IMPORTS_END__. app/docs/layout.tsx. Repository-link imports (block removed when no repo is set). {/* __PROJECT_REPO_LINK_START__ */} \u2026 {/* __PROJECT_REPO_LINK_END__ */} app/docs/layout.tsx. Repository link markup (block removed when no repo is set). {/* __PROJECT_HEADER_LOGO_START__ */} \u2026 {/* __PROJECT_HEADER_LOGO_END__ */} app/docs/layout.tsx. Project header brand/badge markup (block removed when no header is configured). // __PROJECT_HEADER_ACTION_IMPORTS_START__ \u2026 // __PROJECT_HEADER_ACTION_IMPORTS_END__. app/docs/layout.tsx. ProjectHeaderActions import (removed when no header actions are configured). {/* __PROJECT_HEADER_ACTIONS_START__ */} \u2026 {/* __PROJECT_HEADER_ACTIONS_END__ */} app/docs/layout.tsx. Header action / version-selector markup (removed when no header actions are configured). // __FOLIO_COMPONENT_IMPORTS__. mdx-components.tsx. Reserved for internal Folio use: import lines Folio may inject for additional MDX components. // __FOLIO_COMPONENT_ENTRIES__. mdx-components.tsx. Reserved for internal Folio use: entries Folio may add to the MDX components map. The two mdx-components.tsx markers are single comment lines (not pairs). They are reserved for internal Folio use. If they are absent but Folio needs to inject components, it falls back to prepending imports and inserting entries next to an existing ...components, spread \u2014 so keeping the markers gives you deterministic placement. Troubleshooting. Custom-template failures surface as one of a few recognizable symptoms. Use this table to map the symptom to its cause and fix. SymptomCauseFix Build fails listing missing required files. The template directory is missing an anatomy file Folio expects (for example next.config.mjs, mdx-components.tsx, or the app/docs route). Add the missing files. Copy the anatomy skeleton from the bundled template rather than recreating it. Build fails listing missing MDX components. mdx-components.tsx does not export a required Folio component name, so generated MDX would import a component that does not exist. Export the named components from mdx-components.tsx. See the MDX Component Contract. Build fails with missing required marker X in file Y (one (file, marker) pair per missing entry). A load-bearing injection marker \u2014 or the file that owns it \u2014 is absent, so Folio cannot inject project metadata, the docs route base, or the base path. Restore the marker verbatim in the named file. The load-bearing set lives in app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, and next.config.mjs; copy those files from the bundled template and edit around the markers. Warning that the overlay is ignored. Both template.path and template.overlay_path are set. They are mutually exclusive; template.path wins. Remove whichever key you do not want. Use template.path for a full fork, template.overlay_path for a partial override. Config error mentioning an unsafe URL scheme. A header URL uses a rejected scheme (javascript:, data:), or a repository URL uses a scheme that could execute script or read local files. Use an http(s), mailto:, or relative URL for header links; repository URLs may also use ssh://, git://, git+https://, or scp-style forms. See header URL validation. Optional markers (paired feature blocks, the mdx-components.tsx markers, and markers that only live in optional files such as the landing page or theme configurator) do not fail the build. When one is absent Folio skips it silently at normal verbosity. To see what happened, enable debug logging: Folio logs each skipped optional target at debug level and always emits an info-level summary of the files it injected. If a template change did not take effect and the build still succeeded, that injected-files summary is the first place to look \u2014 a target missing from the summary was never injected. Config markers. These are inline // __MARKER__ comments appended to a real source line. Folio replaces the entire line (the value plus the marker comment) with the configured value, so both the placeholder assignment and the trailing marker must be present verbatim. Marker stringExpected fileWhat Folio substitutes const configuredDefaultPresetId = \"organic-editorial\" // __FOLIO_THEME_PRESET__. components/theme-configurator.tsx. Rewrites the line to the configured theme.preset. const configuredBasePath = '' // __FOLIO_BASE_PATH__. next.config.mjs. Rewrites the line to the resolved deploy base path. next.config.mjs is also edited by other substitutions: contentDirBasePath is rewritten to the docs route base, NEXT_PUBLIC_FOLIO_DOCS_ROUTE_BASE is appended next to NEXT_PUBLIC_FOLIO_BASE_PATH, and the __I18N_CONFIG__ marker is replaced with an i18n block (or stripped when no locales are configured). Generated Files. After Folio copies the template into .build/, it writes generated data into reserved paths: Path in .build/Purpose content/ Generated MDX pages and Nextra _meta.ts files. public/_folio/markdown/ Plain Markdown versions used by page actions and LLM workflows. lib/search-index.ts. Static search document metadata for the navbar search component. lib/folio-template.ts. Build-time project metadata and template params for custom templates. lib/folio-mdx-contract.ts. Versioned list of Folio MDX component names and prop contracts. .folio-manifest.json. Incremental rebuild cache. .folio-build.log. Last frontend build log. template.params contract. template.params is the supported channel for passing project-specific configuration from docs.yaml into a custom template. The contract is small and deliberately strict so that what reaches the frontend is predictable: JSON-serializable. template.params must be a mapping whose values are JSON-serializable (strings, numbers, booleans, null, lists, and nested mappings). Folio validates this at config-load time. A non-serializable value (for example a custom YAML tag that parses to a Python object) fails the build with a clear template.params must be JSON-serializable error. Normalized at load time. An absent params key and an explicit null both become {}. A params value that is not a mapping (a list, string, or number) is ignored with a warning and also becomes {}, so the template still builds with empty params instead of crashing. Emitted frozen as as const. Folio writes the validated mapping verbatim into lib/folio-template.ts as folioTemplateParams, serialized with JSON.stringify and suffixed with as const. The as const makes the object and its string/number/boolean literals read-only at the type level, so editors infer literal types (\"dense\", not string). NOT type-checked. Folio does not generate or validate a TypeScript type for your params. folioTemplateParams is typed only by the literal values that happened to be present in the last build. The template owns the meaning of each field and is responsible for any narrowing, defaulting, or runtime validation it needs. Treat unknown fields as unknown and guard before use. Worked example. Pass a navbar variant and product name through docs.yaml: Folio emits the params into lib/folio-template.ts (other exports omitted): A template component then branches on a field, defaulting when it is absent: template.params are build-time/static-site visible. Do not put secrets, API tokens, private keys, or environment-specific credentials in them. MDX Component Contract. Generated API and guide pages can reference Folio component names. A custom template must expose compatible components from mdx-components.tsx, or the Next.js build will fail when generated MDX imports a missing component. Folio writes the current contract to lib/folio-mdx-contract.ts with folioMdxContractVersion. The build also checks custom templates for required component names before running the frontend build, so missing required components fail early. At minimum, custom templates should provide these names when they render Folio\u2019s own generated API and guide output: ComponentUsed for ParamTable. Function and method parameter tables. ClassOverview. Class summaries and inheritance information. SourceLink. Links back to repository source files. ApiReferenceIndex. Generated API reference landing page. Callout, Tabs, TabItem, Mermaid. Converted Markdown/MDX guide content. You can re-export Folio-compatible components, wrap them in product styling, or replace them entirely. The important contract is the component name and props that generated MDX expects. Sidebar Metadata Contract. Folio regenerates Nextra _meta.ts files on every build. Custom templates may rely on these stability guarantees: Root entries preserve Folio\u2019s documented page order first, then append remaining discovered pages. Nested directory index pages are emitted as { \"display\": \"hidden\" } so the folder opens to its index page without duplicating an Index item. Folio does not emit collapse state such as open or defaultCollapsed. Templates that need collapsed sections must implement that behavior in the template navigation layer. What Default Options Mean. When template.path is omitted, Folio uses the bundled template and applies Folio-owned options such as theme.preset, logo, favicon, Pagefind search, and the default docs layout. When template.path is set, those options are not a guarantee that Folio can style your template. A custom template may choose to read the generated project metadata, use template.params, or ignore Folio\u2019s bundled theme system entirely. That separation is intentional: expert templates get full control instead of a partial override layer. Compatibility Checklist. Before committing a custom template, verify that it: Builds with pnpm install --frozen-lockfile and pnpm run build. Uses Next static export and writes out/. Renders generated content from content/ under the configured docs route. Supports Nextra _meta.ts files if it uses Nextra navigation. Exposes Folio-compatible MDX components from mdx-components.tsx. Handles deploy.base_path or FOLIO_BASE_PATH if the site is deployed under a subpath. Implements search, dark mode, version selection, landing pages, or i18n itself when those experiences are needed. Keeps template source files under the configured template directory. Treats .build/, content/, public/_folio/, lib/search-index.ts, lib/folio-template.ts, and lib/folio-mdx-contract.ts as generated build output. Custom templates execute frontend install and build scripts. Use templates you own or trust, review dependency changes, and keep the lockfile checked in with the template."},{"url":"docs/components/copy_page_button/index.html","title":"Page Actions","content":"Ask AI. Page Actions. Folio renders a compact Ask AI page-actions button above every documentation page. It is meant for source-heavy docs: copy the current page, or send the current page context to ChatGPT without adding extra setup. PageActionsButton. A single outline menu button that includes Copy page, View Markdown, a ChatGPT-only assistant action, and MCP JSON actions. The ChatGPT row uses the bundled brand icon and opens ChatGPT with a short Read from ... prompt using the current page\u2019s Markdown URL, including configured deploy base paths. MCP copies a JSON page-context payload for local tooling. PageActionsButton. This component takes no props. CopyPageButton. A standalone copy-only button. It remains available for custom MDX pages, but the default docs layout uses PageActionsButton. CopyPageButton. This component takes no props. Layout. Layout Component. PageActionsButton is rendered by the theme layout above every documentation page."},{"url":"docs/components/doc_preview/index.html","title":"DocPreview","content":"Ask AI. DocPreview. Use DocPreview when a guide needs to show a generated documentation route or a small generated example instead of describing the result with another code block. It renders a responsive iframe with a short caption, a source view with a collapsible file drawer for the focused source file, a line-numbered code preview, and direct links to the page and source file. PreviewSource. API reference preview. Show the generated reference page directly in the guide so readers can inspect the output before building their own project. Open page. For tutorial examples, place a complete Folio project under docs/examples/<name>/ with its own docs.yaml, docs/, and optional src/ files. The docs build runs Folio for that project, serves the generated static output at /_folio/examples/<name>/, and shows the exact source files that produced it. PreviewSource. Generated docs site. A tiny docs site generated from a real example project. Open page."},{"url":"docs/components/mermaid/index.html","title":"Mermaid","content":"Ask AI. Mermaid. Renders Mermaid diagrams as SVG directly in the browser. Supports flowcharts, sequence diagrams, class diagrams, state diagrams, ER diagrams, Gantt charts, and all other Mermaid diagram types. Automatically switches between light and dark themes to match the site. API. Mermaid (JSX component). PropTypeDefaultDescription chart. string. \u2014 The Mermaid diagram definition string. Fenced code block (recommended). You can also use standard Mermaid fenced code blocks. Folio automatically converts these into <Mermaid> components during the build: Example. Fenced code block syntax. graph TD A[Start] --> B{Decision} B -->|Yes| C[OK] B -->|No| D[Cancel] JSX component syntax. graph TD A[Start] --> B{Decision} B -->|Yes| C[OK] B -->|No| D[Cancel] Sequence diagram. sequenceDiagram Client->>Server: POST /train Server->>Worker: dispatch(job) Worker-->>Server: result Server-->>Client: 200 OK. Class diagram. classDiagram class Node { +str address +connect() +disconnect() } class FederatedNode { +train() +aggregate() } Node <|-- FederatedNode."},{"url":"docs/plugins/roadmap/index.html","title":"Roadmap","content":"Ask AI. Roadmap. The official folio_docs.docs.integrations.roadmap plugin renders source-defined phases as a release timeline; the view below is powered by the same data as the standalone /roadmap/ route. The roadmap plugin ships with Folio as a first-party default plugin: it is loaded on every build, with no plugins: entry and no environment variable required. It stays inert until a roadmap: section appears in docs.yaml \u2014 the config key is the activation switch. Activate it in docs.yaml: The timeline is rendered from source: change phases in docs.yaml, commit the file, and rebuild the site to update this view. Give phases a project to render independent release sequences from one roadmap. A phase\u2019s optional milestone is the stable value cards use when two projects can both have a 0.1. Routes are opt-out: routes.docs (default true) publishes a generated /docs/roadmap/ page when your sources do not already contain one, and routes.public (default false) adds the standalone /roadmap/ page. The folio roadmap CLI command prints the configured phases as a table. The optional top-level description is the page\u2019s own copy: the /roadmap/ page shows it under the title, above the releases. Releases are ordered by version, never by status. Naming the projects. Give a phase a project and the plugin groups the roadmap by it. On the standalone /roadmap/ page each project becomes a card, and every card holds that product\u2019s whole plan: the release list on the left, the release you pick opened beside it. Projects are numbered independently, so two products that ship separately each start at their own 0.1. An optional projects: block names them. A label titles the card and a description sits under it; without either, the project key is shown as written. Every card is expanded by default, and each one can be collapsed to its heading. ?product=<key> opens one card and collapses the rest, which is the link a product\u2019s own landing page should use: /roadmap/?product=agents. A release is addressable too, scoped by project: /roadmap/#agents-0.2. Collapsing hides a card\u2019s releases; it does not drop them. Every release is in the served HTML whatever the page is showing, so a crawler, a Markdown mirror or a reader with JavaScript off still sees the whole roadmap. Listing folio_docs.docs.integrations.roadmap under plugins: is harmless \u2014 default plugins are deduplicated against explicit entries \u2014 but unnecessary. Internally, the plugin uses the same extension primitives available to custom plugins: it registers a Roadmap component for embedding, a RoadmapPage component for the standalone route, writes typed data, and creates the /roadmap/ view with the required folio_docs.public layout. Live demo. This is Folio\u2019s own roadmap \u2014 the code tab shows the shape of the roadmap: section in this repository\u2019s docs.yaml, the preview tab is the component rendering the real data: Folio's release lines. v0.1 Foundation. The proof of concept is Python: source in, documented site out. Source analysis One command sets the project up. Reads your source, never runs it. API reference and your guides in one site. Types and symbols link to their pages. Edit a docstring, see the page. A component library and full-text search. llms.txt from day one. v0.2 Theming. Own the look at any depth, then ship it anywhere static files go. Visual ownership Presets, shareable packages, or your own template. Readers retheme the site from the site. A preview site per pull request. Social preview images. v0.3 Plugin Platform. In progress. Built-in and project extensions share one plugin contract, and this site runs on it. One platform Plugins ship pages, components, and commands. Your own React components in any page. Every project ships its authoring contract. Every page gets a portable Markdown mirror. Roadmap, landing, and OpenAPI integrations. A briefing for your coding agent. v0.4 More Languages. Up next. New languages arrive as parsers, not new toolchains, so adding one never means starting over. Reach Python, TypeScript, Rust, Go, C#, and Java in one site. One search, one llms.txt, every language. References that cross languages. Type information inline in examples. A public contract for adding a language. Docs for every version of your library. Sites in your readers' languages. A native fast path in Rust for big repos. v0.5 Migrations. Up next. Bring your existing docs over in one command, with an honest report of what didn't convert. Adoption One-command import from MkDocs, Mintlify, and Read the Docs. Sphinx cross-links carry over. A theme preset that feels familiar. Redirects for every moved page. One site across products and repos. v0.6 API Portal. Later. API workflow Field-level API components. A page per endpoint. Builds fail on breaking changes. Try requests on the page. SDK examples verified against the code. Event APIs too. v0.7 Public Beta. Up next. A coordinated beta with a settled first run, an installable release, and clear stability boundaries. Distribution folio score \u2014 how agent-ready any docs site is. Starter templates. A plugin catalog outsiders can publish into. Live plugin demos with their source. v0.1 Repository Board. In progress. Agents and humans operate one reviewable board made of Markdown files in git. Shared work state One permanent file per card. Acceptance criteria, trails, comments, and artifacts stay with the work. Validated commands make narrow, reviewable edits. A repository-owned protocol teaches every coding harness the same workflow. An optional browser canvas publishes the board and attached artifacts. v0.2 Session Memory. Up next. A useful agent session leaves a concise conclusion that the next session can find and reuse. Durable context Session conclusions attach to the card they moved. Decisions and reusable snippets remain readable in the repository. Teams choose which artifacts become published pages. Context can be condensed without losing confirmed decisions. v0.3 Project Coordination. Up next. The board becomes a dependable projection of releases, dependencies, and active ownership. Repository operations Roadmap phases project from product-owned board milestones. Agents claim work without hiding ownership from humans. Issue and CI state reconcile with the repository board. Quality gates fail before invalid work state is published. v0.4 Harness Network. Later. Shared ecosystem Discover reusable skills and operating protocols. Publish portable context contracts between repositories. Keep identity, work state, and permissions under each repository's control."},{"url":"docs/api-reference/folio_docs/agent_output/contract/index.html","title":"folio_docs.agent_output.contract source","content":"Ask AI. folio_docs.agent_output.contract source. Functions. strip_js_comments source. Remove JS/TS line and block comments, leaving string literals intact. A small scanner instead of a regex so that // inside a string or template literal (e.g. \"https://example.com\") is not treated as the start of a line comment. NameTypeDefaultDescription code. str. - Returns: str - import_statements source. Return every import statement found in comment-stripped code. NameTypeDefaultDescription code. str. - Returns: list[str] - strip_import_statements source. Remove import statements from comment-stripped code. NameTypeDefaultDescription code. str. - Returns: str - has_component_entry source. Report whether name is wired as a components-mapping entry. Accepts the object-property forms (Name, / Name: / Name }, at any indentation) and as Name re-exports. code must already be comment- and import-stripped so a merely imported symbol never counts as a mapping entry. NameTypeDefaultDescription code. str. - name. str. - Returns: bool - build_contract source. Derive the MDX component contract from component definitions. Membership is the explicit contract flag on the definition and the contract source field comes from source_label (never from category, which is pure taxonomy). Passing the components of a live registry lets plugin components carrying contract=True join the emitted contract; the default is the builtin manifest, keeping. NameTypeDefaultDescription components. Iterable[ComponentDefinition] | None. None. Returns: list[dict[str, Any]] - required_component_names source. Returns: list[str] - render_mdx_contract_module source. NameTypeDefaultDescription components. Iterable[ComponentDefinition] | None. None. Returns: str - build_authoring_contract source. Assemble what a page in this project may contain, as one JSON payload. Three answers, one envelope: which components MDX pages can use, which top-level docs.yaml keys this project accepts, and which pages the build emitted. components takes the live registry so plugin and config components flagged contract=True are described too; None falls back to the builtin manifest. NameTypeDefaultDescription folio_version. str. - generated_at. str. - components. Iterable[ComponentDefinition] | None. None. config_keys. Iterable[str] (). routes. Iterable[str] (). Returns: dict[str, Any] - render_authoring_contract source. Serialize :func:build_authoring_contract as the published file text. NameTypeDefaultDescription folio_version. str. - generated_at. str. - components. Iterable[ComponentDefinition] | None. None. config_keys. Iterable[str] (). routes. Iterable[str] (). Returns: str - validate_template_mdx_contract source. NameTypeDefaultDescription template_dir. str | Path. - Returns: list[str] -"},{"url":"docs/components/theme_configurator/index.html","title":"ThemeConfigurator","content":"Ask AI. ThemeConfigurator. A drawer popover widget that replaces the default light/dark control when documentation presets are available. A preset owns color tokens, document rhythm, code block treatment, borders, radius defaults, typography defaults, and its own controls. Changes are persisted in localStorage under a project-scoped key derived from the configured default preset and applied immediately. The default theme CSS is also rendered into the page so the generated site does not flash or fall back to Folio\u2019s bundled typography before hydration. API. ThemeConfigurator. This component takes no props. It is rendered in the documentation sidebar drawer beside the existing appearance controls. Theme Flow. The drawer palette uses a compact two-step flow: StepPurpose Grouped library. Choose a visual direction from Workspace, Product Docs, Reference, or Expressive. Customize. Tune the selected preset\u2019s controls, surface color, accent color, shell spacing, content width, reading rhythm, borders, code blocks, typography, and corner radius. The Back button returns readers from Customize to the grouped library. Light and dark selection lives in the same drawer palette as preset selection, while the keyboard shortcut still toggles between light and dark. Preset Library. All built-in visual directions are presets. The drawer picker shows a current theme summary before the grouped library. Each group is rendered as a compact carousel row so the panel stays short as the preset library grows. GroupPresets Workspace. Workshop, Canopy. Product Docs. Beacon, Aperture, Ledger. Reference. Atlas, Stacks, Draftline, Proof. Expressive. Organic Editorial, Carbon. PresetPurpose Workshop. Warm generated-site workspace based on the saved inspiration example, with light paper contrast and botanical accents. Canopy. Compact green workspace for simple examples, source snippets, and generated guides. Beacon. Main product-docs preset with endpoint cards, compact API workflows, and high-contrast examples. Atlas. Classic reference documentation with paper rhythm and sharp source examples. Ledger. Dense API tables and register pages for fast scanning. Proof. Printed-manual hierarchy with strong rules and editorial emphasis. Stacks. Calm catalog reading for long guides and stable navigation. Draftline. Working-document warmth for docs before release. Aperture. Neutral developer-docs style with compact spacing and rounded code panels. Organic Editorial. Poster-scale typography with cobalt organic image language for launches, programs, and editorial docs. Carbon. Stark monochrome technical mode. Organic Editorial is the default preset because it makes generated sites feel distinctive on first load while still keeping docs controls, code blocks, and API reference pages available through the same preset system. Each preset can be selected directly. Selecting a preset applies its default controls, typography, accent, radius, and layout defaults. Customize keeps the selected preset active while changing its options. Workshop and Canopy include a Borders control for switching between fine, structured, and ruled outlines. This keeps the saved inspiration\u2019s framed workspace feel available without forcing every generated page into the same border weight. Shared Controls. Every preset can expose its own controls. The global controls are always available and override the selected preset when changed: ControlPurpose Surface color. Override the page, sidebar, card, muted, and border palette. Shell spacing. Expose the outer page padding used by framed workspace themes. Content width. Adjust the prose and component measure without changing presets. Reading rhythm. Override base type size, line height, section gaps, and card padding. Borders. Tune card and shell rule strength globally. Code blocks. Switch source examples between soft, framed, plate, and terminal treatments. Typography. Switch heading, body, and code font treatment. Accent color. Override the preset\u2019s primary accent. Corner radius. Adjust shared UI and card radius. Radius Options. OptionValue None. 0. Sm. 0.3rem. Md. 0.5rem. Lg. 0.75rem. Full. 1rem. The \u201cReset appearance\u201d button restores the configured default preset and tuning. Project Theme Contract. Projects can define their own ThemeConfigurator preset in docs.yaml without forking the bundled template. During template preparation, Folio writes theme/project-theme.ts with a typed ThemePreset that merges Folio\u2019s base docs tokens with the project\u2019s overrides. tokens.light and tokens.dark accept CSS custom properties such as shadcn tokens (--background, --card, --border, --chart-1) and project tokens (--status-running). style accepts ThemeConfigurator layout variables such as --folio-content-max-width, --folio-section-gap, --folio-card-padding, --folio-code-bg, --folio-workspace-shell-topbar, --folio-workspace-shell-topbar-blur, and --folio-workspace-shell-topbar-border. Un-prefixed legacy names such as --content-max-width are still accepted for compatibility but are deprecated; use the --folio-* names. header.brand and header.badge replace the default docs navbar wordmark. header.repo, header.theme_toggle, header.action_label, and header.action_href replace the default docs navbar actions with project-owned actions; header.search: false hides the navbar search field while leaving the generated search index controlled by search.enabled. variants defines project-owned preset controls; each option can set a swatch for the control UI, override preview, style, and light/dark tokens while inheriting the base project theme. If an option has swatch but no full light/dark preview, Folio uses the swatch for both preview modes. For safety, token and style keys must be CSS custom properties beginning with --, values must be strings without CSS statement or block delimiters, and header and variant labels cannot include markup. Unknown tune keys are ignored with a warning. theme.radius must be one of the fixed radius scale values \"0\", \"0.3rem\", \"0.5rem\", \"0.75rem\", or \"1rem\", or a named alias (\"none\", \"sm\", \"md\", \"lg\", \"full\") that maps onto the same scale; any other value fails config validation. theme.variants is capped at 256 option combinations across all controls (the product of each control\u2019s option count) because every combination is resolved and embedded into each generated page; a larger product fails config validation. Tune aliases map to the shared controls: YAML keyRuntime control font. fontId. accent / color. colorId. surface. surfaceColorId. shell. shellPaddingId. width / content_width. contentWidthId. rhythm / reading. rhythmId. borders / border. borderId. code / code_blocks. codeTreatmentId. font: \"geist\" selects the bundled Geist/Geist Mono pair and maps the public --font-sans / --font-mono tokens used by Tailwind utility classes. If theme.preset matches a built-in preset and the project only provides tune, Folio keeps the built-in preset and applies the configured defaults. If the project supplies name, description, preview, style, tokens, or variants, Folio adds a Project group to the drawer and places the project preset before the built-in library. Theme Packages. Theme packages can replace the bundled configurator, project header actions, or theme/project-theme.ts while Folio still supplies generated content and metadata. See Theme Packages for the ownership model, file overlay rules, and validation checklist. Create a Custom Preset. Custom presets live in template/theme/presets.ts and use the interfaces from template/theme/preset-types.ts. Create a new object that satisfies ThemePreset. Give it stable defaultOptions, optional defaultRadiusIndex, optional defaultCustomization, and matching controls. Implement resolve(options) so every option combination returns light, dark, style, radius, and preview. Add the exported preset to presets. The style object must use the namespaced --folio-* keys defined by ThemeStyle in template/theme/theme-contract.generated.ts (generated from folio/schemas/theme_contract.py). Un-prefixed keys such as --card-shadow fail the TypeScript check against ThemeStyle and are not read by the generated CSS. Example: Then register it: Generate a Preset with ChatGPT. Paste this prompt into ChatGPT, then add the returned object to template/theme/presets.ts. After ChatGPT returns a preset: Paste it into template/theme/presets.ts. Add the exported preset to presets. Run pnpm run lint from template. Build the docs and open the drawer palette button. Test each generated control in light and dark mode. Example. Layout Component. ThemeConfigurator is rendered in the documentation drawer when theme presets are available. If presets are disabled, Nextra\u2019s native light/dark control remains in place."},{"url":"docs/api-reference/folio_agents/worktree/index.html","title":"folio_agents.worktree source","content":"Ask AI. folio_agents.worktree source. Read a board branch through an isolated Git worktree. Functions. sync_board_worktree source. Return a clean detached worktree at the current board ref. A local branch wins so a repository with a dedicated board worktree can preview its latest committed state without pushing. CI normally has no local board branch, so it fetches the named branch from origin and checks out that remote commit instead. NameTypeDefaultDescription project_dir. Path. - ref. str. - Returns: Path - _branch_name source. NameTypeDefaultDescription ref. str. - Returns: str - _resolve_commit source. NameTypeDefaultDescription repo. Path. - branch. str. - Returns: str - _try_commit source. NameTypeDefaultDescription repo. Path. - ref. str. - Returns: str - _worktree_name source. NameTypeDefaultDescription branch. str. - Returns: str - _git source. NameTypeDefaultDescription cwd. Path. - *args. str. - Returns: None - _git_output source. NameTypeDefaultDescription cwd. Path. - *args. str. - Returns: str -"},{"url":"docs/plugins/authoring/index.html","title":"Plugins","content":"Ask AI. Plugins. Extend the build pipeline with custom components, data, views, and hooks. Plugins are enabled by default: list them under the plugins: key in docs.yaml and Folio Docs loads them for every build. How Plugins Work. The plugin system is built on three concepts: Hook specifications define the extension points (what plugins can do). Hook implementations are functions you write that match a hook specification. The plugin manager discovers and calls your implementations at the right time during the build. When Folio Docs loads your plugin, it scans the module for functions decorated with @hookimpl and calls them at the appropriate stage of the build. During folio build, the same loaded plugin manager is used for config-key discovery, config normalization, document collection, extension registration, asset emission, and post-build hooks. Extension lifecycle. 1 Config keys. config_keys. Declare plugin-owned top-level docs.yaml keys before validation. 2 Configure. configure. Normalize raw plugin config and store data on config.extra. 3 Collect docs. collect_docs. Add Markdown sources to Folio's normal page pipeline. 4 Register UI. register_extensions. Register components, typed data modules, layouts, and generated views. 5 Emit assets. emit_assets. Write generated files into the prepared site before the frontend build. 6 Post-build. post_build. Run after the static output directory is written. 7 CLI. register_cli. Add plugin-owned commands to the shared folio CLI application. Available Hooks. These are the hooks called by the current build pipeline. New plugins should use register_extensions for UI work and data-backed views. Typed plugins can import the public boundary types from Folio: PluginConfig exposes project_name, version, output_dir, project_dir, and the mutable extra mapping where plugins store their normalized configuration. Extension Model. Folio keeps customization simple by using four primitives: Component \u2014 a named React export that can be exposed to MDX pages or plugin views. Data \u2014 JSON-serializable values emitted as typed frontend modules. Layout \u2014 a route shell with named slots. Views must choose a layout. View \u2014 a route composed from a layout, slot blocks, components, and data. Plugins should prefer the extension registry over writing arbitrary files. Folio owns import generation, route placement, and build safety. config_keys. Build-wired. Declare top-level docs.yaml keys that belong to your plugin. Folio uses this before validation so plugin-owned configuration does not produce unknown-key warnings. Return a list or tuple of key names; a single string is rejected because it is ambiguous. configure. Build-wired. Read plugin-owned configuration from raw_config and store normalized data on config.extra. This keeps plugin data out of the core config model while making it available to later build hooks. config.project_dir is set before this hook runs, so resolve relative paths from your plugin\u2019s config against it rather than the process working directory. register_extensions. Build-wired. Register components, layouts, data modules, and views. This is the preferred hook for custom UI, plugin-owned routes, and typed frontend data. collect_docs. Build-wired. Return Markdown or MDX files that should become ordinary Folio documentation pages. Each PluginDocument carries a source Path and a clean relative route: Folio parses the source and puts it through the same page generation, sidebar, local-image copying, link validation, search, sitemap, Markdown mirror, llms.txt, and incremental manifest as files under source.docs. A route collision with a project document or another plugin fails before either page is written. Use emit_assets instead for files that should be served verbatim. PluginDocument(..., unlisted=True) keeps every surface except the sidebar: the page still compiles at its route and enters search, the sitemap, the Markdown mirror, and llms.txt, but never appears in the docs navigation \u2014 a folder holding only unlisted pages is hidden with it. For plugin output that answers to a URL without belonging to the documentation\u2019s table of contents; the kanban plugin publishes card documents this way. register_components. Build-wired compatibility hook for older MDX-only component plugins. It receives the same extension registry as register_extensions; prefer register_extensions in new plugins so components, data, layouts, and views are registered in one place. post_build. Build-wired. Run actions after the build completes. The site_dir parameter is the path to the generated output directory. Common uses include copying additional assets, generating sitemaps, or running link checkers. emit_assets. Build-wired. Write generated files into the prepared site before search, dependency checks, and the static build run. Use this for generated MDX pages or public assets. Prefer register_extensions for typed data modules and layout-backed views. The builder implements the AssetBuilder protocol: write_page(route, content) \u2014 write an MDX page into the content directory. The route is recorded as a live page automatically, so a successful write_page needs no separate register_route call. page_exists(route) \u2014 report whether a page for the route is already on disk (for example, persisted from a prior warm build). read_page(route) / remove_page(route) \u2014 read or delete a page in the content directory. Together with a marker string embedded in your generated content, these carry the warm-build contract: refresh a page only when it still carries your marker, and never touch a user-authored page at the same route. list_pages(prefix) \u2014 routes of the pages currently on disk under a content-directory prefix. Use it when your plugin generates a variable set of pages: on a warm build, list your namespace, and remove the marker-tagged pages whose source of truth no longer exists. register_route(route) \u2014 record a route as a live page so the link checker treats internal links to it as valid. Call this for every page your plugin owns even when you skip write_page because the page persists from a prior build; otherwise links to your page are flagged as broken on warm builds. Only register a route once the page is guaranteed to exist on disk \u2014 write the page first, or confirm with page_exists \u2014 because a registered route with no page passes link checking and then 404s on the deployed site. read_meta(directory) / write_meta(directory, meta_json) \u2014 read and write the _meta.ts sidebar file for a content directory. read_meta returns the raw file text (an empty string when the file does not exist). Always merge your entry into the existing content instead of rewriting the file: _meta.ts files written by the sidebar generator contain nested object entries (folder titles, collapse state) that must be preserved. copy_static_asset(relative, source) \u2014 copy one source file into the workspace public/ directory at a containment-checked relative path so the static site serves it verbatim. remove_static_tree(relative) \u2014 remove an owned subtree below public/ before republishing it on a warm build. It refuses the public root and escaping paths. emitted_routes() \u2014 the set of routes recorded via write_page/register_route in this build. write_llm_files(llms_txt, llms_full_txt) \u2014 override the generated llms.txt / llms-full.txt files. register_cli. Add plugin-owned commands to the folio CLI. app is the typer.Typer application; add commands with @app.command(...). This is the hookspec first-party build plugins use for their commands, and it is dispatched for project plugins too. The hook runs when the CLI starts up, before command-line arguments are parsed. Because the command table is finalized at startup, project plugins listed in docs.yaml contribute CLI commands only when you run folio from inside the project directory. Running from outside still loads project plugins for build hooks; only their extra CLI commands need the project cwd. A plugin that fails during register_cli is skipped with a warning and never takes down the CLI. Installed Folio products use a narrower mechanism: a callable entry point in the folio.cli group receives the Typer application and registers its command group. Folio for Agents uses this to expose folio board. These product CLI entry points load automatically, but do not activate build hooks or config keys; build plugins remain opt-in through docs.yaml. Postponed annotations sharp edge. Type annotations on a command defined inside register_cli are evaluated against the plugin module\u2019s globals when the command runs. Under from __future__ import annotations, any name used in a command signature (for example Path) must be imported at the top of the plugin module \u2014 importing it only inside register_cli fails at dispatch time with a NameError. Dividing Work Between the Build Hooks. Three hooks split a plugin\u2019s output surface, and a plugin may use more than one: register_extensions owns everything typed and registry-managed: components, typed data modules, layouts, and views. Folio generates the imports, the TypeScript modules, and the view routes for you, and validates names and references at registration time. collect_docs owns Markdown and MDX source files: Folio turns them into normal documentation pages and includes every standard generated surface. emit_assets owns raw or programmatically generated files in the prepared site: verbatim public assets and pages that do not exist as Markdown source files. A typical shape: register_extensions registers a component and writes the typed data it renders; collect_docs contributes authored Markdown that uses it; emit_assets publishes any raw bundle the page links to. The kanban plugin uses all three, on the same extension primitives available to any custom plugin: it registers a KanbanBoard component, writes typed data to lib/kanban-data.ts, and creates the /kanban/ view with the required folio.public layout. See the OpenAPI example below for programmatically generated pages. Writing a Plugin. A plugin is a Python module or class that implements one or more hooks. Here is a step-by-step guide. Step 1: Import the hook decorator. Step 2: Write your hook implementations. You can write plugins as either a class or a plain module. Both approaches work equally well. Class-based plugin: Module-based plugin: Step 3: Register the plugin in your config. Add the plugin to the plugins list in docs.yaml: First-party default plugins (folio_docs.docs.integrations.roadmap and folio_docs.docs.integrations.landing) load automatically before plugins: entries. They stay inert until their roadmap: or landing: key appears. Official Example: Roadmap. The first-party roadmap plugin shows the intended shape for plugin-owned data, CLI behavior, and generated routes. It is a default plugin: Folio loads it on every build without a plugins: entry (listing it explicitly is deduplicated, not an error), and the roadmap: config key is what activates it \u2014 without the key the plugin emits nothing: The plugin declares roadmap as a config key, stores normalized data under config.extra[\"roadmap\"], emits lib/roadmap-data.ts, and can generate a standalone /roadmap/ route. The docs route (/docs/roadmap/) is for explanation; the public route (/roadmap/) is for displaying the same real data outside the docs shell. The implementation follows the four-primitives model: it registers the Roadmap component, writes typed roadmap data, and declares /roadmap/ as a folio.public layout-backed view. The dedicated-page contract. Plugins that need a standalone page should follow the grammar the first-party roadmap and kanban views share, so every plugin page feels like one product: Declare the view on the folio.public layout. Its band renders the Home link, optional sibling cross-links (props={\"links\": [...]}), the title, and an optional description \u2014 and props={\"narrow\": True} switches the page from board width to a centered document column. Open the content with the ViewHeaderRule component (mono micro-label, hairline, metadata or controls on the right) \u2014 the rule the roadmap\u2019s \u201cReleases\u201d row and the board\u2019s \u201cBoard\u201d row both use. Cross-link a sibling view only after checking its route is actually enabled in config.extra, so a disabled plugin never produces a dead link. Official Example: OpenAPI. The first-party OpenAPI plugin is the reference for the register_extensions / emit_assets division of labor: Walking through its hooks in build order: config_keys declares openapi as a plugin-owned top-level key, so it does not trigger unknown-key warnings. configure loads and normalizes each spec and stores the result under config.extra[\"openapi\"]. Relative path values are resolved against the project directory, so builds work from any working directory. register_extensions registers the OpenApiReference component and writes a typed data module (lib/openapi-data.ts). The type_source parameter of registry.write_data_module carries the hand-authored TypeScript interfaces, and type_annotation types the exported constant. emit_assets writes one MDX page per source, merges a _meta.ts sidebar entry for it without disturbing the entries the sidebar generator wrote, and registers each route \u2014 even when the page already exists from a prior warm build \u2014 so internal links to the page stay valid. Registering Plugins. There are two ways to specify a plugin in docs.yaml: Module path. Use a dotted Python module path. The module must be importable from the environment where you run Folio Docs (i.e., it needs to be installed or on PYTHONPATH). File path. Use a relative file path starting with ./ to load a plugin directly from a file. In CLI commands, the path is resolved from the active project directory, either the current directory, the positional directory argument, or --project-dir; when using load_config() directly, it is resolved from the directory containing the config file. Security note: File paths must resolve to a location within the plugin base directory. Paths that resolve outside that directory are rejected with a ValueError. Example: Post-Build Hook. This plugin copies a CNAME file into the output directory after every build, which is useful for GitHub Pages custom domains: Register it in docs.yaml: Example: Custom Components. Register a custom React component for use in your documentation. For a single component file (or a directory of them), the components: key in docs.yaml is lighter than a plugin \u2014 reach for a plugin when the component needs data modules, layouts, views, or other hooks alongside it. After registering, these components are available in any MDX page without import statements: register_component parameters. name \u2014 the JSX name pages use. Must be a valid JavaScript identifier. import_path \u2014 the module path the generated import uses (for example @/components/__folio_components/interactive-demo). export_name \u2014 the export to import when it differs from name. expose_mdx \u2014 expose the component to MDX pages without imports (default True). source_path \u2014 a component source file copied into the build. Relative paths are resolved against the project directory, not the process working directory. props \u2014 a mapping of prop names to TypeScript type strings. This is documentation metadata; it does not by itself validate props or publish the component anywhere. required \u2014 mark the component as part of the template contract: custom templates must wire it in their mdx-components.tsx, and template validation fails when it is missing. category \u2014 a free-form taxonomy label. It plays no role in the MDX component contract. contract / source_label \u2014 mark the component as part of the published MDX component contract (folioMdxComponents); source_label carries the contract source string. Contract membership is explicit: declaring props alone does not add a component to the contract. A flagged plugin or config component joins the builtins in both places the contract is published: the lib/folio-mdx-contract.ts module written for the template, and the /_folio/contract.json file in the exported site. Name collisions and shadowing builtins. Registering a component whose name matches a Folio builtin (for example Callout or Timeline) is allowed: the config or plugin component shadows the builtin and folio emits a UserWarning noting the override. Registering the same name twice from config or plugins is still an error \u2014 the second registration raises a ValueError that names both origins. The published authoring contract. Every build writes /_folio/contract.json into the exported site. It answers, for this project, the three questions an agent otherwise has to guess at: which components a page may use, which top-level docs.yaml keys the project accepts, and which docs pages the build emitted. Abridged \u2014 a real build lists every component, every accepted key, and every route: components \u2014 the MDX component contract for this build: the Folio builtins plus every config or plugin component registered with contract=True. The same set Folio writes into lib/folio-mdx-contract.ts for the template. configKeys \u2014 the core Folio keys unioned with the keys the loaded plugins claim through their config_keys hook. Anything outside this list produces an unknown-key warning when docs.yaml is read. routes \u2014 the docs URLs this build emitted, plugin-owned pages included. instructions \u2014 one line, addressed to the reader: tolerate fields you do not know. Later Folio releases add fields, and a reader that rejects unknown ones breaks on the next upgrade. Read the payload defensively. mdxContractVersion versions the components list, and folioVersion moves with every release, so neither signals a change to the envelope itself. Treat unknown fields as additions rather than errors. The file is written during the build, from that build\u2019s configuration and plugin set, and it describes the site published next to it. Read it as a snapshot of that build, not as a view of the repository as it stands now. Folio publishes it through the workspace public/ directory, which the Next static export carries through unchanged, so the path is the same on every deployment: https://<site>/_folio/contract.json. Your own plugin can publish a file the same way from emit_assets, by writing into Path(builder.build_dir) / \"public\". Error Handling. If a plugin fails to load, folio raises a RuntimeError with a descriptive message: Common causes of plugin load failures: Module not found: The Python module is not installed or not on PYTHONPATH. Make sure the package is installed in the same environment as folio. Import error: The plugin module has a syntax error or missing dependency. Path outside project directory: A file-path plugin (starting with ./) resolves to a location outside the project/config directory. Missing hookimpl decorator: Functions without the @hookimpl decorator are ignored silently \u2014 they won\u2019t cause an error, but they won\u2019t be called either. Hookwrapper implementations: folio dispatches hooks one implementation at a time with per-plugin failure isolation, so pluggy\u2019s @hookimpl(hookwrapper=True) (and wrapper=True) implementations are not supported. They are rejected loudly at load time rather than silently skipped. Hook failure policies. Hook failures during a build are isolated per plugin and attributed to the plugin under the name you used in plugins:. Two policies apply: Fail fast (configure, collect_docs, register_components, register_extensions): the first failing plugin aborts the build with Plugin '<name>' failed in hook '<hook>': <error>. These hooks shape configuration, source ownership, and the registry, where continuing would silently corrupt output. Warn and skip (config_keys, emit_assets, post_build, register_cli): a failing plugin is reported as a warning and skipped, and the build continues with the remaining plugins. Plugin Discovery via Entry Points. Installed packages can expose plugins through the folio-docs entry-point group: Entry-point plugins are strictly opt-in: installing a package never activates its plugin. List the entry-point name in plugins: to load it: Resolution rules for a name in plugins:: The name is first matched against installed folio-docs entry points; only when no entry point matches is it imported as a dotted module path. If a name matches both an installed entry point and an importable module, the entry point wins and folio emits a UserWarning naming the distribution that shadowed the module. If multiple installed distributions declare the same entry-point name, the alphabetically first distribution wins (deterministically) and a UserWarning names all contenders. Declaring a Plugin API Version. The Python hook API is versioned independently of the emitted MDX component contract; the current host version is 1.1. A plugin can declare the API version it targets with a module-level (or class-level) constant: Accepted forms are \"1\", \"1.0\", and \"1.0.0\" \u2014 a missing minor defaults to 0, and any patch component is ignored. A bare integer (FOLIO_PLUGIN_API = 1) also works. A missing declaration is allowed; the plugin loads without a version check. A different major version refuses to load the plugin with a ValueError. A newer minor than the host emits a warning but still loads the plugin. Publishing a plugin. A Folio plugin is an ordinary Python distribution. There is no folio-plugin.toml, no submission form, and no registry account: the packaging metadata is the manifest. Step 1: Declare the entry point. The name on the left of the = is literally the string a user pastes into plugins:, so choose it as carefully as you would choose the distribution name: Installing the distribution does not activate the plugin; only the plugins: entry does. Step 2: Add the folio-plugin GitHub topic. Add the topic folio-plugin to the public repository. A plugin catalog is on the roadmap (Ecosystem, 1.0), and that topic is the only signal it will use: no listing request, no review queue, no account. Nothing indexes the topic today, so adding it now costs one click and is the whole publishing step when the catalog arrives. Step 3: Optionally declare catalog metadata. A [tool.folio-docs.plugin] table lets a catalog entry be built from the distribution itself instead of a hand-maintained list. Folio reads none of it during a build, and every field is optional: requires_folio \u2014 a PEP 440 version specifier for the Folio versions the plugin supports. This is catalog metadata, not an install constraint: declare the real dependency in project.dependencies as well. summary \u2014 one sentence, in the same register as a PyPI summary. docs \u2014 the URL of the plugin\u2019s own documentation. config_keys \u2014 the top-level docs.yaml keys the plugin owns. Keep the list in step with what your config_keys hook returns; it is what tells a reader which keys appear in their config because of your plugin. Before you publish, read Trust & Safety. A plugin runs inside its users\u2019 builds, and this publishing convention carries no vetting by design. Tips. You can list as many plugins as you want. They are loaded in the order they appear in docs.yaml. Plugins that implement the same hook are all called \u2014 pluggy handles the dispatch. The order follows plugin registration order. Keep plugins focused. A single plugin implementing one or two hooks is easier to maintain than a monolithic one. Test your plugin by creating a small test project with a minimal docs.yaml and running folio build -v."},{"url":"docs/api-reference/folio_docs/features/index.html","title":"folio_docs.features source","content":"Ask AI. folio_docs.features source. Functions. _experimental_features source. Feature names enabled via the FOLIO_EXPERIMENTAL environment variable. NameTypeDefaultDescription env. Mapping[str, str] | None. None. Returns: frozenset[str] - is_feature_enabled source. NameTypeDefaultDescription feature. str. - env. Mapping[str, str] | None. None. Returns: bool - experimental_feature_state source. NameTypeDefaultDescription env. Mapping[str, str] | None. None. Returns: str - disabled_doc_feature_for_route source. Feature gating route, or None when the page may be published. A route stays gated only while its feature is off, so a feature enabled through FOLIO_EXPERIMENTAL publishes its pages instead of leaving them out of navigation, search, and the llms output. NameTypeDefaultDescription route. str. - env. Mapping[str, str] | None. None. Returns: str | None - disabled_api_feature_for_module source. Feature gating module_name\u2019s API pages, or None when publishable. Membership in the map is itself the gate \u2014 unlike is_feature_enabled, which only gates names listed in MVP_DISABLED_FEATURES. Only naming the feature in FOLIO_EXPERIMENTAL publishes the module. NameTypeDefaultDescription module_name. str. - env. Mapping[str, str] | None. None. Returns: str | None - disabled_feature_message source. NameTypeDefaultDescription feature. str. - Returns: str -"},{"url":"docs/api-reference/folio_docs/watcher/index.html","title":"folio_docs.watcher source","content":"Ask AI. folio_docs.watcher source. Classes. @dataclass. class _PythonModuleCache. source @classmethod. from_config source. NameTypeDefaultDescription resolved. Config. - Returns: _PythonModuleCache - all_modules source. Returns: list[ModuleIR] - contains source. NameTypeDefaultDescription source_file. Path. - Returns: bool - upsert source. NameTypeDefaultDescription module. ModuleIR. - Returns: None - remove source. NameTypeDefaultDescription source_file. Path. - Returns: None - Functions. _preview_examples_dir source. NameTypeDefaultDescription project_dir. Path. - Returns: Path - _watch_dirs_with_preview_examples source. NameTypeDefaultDescription watch_dirs. list[Path] - project_dir. Path. - Returns: list[Path] - _plugin_watch_dirs source. Directories plugins asked the watcher to care about (existing only). NameTypeDefaultDescription plugin_manager. Any. - resolved. Config. - Returns: list[Path] - _dispatch_plugin_change source. Offer a change under a plugin-watched directory to the plugins. The handler, not the watcher, knows what the files mean \u2014 a board directory holds .md cards and a .yaml column set, and future plugins hold whatever they hold. Returns True when some plugin handled it. NameTypeDefaultDescription plugin_manager. Any. - plugin_dirs. list[Path] - builder. Any. - resolved. Config. - change. Change. - path. Path. - Returns: bool - _is_under source. NameTypeDefaultDescription path. Path. - dirs. list[Path] - Returns: bool - _module_name_from_path source. NameTypeDefaultDescription path. Path. - src_dir. Path. - Returns: str - _route_from_doc_path source. NameTypeDefaultDescription path. Path. - doc_dir. Path. - Returns: str - _parse_python_modules source. NameTypeDefaultDescription resolved. Config. - _parse_python_module source. NameTypeDefaultDescription path. Path. - module_name. str. - resolved. Config. - Returns: ModuleIR - _disabled_api_feature_for_module source. NameTypeDefaultDescription module_name. str. - Returns: str | None - _published_modules source. NameTypeDefaultDescription modules. list[ModuleIR] - Returns: list[ModuleIR] - _is_python_excluded source. NameTypeDefaultDescription path. Path. - resolved. Config. - Returns: bool - _write_api_reference_index source. NameTypeDefaultDescription modules. list[ModuleIR] - builder. SiteBuilder. - Returns: None - _write_meta_for_modules source. NameTypeDefaultDescription all_modules. list[ModuleIR] - resolved. Config. - config. Config. - builder. SiteBuilder. - Returns: None - _regenerate_meta source. NameTypeDefaultDescription python_source_dirs. list[Path] - resolved. Config. - config. Config. - builder. SiteBuilder. - Returns: None - _handle_python_change_incremental source. NameTypeDefaultDescription change. Change. - path. Path. - module_name. str. - route. str. - module_cache. _PythonModuleCache. - config. Config. - resolved. Config. - builder. SiteBuilder. - project_dir. Path. - console. Console. - verbose. bool. - Returns: bool - _handle_python_change source. NameTypeDefaultDescription change. Change. - path. Path. - python_source_dirs. list[Path] - config. Config. - resolved. Config. - builder. SiteBuilder. - project_dir. Path. - console. Console. - verbose. bool. - module_cache. _PythonModuleCache | None. None. Returns: None - _handle_doc_change source. NameTypeDefaultDescription change. Change. - path. Path. - doc_source_dirs. list[Path] - config. Config. - resolved. Config. - builder. SiteBuilder. - python_source_dirs. list[Path] - console. Console. - verbose. bool. - Returns: None - _handle_preview_example_change source. NameTypeDefaultDescription _change. Change. - path. Path. - examples_dir. Path. - builder. SiteBuilder. - console. Console. - verbose. bool. - Returns: None - _watcher_loop source. NameTypeDefaultDescription stop_event. threading.Event. - watch_dirs. list[Path] - python_source_dirs. list[Path] - doc_source_dirs. list[Path] - preview_examples_dir. Path. - config. Config. - resolved. Config. - builder. SiteBuilder. - project_dir. Path. - console. Console. - verbose. bool. - plugin_manager. Any. None. plugin_watch_dirs. list[Path] | None. None. Returns: None - start_watcher source. Start file watcher in a daemon thread. Returns stop event. NameTypeDefaultDescription watch_dirs. list[Path] - config. Config. - resolved. Config. - builder. SiteBuilder. - project_dir. Path. - console. Console. - verbose. bool. False. plugin_manager. Any. None. Returns: threading.Event -"},{"url":"docs/kanban/cards/artifacts-read-from-the-canvas/canvas-reading-compared/index.html","title":"Reading an artifact from the canvas \u2014 four prototypes compared","content":"Ask AI. Reading an artifact from the canvas \u2014 four prototypes compared. Built 2026-08-24 to answer one question: a card can attach what it produced, but reading it means leaving the board, either for a compiled page under /docs wearing documentation chrome or for a raw file in a new tab. Four layouts were built in full against the real 44-card board and verified adversarially in a headless browser. The sibling note where-card-pages-publish.md answers the other half of the owner\u2019s verdict; this document takes its conclusion as given and says so where a prototype argued against it. The demo path every variant had to survive is the card the-board-reads-as-a-tree: one long markdown document and five HTML prototypes built for a full 1440px window. That card decided more of this comparison than any principle did. Reading overlay. What it argues. Reading is a mode you enter deliberately. The artifact takes the viewport over a dimmed rim of board; two native dialogs stack in the top layer, the mail below and the reader above, and Esc unwinds exactly one level with focus returned to the element that opened what just closed. Strongest. It is the only in-place variant that gives the demo card\u2019s artifacts the room they were made for: the document reads at a 66ch measure and the prototypes embed near the width they were designed at. The implementation leans on the platform \u2014 showModal() supplies the backdrop, the inert canvas and per-level Esc, and the script\u2019s own work is bookkeeping. It is also the only in-place variant that applies the URL on hash change, not only at load; the board reader page\u2019s URL is alive too, but reading there has left the canvas. Where it fails. It is modal. You cannot read and scan the columns at once, and comparing two artifacts means closing one to open the other. Focus inside an embedded prototype takes the keys with it, an honest limit of opaque-origin file:// iframes. The URL keys artifacts by target basename, which collides the day a card attaches two files with the same name. Wheel input over the dimmed rim still scrolls the board behind, and card descriptions render as plain text, markdown punctuation and all. Verification. Passed. The pass found nothing the notes panel had not already admitted, which is worth stating as credit; the one issue logged is the shared data gap covered below. Reading rail. What it argues. Reading and the board share the screen. The artifact opens as a right rail, clamp(500px, 44vw, 680px), beside columns that stay fully live; the card dialog collapses into the rail\u2019s crumb so only one reading surface exists at a time. Strongest. It is the only variant where \u201creadable from the canvas\u201d is literally true while reading: the columns stay interactive, the card being read is marked on the board, the filter keeps working, and when the filter hides the very card whose artifact is open, closing the rail still lands focus somewhere sensible. The housekeeping is careful: the closed rail is inert and its iframe is parked on about:blank. Where it fails. Half a screen is a poor page, and the builder\u2019s own numbers say so: the comparison document reads in roughly a 45%-viewport measure, and prototypes built for 1440px embed at about 600px, so the rail under-sells exactly the artifacts the demo card compares. The compressed board keeps two or three columns in view and grows a horizontal scrollbar; two scroll regions share one wheel. Board, dialog and rail together have more state corners than one modal, and the corners are reachable: the dialog can sit beside the rail showing a different card, with Tab trapped in the dialog while the rail stays click-reachable. The [ and ] shortcuts die inside the embedded iframe, and Open full for the document points at the compiled /docs page, served site only. Verification. Passed on fresh loads. It found what the notes do not say: there is no hashchange listener, so the URL is honoured only at load, and a deep link restores rail and crumb but not the dialog, by design but undeclared. Dialog reader. What it argues. Everything about a card, including what it produced, reads in one surface. The mail dialog widens into letter and reading pane; the band stays on the left with the open attachment marked, so there is no second place to learn and no second layer to unwind. Strongest. The least new geometry of the four: one dialog, no extra layer, no change to the page behind it. Band position stays visible while reading. It is the only variant that renders card prose with a small markdown pass, and it handles the closed-door rule most exactly: Open full is absent for the markdown artifact rather than pointing anywhere dead. Where it fails. The reading pane never gets more than about two thirds of a 1440px screen because the compressed letter keeps 340px, so the five embedded prototypes render at a width nobody uses them at; Open full is the admission. The modal blocks the canvas it claims to preserve, and the letter reshapes the moment a reader opens, so the surface stays one but does not stay still. Focus inside the iframe eats Esc and the arrows; ArrowLeft and ArrowRight always walk the band while reading, stealing horizontal scrolling from wide code blocks; and Chrome\u2019s close-watcher grouping can let a rapid second Esc close the whole dialog. Verification. Passed on fresh loads, with the same undeclared load-only URL as the rail. One thing no notes panel carries: the URL names the artifact by band index (&a=1), so a pasted link silently changes meaning if the card ever reorders its artifacts; the overlay\u2019s basename and the page\u2019s slug both survive a reorder. Board reader page. What it argues. An artifact\u2019s URL is a board page: /kanban/<card>/<artifact>, board chrome, a reading column, the card\u2019s other artifacts in a rail. Opening is a real navigation; Back unwinds perfectly. The route model in the script\u2019s header comment is the most considered of the four: pushState per level, replaceState per band step, a depth counter in history.state so Esc never backs out of the app, and a slug grammar that tries the target\u2019s stem, then its filename, then its index on collision. What happened. The builder\u2019s session died mid-file: the script stopped at line 329, the IIFE never closed, and nothing ran. The back half was written afterwards: the dialog, the reader page and the navigation, built against the completed HTML and CSS and the route model in the script\u2019s own header, then verified like the others. Verification. Passed, and the route model holds up. One press of Esc is one history entry when the history is in-app, and a written parent state when it is not, so a deep link never backs out of the app. Band steps and rail switches replace in place, so Back never replays a reading session; a fresh load of the route restores the page; the closed door is a div marked \u201cpath only\u201d. It adopts the dialog reader\u2019s rule and hides Open full for the markdown artifact rather than pointing at a page file:// cannot serve. Where it fails. Reading leaves the canvas entirely, which its own notes admit, and the route it wants is the one option (b) of where-card-pages-publish.md already costed out and rejected: a second catch-all outside /docs, with search, sitemap, mirrors and link validation rebuilt around it. Complete and verified, it still argues against the sibling note\u2019s conclusion. The cuts. What the reader keeps seeing. Only the rail keeps a live board. The overlay keeps a dimmed inert rim, the dialog reader the same canvas behind a backdrop, the page nothing. What the residual board is worth is the real question: with the rail it buys real work and costs half the reading surface; with the overlay it buys only the sense of not having left, and costs nothing. The Esc stack. The overlay has two levels enforced by the platform: Esc arrives as cancel on the topmost dialog only, so one press is always one level. The dialog reader has two levels enforced by hand inside one dialog, and Chrome\u2019s Esc grouping can eat one. The rail has up to three presses from its deepest corner. The page\u2019s Esc is Back when the history is in-app and a written parent state when it is not, one press per level, verified. What the URL means. All four write the reading position; they disagree on what the words are. The overlay writes card id plus artifact basename, applied on load and on hash change. The rail writes the artifact target plus the filter, load only. The dialog reader writes card id plus band index, load only, and an index is the weakest name of the three. The page writes a route with real history, the only variant where Back means something and the only grammar that survives both a duplicate filename and a reorder. What is left for the compiled page. Every notes panel overreached here. The overlay says a deep link no longer needs the compiled page; the rail demotes it to the Open full target, \u201cindexed nowhere\u201d; the dialog reader says it can go entirely; the page variant retires it into a redirect. All four contradict where-card-pages-publish.md, and this document sides with the note. The compiled page keeps the jobs a canvas reader cannot do: the durable address, the search hit, the llms.txt line, the markdown mirror. It loses only the sidebar seat and the human reading. The rail\u2019s own Open full is the demonstration: a long document eventually wants a full window with a stable address, and that address already exists. Cost in the real component. The board is one client component, template/components/kanban-board.tsx, and the card dialog already exists in it. The overlay is a second dialog element inside that component; the dialog reader is a width state and a second pane inside the existing dialog; the rail is layout, the component\u2019s root becoming a two-pane stage that touches the board\u2019s own scrolling and width logic. None of the three needs a route. The page needs a real route under /kanban, which is the option (b) bill; it is a build-system decision disguised as a layout. A shared gap, not a variant defect. The brief requires that tables read well, and the document every variant demos contains none: verified identically in the source markdown, the compiled page and reader-data.js. Every variant\u2019s table CSS was proven only by injecting a probe table. Whichever variant ships should be re-checked against a document that actually has tables. Recommendation. Reading overlay, as the primary, paired with the compiled page exactly as the sibling note leaves it: the overlay is where a human reads from the canvas, and the delisted /docs/kanban/<id>/<stem>/ page stays the durable full-window address that deep links, search and agents land on. That pairing is a combination, but not of two prototypes; the second half already exists. The reason is the demo card. This board attaches long documents and full-width prototypes, and both want the whole viewport. The rail preserved the canvas and under-sold the artifacts; the dialog reader preserved the surface and cramped the reading; the page preserved the URL and lost the canvas. The overlay preserved the thing being read, and reading was the point. It is also the cheapest implementation in the real component and the only in-place variant whose URL is alive rather than load-only; the page\u2019s URL is alive too, but it is not on the canvas. Reading two artifacts side by side is the one need it cannot meet, and the answer to that is the compiled page in a second window, not a rail. Taken from the losing variants: the board reader page\u2019s slug grammar replaces the overlay\u2019s bare basename in the URL; the dialog reader\u2019s rule of hiding Open full when there is nothing full to open, and its small markdown pass for card prose; the rail\u2019s focus rule for when the card that owns the reading has been filtered off the board. Left for the builders: the load-only URLs of the rail and the dialog reader are worth a line in whichever card carries the product work, because the real board already treats its URL as live."},{"url":"docs/theming/custom_templates/index.html","title":"Custom Templates","content":"Ask AI. Custom Templates. [!WARNING] Security A custom template is executed as trusted code during every build. Folio runs pnpm install --frozen-lockfile (which executes dependency lifecycle scripts) and next build (which runs the template\u2019s next.config.mjs in Node), so pointing template.path at a directory is equivalent to running that code on your machine. Only point template.path at frontend code you have reviewed and trust. Building an untrusted repository or branch in CI inherits the same remote-code-execution surface \u2014 treat a docs build like any other untrusted-code execution. Never put secrets, API tokens, or credentials in template.params: they are baked into the static export and published. Custom templates are the expert escape hatch for teams that need complete control over their documentation site. A template is a full frontend workspace. It is not a color-token override system: it owns layout, routes, CSS, JavaScript, components, package dependencies, search UI, navigation chrome, and brand behavior. Use personalization when the bundled Folio template is close enough. Use theme packages when you only need to overlay selected files on the bundled template. Use template.overlay_path when you want to override just one or a few template files while inheriting the rest of the bundled template. Use template.path when the documentation must live inside a product-specific frontend shell. Configure a Template. Point Folio at a local template directory from docs.yaml: template.path is resolved relative to the project directory. It must point to a trusted local directory inside the project, and it cannot point at .build/ or the configured output directory. template.params is an arbitrary JSON-serializable mapping. Folio does not interpret these values; the selected template owns their meaning. See the template.params contract below for the full rules and a worked example. template.docs_route_base controls where documentation pages are served. It defaults to /docs. Set it when the generated docs need to live at a product route such as /reference/docs. Folio uses this path for generated links, search URLs, sitemap entries, canonical metadata, LLM output, and the copied Next.js route folder. Overlay (partial override). template.path is all-or-nothing: you must fork the entire bundled template to change a single file. When you only need to override one or a few files, use the opt-in template.overlay_path instead. In overlay mode Folio starts from the bundled template, then copies your overlay files on top \u2014 your files win, and any file you do not provide falls back to the bundled template. Only place the files you want to change inside the overlay directory, mirroring their path in the template tree: After the overlay is merged, Folio runs the normal injection pipeline on the result, so project metadata, the docs route base, search, versions, and theme configuration are injected exactly as they would be for the bundled template. [!IMPORTANT] template.overlay_path and template.path are mutually exclusive. template.path is a full replacement; template.overlay_path is a partial override. If both are set, template.path wins and the overlay is ignored with a warning. The overlay directory is held to the same guards as template.path: it must live inside the project, may not point at .build/ or the output directory, and may not contain symlinks. Because the merged template still has to satisfy the injection-marker contract, any marker-bearing file you override must keep the required markers verbatim \u2014 the safest approach is to copy the file from the bundled template and edit around the markers. Limitations. The overlay is a file-level merge, not a deep merge. Overriding a file replaces it entirely; Folio does not merge JSON (for example package.json) or splice individual functions inside a file. Deleting a bundled file via the overlay is not supported \u2014 an overlay can only add or replace files. For deeper changes than a handful of file replacements, fork the whole template with template.path. Ownership Model. Folio owns generated documentation data. Your template owns presentation. OwnerResponsibilities Folio. Parse Python and Markdown sources, generate MDX pages, write _meta.ts, emit API reference pages, write Markdown exports, build the search index, link-check generated routes, and export static files. Template. Render the configured docs route, provide compatible MDX components, style the whole site, implement navigation/search/version UI, ship dependencies, and decide how template.params affect the frontend. Do not edit .build/. Folio copies the template there, removes template demo content, injects generated content and metadata, runs the frontend build, and may overwrite reserved generated files on every build. Template Anatomy. Folio currently supports local Next/Nextra-compatible templates. A custom template must include a source docs route at app/docs; Folio relocates that folder to template.docs_route_base inside .build/ when the route base is not /docs. The build runtime expects pnpm install --frozen-lockfile, pnpm run build, and a static export in out/. Keep app/docs as the template source route even when the public route is configured differently. Injection markers. Folio does not template these files with a templating engine. It performs literal string substitutions and regex block edits against specific source files after copying the template into .build/. The marker strings below are a load-bearing contract: a custom template must contain them verbatim, in the expected files, or Folio cannot inject project metadata, the docs route base, the repository link, the header, search, versions, the theme preset, the base path, or Folio-managed MDX component wiring. [!IMPORTANT] Folio validates the load-bearing markers before building. If a required marker (or the file that owns it) is missing, the build fails fast with a ValueError that names every missing (file, marker) pair, alongside the existing missing-files and MDX-contract checks. The load-bearing set is the always-applied project metadata in app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, and the base-path marker in next.config.mjs. Optional targets \u2014 paired blocks that are stripped when a feature is off, the mdx-components.tsx component markers (which fall back to a ...components, spread), and markers that only live in optional files such as the landing page or theme configurator \u2014 are not hard failures. When one is absent Folio logs the skip at debug level and emits an info-level summary of the files it injected, instead of failing or silently no-op\u2019ing. The safest way to author a custom template is still to copy the marker-bearing files verbatim from the bundled template and edit around the markers, rather than recreating them from scratch. Text replacements. Each of these is a literal string \u2192 value replacement. The marker may appear more than once in a file; all occurrences are replaced. Marker stringExpected file(s)What Folio substitutes __PROJECT_NAME__. app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx, app/page.tsx. Project name from docs.yaml. __PROJECT_DESCRIPTION__. app/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx. Documentation for <name>. __SITE_URL__. app/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, app/sitemap.ts, app/robots.ts. Configured site URL (trailing slash trimmed). __PROJECT_MONOGRAM__. app/icon.svg, app/docs/layout.tsx, app/opengraph-image.tsx, app/docs/opengraph-image.tsx, app/page.tsx. Two-letter lowercase monogram derived from the name. __PROJECT_REPO__. app/docs/layout.tsx. HTML-escaped repository URL (emptied when no repo is set). __DOCS_INDEX_CANONICAL_PATH__. app/docs/[[...mdxPath]]/page.jsx. Canonical docs index path (/ when a landing page is enabled, otherwise the docs route base). __DOCS_ROUTE_BASE__. app/sitemap.ts, app/robots.ts. Configured template.docs_route_base. __INCLUDE_DOCS_INDEX__. app/sitemap.ts, app/robots.ts. true/false depending on whether the landing page is enabled. __FOLIO_DOCS_ROUTE_BASE__. next.config.mjs. Configured template.docs_route_base. __VERSIONS__, __CURRENT_VERSION_PATH__. components/version-selector.tsx. JSON version list and the current version path. __LANDING_*__, __LANDING_*_JSON__. app/page.tsx, components/landing-navbar.tsx. Landing hero/CTA/feature/section data (raw and JSON-encoded forms). __PROJECT_NAME_JSON__, __PROJECT_MONOGRAM_JSON__. app/page.tsx, components/landing-navbar.tsx. JSON-encoded name/monogram for the landing page. Paired block markers. These are START/END comment pairs that wrap a region. Folio either replaces the region\u2019s contents (preserving indentation) or strips the whole block \u2014 and its marker lines \u2014 when the corresponding feature is not configured. Include both the start and end marker, and the placeholder content in between, exactly as in the bundled template. Marker pairExpected fileWhat Folio substitutes // __PROJECT_REPO_IMPORTS_START__ \u2026 // __PROJECT_REPO_IMPORTS_END__. app/docs/layout.tsx. Repository-link imports (block removed when no repo is set). {/* __PROJECT_REPO_LINK_START__ */} \u2026 {/* __PROJECT_REPO_LINK_END__ */} app/docs/layout.tsx. Repository link markup (block removed when no repo is set). {/* __PROJECT_HEADER_LOGO_START__ */} \u2026 {/* __PROJECT_HEADER_LOGO_END__ */} app/docs/layout.tsx. Project header brand/badge markup (block removed when no header is configured). // __PROJECT_HEADER_ACTION_IMPORTS_START__ \u2026 // __PROJECT_HEADER_ACTION_IMPORTS_END__. app/docs/layout.tsx. ProjectHeaderActions import (removed when no header actions are configured). {/* __PROJECT_HEADER_ACTIONS_START__ */} \u2026 {/* __PROJECT_HEADER_ACTIONS_END__ */} app/docs/layout.tsx. Header action / version-selector markup (removed when no header actions are configured). // __FOLIO_COMPONENT_IMPORTS__. mdx-components.tsx. Reserved for internal Folio use: import lines Folio may inject for additional MDX components. // __FOLIO_COMPONENT_ENTRIES__. mdx-components.tsx. Reserved for internal Folio use: entries Folio may add to the MDX components map. The two mdx-components.tsx markers are single comment lines (not pairs). They are reserved for internal Folio use. If they are absent but Folio needs to inject components, it falls back to prepending imports and inserting entries next to an existing ...components, spread \u2014 so keeping the markers gives you deterministic placement. Troubleshooting. Custom-template failures surface as one of a few recognizable symptoms. Use this table to map the symptom to its cause and fix. SymptomCauseFix Build fails listing missing required files. The template directory is missing an anatomy file Folio expects (for example next.config.mjs, mdx-components.tsx, or the app/docs route). Add the missing files. Copy the anatomy skeleton from the bundled template rather than recreating it. Build fails listing missing MDX components. mdx-components.tsx does not export a required Folio component name, so generated MDX would import a component that does not exist. Export the named components from mdx-components.tsx. See the MDX Component Contract. Build fails with missing required marker X in file Y (one (file, marker) pair per missing entry). A load-bearing injection marker \u2014 or the file that owns it \u2014 is absent, so Folio cannot inject project metadata, the docs route base, or the base path. Restore the marker verbatim in the named file. The load-bearing set lives in app/layout.tsx, app/docs/layout.tsx, app/docs/[[...mdxPath]]/page.jsx, and next.config.mjs; copy those files from the bundled template and edit around the markers. Warning that the overlay is ignored. Both template.path and template.overlay_path are set. They are mutually exclusive; template.path wins. Remove whichever key you do not want. Use template.path for a full fork, template.overlay_path for a partial override. Config error mentioning an unsafe URL scheme. A header URL uses a rejected scheme (javascript:, data:), or a repository URL uses a scheme that could execute script or read local files. Use an http(s), mailto:, or relative URL for header links; repository URLs may also use ssh://, git://, git+https://, or scp-style forms. See header URL validation. Optional markers (paired feature blocks, the mdx-components.tsx markers, and markers that only live in optional files such as the landing page or theme configurator) do not fail the build. When one is absent Folio skips it silently at normal verbosity. To see what happened, enable debug logging: Folio logs each skipped optional target at debug level and always emits an info-level summary of the files it injected. If a template change did not take effect and the build still succeeded, that injected-files summary is the first place to look \u2014 a target missing from the summary was never injected. Config markers. These are inline // __MARKER__ comments appended to a real source line. Folio replaces the entire line (the value plus the marker comment) with the configured value, so both the placeholder assignment and the trailing marker must be present verbatim. Marker stringExpected fileWhat Folio substitutes const configuredDefaultPresetId = \"organic-editorial\" // __FOLIO_THEME_PRESET__. components/theme-configurator.tsx. Rewrites the line to the configured theme.preset. const configuredBasePath = '' // __FOLIO_BASE_PATH__. next.config.mjs. Rewrites the line to the resolved deploy base path. next.config.mjs is also edited by other substitutions: contentDirBasePath is rewritten to the docs route base, NEXT_PUBLIC_FOLIO_DOCS_ROUTE_BASE is appended next to NEXT_PUBLIC_FOLIO_BASE_PATH, and the __I18N_CONFIG__ marker is replaced with an i18n block (or stripped when no locales are configured). Generated Files. After Folio copies the template into .build/, it writes generated data into reserved paths: Path in .build/Purpose content/ Generated MDX pages and Nextra _meta.ts files. public/_folio/markdown/ Plain Markdown versions used by page actions and LLM workflows. lib/search-index.ts. Static search document metadata for the navbar search component. lib/folio-template.ts. Build-time project metadata and template params for custom templates. lib/folio-mdx-contract.ts. Versioned list of Folio MDX component names and prop contracts. .folio-manifest.json. Incremental rebuild cache. .folio-build.log. Last frontend build log. template.params contract. template.params is the supported channel for passing project-specific configuration from docs.yaml into a custom template. The contract is small and deliberately strict so that what reaches the frontend is predictable: JSON-serializable. template.params must be a mapping whose values are JSON-serializable (strings, numbers, booleans, null, lists, and nested mappings). Folio validates this at config-load time. A non-serializable value (for example a custom YAML tag that parses to a Python object) fails the build with a clear template.params must be JSON-serializable error. Normalized at load time. An absent params key and an explicit null both become {}. A params value that is not a mapping (a list, string, or number) is ignored with a warning and also becomes {}, so the template still builds with empty params instead of crashing. Emitted frozen as as const. Folio writes the validated mapping verbatim into lib/folio-template.ts as folioTemplateParams, serialized with JSON.stringify and suffixed with as const. The as const makes the object and its string/number/boolean literals read-only at the type level, so editors infer literal types (\"dense\", not string). NOT type-checked. Folio does not generate or validate a TypeScript type for your params. folioTemplateParams is typed only by the literal values that happened to be present in the last build. The template owns the meaning of each field and is responsible for any narrowing, defaulting, or runtime validation it needs. Treat unknown fields as unknown and guard before use. Worked example. Pass a navbar variant and product name through docs.yaml: Folio emits the params into lib/folio-template.ts (other exports omitted): A template component then branches on a field, defaulting when it is absent: template.params are build-time/static-site visible. Do not put secrets, API tokens, private keys, or environment-specific credentials in them. MDX Component Contract. Generated API and guide pages can reference Folio component names. A custom template must expose compatible components from mdx-components.tsx, or the Next.js build will fail when generated MDX imports a missing component. Folio writes the current contract to lib/folio-mdx-contract.ts with folioMdxContractVersion. The build also checks custom templates for required component names before running the frontend build, so missing required components fail early. At minimum, custom templates should provide these names when they render Folio\u2019s own generated API and guide output: ComponentUsed for ParamTable. Function and method parameter tables. ClassOverview. Class summaries and inheritance information. SourceLink. Links back to repository source files. ApiReferenceIndex. Generated API reference landing page. Callout, Tabs, TabItem, Mermaid. Converted Markdown/MDX guide content. You can re-export Folio-compatible components, wrap them in product styling, or replace them entirely. The important contract is the component name and props that generated MDX expects. Sidebar Metadata Contract. Folio regenerates Nextra _meta.ts files on every build. Custom templates may rely on these stability guarantees: Root entries preserve Folio\u2019s documented page order first, then append remaining discovered pages. Nested directory index pages are emitted as { \"display\": \"hidden\" } so the folder opens to its index page without duplicating an Index item. Folio does not emit collapse state such as open or defaultCollapsed. Templates that need collapsed sections must implement that behavior in the template navigation layer. What Default Options Mean. When template.path is omitted, Folio uses the bundled template and applies Folio-owned options such as theme.preset, logo, favicon, Pagefind search, and the default docs layout. When template.path is set, those options are not a guarantee that Folio can style your template. A custom template may choose to read the generated project metadata, use template.params, or ignore Folio\u2019s bundled theme system entirely. That separation is intentional: expert templates get full control instead of a partial override layer. Compatibility Checklist. Before committing a custom template, verify that it: Builds with pnpm install --frozen-lockfile and pnpm run build. Uses Next static export and writes out/. Renders generated content from content/ under the configured docs route. Supports Nextra _meta.ts files if it uses Nextra navigation. Exposes Folio-compatible MDX components from mdx-components.tsx. Handles deploy.base_path or FOLIO_BASE_PATH if the site is deployed under a subpath. Implements search, dark mode, version selection, landing pages, or i18n itself when those experiences are needed. Keeps template source files under the configured template directory. Treats .build/, content/, public/_folio/, lib/search-index.ts, lib/folio-template.ts, and lib/folio-mdx-contract.ts as generated build output. Custom templates execute frontend install and build scripts. Use templates you own or trust, review dependency changes, and keep the lockfile checked in with the template."},{"url":"docs/agents/board-component/index.html","title":"KanbanBoard","content":"Ask AI. KanbanBoard. Optional Folio Docs canvas for a Folio for Agents board, with filtering, staged drag-and-drop, and a progressive public workspace. API. PropTypeDefaultDescription compact. boolean. false. Hides the filter bar, card detail dialog, staging card, and URL parameter syncing. For embedding boards in documentation pages. maxCardsPerColumn. number. \u2014 Caps the number of cards shown per column in embeds. Example. Activate the kanban plugin with your own source to render this preview. Preview unavailable. The standalone CLI reads the board configured by board.source in agents.yaml. The optional Folio Docs adapter reads kanban.source in docs.yaml. Folio\u2019s own operational board lives on its independent board branch, so this release documentation shows the invocation without coupling the code release to that board\u2019s update cycle. Progressive workspace. The public board keeps one continuous surface order: filters, canvas, selected card, selected artifact. Selecting something adds its context after its parent; it never covers the canvas. A wide viewport continues the chain to the right. When the chain no longer fits, the card or artifact continues below without changing document or keyboard order. Each hairline between surfaces is a resizer. Drag it, use its arrow keys, or double-click it to restore the default. Closing the artifact returns to the card; closing the card also closes its artifact. Filters close independently. The URL carries ?card= and ?artifact=, and a readable artifact can enter and leave the browser\u2019s native full-screen mode. Open context is reading space, not a preview. The card starts at 760px in its active dimension and the artifact at 1040px wide or 900px high. Resizing cannot reduce the card below 440px wide or 420px high, nor the artifact below 560px wide or 480px high. The card continues below through 1535px, the artifact through 2559px, and only larger viewports use all four surfaces in one row. Boards embedded inside documentation keep the modal card and artifact drawer. Theming. Every structural node carries a data-slot attribute for styling. Additional attributes mark dynamic state. AttributeTarget data-slot=\"kanban\" Root container. data-slot=\"kanban-filters\" Filter bar. data-slot=\"kanban-filter-panel\" Filter composer rail or drawer. data-slot=\"kanban-workspace\" Responsive layout owner for the public canvas. data-slot=\"kanban-workspace-divider\" Keyboard- and pointer-resizable separator between workspace surfaces. data-slot=\"kanban-canvas-shell\" Toolbar and board canvas surface. data-slot=\"kanban-board\" Board canvas holding all columns. data-slot=\"kanban-column\" Single column container. data-slot=\"kanban-column-header\" Column title and count. data-slot=\"kanban-card-list\" List of cards within a column. data-slot=\"kanban-card\" Individual card tile. data-slot=\"kanban-card-dialog\" Card detail dialog. data-slot=\"kanban-card-panel\" Selected card in the public workspace. data-slot=\"kanban-artifact-drawer\" Reading drawer for a published artifact, inside the card dialog. data-slot=\"kanban-artifact-reader\" Selected artifact in the public workspace. data-slot=\"kanban-staging\" Move staging card shown when moves are pending. data-workspace-surface. Surface identity (canvas, card, or artifact) in the public workspace. data-column. Column identifier on column nodes. data-card. Card identifier on card nodes. data-dragging. Present on cards during drag operations. The staging banner and its slot describe the current browser-staged flow; the write-path direction is tracked on the project board. The board page renders on pure white in light mode. Dark mode and every preset keep their own background."},{"url":"docs/components/pull-quote/index.html","title":"PullQuote","content":"Ask AI. PullQuote. A high-emphasis statement block for the one sentence the page exists to deliver \u2014 verdicts, theses, the line you want quoted back. API. PullQuote. PropTypeDefaultDescription children. ReactNode. \u2014 The statement. kicker. string. \u2014 Small uppercase label above it. attribution. string. \u2014 Source line below it. Example. Verdict. \u201c The short version. Nobody remembers the third-best espresso in the district."},{"url":"docs/components/page-feedback/index.html","title":"PageFeedback","content":"Ask AI. PageFeedback. A \u201cWas this page helpful?\u201d widget with thumbs up/down buttons. After clicking, the widget displays a confirmation message and stores the response in the browser\u2019s localStorage under the key page-feedback, keyed by page path. This component is used internally by the theme layout and is typically placed at the bottom of documentation pages. It takes no props. API. PageFeedback. This component takes no props. Behavior: Displays thumbs up/down buttons. On click, shows \u201cGlad it helped!\u201d or \u201cThanks, we\u2019ll improve this page.\u201d Feedback is stored per-page in localStorage and cannot be resubmitted (until localStorage is cleared). Example. Layout Component. PageFeedback is rendered at the bottom of every documentation page by the theme layout. Scroll to the bottom of this page to see the \u201cWas this helpful?\u201d widget in action."},{"url":"docs/api-reference/folio_docs/__main__/index.html","title":"folio_docs.main source","content":"Ask AI. folio_docs.main source."},{"url":"docs/api-reference/folio_docs/link_checker/index.html","title":"folio_docs.link_checker source","content":"Ask AI. folio_docs.link_checker source. Build-time link checker for internal documentation links. Classes. @dataclass. class BrokenLink. source Represents a broken internal link found during validation. Functions. _route_from_mdx_path source. Derive the route string from an MDX file path relative to content_dir. For example: content_dir / \u201cinstallation.mdx\u201d -> \u201cinstallation\u201d content_dir / \u201capi-reference/folio/config.mdx\u201d -> \u201capi-reference/folio/config\u201d content_dir / \u201cindex.mdx\u201d -> \u201cindex\u201d NameTypeDefaultDescription mdx_path. Path. - content_dir. Path. - Returns: str - _normalize_target source. Normalize an internal link target to a route string. Returns the normalized route, or None if the link should be skipped (external, anchor-only, mailto, tel, etc.). NameTypeDefaultDescription href. str. - source_route. str. - docs_route_base. str. '/docs' Returns: str | None - _strip_inline_code source. NameTypeDefaultDescription line. str. - Returns: str - _links_in_line source. NameTypeDefaultDescription line. str. - Returns: list[str] - _static_target_exists source. Return whether a site-absolute URL maps to a published static asset. NameTypeDefaultDescription target. str. - static_root. Path | None. - Returns: bool - check_links source. Check all MDX files in content_dir for broken internal links. NameTypeDefaultDescription content_dir. Path. - Path to .build/content/ pages. list[str] - list of valid routes (e.g., [\"index\", \"installation\", \"api-reference/folio/config\"]). docs_route_base. str. '/docs' site_routes. set[str] | None. None. valid site-absolute routes outside the docs space (e.g., {\"/\", \"/roadmap\"}); the site root is always valid. static_root. Path | None. None. public directory containing emitted static assets. An absolute URL is valid when it resolves to a file under this root. Returns: list[BrokenLink] - A list of BrokenLink instances for every internal link that doesn\u2019t match a known page route."},{"url":"docs/api-reference/folio_docs/paths/index.html","title":"folio_docs.paths source","content":"Ask AI. folio_docs.paths source. Shared path-resolution helpers with project containment guards. Functions. resolve_contained_dir source. Resolve raw_path against the project root with containment guards. This is the single implementation of the guard applied to every user-configurable directory that Folio reads from or copies into the build workspace (theme.package, template.path, template.overlay_path). Contract: A relative raw_path is resolved against project_root; an absolute path is resolved as-is. The resolved path must stay within project_root (the root itself is allowed); otherwise ValueError with \"{label} must stay within the project directory\". The resolved path must not be <project_root>/.build or anything inside it; otherwise ValueError with \"{label} cannot point inside the .build directory\". The resolved path must not be output_dir or anything inside it; otherwise ValueError with \"{label} cannot point inside the output directory\". With must_exist=True the resolved path must be an existing directory; otherwise FileNotFoundError with \"{label} does not exist: {resolved}\". Returns the fully resolved absolute :class:~pathlib.Path. NameTypeDefaultDescription raw_path. str | Path. - project_root. Path. - output_dir. str | Path. - label. str. - must_exist. bool. True. Returns: Path -"},{"url":"docs/api-reference/folio_agents/integrations/index.html","title":"folio_agents.integrations source","content":"Ask AI. folio_agents.integrations source. Optional integrations with other products in the Folio family."},{"url":"docs/components/config_panel/index.html","title":"ConfigPanel","content":"Ask AI. ConfigPanel. Pair a YAML example with short field notes. Use it when a configuration snippet needs explanation but a full reference table would interrupt the guide. API. PropTypeDefaultDescription title. string. \u2014 Section title. description. string. \u2014 Short explanation below the title. fields. ConfigField[] [] Field notes shown below the example. children. ReactNode. \u2014 Usually a fenced YAML block. ConfigField supports name, type, default, and description. Example. source.python. Tell Folio where your package code lives. paths Python source directories to scan. list[string] docstring_style Parser for Google or NumPy style docstrings. stringdefault: auto."},{"url":"docs/kanban/cards/how_a_published_board_accepts_a_change/panel_verdict/index.html","title":"Panel verdict: the write path lives in folio serve","content":"Ask AI. Panel verdict: the write path lives in folio serve. Three architectures were designed independently and attacked by four adversarial judges (simplicity, data integrity, security and deployment, product doctrine). Every repository claim the designs made was verified against the code before judging. This document is the durable record of what was considered, what won, and what the winner must do differently. The three candidates. serve-integrated. A stdlib HTTP thread inside the folio serve process. The web UI posts small operations; each one runs through the same kanban_edit surgery and the same one-operation-one-commit discipline the CLI already has; the existing watcher turns the file change into a refreshed board in every open tab. Zero new dependencies, zero new processes. The published static export stays byte-identical. standalone-sidecar. One separate stdlib process that serves the built _site and mounts the same write API, deployable on any small machine. The board probes for the API on its own origin and stays read-only when nothing answers. forge-mediated. No server at all: the browser writes through the GitHub Contents API with a fine-grained token, reproducing the CLI\u2019s commits with GitHub\u2019s compare-and-swap on the file sha as the concurrency guard. Verdict. All four judges ranked serve-integrated first (8, 8, 8, 8.5 out of 10) and none found a fatal flaw in it. The deciding fact: the backend already exists as tested pieces \u2014 kanban_edit.py writes cards with re-parse verification, kanban_cli.py holds the operations and the commit discipline, and the serve watcher already turns any card-file change into a refreshed board in about four seconds. The design adds only the missing HTTP surface, inside the process the owner already runs. The sidecar died on state: its server-side board cache could diverge from the files for every non-web writer, and a compare-and-swap against a stale cache blesses exactly the clobber it exists to prevent. The forge path died on duplication and custody: a second surgery implementation in TypeScript policed by corpus infrastructure, a vendor credential in the write path, and no improvement at all to the local flow where the board actually gets used. Two ideas from the losers were adopted: commit-per-write as the default (the forge design was the only one that got this right), and the sidecar\u2019s capability handshake plus expectation guards \u2014 every mutating operation carries what it believes the current state is, and a mismatch is a refusal, never a wrong write. Mandated changes to the winning design. Commit-per-operation defaults to on. Uncommitted web edits erode \u201cgit is the database\u201d at its most casual surface. Server-originated commits use a pathspec narrowed to the touched card file. The CLI\u2019s current whole-directory git add would silently sweep unrelated hand edits into a web gesture\u2019s commit. next dev binds to 127.0.0.1. It listens on all interfaces today, which is a pre-existing exposure the write API would inherit. The discovery file (serve.json) is deleted and rewritten at every serve start and is already deleted on static export; a killed process must not leave a stale token behind. No new hookspec for API routes while exactly one plugin wants them; the dev server calls the kanban plugin directly. A non-writing tab re-seeds its board state when the baked data module changes, so every tab converges after the watcher echo. The write envelope gets adversarial tests (form-encoded posts that skip preflight, absent Origin, rebound Host values), and kanban_edit\u2019s verified write becomes atomic (temp file and rename), which also protects the CLI against torn writes. Accepted limitation: the millisecond race between an editor save and a server write stays unguarded. With commit-per-operation on, a clobber is a visible commit; a cross-process lockfile is real complexity for a race no judge could construct outside a thought experiment. Owner delta, 2026-08-26. The published tier is not deferred indefinitely. The owner\u2019s direction: a deployed board (GitHub Pages) must be able to name a sync backend in the board\u2019s own configuration \u2014 a sync: key in docs.yaml \u2014 and write through it. That is the decision card\u2019s path 3, and it is the same server component grown a front door: folio kanban server running on a checkout, receiving the same operations, committing and pushing, and serving fresh board data back so the board is live even while the static host waits for its rebuild. The two implementation cards carry the plans: folio-serve-accepts-board-edits \u2014 the local write path, first. a-published-board-syncs-through-a-configured-backend \u2014 the deployed tier, on top of the same operations module. Owner delta, second round (2026-08-27). The deployed tier gains a same-origin mount. When sync.url is a path (/api/kanban), the backend is the static host\u2019s own function runtime \u2014 Vercel\u2019s Python functions beside the exported site \u2014 and git happens through the forge\u2019s Contents API with the file sha as compare-and-swap. The real kanban_edit surgery runs inside the function, which recovers the one virtue of the rejected forge design without either of its fatal flaws: no credential in the browser bundle, no second surgery implementation. Credentials are personal, by owner directive: either the deployer\u2019s fine-grained token in the deployment\u2019s own configuration, or each visitor\u2019s own credential passed through per request and never stored, with the repository\u2019s permissions as the access control. The security invariants were designed before the code \u2014 they are a numbered section of the sync card\u2019s plan."},{"url":"docs/api-reference/folio_docs/docs/theme_contract_codegen/index.html","title":"folio_docs.docs.theme_contract_codegen source","content":"Ask AI. folio_docs.docs.theme_contract_codegen source. Generate TypeScript type definitions from Python theme contract. This module provides code generation for TypeScript theme types that are consumed by template/theme/preset-types.ts and written into build workspaces. Functions. generate_typescript_contract source. Generate TypeScript type definitions from the Python theme contract. Returns: str - TypeScript source code defining ThemeStyle, ThemeTuneKey, ThemeVars, and themeRadiusScale."},{"url":"docs/api-reference/folio_docs/parser/index.html","title":"folio_docs.parser source","content":"Ask AI. folio_docs.parser source."},{"url":"docs/components/pull_quote/index.html","title":"PullQuote","content":"Ask AI. PullQuote. A high-emphasis statement block for the one sentence the page exists to deliver \u2014 verdicts, theses, the line you want quoted back. API. PullQuote. PropTypeDefaultDescription children. ReactNode. \u2014 The statement. kicker. string. \u2014 Small uppercase label above it. attribution. string. \u2014 Source line below it. Example. Verdict. \u201c The short version. Nobody remembers the third-best espresso in the district."},{"url":"docs/kanban/cards/artifact-viewer-becomes-a-workspace/progressive-canvas-direction/index.html","title":"Progressive artifact canvas","content":"Ask AI. Progressive artifact canvas. Objective. Replace the current dialog and artifact overlay with one continuous kanban workspace. Each selection adds its next context without obscuring or replacing the context that produced it. Confirmed direction. The logical and document order is: Filters refine the board. The canvas presents the board. Selecting a card opens the card after the canvas. Selecting an artifact opens the artifact after the card. On a very wide viewport, \u201cafter\u201d means farther right. When that horizontal chain no longer fits, the artifact continues below the canvas and card. At a narrower width, card and artifact both continue downward. The responsive layout changes placement, never reading or keyboard order. Surfaces share hairline boundaries rather than appearing as framed windows. Filters, card, and artifact remain closable and resizable. Closing the artifact returns to the card. Closing the card also closes its artifact because a child surface cannot outlive its context. Filters remain independent. The artifact offers a full-screen mode. Exiting it restores the complete progressive chain and its sizes. Product mapping. Keep the current artifact rendering contract: A compiled document is fetched from the page Folio already emitted and its article body is reused. A raw HTML or file artifact stays inside a sandboxed iframe. An unpublished target remains plain text rather than becoming a dead reader. Change the page composition: The filter panel becomes the first resizable rail. The board remains the central canvas and keeps its scroll and drag state. The card dialog becomes the next in-flow surface. The artifact drawer becomes the final in-flow surface. Card, artifact, open surfaces, and sizes are restored from the URL or local layout state without changing their semantic order. Open choice. The structure is confirmed. Stage 2 needs one owner confirmation that the responsive breakpoint behavior in the rendered prototype is close enough to integrate. Exact breakpoint values and default sizes should be tuned against the real Folio docs during implementation, not treated as product constants from the standalone candidate. Validation state. The standalone prototype covers the wide rightward chain, the laptop layout with the artifact below, a narrower downward sequence, pointer and keyboard resizing, progressive close behavior, document and prototype artifacts, URL updates, theme switching, and full-screen entry/exit. Its inline script passes node --check; the board passes folio kanban check; the Folio build and dev server complete; and the prototype, compiled direction, and filtered board URLs return HTTP 200. Visual automation remains unavailable in this session, so owner review is the remaining Stage 1 validation."},{"url":"docs/api-reference/folio_docs/agent_output/llm_output/index.html","title":"folio_docs.agent_output.llm_output source","content":"Ask AI. folio_docs.agent_output.llm_output source. Functions. _inline source. Collapse a value onto a single line of plain text. NameTypeDefaultDescription text. object. - Returns: str - _table_cell source. NameTypeDefaultDescription text. object. - Returns: str - _absolute_link source. NameTypeDefaultDescription path. str. - site_url. str. '' Returns: str - _doc_link source. NameTypeDefaultDescription route. str. - site_url. str. '' docs_route_base. str. '/docs' Returns: str - _api_link source. NameTypeDefaultDescription module_name. str. - site_url. str. '' docs_route_base. str. '/docs' Returns: str - _source_citation source. Render a path:line citation, relative to the project directory. NameTypeDefaultDescription source_file. str. - line_number. int. - project_dir. str. '' Returns: str - _heading_block source. NameTypeDefaultDescription heading. str. - citation. str. '' url. str. '' Returns: str - _prose_blocks source. NameTypeDefaultDescription docstring. DocstringIR. - Returns: list[str] - _tail_blocks source. NameTypeDefaultDescription docstring. DocstringIR. - Returns: list[str] - _arg_table source. Render the argument table for a function, or an empty string. NameTypeDefaultDescription func. FunctionIR. - Returns: str - _function_blocks source. NameTypeDefaultDescription func. FunctionIR. - heading. str. - project_dir. str. - Returns: list[str] - _class_blocks source. NameTypeDefaultDescription project_dir. str. - Returns: list[str] - _module_blocks source. NameTypeDefaultDescription mod. ModuleIR. - config. Config | None. - Returns: list[str] - _doc_blocks source. Split a document into its heading block and its Markdown body. NameTypeDefaultDescription doc. MarkdownResult. - config. Config | None. - Returns: list[str] - generate_llms_txt source. Generate llmstxt.org format with project name, doc links, API reference links. NameTypeDefaultDescription config. Config. - modules. list[ModuleIR] - docs. list[MarkdownResult] - Returns: str - generate_llms_full_txt source. Generate full content concatenated, separated by ---. When config is supplied every section carries the page URL it was built from, so an agent reading the file can cite the published page. NameTypeDefaultDescription modules. list[ModuleIR] - docs. list[MarkdownResult] - config. Config | None. None. Returns: str -"},{"url":"docs/plugins/trust/index.html","title":"Trust & Safety","content":"Ask AI. Trust & Safety. What a plugin can do on your machine, what Folio guarantees, and where those guarantees stop. A plugin is code you run. A Folio plugin is Python. Folio imports it and calls its hooks inside your build, in the same interpreter process as Folio itself. It runs with your environment variables, your filesystem permissions, and your network access. Anything your user account can read, write, or send, a listed plugin can read, write, or send. The risk class is not new. Adding a plugin is the same trust decision you already make when you install a package and import it: you are choosing to execute someone else\u2019s code with your own privileges. Judge a plugin the way you judge a library, by its author, its source, and its release history, and not the way you judge a config file. This is deliberate. Plugins get the whole Python process because that is what makes the extension surface worth having: the first-party roadmap, kanban, landing, and OpenAPI plugins go through the exact same hooks a third-party plugin uses. A sandbox that could stop a hostile plugin would also stop the useful ones from reading your source tree, writing pages, and adding CLI commands. Plugins load before your command runs. This is the sharpest edge in the system. Project plugins listed under plugins: in ./docs.yaml load when the CLI starts, not when a build starts. Typer finalizes its command table while folio.cli is imported, before any argument is parsed, so a plugin that wants to contribute a command has to be imported by then. Folio therefore reads docs.yaml from the current working directory at import time and loads every plugin it lists. The consequence: running any folio command from a directory whose docs.yaml lists plugins executes those plugins\u2019 module-level code. folio --help is enough. So is a typo that never reaches a command. Before you run folio in a repository you did not write. Read its docs.yaml first. The plugins: list is the code you are about to execute, and a file-path entry such as ./plugins/build_hooks.py is right there in the tree for you to read. Two situations where this matters most: Cloning an unfamiliar repository. Inspecting a project\u2019s docs config costs one cat; running folio --help inside it does not ask first. CI. A pull request that adds a plugins: entry, or edits a file-path plugin already listed, runs that code in the job that builds the docs. Treat a docs build of an untrusted branch exactly like running its test suite. A plugin that fails while the CLI is starting is reported as a warning and skipped, so a broken plugin never takes down the CLI. Being skipped after a failure is not containment: by then the module-level code has already run. What Folio does guarantee. Four properties are enforced in code, and each one is a real load-time or failure-time boundary: Installing a build plugin never activates it. A folio-docs build-plugin entry point is inert until its name appears in plugins:. The separate folio.cli group is reserved for installed Folio products that intentionally add commands to the shared CLI; loading one does not activate build hooks. File-path plugins cannot resolve outside the project directory. A ./plugins/x.py entry is resolved against the project directory and rejected when the resolved path lands outside it. Because the path is fully resolved first, a symlink pointing out of the tree is rejected too. Loading reports the refusal as a RuntimeError naming the plugin, with the original ValueError as its cause. A plugin built for another API major refuses to load. A plugin may declare FOLIO_PLUGIN_API; if its major differs from the host\u2019s, it is refused instead of running hooks it was not written for. The declaration is optional, so an undeclared plugin loads without this check. Hook failures are isolated and attributed. Folio dispatches every hook one implementation at a time rather than as a single pluggy broadcast, so a failure names the plugin under the string you wrote in plugins:. Config and registry hooks fail the build; config_keys, emit_assets, post_build, and register_cli warn and skip. emit_assets also runs under a rollback guard: when an implementation raises, the routes it registered are restored to the pre-hook snapshot, so a route with no page behind it cannot pass link checking and then 404 on the deployed site. The full policy table is in Writing Plugins. None of these is a security boundary around a running plugin. They constrain what gets loaded and how a failure is contained, not what a loaded plugin may do. What you can do. Read docs.yaml before running folio in an unfamiliar repository. The plugins: list is the entire build-plugin surface. Prefer plugins you can read. A file-path plugin is in the repository in front of you. An entry-point plugin is a published distribution with a source repository, a release history, and an author. Pin the distribution. Version-pin a plugin like any other dependency and review the diff when you raise the pin. Build untrusted branches without secrets. Folio\u2019s own pull-request preview workflow splits the build in two: the job that runs pull-request code has read-only permissions and produces a static artifact, and only that artifact reaches the privileged job that deploys it. Any docs build of untrusted code wants that shape, or a container, or a throwaway environment. Keep credentials out of docs.yaml and out of plugin code. Static docs are published; anything a build embeds ships to readers. Report what looks wrong. A plugin nobody listed getting loaded, or a file-path plugin resolving outside the project directory, is a bug in Folio. See SECURITY.md . The boundary. Folio does not review, sandbox, or vet plugins. There is no signing, no permission model, no allowlist, and no restriction on what plugin code may do once you have listed it. The folio-plugin GitHub topic described in Publishing a plugin is a discovery convention, not an endorsement. A catalog built on it is on the roadmap, and a listing there would mean exactly one thing: a public repository carries a topic. It would not mean the code was read by anyone. The trust decision stays with you. It is the same one you make for every other dependency in your environment, and Folio\u2019s job is to make sure you can see what you are deciding about. The equivalent decision for frontend code is documented in Theme Packages and Custom Templates."},{"url":"docs/theming/theme_packages/index.html","title":"Theme Packages","content":"Ask AI. Theme Packages. [!WARNING] Security A theme package is executed as trusted code during every build. The package files are overlaid onto the template that Folio then builds with pnpm install --frozen-lockfile (which executes dependency lifecycle scripts) and next build (which runs next.config.mjs in Node), so pointing theme.package at a directory is equivalent to running that code on your machine. Only point theme.package at frontend code you have reviewed and trust. Building an untrusted repository or branch in CI inherits the same remote-code-execution surface \u2014 treat a docs build like any other untrusted-code execution. Never put secrets, API tokens, or credentials in theme code or docs.yaml: static docs are published. Theme packages are the middle ownership level. They let a project ship a local theme overlay that is copied over the bundled Folio template before generated content and metadata are injected. Use a theme package when YAML personalization is too limited, but a full custom template would duplicate too much of Folio\u2019s default docs runtime. Configure a Package. theme.package is resolved relative to the project directory. The directory is trusted frontend code: it can override template files, import dependencies already available to the template, and replace Folio\u2019s default theme modules. Ownership Model. OwnerResponsibilities Folio. Copy the bundled template, apply the theme package overlay, generate content, write metadata, inject fallback theme config, run the frontend build, and export static files. Theme package. Own selected frontend files such as layouts, global CSS, the configurator UI, project header actions, or theme/project-theme.ts. Folio still writes generated docs content and reserved build files into .build/. Do not edit .build/ directly; change the package source instead. Package Anatomy. A package can be small and only override the files it owns: Common override points: FilePurpose app/layout.tsx. Own font loading, metadata shell, providers, and global layout behavior. app/docs/layout.tsx. Own the docs navbar, sidebar placement, search slot, and ThemeConfigurator mount. components/theme-configurator.tsx. Replace the bundled configurator UI with a project-specific implementation. components/project-header-actions.tsx. Replace the default generated header action component. theme/project-theme.ts. Own all presets, controls, variants, defaults, and resolved tokens in TypeScript. app/globals.css. Override or replace template-level CSS when a project needs exact visual parity. YAML and TypeScript Together. If the package supplies theme/project-theme.ts, Folio does not overwrite it with a YAML-generated module. The package owns the preset implementation. If the package omits theme/project-theme.ts, Folio still emits the safe docs.yaml-driven project preset described in Personalization. This lets a project start with YAML and graduate individual surfaces to TypeScript only when needed. Register a Custom Preset. A theme package can register a custom preset without forking presets.ts. From theme/project-theme.ts, import registerPreset and call it before exporting the projectThemePreset. The registerPreset function is the supported extension point. The optional second argument (\"project\") adds the preset to that display group in the configurator UI. If a preset with the same id already exists, it will be replaced with a console warning. See ThemeConfigurator for the full ThemePreset contract, including controls, resolve, and all theme fields. What Packages Should Not Do. Do not assume files written under .build/ are stable source files. Do not put secrets in theme code or docs.yaml; static docs are public. Do not use a package when the project needs a different app structure, dependency graph, route model, or product shell. Use custom templates for that level. Validation Checklist. Folio validates theme packages at build time before any overlay. If validation fails, the build halts with a clear ValueError listing all violations. Reserved Paths. A theme package must NOT contain any of these paths. Folio generates them at build time, and including them will cause a validation error: content/ \u2014 Generated docs pages and metadata live here. lib/folio-template.ts \u2014 Folio internal contract file. lib/folio-mdx-contract.ts \u2014 Folio internal contract file. .next/ \u2014 Next.js build cache. node_modules/ \u2014 Dependency install directory. Required Exports. If a theme package includes theme/project-theme.ts, that file MUST export both: projectThemePreset \u2014 The preset definition (colors, spacing, variants). projectThemeDefaultConfig \u2014 The default configuration object. Folio checks for export const <name> or export { <name> } forms. Missing exports fail validation. Example Validation Error. Why This Fails Early. Validation runs before any file overlay. You get a clear Python error with actionable messages instead of a cryptic Next.js build failure or runtime import breakage."},{"url":"docs/docstrings/index.html","title":"Writing Docstrings","content":"Ask AI. Writing Docstrings. How to write docstrings that produce clear, complete API documentation. folio supports Google-style and NumPy-style docstrings. By default, Folio auto-detects the style for each docstring. You can force a parser with source.python.docstring_style in docs.yaml (see Configuration). Google-style format. Google-style parsing is powered by the docstring_parser library with Style.GOOGLE. Set source.python.docstring_style to \"google\" when you want to force this parser for every docstring: A basic Google-style docstring looks like this: NumPy-style format. To use NumPy-style docstrings, set source.python.docstring_style to \"numpy\" in your docs.yaml: A NumPy-style docstring uses section headers with underlines instead of trailing colons: The default source.python.docstring_style value is \"auto\", which detects the style on a per-docstring basis. This is useful for projects that mix both conventions or are migrating from one style to the other. Docstring sections. folio recognizes the following sections in a docstring. Each section must use the exact heading shown below, followed by a colon (for Google-style) or an underline (for NumPy-style). Args. Document function parameters under the Args: section. Each parameter is listed on its own line with an indented description: Returns. Describe the return value under Returns:: Raises. List exceptions the function may raise under Raises:: Examples. Provide usage examples under Examples:. These are rendered as code blocks in the generated docs: Notes. Add extra context under Notes:. This section is useful for implementation details, caveats, or related information: Type annotations. folio extracts type information from two places, in order of priority: Function signatures (preferred) \u2014 type annotations on parameters and return types. Docstring type fields \u2014 the (type) syntax in Google-style docstrings. If a parameter has a type annotation in the function signature, that takes precedence. If there is no signature annotation, folio falls back to the type specified in the docstring. Both styles produce the same output. When both are present, the signature annotation wins. Decorators and method kinds. folio detects decorators on functions and methods and renders them differently based on their kind. @property. Properties are rendered with a \u201cproperty\u201d badge. The getter docstring is used as the description. Parameters (like self) are automatically excluded: @staticmethod. Static methods are marked with a \u201cstaticmethod\u201d badge. Since they don\u2019t receive self or cls, all parameters are documented: @classmethod. Class methods are marked with a \u201cclassmethod\u201d badge. The cls parameter is automatically excluded from the documentation, just like self: Custom decorators. All decorators are recorded and displayed in the generated documentation. Custom decorators appear as-is: *args and **kwargs. Variable positional and keyword arguments are fully supported. They appear in the documentation with their * or ** prefix implied by their argument kind: The parser tracks argument kinds internally: var_positional for *args and var_keyword for **kwargs. Keyword-only arguments (those after * in the signature) are tracked as keyword_only. Controlling what gets documented with __all__. By default, folio documents every public class, function, and constant in a module. You can restrict what appears in the documentation by defining __all__: When __all__ is defined, only the names listed in it are included in the generated documentation. When __all__ is not defined, all top-level definitions are documented (including those without a leading underscore). __all__ must be a simple list or tuple of string literals. folio parses it statically from the AST, so dynamic constructions like __all__ = get_exports() are not supported. Nested classes. folio fully supports nested (inner) classes. They are parsed recursively and appear as sub-sections within their parent class documentation: Inner classes have their own methods, class variables, and can even contain further nested classes. Module-level constants. folio documents module-level constants in two ways: With type annotations \u2014 any top-level annotated assignment is captured: Without type annotations \u2014 only UPPER_CASE names (without a leading underscore) are captured: The constant\u2019s name, type (if annotated), and assigned value are all shown in the generated documentation. Class variables. Annotated assignments inside a class body are documented as class variables: These appear in a dedicated \u201cAttributes\u201d section in the class documentation, showing the variable name, type, and default value. Async functions. Async functions and methods are fully supported. They are rendered with an async badge to distinguish them from synchronous functions: Tips for writing good docstrings. Start with a one-line summary. The first line of your docstring becomes the short description shown in module overviews and search results. Keep it concise and action-oriented (e.g., \u201cConnect to the database\u201d not \u201cThis function connects to the database\u201d). Separate the summary from the body. Leave a blank line between the summary and the longer description. The parser treats the first line as short_description and everything after the blank line as long_description. Document all public parameters. Every parameter that appears in the function signature should have a corresponding entry in the Args: (Google) or Parameters (NumPy) section. Missing descriptions will render as empty in the docs. Use type annotations in signatures. Prefer def foo(x: int) over documenting types in the docstring. Signature annotations are more reliable and are checked by type checkers like mypy. Keep examples runnable. Examples in the Examples: section should be valid Python that a reader could copy-paste. Use >>> prefix for interactive-style examples. Document exceptions explicitly. If your function raises exceptions, list them in the Raises: section. This helps users write proper error handling code. Complete example. Here is a well-documented class that demonstrates all the features described in this guide:"},{"url":"docs/api-reference/folio_docs/agent_output/artifacts/index.html","title":"folio_docs.agent_output.artifacts source","content":"Ask AI. folio_docs.agent_output.artifacts source. Publication boundary for Folio for Agents artifacts. Classes. class AgentArtifacts. source Write machine-readable artifacts beside the Folio Docs site. The class owns only agent-facing output. It receives resolved site paths from the shared build pipeline and never builds the human frontend. __init__ source. NameTypeDefaultDescription config. Config. - build_dir. str | Path. - output_dir. str | Path. - base_path. str. '' Returns: None - @property. markdown_root source. Type: Path - markdown_path source. NameTypeDefaultDescription route. str. - Returns: Path - write_markdown_mirror source. NameTypeDefaultDescription route. str. - content. str. - Returns: Path - markdown_mirror_exists source. NameTypeDefaultDescription route. str. - Returns: bool - remove_markdown_mirror source. NameTypeDefaultDescription route. str. - Returns: None - write_authoring_contract source. NameTypeDefaultDescription generated_at. str. - components. Iterable[ComponentDefinition] | None. - config_keys. Iterable[str] - routes. Iterable[str] - Returns: Path - write_llm_files source. NameTypeDefaultDescription llms_txt. str | None. None. llms_full_txt. str | None. None. serve. bool. False. Returns: None - @staticmethod. _write_or_remove source. NameTypeDefaultDescription destination. Path. - name. str. - content. str | None. - Returns: None - _point_robots_at_files source. NameTypeDefaultDescription destination. Path. - names. list[str] - Returns: None - _artifact_url source. NameTypeDefaultDescription name. str. - Returns: str -"},{"url":"docs/components/doc-preview/index.html","title":"DocPreview","content":"Ask AI. DocPreview. Use DocPreview when a guide needs to show a generated documentation route or a small generated example instead of describing the result with another code block. It renders a responsive iframe with a short caption, a source view with a collapsible file drawer for the focused source file, a line-numbered code preview, and direct links to the page and source file. PreviewSource. API reference preview. Show the generated reference page directly in the guide so readers can inspect the output before building their own project. Open page. For tutorial examples, place a complete Folio project under docs/examples/<name>/ with its own docs.yaml, docs/, and optional src/ files. The docs build runs Folio for that project, serves the generated static output at /_folio/examples/<name>/, and shows the exact source files that produced it. PreviewSource. Generated docs site. A tiny docs site generated from a real example project. Open page."},{"url":"docs/folio_docs/index.html","title":"Folio Docs","content":"Ask AI. Folio Docs. Folio Docs reads Python source and Markdown guides, then builds the static site people use: API reference, prose pages, navigation, search, theming, and deployable HTML. It never imports the package it documents. folio build and folio serve are the Docs command groups. The package, version, and release process do not depend on Folio for Agents; when Agents is installed, it adds folio board to the same CLI. Code boundary. Documentation generation lives in folio_docs: SiteBuilder owns the generated content workspace and search index. The MDX writer, sidebar, template workspace, theme support, and Next runtime live beside it. Parsing, IR, configuration, plugins, and orchestration are owned by Folio Docs and ship in the same wheel. The old folio namespace is not retained. Integrations use folio_docs and the public plugin contracts. Agent-readable output. Folio Docs itself writes the Markdown mirror for every generated page, the LLM indexes, and the published authoring contract. These are documentation output, not a runtime dependency on Folio for Agents. Continue with the quick start."},{"url":"docs/api-reference/folio_docs/schemas/index.html","title":"folio_docs.schemas source","content":"Ask AI. folio_docs.schemas source."},{"url":"docs/agents/board/start/index.html","title":"Start a board","content":"Ask AI. Start a board. Folio for Agents is complete without a documentation site. The board is a set of files in git and the CLI is its validator and editor. init creates a board branch by default, switches to it, and writes: agents.yaml points the CLI at the board. board.yaml declares the columns and WIP limits. Each Markdown file in cards/ is one card. Its status field chooses the column, so unrelated card moves do not contend for one YAML list. SKILL.md is the repository-owned operating protocol for coding agents. The command refuses to overwrite an existing board, config section, or branch. Use --no-branch only when planning and implementation deliberately share a history. Use --commit to create the initial board: init commit. Daily use. Run folio board check in CI and before pushing board changes. It checks the same cardfile rules used by every write command. Optional visual canvas. The browser board is an optional integration, not part of the Agents runtime. Install it together with Folio Docs: Then add the integration explicitly to the Docs project\u2019s docs.yaml: folio serve now exposes /kanban/; folio build includes the same canvas in the static export. Dragging only stages moves in the browser and exports reviewed folio board move commands. It never writes the repository remotely. When planning lives on a separate branch, point the integration at that ref. Folio creates a detached worktree without merging the board into the code checkout: Each sources key becomes an independent canvas. A local branch is used when available; CI fetches the branch from origin before creating the worktree. Continue with Board formats, the CLI reference, or the agent protocol."},{"url":"docs/kanban/cards/artifacts_read_from_the_canvas/brief/index.html","title":"Prototype brief \u2014 reading a card\u2019s artifacts from the canvas","content":"Ask AI. Prototype brief \u2014 reading a card\u2019s artifacts from the canvas. These are decision prototypes. Nothing here ships as written. They exist so the owner can look at several answers side by side and pick one. Everything lives in board/cards/artifacts-read-from-the-canvas/; do not modify anything outside it. The problem. A card can attach what it produced, and the build publishes it. But reading an artifact means leaving the board. A doc: artifact opens a compiled page at /docs/kanban/<id>/<stem>/ \u2014 full documentation chrome, sidebar, breadcrumbs, a place in the docs tree that has nothing to do with the documentation. A file: artifact opens the raw published file in a new tab. Either way the canvas is gone: the column you were scanning, the dialog you were reading, the filter you had typed. The owner\u2019s direction, verbatim in spirit: artifacts must be readable from the canvas, and card pages indexing into /docs is weird. These prototypes answer the first half. A sibling note (where-card-pages-publish.md) answers the second. What already exists (do not redesign it). The board at /kanban renders status columns; clicking a card opens a dialog styled as a mail: header, description, criteria, comments, trail, and an artifact band at the foot (tinted kind icon, label, mono target path). artifacts: entries are typed: doc, file, pr, api, url. A target inside the card\u2019s own directory gets an href at build time; everything else keeps href: \"\" and renders as a plain path, deliberately not a link. The board already writes its filter to the URL (/kanban/?q=...); state that survives a reload and can be pasted into a report is an established habit, not a novelty. The site is static. Nothing writes; nothing needs a server. Your data. board-data.js sets window.BOARD \u2014 the real board, 44 cards, converted from the build\u2019s own generated data module. Card shape: reader-data.js sets window.READER: a map from artifact target to what a reader can show. type: \"markdown\" entries carry pre-rendered html; type: \"html\" entries carry a relative iframe src. An artifact whose target has no READER entry cannot open \u2014 render it as the path it is, visibly not a link. That rule is real: the board holds a doc: into design/research/ and one into docs/guide/ that nothing publishes, and a bare pr: 23. Load both with plain <script src> tags \u2014 no fetch(), these files open over file:// where fetch is blocked. The card that matters for the demo is the-board-reads-as-a-tree: one markdown artifact (a long comparison document with tables and code) and five HTML prototypes that embed live. Make sure the whole demo path works on it. Non-negotiable output shape. Three files, named for your variant slug: <slug>.html, <slug>.css (linked stylesheet), <slug>.js (deferred script). Opens by double-clicking the HTML. No build step, no CDN, no external fonts, no network. Plain HTML + CSS + vanilla JS. Works at 1440px and at 900px. Write files in pieces \u2014 never attempt a single write longer than about 400 lines; build long files up with follow-up edits. Visual language \u2014 match Folio, do not invent a look. Folio reads as a technical publishing system: quiet, exact, source-driven. Thin rules, flat surfaces, --radius: 0 \u2014 square corners. No gradients, no decorative shadows, no icon packs, no emoji. Body in a system sans stack, labels and paths in mono, small uppercase mono labels with wide tracking (11px, letter-spacing .14em) for metadata. Copy these tokens verbatim: Dark is first-class (toggle Auto/Light/Dark like the sibling prototypes): Read ../the-board-reads-as-a-tree/tree-table.html and its CSS for the register in practice (page head, notes panel, theme toggle), and ../the-board-reads-as-a-tree/tree-rail-detail.js for a detail-pane precedent. Do not copy their layout; copy their manners. Required behaviour, whatever the layout. The canvas is real. Render the actual board (columns, all 44 cards) and open a card the way the board does. A minimal dialog is fine (title, metadata line, description, artifact band); the artifact band is the subject, so it is not minimal. A markdown artifact reads well. Comfortable measure, working headings, tables, code blocks, in both themes. The comparison document is ~8k of rendered HTML; pagination is not required, good typography is. An HTML artifact embeds live (iframe), with a visible way to open it full, in its own tab, for when embedding is not enough. A closed door looks closed. No READER entry: the tile renders kind, label, and mono path, and is not clickable. Never a dead link. Walk the band. Previous/next moves through the artifacts of the card being read without going back to the dialog. Esc unwinds one level at a time: reader to card, card to board. Focus returns to the element that opened what you just closed. The reading position is a URL. Opening an artifact writes it to the query or hash; loading that URL reopens board, card, and artifact. The board already does this for filters; reading joins it. Keyboard end to end, visible focus throughout. Both themes, via the same Auto/Light/Dark toggle as the sibling prototypes. Also required: a notes panel in the page itself. A collapsible section titled \u201cWhat this variant argues\u201d with: the one-sentence thesis; what happens to the compiled /docs/kanban/... page if this variant wins (does a deep link still need it, and as what); and what this design is bad at \u2014 be honest, the owner is comparing, and a variant that hides its weakness wastes the comparison. Rules of engagement. Do not touch anything outside board/cards/artifacts-read-from-the-canvas/. Do not run folio build, start a server, or git commit. Build the whole thing: all cards render, every artifact case handled \u2014 a prototype with three fake cards is useless."},{"url":"docs/api-reference/folio_docs/plugin/index.html","title":"folio_docs.plugin source","content":"Ask AI. folio_docs.plugin source. Classes. class PluginConfig(Protocol). source @dataclass(frozen=True). class PluginDocument. source A Markdown source contributed to Folio\u2019s normal document pipeline. unlisted delists the page from the docs sidebar and nothing else: it still compiles at its route and enters search, the sitemap, llms.txt, and the Markdown mirrors. For plugin output that answers to a URL without belonging to the documentation\u2019s own table of contents. class FolioPlugin(Protocol). source Namespace scanned by pluggy for decorated hook implementations. __dir__ source. Returns: Iterable[str] - class AssetBuilder(Protocol). source page_exists source. NameTypeDefaultDescription route. str. - Returns: bool - read_page source. NameTypeDefaultDescription route. str. - Returns: str - write_page source. NameTypeDefaultDescription route. str. - content. str. - Returns: None - remove_page source. NameTypeDefaultDescription route. str. - Returns: None - list_pages source. NameTypeDefaultDescription prefix. str. - Returns: list[str] - register_route source. NameTypeDefaultDescription route. str. - Returns: None - copy_static_asset source. NameTypeDefaultDescription relative. str. - source. Path. - Returns: None - remove_static_tree source. NameTypeDefaultDescription relative. str. - Returns: None - emitted_routes source. Returns: set[str] - write_meta source. NameTypeDefaultDescription directory. str. - meta_json. str. - Returns: None - read_meta source. NameTypeDefaultDescription directory. str. - Returns: str - write_llm_files source. NameTypeDefaultDescription llms_txt. str | None. None. llms_full_txt. str | None. None. Returns: None - class HookSpec. source config_keys source. Returns: ConfigKeyNames - configure source. NameTypeDefaultDescription config. PluginConfig. - raw_config. RawConfig. - Returns: None - register_extensions source. NameTypeDefaultDescription registry. ExtensionRegistry. - config. PluginConfig. - Returns: None - register_components source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - collect_docs source. NameTypeDefaultDescription config. PluginConfig. - Returns: Iterable[PluginDocument] - post_build source. NameTypeDefaultDescription site_dir. str. - Returns: None - emit_assets source. NameTypeDefaultDescription builder. AssetBuilder. - config. PluginConfig. - Returns: None - watch_paths source. NameTypeDefaultDescription config. PluginConfig. - Returns: Iterable[str] - on_watched_change source. NameTypeDefaultDescription builder. AssetBuilder. - config. PluginConfig. - path. str. - change. str. - Returns: bool - register_cli source. NameTypeDefaultDescription app. object. - Returns: None - class PluginHookError(Exception). source A plugin raised while a fail-fast hook was being dispatched. Carries the user-facing plugin label and the hook name so the top-level build error attributes the failure to the offending plugin. __init__ source. NameTypeDefaultDescription plugin_label. str. - hook_name. str. - original. BaseException. - Returns: None - class PluginManager. source __init__ source. NameTypeDefaultDescription base_dir. Path | None. None. Returns: None - register source. NameTypeDefaultDescription plugin. FolioPlugin. - name. str | None. None. Returns: str | None - call_isolated source. Dispatch a hook one implementation at a time with failure isolation. policy='fail_fast' re-raises the first failure as a. NameTypeDefaultDescription hook_name. str. - policy. HookPolicy. 'fail_fast' on_warn. Callable[[str], None] | None. None. impl_guard. Callable[[], Callable[[], None]] | None. None. **kwargs. object. - Returns: list[object] - _reject_wrapper_hookimpls source. Refuse hookwrapper/wrapper hookimpls loudly at registration time. folio\u2019s :meth:call_isolated invokes each hookimpl as a plain function, which would silently turn a pluggy wrapper into a no-op (an un-started generator). Fail at load instead. NameTypeDefaultDescription plugin. object. - label. str. - Returns: None - _plugin_label source. NameTypeDefaultDescription impl. object. - Returns: str - load_default_plugins source. Register the first-party :data:DEFAULT_PLUGINS. Bundled defaults are imported directly by module path \u2014 never through the folio entry-point lookup used for plugins: entries \u2014 so an installed distribution declaring an entry point named after a default plugin (e.g. folio_docs.docs.integrations.landing) can never shadow the first-party module. A default plugin that fails to load degrades to a warning instead of raising: builds and CLI startup of projects that never asked for the plugin must not break because of it. Returns: None - load_plugins source. NameTypeDefaultDescription plugin_names. list[str] - base_dir. Path | None. None. Returns: None - _register_plugin_module source. NameTypeDefaultDescription mod. object. - name. str. - Returns: None - Functions. user_plugin_names source. Validated plugins: entries with default plugins removed. None (an empty plugins: key) means no entries. Any other non-list value \u2014 e.g. the common YAML mistake plugins: my_plugin instead of a one-item list \u2014 raises ValueError so the misconfiguration fails the build loudly instead of silently skipping the user\u2019s plugins. Entries naming a default plugin are dropped: defaults are always registered exactly once, via :meth:PluginManager.load_default_plugins. NameTypeDefaultDescription plugin_names. object. - Returns: list[str] - _parse_api_version source. Parse an API version into (major, minor). Accepts \"1\", \"1.0\", \"1.0.0\", or a bare int major; a missing minor defaults to 0 and any patch component is ignored. Raises ValueError on anything else. NameTypeDefaultDescription value. object. - Returns: tuple[int, int] - check_plugin_api_version source. Validate a plugin\u2019s declared target API version against the host. Refuses (raises ValueError) on an incompatible major or an unparseable version; warns when the plugin targets a newer minor than the host; allows a missing declaration or an older/equal minor. NameTypeDefaultDescription declared. str | int | None. - plugin_name. str. - host_version. str. FOLIO_PLUGIN_API_VERSION. Returns: None - normalize_config_key_names source. NameTypeDefaultDescription result. object. - Returns: ConfigKeyNames - load_installed_cli_plugins source. Register commands contributed by installed Folio products. CLI entry points are intentionally separate from build plugins. Installing a product may extend the shared folio command, but it does not activate that product in a documentation build or add config keys implicitly. Failures degrade to warnings so one broken optional product cannot make the core CLI unusable. NameTypeDefaultDescription app. object. - Returns: list[str] - _find_entry_point source. Return the installed folio entry point matching name, if any. Entry-point plugins are only loaded when explicitly listed in plugins: (opt-in); installed packages are never auto-activated without consent. When multiple installed distributions declare the same entry-point name, the one from the alphabetically first distribution wins (deterministic) and a UserWarning names all contenders. NameTypeDefaultDescription name. str. - Returns: object | None - _entry_point_dist_name source. NameTypeDefaultDescription entry_point. object. - Returns: str - _module_is_importable source. NameTypeDefaultDescription name. str. - Returns: bool - _is_file_plugin source. NameTypeDefaultDescription name. str. - Returns: bool - _module_name_for_path source. NameTypeDefaultDescription path. Path. - Returns: str -"},{"url":"docs/api-reference/folio_agents/text/index.html","title":"folio_agents.text source","content":"Ask AI. folio_agents.text source. Small text and link primitives owned by Folio for Agents. Functions. slugify source. Convert human-readable text to a stable file-safe identifier. NameTypeDefaultDescription text. str. - Returns: str - safe_href source. Accept web URLs and relative paths while rejecting executable schemes. NameTypeDefaultDescription raw_value. Any. - path. str. - Returns: str -"},{"url":"docs/api-reference/folio_docs/extensions/index.html","title":"folio_docs.extensions source","content":"Ask AI. folio_docs.extensions source. Classes. @dataclass(frozen=True). class ComponentDefinition. source @property. imported_name source. Type: str - @dataclass(frozen=True). class LayoutDefinition. source @dataclass(frozen=True). class DataModuleDefinition. source @dataclass(frozen=True). class ViewBlock. source @dataclass(frozen=True). class ViewDefinition. source class ExtensionRegistry. source __init__ source. Returns: None - register_component source. NameTypeDefaultDescription name. str. - import_path. str. - export_name. str | None. None. expose_mdx. bool. True. source_path. str | Path | None. None. props. Mapping[str, str] | None. None. required. bool. False. category. str. 'general' contract. bool. False. source_label. str. '' origin. str. 'plugin' Returns: ComponentDefinition - register_layout source. NameTypeDefaultDescription name. str. - import_path. str. - export_name. str. - slots. list[str] | tuple[str, ...] ('main',). Returns: LayoutDefinition - write_data_module source. NameTypeDefaultDescription name. str. - export_name. str. - data. Any. - type_source. str. '' type_annotation. str. '' module_path. str. '' Returns: DataModuleDefinition - add_view source. NameTypeDefaultDescription path. str. - layout. str. - slots. dict[str, list[dict[str, Any]] | tuple[ViewBlock, ...]] - title. str. '' props. dict[str, Any] | None. None. Returns: ViewDefinition - @staticmethod. _normalize_view_path source. NameTypeDefaultDescription path. str. - Returns: str - @staticmethod. _validate_identifier source. NameTypeDefaultDescription value. str. - label. str. - Returns: None - Functions. register_builtin_extensions source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - register_config_components source. Register docs.yaml components: entries into the registry. Directory entries are expanded first (each top-level .tsx/.jsx file becomes a component named after its PascalCased stem), then named specs are registered. Both share the same import-stem deduplication, so a directory file and a from: spec with the same filename stem get distinct generated import paths. NameTypeDefaultDescription registry. ExtensionRegistry. - config. Any. - Returns: None - _component_dir_specs source. Expand components: directory entries into named component specs. Relative directories are anchored to the project directory (mirroring how the emitter anchors relative source_path values). A missing directory fails the build loudly; a directory without component files warns so a typo\u2019d but existing path does not silently register nothing. NameTypeDefaultDescription config. Any. - Returns: list[dict[str, Any]] - _component_name_from_stem source. Derive a PascalCase component name from a file stem. hero.tsx -> Hero; my-chart.tsx -> MyChart. The file must export a named export matching the derived name (same default as the export: field of a named spec). NameTypeDefaultDescription stem. str. - Returns: str - _component_import_stem source. NameTypeDefaultDescription source_stem. str. - component_name. str. - duplicate_source_stem. bool. - used_import_stems. set[str] - Returns: str - _component_file_segment source. NameTypeDefaultDescription value. str. - Returns: str -"},{"url":"docs/kanban/cards/artifact_viewer_becomes_a_workspace/progressive_canvas_direction/index.html","title":"Progressive artifact canvas","content":"Ask AI. Progressive artifact canvas. Objective. Replace the current dialog and artifact overlay with one continuous kanban workspace. Each selection adds its next context without obscuring or replacing the context that produced it. Confirmed direction. The logical and document order is: Filters refine the board. The canvas presents the board. Selecting a card opens the card after the canvas. Selecting an artifact opens the artifact after the card. On a very wide viewport, \u201cafter\u201d means farther right. When that horizontal chain no longer fits, the artifact continues below the canvas and card. At a narrower width, card and artifact both continue downward. The responsive layout changes placement, never reading or keyboard order. Surfaces share hairline boundaries rather than appearing as framed windows. Filters, card, and artifact remain closable and resizable. Closing the artifact returns to the card. Closing the card also closes its artifact because a child surface cannot outlive its context. Filters remain independent. The artifact offers a full-screen mode. Exiting it restores the complete progressive chain and its sizes. Product mapping. Keep the current artifact rendering contract: A compiled document is fetched from the page Folio already emitted and its article body is reused. A raw HTML or file artifact stays inside a sandboxed iframe. An unpublished target remains plain text rather than becoming a dead reader. Change the page composition: The filter panel becomes the first resizable rail. The board remains the central canvas and keeps its scroll and drag state. The card dialog becomes the next in-flow surface. The artifact drawer becomes the final in-flow surface. Card, artifact, open surfaces, and sizes are restored from the URL or local layout state without changing their semantic order. Open choice. The structure is confirmed. Stage 2 needs one owner confirmation that the responsive breakpoint behavior in the rendered prototype is close enough to integrate. Exact breakpoint values and default sizes should be tuned against the real Folio docs during implementation, not treated as product constants from the standalone candidate. Validation state. The standalone prototype covers the wide rightward chain, the laptop layout with the artifact below, a narrower downward sequence, pointer and keyboard resizing, progressive close behavior, document and prototype artifacts, URL updates, theme switching, and full-screen entry/exit. Its inline script passes node --check; the board passes folio kanban check; the Folio build and dev server complete; and the prototype, compiled direction, and filtered board URLs return HTTP 200. Visual automation remains unavailable in this session, so owner review is the remaining Stage 1 validation."},{"url":"docs/components/browser_frame/index.html","title":"BrowserFrame","content":"Ask AI. BrowserFrame. Browser window chrome around any content: three dots, a mono URL bar, and an optional right-aligned status label. Use it to frame live embeds \u2014 board miniatures, rendered components, page excerpts \u2014 as the page they ship on, instead of pasting a screenshot. API. PropTypeDefaultDescription url. string. \u2014 Address shown in the mono URL bar. label. string. \u2014 Optional right-aligned mono label, e.g. \"\u25cf LIVE\". children. ReactNode. \u2014 Content rendered inside the window body. Example. folio-docs.dev/roadmap. \u25cf LIVE. docs releases. 2 / 7. v0.1 Foundation. v0.2 Theming. v0.3 Plugin Platform. In progress. v0.4 More Languages. Up next. + 3 more \u00b7 full roadmap \u2192 agents releases. 0 / 4. v0.1 Repository Board. In progress. v0.2 Session Memory. Up next. v0.3 Project Coordination. Up next. v0.4 Harness Network. Later. Notes. Server-renderable: no client state, safe in any MDX page or generated view. not-prose-armored: margins are reset so it sits cleanly inside prose content. Pairs with <Roadmap compact /> for Docs landing-page miniatures. Optional products may register their own framed components."},{"url":"docs/agents/board/index.html","title":"Board","content":"Ask AI. Board. The Folio for Agents board is repository state, not a hosted service. Its columns live in board/board.yaml; every card is a Markdown file under board/cards/; agents.yaml tells the CLI where to find it. The same files are readable by people, coding agents, shell tools, and code review. No server, account, Node.js runtime, or Folio Docs installation is required. What the board owns. Stable card ids derived from filenames. Status, priority, parent and blocker topology. Acceptance criteria, comments, session trails, and attached artifacts. WIP warnings and deterministic ordering. Narrow edits and optional board: commits through folio board. An in-repository SKILL.md describing how agents should operate it. Start a board takes a few commands. Board formats defines the file contract, CLI reference covers every operation, and Operating a board defines the session protocol. What it deliberately does not own. Folio for Agents does not choose an agent, claim work automatically, execute tasks, or push changes. It is a meta-harness over the coding harnesses already in the repository: shared context and work state without replacing them. It also does not require a visual UI. The optional Folio Docs adapter can publish the files as a browser canvas with filtering, progressive card and artifact reading, and staged drag-and-drop. That adapter is installed explicitly through docs.yaml; installing Agents itself only adds its commands to the shared CLI. A card opened in the optional canvas. The canvas never becomes the source of truth. A staged move is exported as a folio board move command, reviewed, and committed to the board branch. The card. On the public canvas, clicking a card extends one continuous workspace: filters, canvas, card, then artifact. Later context opens farther right when space permits and continues below otherwise. Hairline separators resize each surface by pointer or keyboard, Esc unwinds one level, and the URL restores the selected card and artifact. Readable artifacts can use native full screen. Docs embeds keep the modal treatment. The permanent id remains the Markdown filename, not its screen position. Filtering. The canvas accepts one shareable ?q= expression. Spaces mean AND, commas mean OR, and a leading minus excludes a term. Fields such as status, priority, tag, milestone, and assignee narrow the same board without creating a second source of truth. Moving a card. Drag a card or choose a destination in its dialog. The move stays in local browser storage until Export moves produces the corresponding folio board move commands. Reset to source discards staged moves."},{"url":"docs/kanban/cards/the-board-takes-the-name-and-absorbs-the-roadmap/index.html","title":"The board takes the name and absorbs the roadmap","content":"Ask AI. The board takes the name and absorbs the roadmap. Released \u00b7 0.2 \u00b7 plan \u00b7 S. Decision card. The kanban plugin renames to board and the roadmap plugin folds into it: one git-backed store, several views. Milestones move out of docs.yaml into board data (a phase moving next \u2192 active becomes a commit with history), /roadmap stays as a view of that registry with its own\u2026 Panel verdict. Five agents, 2026-08-28: one merge assessor, three name generators (plain-literal, market, prior-art lenses), one adversary with kill authority. The\u2026 The card belongs to the board."},{"url":"docs/kanban/cards/kanban_single_board_with_filters/kanban_references_teardown/index.html","title":"Kanban reference teardown \u2014 SVAR React Kanban and ReUI Kanban","content":"Ask AI. Kanban reference teardown \u2014 SVAR React Kanban and ReUI Kanban. Researched 2026-08-04 for the board redesign. Both libraries were read at the source, not the marketing page. The question was narrow: what should folio\u2019s board take, and should it take a dependency. What they are. SVAR React Kanban (@svar-ui/react-kanban) \u2014 a widget from XB Software, the shop behind the SVAR suite (Gantt, DataGrid, Calendar). The free edition is MIT; a separately sold PRO build adds undo/redo, export, and lazy column loading. The React port is very new: one published version (2.6.0, June 2026), 14 GitHub stars, no issue history. The battle-tested original is the Svelte package. Note the package name wx-react-kanban does not exist; that was the pre-rename scope. ReUI Kanban (reui.io, Keenthemes) \u2014 not an npm package but a shadcn-style copy-paste registry: npx shadcn@latest add @reui/kanban drops a ~950-line MIT file into your components directory. Built on dnd-kit v6 (@dnd-kit/core + /sortable + /utilities). The kanban is in the free tier; the polished \u201cKanban Board\u201d blocks are paid. Styles. SVAR ships compiled CSS with two entry points (12 KB kanban-only, 108 KB for the full widget suite). Theming is 224 --wx-* custom properties supplied by a <Willow> / <WillowDark> wrapper, plus ~20 kanban-specific tokens (--wx-kanban-column-bg, --wx-kanban-drop-placeholder-bg, --wx-kanban-priority-high-bg\u2026). Semantic class names are documented as stable (wx-card, wx-column, wx-over-limit, wx-drop-placeholder) but every rule sits at (0,2,0) specificity behind build-generated hash classes, so overrides need escalated selectors. The default skin: Open Sans 14px, 3px card radius, sky-blue primary, hairline column headers \u2014 closer to a 2019 Trello than to current Jira or Linear. Dark mode changes the brand hue from blue to violet. ReUI is almost unstyled by design: the component carries layout classes only (grid auto-rows-fr gap-4 sm:grid-cols-3, flex flex-col gap-2, opacity and cursor states). All visual identity comes from the surrounding shadcn Card/Badge/Avatar. Its default \u201cnova\u201d style is rounded-xl, a 1px ring rather than a border (ring-1 ring-foreground/10), padding driven by a --card-spacing variable, and no shadow anywhere. Every structural node carries data-slot, plus data-dragging, data-disabled, data-value \u2014 clean CSS hooks that let a theme restyle the board without touching source. Neither skin is a starting point for folio: proposal.html is sharp corners, flat cards on 1px borders, recessed lanes, no foreign hues. The anatomy underneath is what is worth reading. Functionality worth naming. SVAR has the deeper feature set: named store actions with an intercept() veto seam, a card editor with placement=\"sidebar\" | \"modal\" | \"inline\", filters as a Map<string, predicate> where each entry is removable by tag, regrouping by any field via columnAccessor as a {get, set} pair, WIP limits that tint the column, column collapse to a rail, card and column virtualization, and a REST data provider. ReUI is a drag layer plus layout, and honest about it: no filtering, no search, no selection, no card detail, no WIP limits. What it does carry is the right persistence API \u2014 onValueChange for live preview and onValueCommit(value, meta) firing once per completed drag with meta.previousValue, built for optimistic update and rollback \u2014 plus sensor constraints tuned so cards stay clickable (10px mouse distance, 250ms touch long-press). Two defects in ReUI worth recording because they are patterns to avoid, both verified in its source: card keyboard drag is broken as shipped (the item spreads dnd-kit\u2019s attributes while the handle spreads listeners, so a keydown on the focused item never reaches the activator), and the column drag handle is opacity-0 until hover with no focus-visible counterpart, so a keyboard user can focus an invisible control. Dependency verdict: keep the hand-rolled drag. Do not adopt either as a dependency. The problem people reach for dnd-kit to solve is not a drag problem. Our drag breaks under filtering because a drag source is a {columnIndex, cardIndex} pair resolved against the visible board, not because HTML5 drag-and-drop is inadequate. dnd-kit fixes it as a side effect of being id-keyed; so does refactoring to a card id, which our own export path (folio kanban move <id> <column>) already speaks. Sortable\u2019s core value is unusable here. Intra-column order is computed by the Python loader and the CLI writes only status:; column order lives in a committed board.yaml with no command to rewrite it. Both reorder gestures would be theatre. The keyboard argument does not survive contact \u2014 see the ReUI defect above. Our explicit move buttons are more discoverable for a four-column board; they only lack an announcement. Touch is the one genuine gap (HTML5 drag does not fire on touch), and it does not need a library: SVAR\u2019s pointer-event implementation is a ~200-line recipe we can own. ReUI pins dnd-kit v6, the legacy line; the maintainer\u2019s rewrite is a separate package. Adopting v6 today buys a migration tomorrow, in a file folio redistributes to every user. SVAR as a dependency is worse: 14 runtime dependencies including an 833 KB core, for a 27-card board, in a template every folio user carries. What to take. Ordered by user value. None of these needs a dependency. Address cards by id, not by index. The root cause of our worst limitation: because a drag source is a pair of array indices resolved against the visible board, we disable drag and the move buttons whenever a filter is active. Filtering a board and then moving what you found is the most natural thing a user does, and today it is forbidden. The same defect lets the open dialog re-point at a different card, since the selection is also an index pair and the dialog has no focus trap. Fix: one uid per card, moves by uid, and the localStorage overlay becomes a Record<uid, columnId> \u2014 which also survives a baseline edit that merely adds a card, where today the whole overlay is discarded. A drop placeholder at the actual landing position. Today the only drop feedback tints the whole target column, while the move always appends to the end, so a card teleports to the bottom of a long backlog. SVAR\u2019s in-flow placeholder is honest about the landing slot. ReUI\u2019s live reflow is the other honest option, but we must not copy it: we have no write-back for intra-column position, so a gesture implying \u201cthird from top\u201d would lie. Move buttons visible on coarse pointers, and a real focus ring. Our move buttons are hover-revealed, and HTML5 drag does not fire on touch \u2014 so on a phone the only way to move a card is a control that is invisible and still hit-testable. A pointer-coarse variant and a proper focus-visible outline fix both. An aria-live region announcing moves and filter results. A screen reader user pressing the move button gets no feedback today. The best accessibility value per line available, and the one idea worth taking from dnd-kit without taking dnd-kit. Undo the last move. Our only escape from a mis-drop is \u201cReset to source\u201d, which discards every staged move. One snapshot per mutation turns a destructive control into a recoverable one. Take the idea from ReUI\u2019s previousValue snapshot, not from SVAR, whose undo is PRO. Filters as a facet record with predicates. Our filter function takes three positional arguments and is called from eight sites; every new facet edits all of them. SVAR\u2019s per-tag predicate map extends to assignee, priority, or blocked-by without touching the call sites, and matches the removable token pills the redesign already specifies. data-slot attributes on every structural node. Folio ships this component into users\u2019 repositories, and \u201cTheme contract for plugin surfaces\u201d is a live backlog card whose whole point is that themes must restyle plugin pages without forking them. Right now the only styling seam is editing the component. ReUI gets this right for about eight attributes. Bigger ideas, each needing its own card. Pointer-event drag replacing HTML5 drag, for touch support. SVAR\u2019s implementation is the recipe: 4px threshold, touch-action: none, a fixed ghost preserving the measured size and grab offset, elementFromPoint hit testing, a suppressed synthetic click on drop, Escape to cancel. Group the board by a field other than status. SVAR\u2019s columnAccessor {get, set} pair is the best structural idea in either library: the same cards regroup by milestone or assignee, with set writing the field back on drop. Blocked on a write path \u2014 folio kanban move only writes status:. Column collapse to a rail. Our backlog holds two thirds of the cards while two columns are empty, so density is genuinely lopsided. Named actions with an interceptor seam. Every mutation as a named, typed action with one place to observe and one to veto. It would let WIP limits stop being purely advisory, and give the export path, the undo stack, the announcer, and the overlay a single seam. What to reject outright. In-browser card creation and editing (SVAR\u2019s editor, add-card, context menu): it contradicts the file-backed board by design. Card files are hand-authored Markdown that a YAML round-trip would destroy, which is why every mutation is verified line surgery run by the CLI in the repo. The browser captures intent and exports commands; that boundary is the product, not a limitation. Also rejected: the REST provider and dynamic loading (no server), virtualization (27 cards, and it breaks in-page find), drag-to-reorder within a column or between columns (no honest destination), and either default skin. Licensing. Both are MIT with no copyleft and no attribution banner, so ideas and techniques carry no obligation. Copying code verbatim does carry the notice requirement, and this template is redistributed into users\u2019 repositories where it currently has zero third-party notices \u2014 so for anything taken, write it against our own data model rather than pasting. SVAR PRO is out of bounds: the licence forbids SaaS and permits redistribution only \u201cas long as it doesn\u2019t compete with SVAR\u201d, which is a live risk for a product whose model is redistributing a template."},{"url":"docs/kanban/cards/the_board_reads_as_a_tree/brief/index.html","title":"Prototype brief \u2014 nested/decomposition views for the Folio kanban board","content":"Ask AI. Prototype brief \u2014 nested/decomposition views for the Folio kanban board. These are throwaway prototypes. Nothing here ships. They exist so the owner can look at several answers side by side and pick one. Do not modify anything outside .artifacts/kanban-tree/. The problem. The board (/kanban) renders cards in status columns. Cards can decompose into subtasks, but nothing renders that. The owner wants a nested table view so a task breaks into smaller tasks, and calls it central to how the board gets used. What already exists (do not redesign it). parent: is a real, shipped field on every card: one card id, or empty. validated at build: must be an existing id on the board, and not the card itself. settable via folio kanban add --parent <id> and folio kanban update --set parent=<id> already a filter field, so parent:some-id filters today. no cycle detection beyond self-parent \u2014 a\u2192b\u2192a passes the build, so every tree walk you write must be cycle-safe or it hangs the page. The real board has 36 cards and none of them sets parent:. The data file ships an invented hierarchy so there is something to nest. Decided already (do not re-litigate). The view is read + move: you can see the tree, collapse and expand it, and move a card to another column. Moves stage locally and are then applied by copying folio kanban move commands \u2014 the site is static, it never writes. Reparenting from the UI is out of scope. Changing a card\u2019s parent stays a cardfile edit or a CLI call. Do not build drag-to-reparent. Your data. board-data.js sets window.BOARD. Load it with a plain \u201c \u2014 do not fetch() it, these files are opened over file:// and fetch is blocked there. The hierarchy deliberately contains the cases that break naive implementations: Depth 3: kanban-single-board-with-filters \u2192 the-card-dialog-reads-like-a-mail \u2192 the-dialog-renders-markdown. A child in a different column than its parent: theme-contract-for-plugin-surfaces is in Backlog, its parent plugin-system-unification is in In review. This is the case that makes \u201cnested\u201d and \u201ckanban\u201d disagree \u2014 your design has to answer it explicitly, not accidentally. 15 root cards with no children at all, which must not look broken or second-class. Cards with empty milestone / type / size / assignee. Do not render \u201d\u2014\u201d noise across a whole column of blanks; decide what absent looks like. Non-negotiable output shape. Three files, all in .artifacts/kanban-tree/, named for your variant slug: <slug>.html \u2014 markup and the notes section. <slug>.css \u2014 all styling, linked with <link rel=\"stylesheet\" href=\"<slug>.css\"> <slug>.js \u2014 all behaviour, linked with \u201c Relative <link> and <script> work fine over file://; only fetch() does not. Write them one at a time, in separate tool calls, and never attempt a single write longer than about 400 lines. A previous run of this task died because agents tried to emit one enormous file in one call and the run was killed for inactivity. Build up longer files with follow-up edits. Small, frequent writes. Opens correctly by double-clicking the .html (file://). No build step, no bundler. No CDN, no external fonts, no network of any kind. Use system font stacks. The real site uses Sora (body) and JetBrains Mono (code/labels); approximate with ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", sans-serif and ui-monospace, \"SF Mono\", Menlo, monospace. Plain HTML + CSS + vanilla JS. No React, no framework. Works at 1440px and at 900px wide. It does not need to work on a phone. Visual language \u2014 match Folio, do not invent a look. Folio reads as a technical publishing system: quiet, exact, source-driven. Thin rules, flat surfaces, --radius: 0 \u2014 square corners, this is not a rounded UI. No gradients, no shadows used as decoration, no icon packs, no emoji. Copy these tokens verbatim and use oklch() directly: Dark mode is first-class \u2014 support it with prefers-color-scheme or a toggle: Existing register worth carrying: small uppercase mono labels with wide tracking (font-mono, 11px, letter-spacing .14em) for metadata like FEATURE \u00b7 PLUGIN PLATFORM \u00b7 0.3; the card title in semibold sans at normal size; counts as 13/3 when a WIP limit is exceeded (and that overflow reads in --destructive). Read template/components/kanban-board.tsx for the incumbent card face before you design yours. The raw NUL byte discovered during this comparison has since been replaced, so ordinary text tools read the file normally. Required behaviour, whatever the layout. Collapse / expand a parent, and a way to collapse or expand everything. Filter by a text query over title and id, live. Filtering a tree has a real decision in it: when a child matches and its parent does not, does the parent stay as context, or does the child rise to the root? Pick one, make it look deliberate, and say which you picked in your notes. Move a card to another column, staged locally, with a visible count of staged moves and a way to see the resulting folio kanban move <id> <column> commands. Staging must survive collapse/expand and filtering. Progress: a parent\u2019s children carry status. Show how much of a parent is done, in whatever form your design argues for. Keyboard: arrow keys or tab move between rows, and collapse/expand is reachable without a mouse. Visible focus. Also required: a notes panel in the page itself. At the top or in a collapsible footer, a short section titled \u201cWhat this variant argues\u201d with: the one-sentence thesis of this layout; how you answered the cross-column child; how you answered tree filtering; what this design is bad at (be honest \u2014 the owner is comparing, and a variant that hides its weakness wastes the comparison). Rules of engagement. Do not touch template/, folio/, board/, docs/, or tests/. Do not run folio build or start a server. Do not git commit. Build the whole thing. A prototype with three fake rows is useless; render all 35 cards from the real data."},{"url":"docs/deployment/static_hosts/index.html","title":"Static Hosts","content":"Ask AI. Static Hosts. Static hosts only need the generated _site/ directory. The host can either run uv run folio build --clean itself or serve a prebuilt artifact uploaded by your CI pipeline. Vercel. Use Vercel as a static-site host: Install Folio. Add Folio to your project so Vercel can run the same build command as your local machine: Configure the project. In Vercel project settings, use: Framework Preset: Other. Install Command: corepack prepare pnpm@10 --activate && uv sync --locked. Build Command: uv run folio build --clean. Output Directory: _site. Deploy. Push to your production branch. Vercel runs the build command and publishes _site/. Vercel already builds automatically on push. If you need required documentation checks before deploy, add a CI workflow that runs folio coverage --min 80 and folio build --clean as pull request checks. Netlify. Create a netlify.toml that publishes the static artifact: Netlify will rebuild the docs on each deploy and serve _site/ directly. Self-hosted. Any static file server can host the generated site: For containerized deployments, copy _site/ into a small static web server image such as Nginx, Caddy, or any equivalent platform image. Do not copy .build/; it is a generated workspace cache, not the deployable artifact."},{"url":"docs/api-reference/folio_docs/docs/template_workspace/index.html","title":"folio_docs.docs.template_workspace source","content":"Ask AI. folio_docs.docs.template_workspace source. Classes. class TemplateWorkspace. source __init__ source. NameTypeDefaultDescription template_dir. str | Path. - build_dir. str | Path. - content_dir. str | Path | None. None. Returns: None - prepare source. NameTypeDefaultDescription clean. bool. False. Returns: None - _remove_template_content source. Returns: None - class TemplateConfigInjector. source __init__ source. NameTypeDefaultDescription config. Config. - build_dir. str | Path. - Returns: None - _record_injected source. Track a build file the injector actually wrote to, for the summary. NameTypeDefaultDescription path. Path. - Returns: None - _note_skip source. Log that an optional injection target was skipped. Required markers/files are pre-validated in. NameTypeDefaultDescription target. str. - reason. str. - Returns: None - _plugin_view_owns_root source. Check if a plugin view owns the root route (/). Plugin sections conventionally expose public views through routes.public. Inspect every normalized plugin section instead of naming a product-specific key, so optional integrations remain outside the Docs runtime boundary. When a plugin view owns /, the injector skips the docs-index wrapper at app/page.tsx, sets the docs canonical path to \u201c/docs/\u201d, and includes the docs index in the sitemap (no duplicate roots). Returns: bool - inject source. Returns: None - _docs_route_base source. Returns: str - _docs_route_with_trailing_slash source. Returns: str - _docs_route_path source. NameTypeDefaultDescription suffix. str. - Returns: str - _docs_route_segments source. Returns: list[str] - _docs_app_import_path source. Returns: str - _apply_theme_package source. Returns: None - _inject_root_layout source. NameTypeDefaultDescription name. str. - Returns: None - _inject_favicon source. NameTypeDefaultDescription name. str. - Returns: None - _write_template_context source. Returns: None - _inject_docs_layout source. NameTypeDefaultDescription name. str. - Returns: None - _inject_project_header_logo source. NameTypeDefaultDescription content. str. - name. str. - Returns: str - _inject_project_header_actions source. NameTypeDefaultDescription content. str. - Returns: str - _inject_docs_route_page source. NameTypeDefaultDescription name. str. - Returns: None - _inject_previews_page source. NameTypeDefaultDescription name. str. - Returns: None - _inject_docs_repo_link source. NameTypeDefaultDescription content. str. - Returns: str - _inject_search_config source. NameTypeDefaultDescription content. str. - Returns: str - @staticmethod. _inject_layout_search_null source. NameTypeDefaultDescription content. str. - Returns: str - @staticmethod. _inject_search_command_import source. NameTypeDefaultDescription content. str. - Returns: str - _inject_layout_search_component source. NameTypeDefaultDescription content. str. - Returns: str - _inject_og_image source. NameTypeDefaultDescription name. str. - Returns: None - _inject_landing_page source. NameTypeDefaultDescription name. str. - Returns: None - _inject_landing_navbar source. Fill the navbar\u2019s placeholders, landing or no landing. This used to live inside _inject_landing_page, behind its landing_enabled gate. But PublicLayout renders LandingNavbar on every public plugin view, so a project with a board and no landing: key shipped a navbar still containing __PROJECT_NAME_JSON__ and died at prerender with a ReferenceError. The navbar belongs to the site, not to the landing page. Every value below is a config read with its own fallback, so this is correct whether or not the landing is enabled. NameTypeDefaultDescription name. str. - Returns: None - _default_landing_sections source. NameTypeDefaultDescription features. list[dict] - Returns: list[dict] - _inject_docs_index_page source. Returns: None - _inject_sitemap source. Returns: None - _inject_search_postbuild source. Returns: None - _is_package_owned source. True when the theme.package overlay already ships this file. NameTypeDefaultDescription rel_path. str. - Returns: bool - _inject_theme_config source. Returns: None - _write_project_theme_module source. Returns: None - _inject_theme_configurator_mount source. Returns: None - _inject_i18n source. Returns: None - _configured_base_path source. Returns: str - _inject_versions source. Returns: None - _relocate_docs_route source. Returns: None - @staticmethod. _remove_nested_relocation_residue source. Drop leftovers of a previous relocation nested inside source. With a route base nested under /docs (e.g. /docs/v2), a warm rebuild merges the template\u2019s app/docs over a .build tree whose app/docs still contains the previously relocated route dir. Without this cleanup the whole tree \u2014 old relocated copy included \u2014 is moved again, nesting one level deeper on every warm rebuild (app/docs/v2/v2/...). A directory at the nested target that holds the docs catch-all page can only be such residue: the relocation itself put it there, so remove the residue chain before relocating again. NameTypeDefaultDescription source. Path. - target. Path. - Returns: None - Functions. _namespace_style_key source. NameTypeDefaultDescription key. str. - Returns: str - validate_template_marker_contract source. Return (file, marker) pairs whose required marker is missing. Each load-bearing marker in :data:REQUIRED_INJECTION_MARKERS must be present verbatim in its expected file. A missing file surfaces every marker it owns as missing, mirroring the \u201cmissing required files\u201d / MDX-contract checks so a custom template fails fast instead of building with silently dropped project metadata. NameTypeDefaultDescription template_dir. str | Path. - Returns: list[tuple[str, str]] - copytree_ignore source. Ignore callable for every template/theme/overlay copytree call. Single-sourced from :data:_COPY_IGNORED_DIRS (plus the staging marker file) so the symlink scan and the copy can never drift apart. _walk_copied_entries source. Yield every path under root that a Folio copytree would copy. Uses os.walk and prunes :data:_COPY_IGNORED_DIRS in place so large excluded trees (node_modules, .git, \u2026) are never descended into. Directories are yielded before their contents. NameTypeDefaultDescription root. Path. - _reject_symlinks source. Raise if any copied entry under root is a symlink. shutil.copytree dereferences symlinks (symlinks=False), so an untrusted tree containing a symlink could copy files from outside the tree into the published site. Scan the tree and refuse to copy if any symlink is present. Entries inside directories excluded from the copy are skipped, since they are never published. NameTypeDefaultDescription root. Path. - label. str. - Returns: None - collect_copyable_files source. Collect copied file paths (relative to root) in a single traversal. Combines the symlink rejection of :func:_reject_symlinks with file collection so callers that need both (e.g. the theme.package overlay) walk the tree once instead of twice. Raises ValueError on the first symlink. NameTypeDefaultDescription root. Path. - label. str. - Returns: set[Path] - _theme_radius_index source. NameTypeDefaultDescription radius. str. - Returns: int - _has_project_theme_preset source. NameTypeDefaultDescription config. Config. - Returns: bool - _project_theme_default_config source. NameTypeDefaultDescription config. Config. - Returns: dict[str, object] - _project_theme_default_options source. NameTypeDefaultDescription config. Config. - Returns: dict[str, str] - _project_theme_controls source. NameTypeDefaultDescription config. Config. - Returns: list[dict[str, object]] - _project_theme_variant_themes source. NameTypeDefaultDescription config. Config. - Returns: dict[str, dict[str, object]] - _project_theme_option_preview source. NameTypeDefaultDescription option. dict[str, Any] - Returns: dict[str, str] - _project_theme_style source. NameTypeDefaultDescription config. Config. - Returns: dict[str, str] - _render_project_header_logo source. NameTypeDefaultDescription config. Config. - fallback_name. str. - Returns: str - _has_project_header_actions source. NameTypeDefaultDescription config. Config. - Returns: bool - _render_project_header_actions source. NameTypeDefaultDescription config. Config. - Returns: str - _render_project_theme_module source. NameTypeDefaultDescription config. Config. - Returns: str - resolve_base_path source. NameTypeDefaultDescription config. Config. - env. Mapping[str, str] | None. None. Returns: str - _is_github_pages_deploy source. NameTypeDefaultDescription config. Config. - env. Mapping[str, str] - Returns: bool - _github_pages_base_path source. NameTypeDefaultDescription repository. str. - Returns: str -"},{"url":"docs/index.html","title":"Folio Docs","content":"Ask AI. Folio Docs. Folio Docs turns Python source and Markdown guides into a searchable static site, API reference, and agent-readable Markdown mirrors. It is installed, configured, and released independently from Folio for Agents. Get started in 30 seconds. That\u2019s it. Folio reads your pyproject.toml, scans your source code, and generates a full documentation site with source pages, search, and dark mode. How it works. Folio follows a four-stage pipeline: flowchart LR Source[\"\u25a3 Python source (.py)\"] --> Parse[\"\u2699 Parse AST + docstrings\"] Parse --> IRNode[\"\u25c7 IR objects\"] IRNode --> Generate[\"\u25a4 Generate MDX pages\"] Generate --> BuildSite[\"\u25a5 Build Nextra static site\"] Parse \u2014 Reads Python source files via the ast module. Extracts classes, functions, type annotations, decorators, and Google-style docstrings. IR \u2014 Converts parsed data into a clean intermediate representation (ModuleIR, ClassIR, FunctionIR) that captures everything needed for docs. Generate \u2014 Transforms the IR into MDX pages with function signatures, parameter tables, class overviews, and formatted docstring content. Build \u2014 Places MDX files into a Nextra site with shadcn/ui components. Produces a static site with search, dark mode, and responsive layout. Features. Automatic API reference. Point Folio at your source directories and get complete API docs \u2014 classes, methods, parameters, return types, decorators, all extracted and rendered automatically. One config file. A single docs.yaml of about thirty lines replaces Sphinx's conf.py, Makefile, and requirements setup. Modern UI. Dark mode, full-text search, responsive layout, and shadcn/ui components out of the box. No theme hunting. Markdown + API in one site. Write tutorials and guides in Markdown alongside the generated API reference \u2014 everything lives in one cohesive site. Static deployment. Export plain files for GitHub Pages, Vercel, Netlify, or Docker \u2014 no custom server, no vendor in the serving path. LLM-friendly output. Generates llms.txt and llms-full.txt following the llmstxt.org spec, so AI coding assistants understand your library. Plugins included. Roadmap and landing are default plugins activated by one key. OpenAPI and third-party integrations are opt-in. Sphinx migration. A migration guide for converting existing RST pages and Sphinx conventions into Folio's Markdown and MDX inputs. Project status. Folio is under active development. The roadmap shows what\u2019s shipped and what\u2019s in progress. Folio\u2019s operational board lives on the repository\u2019s dedicated board branch and is updated there, never in code or release branches. The board guide covers the card format and the commands that move a card. Next steps. Why Folio \u2014 The comparison, the honest SWOT, and the LLM-era questions. Folio Docs \u2014 The documentation generator and its code boundary. Installation \u2014 Prerequisites and setup. Quick Start \u2014 Build your first docs site step by step. Architecture \u2014 How the CLI, parser, generator, template, and export pipeline fit together. Configuration \u2014 Full docs.yaml reference. CLI Reference \u2014 Every command, flag, and option. Writing Docstrings \u2014 How Folio parses your code. Components \u2014 UI components available in your docs. Theming \u2014 Customize presets, theme packages, and custom templates from one theming model. Deployment \u2014 Static hosts, GitHub Pages, CI/CD, and branch previews. Plugins \u2014 The plugin fleet and how to write your own. Roadmap \u2014 Where Folio is headed, rendered live from docs.yaml by its own plugin. Migrating from Sphinx \u2014 Step-by-step migration guide."},{"url":"docs/components/example_tabs/index.html","title":"ExampleTabs","content":"Ask AI. ExampleTabs. Tabbed panels for showing multiple code examples for the same concept, such as basic vs. advanced usage or different input formats. Each tab displays a syntax-highlighted code block. API. ExampleTabs. PropTypeDefaultDescription examples. Example[] \u2014 Array of example objects. Example object. FieldTypeDefaultDescription label. string. \u2014 Tab label text. code. string. \u2014 The code string to display. language. string. \"python\" Syntax highlighting language. Example. Basic With Config Async. node = Node() node.start()."},{"url":"docs/api-reference/folio_agents/integrations/kanban/index.html","title":"folio_agents.integrations.kanban source","content":"Ask AI. folio_agents.integrations.kanban source. Optional Folio Docs adapter for the kanban: configuration key. The board is git-persisted: its committed cardfiles are the source of truth. In the browser, cards can be dragged between columns; moves are stored as a localStorage overlay keyed to the committed board, and the exports produce ready-to-commit move commands that close the loop. Columns are loaded from a cardfile directory named by kanban.source or from the independent canvases in kanban.sources. kanban.ref can resolve those directories through a managed Git worktree. The kanban.routes.public setting accepts a path to choose where the board view is published (\"/\" for the front page). Functions. config_keys source. Returns: list[str] - configure source. NameTypeDefaultDescription config. Any. - raw_config. dict[str, Any] - Returns: None - _resolve_card_paths source. Fill each card\u2019s file: where it lives, as a project-relative path. A path, never a URL. This used to be two hosting-provider links \u2014 {repo}/blob/... for the path chip and {repo}/edit/... behind the dialog\u2019s pen \u2014 so \u201cedit this card\u201d navigated to a web editor nobody edits in, and to the wrong place entirely for a board that is not on that provider. Artifact and trail refs were resolved the same way. The board is operated locally, through the CLI and an editor, so the honest answer to \u201cwhere do I change this\u201d is the path itself. Rendering it is the reader\u2019s job; a static page cannot open an editor and should not pretend otherwise. NameTypeDefaultDescription kanban. dict[str, Any] - Returns: None - _card_dir_for_card source. NameTypeDefaultDescription kanban. dict[str, Any] - card. dict[str, Any] - Returns: str - _docs_route_base source. Where content pages are served from, defaulting like the builder does. NameTypeDefaultDescription config. Any. - Returns: str - _resolve_artifact_hrefs source. Give an artifact that names a published page or file somewhere to open. A card may keep a directory of its own next to its markdown file \u2014 board/cards/<id>.md and board/cards/<id>/ \u2014 and what is in it is that card\u2019s own output: the prototype a decision was made on, the document that compared them. emit_assets publishes that directory, so a doc: or file: artifact pointing into it becomes a link instead of a path the reader has to go find. The whole directory is published, not only the attached files, because those files reference each other: a prototype page without its stylesheet opens as nothing. Markdown resolves to the page it was built into, not to the file. It went through the same parser and MDX writer as every documentation page, so there is a real page to send the reader to, and sending them to the source instead would be handing over an unstyled download of something the site already renders. The same reasoning covers a doc: naming a file under a docs source: the site publishes exactly that page, so the artifact links to it. Everywhere else, an artifact keeps the empty href it had and renders as the path it is. Attaching a file is not a licence to publish an arbitrary part of the repository \u2014 only what a card owns or the docs already publish opens. Under warn_unreachable (configure\u2019s one pass, so a build says it once) a doc: whose file exists but whose page nothing publishes warns, naming the card and the target as written: a doc: promised a page, and the promise did not hold. NameTypeDefaultDescription kanban. dict[str, Any] - docs_route_base. str. - project_dir. Path. - doc_sources. list[Path] | tuple[Path, ...] (). warn_unreachable. bool. False. Returns: None - _docs_source_route source. The content route a docs source builds for target, or None. The route rule is source_route, the one parse_markdown_directory applies to every docs source \u2014 one rule, imported, so the two can never drift. .md only: that is all a docs source reads. NameTypeDefaultDescription target. Path. - doc_sources. Any. - Returns: str | None - _doc_source_dirs source. Docs source directories as resolved absolute paths. config.doc_sources is relative before resolve_paths runs and absolute after; resolving against the project directory accepts both. NameTypeDefaultDescription config. Any. - project_dir. Path. - Returns: list[Path] - _owned_card_artifact source. Return a safe card-relative target, or None when it is not owned. NameTypeDefaultDescription project_dir. Path. - card_dir. str. - card_id. str. - target. str. - Returns: Path | None - register_extensions source. NameTypeDefaultDescription registry. Any. - config. Any. - Returns: None - collect_docs source. Contribute card-owned Markdown to Folio\u2019s core document pipeline. NameTypeDefaultDescription config. Any. - Returns: list[PluginDocument] - _iter_card_docs source. Yield (card, source, route) for every markdown file a card owns. One iteration feeds both collect_docs and the generated card index, so the pages a card contributes and the pages its index lists cannot drift apart. NameTypeDefaultDescription kanban. dict[str, Any] - project_dir. Path. - watch_paths source. The serve watcher asks what to watch beyond code and docs: a cardfile. board answers with its directory, so a card edited mid-serve reaches the served board without a restart. NameTypeDefaultDescription config. Any. - Returns: list[str] - on_watched_change source. Reload the board from disk when anything under it changes. The whole board reloads rather than one card: membership, ordering, and the column set all live across files, and a full load is the same work the build does. A half-saved card raises out of normalize_kanban; the watcher\u2019s isolation degrades that to a warning and the next save lands cleanly. Card-directory documents republish as raw assets here; their compiled pages still come from a full build. The section is reconstructed from config.extra, not re-read from docs.yaml \u2014 the config file is not watched, so config edits mid-serve still take a restart. NameTypeDefaultDescription builder. Any. - config. Any. - path. str. - change. str. - Returns: bool - emit_assets source. NameTypeDefaultDescription builder. Any. - config. Any. - Returns: None - _sync_board_page source. Write or refresh the generated board page at kanban/index. NameTypeDefaultDescription builder. Any. - kanban. dict[str, Any] - Returns: None - _sync_card_index_pages source. Keep every folder route above a card\u2019s documents resolvable. A card\u2019s documents compile below its own folder route, and a folder whose children exist is a URL readers will try \u2014 trimmed by hand or reached from a breadcrumb. When the card ships no index.md/README.md of its own, a marker-tagged index listing the card\u2019s documents fills the hole; when it does, or when the documents are gone on a warm build, the generated page steps aside. User-authored pages (no marker) are never touched, matching the board page\u2019s contract one level down. The kanban/cards/ folder itself resolves to a directory of the publishing cards. With the board\u2019s own docs page turned off the kanban/ folder is a hole of the same kind, so kanban/index forwards to the public board when one exists and repeats the directory when none does \u2014 a listing, not the board the configuration said no to. With no documents at all the plugin\u2019s generated pages come down and the namespace is silent, exactly as before. NameTypeDefaultDescription builder. Any. - kanban. dict[str, Any] - project_dir. Path. - route_base. str. - docs_route_on. bool. - Returns: None - _folders_above source. Every folder route between a document and root, nearest last. kanban/cards/one-card/sub/report contributes kanban/cards/one-card/sub and kanban/cards/one-card; root itself is the caller\u2019s business and is never yielded. NameTypeDefaultDescription routes. set[str] - root. str. - Returns: list[str] - _user_owns_route source. Whether a page the plugin did not generate sits at route. An unreadable page counts as user-authored: when its content is unknown the only safe reading is that it is not ours to shadow. NameTypeDefaultDescription builder. Any. - route. str. - Returns: bool - _remove_stale_card_indexes source. Drop generated card indexes whose card stopped publishing documents. NameTypeDefaultDescription builder. Any. - keep. dict[str, str] - Returns: None - card_index_mdx source. The folder page for a card\u2019s published output: tiles, not a list. The card root gets the card\u2019s own voice \u2014 its status line, the first paragraph of its description, and one tile per document and artifact. A subtree folder stays lean: the documents below it, nothing else. NameTypeDefaultDescription card. dict[str, Any] - docs. list[tuple[Path, str]] - route_base. str. - status_title. str. '' project_root. Path | None. None. subtree. str | None. None. Returns: str - kanban_redirect_mdx source. The parent folder page when the board lives at a public route. Card documents compile below kanban/ but are read from the board, so the folder forwards there instead of indexing them. The target is written relative to the folder\u2019s own depth \u2014 the page is served under the docs route base, and a site base path must survive \u2014 and RedirectPage carries query and hash across, the same contract the /kanban forwarder keeps. NameTypeDefaultDescription kanban. dict[str, Any] - route_base. str. - Returns: str - kanban_directory_mdx source. A folder page of one tile per publishing card. Two routes use it. kanban/cards/index whenever anything publishes: the documents compile below cards/, so the folder resolves to what it holds \u2014 what each card is about, straight from the card\u2019s own description. And kanban/index when the board is published nowhere at all, rather than the board component the configuration declined; with a public route that folder forwards to the board instead (:func:kanban_redirect_mdx). NameTypeDefaultDescription kanban. dict[str, Any] - docs_by_card. dict[str, dict[str, Any]] - route_base. str. - Returns: str - _jsx_attr source. Make a string safe inside a double-quoted MDX component attribute. NameTypeDefaultDescription value. str. - Returns: str - _plain_excerpt source. The first paragraph as plain prose: markup off, one line, capped. NameTypeDefaultDescription markdown_text. str. - limit. int. - Returns: str - _doc_title source. The document\u2019s first heading, for a link that says what it opens. NameTypeDefaultDescription source. Path. - Returns: str - _doc_excerpt source. The document\u2019s first prose paragraph, flattened for a tile. NameTypeDefaultDescription source. Path. - Returns: str - _publish_card_assets source. Publish each card\u2019s own directory under /_folio/kanban/. Everything in board/cards/<id>/ goes up as it is on disk: the whole directory, because the files in it reference each other and an attached page without its stylesheet is not the page. Markdown is the exception, and it is the point. A .md sibling is not a file to download, it is a page the project wrote, so it goes through the same pipeline every documentation page goes through \u2014 parsed, compiled to MDX, themed, in the search index and in llms.txt. It is still copied here as source too, because a relative link from a prototype to it has to resolve, and because the raw file is what an agent reads. Two things never travel. A dotfile or dot-directory is session scratch by convention (.verify/ full of screenshots is what prompted the rule), and a symlink is refused rather than followed, so a link out of the project cannot publish a file the project does not contain. NameTypeDefaultDescription builder. Any. - kanban. dict[str, Any] - project_dir. Path. - Returns: None - _iter_card_files source. Yield safe, visible files from directories owned by known cards. NameTypeDefaultDescription kanban. dict[str, Any] - project_dir. Path. - _path_has_symlink source. Whether a target path traverses any symlink below root. NameTypeDefaultDescription root. Path. - target. Path. - Returns: bool - _card_page_route source. Content route for one markdown file in a card\u2019s directory. NameTypeDefaultDescription card_id. str. - relative. Path. - Returns: str - _read_page source. The page content on disk, or None when the builder cannot read pages. NameTypeDefaultDescription builder. Any. - route. str. - Returns: str | None - _remove_generated_page source. Remove the plugin\u2019s own marker-tagged docs page, if one persists. Only a page carrying the generated-page marker is removed; when the builder cannot read or remove pages nothing happens (better a stale plugin page than a deleted user page). NameTypeDefaultDescription builder. Any. - Returns: None - _remove_marker_page source. Remove one page, and only when it carries the generated-page marker. NameTypeDefaultDescription builder. Any. - route. str. - Returns: None - _normalize_route_value source. Normalize a route value: bool stays bool, string becomes \u201d/\u201c-prefixed path, else bool(). NameTypeDefaultDescription value. Any. - Returns: bool | str - normalize_kanban source. NameTypeDefaultDescription raw_kanban. Any. - project_dir. Path. - Returns: dict[str, Any] - _resolve_card_icons source. Stamp each card with the icon of its first mapped tag. Resolution happens after every normalization pass because _normalize_card rebuilds card dicts from scratch: the icon is derived data, like paths and artifact hrefs, never trusted from input. NameTypeDefaultDescription columns. list[dict[str, Any]] - icons. dict[str, str] - Returns: None - _normalize_columns source. Normalize columns and enforce board-wide unique card ids. Cardfile boards get uniqueness for free (one file per id); duplicates appearing elsewhere (synthesized ids, manual conflicts) are suffixed and warned about rather than dropped. NameTypeDefaultDescription raw_columns. list[Any] - Returns: list[dict[str, Any]] - _load_source_columns source. Columns (and board title) from a cardfile board directory. A cardfile board is a directory containing board.yaml + one Markdown file per card under cards/ (see kanban_board). Any problem \u2014 a non-string path, a path naming a file instead of a directory, or a missing board directory \u2014 raises ValueError naming the resolved path. configure is dispatched fail-fast, so the build stops loudly instead of shipping an empty or wrong board. NameTypeDefaultDescription raw_source. Any. - source_root. Path. - project_dir. Path. - artifact_root. Path | None. None. Returns: tuple[list[Any], str, dict[str, str]] - _load_multiple_sources source. Merge product-owned boards into one project-aware browser projection. A card\u2019s own track is a workstream inside its product and is left alone; the product it came from lands in project. The two were one field until now, and the merge silently overwrote whatever a card had written with the name of its source. NameTypeDefaultDescription raw_sources. Any. - source_root. Path. - project_dir. Path. - Returns: tuple[list[dict[str, Any]], str, dict[str, str], dict[str, str]] - _rebase_artifact_targets source. Translate a product-relative artifact path into the host worktree. NameTypeDefaultDescription columns. list[dict[str, Any]] - source_root. Path. - project_dir. Path. - Returns: None - _canonical_card_dir source. Canonical project-relative board directory, with no symlink traversal. NameTypeDefaultDescription path. Path. - project_dir. Path. - Returns: str - _normalize_column source. NameTypeDefaultDescription raw_column. Any. - index. int. - Returns: dict[str, Any] | None - _normalize_card source. NameTypeDefaultDescription raw_card. Any. - column_title. str. - Returns: dict[str, Any] | None - _normalize_artifacts source. Typed artifact refs -> {kind, target, display, label, href}. Accepts both the committed one-line form (- doc: research/x.md) and the already-normalized form, so re-normalization is idempotent. url targets go through the shared href scheme policy (fail-fast on javascript:/data:); repo-relative kinds get their href filled in by configure once the project repo URL is known. display is the target as the author wrote it, set by the board loader; where it is absent (a project plugin\u2019s override) the target is what was written. NameTypeDefaultDescription raw_artifacts. Any. - column_title. str. - title. str. - Returns: list[dict[str, Any]] - _normalize_criteria source. NameTypeDefaultDescription raw_criteria. Any. - Returns: list[dict[str, Any]] - _normalize_trail source. NameTypeDefaultDescription raw_trail. Any. - Returns: list[dict[str, Any]] - _normalize_comments source. NameTypeDefaultDescription raw_comments. Any. - Returns: list[dict[str, Any]] - _safe_link source. Sanitize a card link with the same scheme rules as other hrefs. Delegates to the shared href validator (http(s)/mailto or a scheme-less relative path); anything else \u2014 javascript:, data:, \u2026 \u2014 raises, which fails the build loudly under configure\u2019s fail-fast dispatch. NameTypeDefaultDescription raw_link. Any. - column_title. str. - title. str. - Returns: str - active_kanban source. The normalized kanban config, or None when the plugin is inactive. The plugin loads for every build (it is a default plugin) but only a board: section in docs.yaml \u2014 surfaced as config.extra[\"kanban\"] by configure() \u2014 activates its output. NameTypeDefaultDescription config. Any. - Returns: dict[str, Any] | None - get_kanban source. NameTypeDefaultDescription config. Any. - Returns: dict[str, Any] - get_columns source. NameTypeDefaultDescription config. Any. - Returns: list[dict[str, Any]] - docs_page_mdx source. NameTypeDefaultDescription kanban. dict[str, Any] - Returns: str - _escape_mdx_text source. Backslash-escape characters MDX treats as syntax in body text. NameTypeDefaultDescription value. str. - Returns: str -"},{"url":"docs/api-reference/folio_agents/milestones/index.html","title":"folio_agents.milestones source","content":"Ask AI. folio_agents.milestones source. Resolve board milestones against each project\u2019s own release line. Functions. resolve_roadmap_phases source. Attach roadmap phase labels to matching cards and warn on drift. NameTypeDefaultDescription board. dict[str, Any] - raw_roadmap. Any. - Returns: None -"},{"url":"docs/api-reference/folio_docs/index.html","title":"folio_docs source","content":"Ask AI. folio_docs source."},{"url":"docs/deployment/ci-cd/index.html","title":"CI/CD","content":"Ask AI. CI/CD. CI/CD is a deployment strategy: the pipeline builds _site/, checks it, and publishes the same static artifact that local builds produce. This page focuses on GitHub Actions because folio init can generate ready-to-use Pages workflows. GitHub Actions Deployment. folio init creates a GitHub Pages workflow at .github/workflows/pages.yml. It builds docs on every push to main, uploads _site/, and deploys with GitHub Pages: If your docs build needs local project dependencies or versioned documentation, edit the build steps in this same file. Folio\u2019s own repository currently uses uv sync --all-groups --locked before uv run --locked folio build --clean to deploy only the current version. The folio github-pages commands are internal deployment helpers shipped with the folio-docs package. They keep the generated workflow readable while still preserving branch previews, updating the preview index, saving Pages state, and verifying the deployed URLs. Branch Preview Deploys. folio init also creates .github/workflows/branch-previews.yml for GitHub Pages branch previews. It runs from pull_request_target so the workflow definition and deploy steps come from the trusted base branch, while PR branch code is built in a separate unprivileged job. GitHub Pages serves one artifact per site, so Folio does not deploy each branch as an independent Pages site. Instead, every deploy publishes a complete static artifact with this layout: The production workflow and preview workflow cooperate through an internal folio-pages-state branch: A main deploy builds production docs into _site/. It copies any existing _site/previews/ folders from folio-pages-state. It regenerates /previews/index.html. It saves the complete artifact back to folio-pages-state. It deploys the combined artifact to GitHub Pages. A branch preview deploy follows the same rule: publish the complete site, not just the branch folder. The preview workflow resolves the open same-repository PR for the branch. An unprivileged build job checks out the PR head with persisted credentials disabled and builds docs with FOLIO_BASE_PATH=/repo/previews/pr-<number>-<branch>/. That build job uploads only the static _site/ preview artifact. A privileged deploy job revalidates that the PR head is still current before doing any write-capable work. The deploy job checks out the trusted base branch, starts the Pages artifact from the saved folio-pages-state root, and uses a trusted production build only as the first-deploy fallback. It downloads the static preview artifact and replaces only /previews/pr-<number>-<branch>/. It regenerates /previews/index.html, saves state, deploys, and verifies the preview URL. It creates or updates a sticky PR comment with the preview URL, preview index, branch, and commit. Production therefore remains at the normal Pages URL, while each open PR gets a stable preview URL such as: The preview folder name includes the PR number plus a URL-safe version of the branch name, which keeps similarly named branches from overwriting each other. If a PR is stale, closed, still a draft, or comes from a fork, the preview deploy exits early and leaves the current Pages site unchanged. The PR comment uses a hidden marker so the workflow updates the same comment on each deploy. Reviewers get the latest preview link directly in the conversation without a new comment on every push. The workflow still writes the same links to the Actions summary for debugging failed runs. Pull Request Checks. Run docs build and coverage checks on every PR: Coverage Gates. Use folio coverage to enforce minimum docstring coverage in CI: This exits with code 1 if any module falls below the threshold, failing the CI pipeline. Adjust the threshold to match your project\u2019s standards. The coverage report shows per-module statistics: Pre-commit Hook. Run a quick coverage check before every commit:"},{"url":"docs/api-reference/folio_docs/docs/next_runtime/index.html","title":"folio_docs.docs.next_runtime source","content":"Ask AI. folio_docs.docs.next_runtime source. Classes. class NextRuntime. source __init__ source. NameTypeDefaultDescription template_dir. str | Path. - build_dir. str | Path. - output_dir. str | Path. - verbose. bool. False. Returns: None - install_deps source. Install dependencies if needed. Returns True if install ran. Returns: bool - build source. NameTypeDefaultDescription log_path. str | Path | None. None. output_callback. Callable[[str], None] | None. None. Returns: None - _remove_stale_build_artifacts source. Returns: None - copy_static_output source. Returns: None - serve source. NameTypeDefaultDescription port. int. 4321. kill_existing. bool. False. Returns: subprocess.Popen - _check_dependencies source. Returns: None - _has_next_binary source. Returns: bool - _has_working_next source. Returns: bool - _patch_nextra_schema source. Patch nextra-theme-docs Zod schema bug where children is validated. as nonoptional but has already been destructured out of props. Returns: None - _patch_nextra_generated_content_timestamps source. Skip Git timestamp lookups for Folio-generated MDX content. Returns: None - @staticmethod. _file_hash source. NameTypeDefaultDescription path. Path. - Returns: str - @staticmethod. is_port_in_use source. NameTypeDefaultDescription port. int. - Returns: bool - @staticmethod. kill_port source. Kill any process listening on the given port. Returns True if a process was killed. NameTypeDefaultDescription port. int. - Returns: bool - Functions. _tool_version_output source. NameTypeDefaultDescription command. list[str] - Returns: str - _parse_version source. NameTypeDefaultDescription raw. str. - Returns: tuple[int, ...] - preflight_check source. Verify Node and pnpm exist at usable versions before expensive work. Raises RuntimeError with every problem listed and an actionable fix, so a missing toolchain fails in the first second of a build instead of minutes in. Returns: None -"},{"url":"docs/api-reference/index.html","title":"Source Code - Folio","content":"Ask AI. Python source catalog. Open the right module before reading every symbol. This source catalog covers 55 modules across 2 packages with 46 classes and 505 functions. Use this page as a routing layer: scan the surface, open the module, then jump into classes, functions, parameters, and source links. 55 modules46 classes505 functions. Fast paths. Start from the question in front of you. Start at the root folio_agents. Inspect classes 46 classes. Find functions 505 functions. folio_agents package. 10 modules in this namespace. 0 classes / 0 functionsfolio_agentsGit-backed board domain for Folio for Agents.Open module0 classes / 1 functionfolio_agents.cli_commandsThe ``folio board`` command group.Open module1 class / 15 functionsfolio_agents.editLine-surgery editor for cardfile board cards.Open module0 classes / 0 functionsfolio_agents.integrationsOptional integrations with other products in the Folio family.Open module0 classes / 54 functionsfolio_agents.integrations.kanbanOptional Folio Docs adapter for the ``kanban:`` configuration key.Open module0 classes / 14 functionsfolio_agents.loaderCardfile board loader for ``board.source: <dir>/``.Open module0 classes / 1 functionfolio_agents.milestonesResolve board milestones against each project's own release line.Open module4 classes / 15 functionsfolio_agents.opsBoard operations as importable functions \u2014 the write path every surface shares.Open module0 classes / 2 functionsfolio_agents.textSmall text and link primitives owned by Folio for Agents.Open module0 classes / 7 functionsfolio_agents.worktreeRead a board branch through an isolated Git worktree.Open module. folio_docs package. 45 modules in this namespace. 0 classes / 0 functionsfolio_docsModule documentation.Open module0 classes / 0 functionsfolio_docs.__main__Module documentation.Open module0 classes / 0 functionsfolio_docs.agent_outputAgent-readable documentation emitted by Folio Docs.Open module1 class / 0 functionsfolio_docs.agent_output.artifactsPublication boundary for Folio for Agents artifacts.Open module0 classes / 10 functionsfolio_docs.agent_output.contractModule documentation.Open module0 classes / 16 functionsfolio_docs.agent_output.llm_outputModule documentation.Open module0 classes / 5 functionsfolio_docs.agent_output.markdownMarkdown mirrors for Folio for Agents.Open module0 classes / 5 functionsfolio_docs.brandingModule documentation.Open module4 classes / 42 functionsfolio_docs.buildModule documentation.Open module0 classes / 2 functionsfolio_docs.builtinsSingle source of truth for Folio's builtin MDX components.Open module2 classes / 60 functionsfolio_docs.cliModule documentation.Open module1 class / 36 functionsfolio_docs.configModule documentation.Open module1 class / 7 functionsfolio_docs.coverageCoverage analysis for Python documentation.Open module0 classes / 0 functionsfolio_docs.docsFolio Docs: human-facing documentation generation and site output.Open module0 classes / 12 functionsfolio_docs.docs.builtin_driftGuard the builtin manifest against the bundled MDX components.Open module1 class / 0 functionsfolio_docs.docs.extension_emitterModule documentation.Open module0 classes / 0 functionsfolio_docs.docs.integrationsFirst-party integrations published through Folio Docs.Open module0 classes / 28 functionsfolio_docs.docs.integrations.landingFirst-party Folio Docs landing integration.Open module0 classes / 25 functionsfolio_docs.docs.integrations.openapiModule documentation.Open module0 classes / 16 functionsfolio_docs.docs.integrations.roadmapModule documentation.Open module0 classes / 18 functionsfolio_docs.docs.mdx_writerModule documentation.Open module1 class / 3 functionsfolio_docs.docs.next_runtimeModule documentation.Open module0 classes / 13 functionsfolio_docs.docs.sidebarModule documentation.Open module1 class / 0 functionsfolio_docs.docs.site_builderModule documentation.Open module1 class / 0 functionsfolio_docs.docs.static_rewriterModule documentation.Open module2 classes / 21 functionsfolio_docs.docs.template_workspaceModule documentation.Open module0 classes / 1 functionfolio_docs.docs.theme_contract_codegenGenerate TypeScript type definitions from Python theme contract.Open module0 classes / 2 functionsfolio_docs.docs.theme_package_validatorValidator for theme packages to ensure they don't contain reserved paths.Open module0 classes / 5 functionsfolio_docs.docs.xrefModule documentation.Open module6 classes / 6 functionsfolio_docs.extensionsModule documentation.Open module0 classes / 6 functionsfolio_docs.featuresModule documentation.Open module8 classes / 0 functionsfolio_docs.irModule documentation.Open module1 class / 6 functionsfolio_docs.link_checkerBuild-time link checker for internal documentation links.Open module0 classes / 0 functionsfolio_docs.parserModule documentation.Open module1 class / 4 functionsfolio_docs.parser.markdown_parserModule documentation.Open module0 classes / 10 functionsfolio_docs.parser.python_parserModule documentation.Open module0 classes / 1 functionfolio_docs.pathsShared path-resolution helpers with project containment guards.Open module7 classes / 10 functionsfolio_docs.pluginModule documentation.Open module0 classes / 0 functionsfolio_docs.schemasModule documentation.Open module0 classes / 0 functionsfolio_docs.schemas.theme_contractCanonical theme contract for Folio.Open module0 classes / 1 functionfolio_docs.signaturesShared rendering for source signatures consumed by both products.Open module0 classes / 1 functionfolio_docs.slugsShared URL and identifier slug normalization.Open module2 classes / 2 functionsfolio_docs.sourcesModule documentation.Open module1 class / 21 functionsfolio_docs.watcherModule documentation.Open module0 classes / 1 functionfolio_docs.workflowsModule documentation.Open module."},{"url":"docs/deployment/index.html","title":"Deployment","content":"Ask AI. Deployment. Folio builds a static site into _site/. Deploy that folder to any static host. The internal .build/ workspace is only a cache and should not be committed or used as the public artifact. Deployment has one artifact and several delivery paths: PathUse when Static hosts. You want Vercel, Netlify, or a static web server to build or serve _site/. GitHub Pages. You want production docs and optional PR previews hosted by GitHub Pages. CI/CD. You want GitHub Actions or another pipeline to build, check, and publish docs automatically. Static Hosts. Deploy _site/ to Vercel, Netlify, or a small self-hosted static server. GitHub Pages. Configure base paths, Pages artifacts, production deploys, and branch previews. CI/CD. Automate deployment, branch previews, pull request checks, and coverage gates. Build Once. Run the build from your project root: The _site/ directory contains HTML, assets, Pagefind search data, llms.txt, and llms-full.txt. flowchart LR Source[\"Python and Markdown sources\"] --> Build[\"uv run folio build --clean\"] Build --> Site[\"_site/ static artifact\"] Site --> Host[\"Static host\"] Site --> Pages[\"GitHub Pages\"] Site --> Pipeline[\"CI/CD publish job\"] Base Paths. Most hosts serve docs at the root. Use a base path when the generated site is published under a subpath such as /my-repo. Base path priority is: FOLIO_BASE_PATH environment variable. deploy.base_path in docs.yaml. GitHub Pages inference when deploy.provider: \"github-pages\" or FOLIO_DEPLOY_PROVIDER=github-pages is active in GitHub Actions. No base path. project.url is metadata only. It feeds sitemap and canonical URLs, but it does not control local routing or static asset paths. Choose a Strategy. Use Static Hosts for Vercel, Netlify, Docker, Caddy, Nginx, or any static file server. Use GitHub Pages when the public artifact is a GitHub Pages site and you need Pages-specific base path behavior. Use CI/CD when deployment should happen automatically on pushes, releases, pull requests, or scheduled workflows."},{"url":"docs/api-reference/folio_docs/parser/markdown_parser/index.html","title":"folio_docs.parser.markdown_parser source","content":"Ask AI. folio_docs.parser.markdown_parser source. Classes. @dataclass. class MarkdownResult. source Functions. _extract_first_paragraph source. NameTypeDefaultDescription content. str. - Returns: str - parse_markdown_file source. NameTypeDefaultDescription path. Path. - Returns: MarkdownResult - source_route source. The route a docs source publishes for a Markdown file at relative. One rule, shared with everything that names a published page (the kanban plugin resolves doc: artifacts through it): the .md suffix comes off, and a README is its folder\u2019s page. NameTypeDefaultDescription relative. Path. - Returns: str - parse_markdown_directory source. NameTypeDefaultDescription directory. str. - route_prefix. str. '' Returns: list[MarkdownResult] -"},{"url":"docs/api-reference/folio_docs/docs/index.html","title":"folio_docs.docs source","content":"Ask AI. folio_docs.docs source. Folio Docs: human-facing documentation generation and site output."},{"url":"docs/components/steps/index.html","title":"Steps","content":"Ask AI. Steps. Numbered step-by-step instructions with a vertical timeline. Steps are numbered automatically using CSS counters \u2014 you do not provide numbers manually. Use Steps for tutorials, setup guides, and any sequential process. API. Steps. PropTypeDefaultDescription children. ReactNode. \u2014 One or more <Step> elements. Step. PropTypeDefaultDescription title. string. \u2014 The step heading. children. ReactNode. \u2014 The step content (text, code, components, etc.). Example. Install the package. Run uv add my-lib to install. Create a config file. Run my-lib init to generate a default configuration. Start the server. Run my-lib serve to start the development server."},{"url":"docs/api-reference/folio_docs/docs/extension_emitter/index.html","title":"folio_docs.docs.extension_emitter source","content":"Ask AI. folio_docs.docs.extension_emitter source. Classes. class ExtensionEmitter. source __init__ source. Emit registry extensions into build_dir. inject_builtins=False keeps builtin-origin components out of mdx-components.tsx \u2014 used for custom template.path builds that do not bundle the builtin component files. project_dir anchors relative component source_path values (instead of the process CWD). NameTypeDefaultDescription build_dir. str | Path. - inject_builtins. bool. True. project_dir. str. '' Returns: None - apply source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - _copy_components source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - _component_target source. NameTypeDefaultDescription component. ComponentDefinition. - source. Path. - Returns: Path - _write_data_modules source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - _write_data_module source. NameTypeDefaultDescription module. DataModuleDefinition. - Returns: None - _inject_mdx_components source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - _write_views source. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None - _write_view source. NameTypeDefaultDescription view. ViewDefinition. - registry. ExtensionRegistry. - Returns: None - _app_route_path source. NameTypeDefaultDescription route. str. - Returns: Path - @staticmethod. _component_import_line source. NameTypeDefaultDescription component. ComponentDefinition. - Returns: str - @staticmethod. _layout_import_line source. NameTypeDefaultDescription layout. LayoutDefinition. - Returns: str -"},{"url":"docs/components/code-blocks/index.html","title":"Code Blocks","content":"Ask AI. Code Blocks. Fenced code blocks with syntax highlighting, line highlighting, line numbers, filenames, and word highlighting \u2014 powered by Shiki and rehype-pretty-code . Code blocks use standard Markdown fenced code block syntax with meta strings for additional features. Copy controls appear when readers hover over or focus a code block. API. All features are controlled via the meta string after the language identifier: FeatureSyntaxDescription Line highlighting. {2,4-6} Highlight specific lines. Commas separate individual lines, dashes define ranges. Line numbers. showLineNumbers. Display line numbers in the gutter. Filename. filename=\"app.py\" Display a filename header above the code block. Word highlighting. \"print\" Highlight all occurrences of a specific word or string. Disable copy. copy=false. Hide the hover and focus copy control for a specific block. Features can be combined in any order on the same code block. Example. Line highlighting. Highlight specific lines with {lines} after the language: Line numbers. Add showLineNumbers to display line numbers: Filename. Add filename=\"...\" to show a file header: Word highlighting. Highlight specific words with quoted strings: Combined features. All meta features work together:"},{"url":"docs/kanban/cards/how-a-published-board-accepts-a-change/index.html","title":"How a published board accepts a change","content":"Ask AI. How a published board accepts a change. Ideas \u00b7 0.2 \u00b7 plan. A published board is read-only until you give it a write path. The dialog exports git commands, but those land nowhere: a GitHub Pages site has no repository to receive them. Three candidate paths exist, each with its cost and what it does to PRODUCT.md\u2019s \u201cstatic forever\u201d commitment. Panel verdict. Three architectures were designed independently and attacked by four adversarial judges (simplicity, data integrity, security and deployment, product\u2026 The card belongs to the board."},{"url":"docs/deployment/github_pages/index.html","title":"GitHub Pages","content":"Ask AI. GitHub Pages. GitHub Pages serves static files. Folio\u2019s Pages integration publishes the same _site/ artifact used by other hosts, with extra handling for project-site base paths and branch previews. Base Path Setup. Set project.url in docs.yaml to the final Pages URL before deploying so sitemap and metadata use the public URL. For GitHub Pages builds, set FOLIO_DEPLOY_PROVIDER=github-pages in the build step or add deploy.provider: \"github-pages\" to docs.yaml. Folio then infers /repo-name for project pages from GITHUB_REPOSITORY, while user and organization pages such as owner.github.io stay at /. Use FOLIO_BASE_PATH or deploy.base_path only when you need an explicit override. Production Deploys. Build your docs. Publish the static site. Upload _site/ as the GitHub Pages artifact. Automate with GitHub Actions. Use the workflow in CI/CD when Pages should deploy on push. Branch Previews. folio init creates a second GitHub Actions workflow for branch previews on the same GitHub Pages site. It runs from pull_request_target: PR branch code is built in an unprivileged job, and the privileged deploy job only consumes the static _site/ artifact. The preview appears under /previews/pr-<number>-<branch>/, for example https://owner.github.io/project/previews/pr-17-docs-redesign/. GitHub Pages publishes one artifact for the whole site. Folio keeps production and branch previews together in that single artifact: A main deploy builds production docs at the site root. It restores existing preview folders from the internal folio-pages-state branch. A branch deploy starts from the saved folio-pages-state root, downloads the static preview artifact, replaces only /previews/pr-<number>-<branch>/, and deploys the complete artifact again. That means the production URL stays stable while PR previews are updated independently. The preview index is available at /previews/, and each deploy prints the preview URL and index URL in the Actions summary before verifying both return HTTP 200. Successful preview deploys also create or update one sticky PR comment with the latest preview link, so reviewers do not need to open the Actions run to find it. State Branch. The production workflow and preview workflow cooperate through an internal folio-pages-state branch. This branch stores the last complete Pages artifact so a preview deploy can replace one preview folder without dropping production docs or other previews. Do not edit the state branch manually unless you are intentionally repairing the published Pages artifact."},{"url":"docs/kanban/cards/artifacts_read_from_the_canvas/index.html","title":"Artifacts read from the canvas","content":"Ask AI. Artifacts read from the canvas. Released \u00b7 0.1 \u00b7 feature \u00b7 L. The build publishes what a card produced; reading it still means leaving the board. A doc: artifact opens a compiled page at /docs/kanban/<id>/<stem>/ wearing full documentation chrome, and a \u201cKanban\u201d folder sits in the docs sidebar between \u201cMigrating from Sphinx\u201d and \u201cSource Code\u201d as if board\u2026 Prototype brief \u2014 reading a card's artifacts from the canvas. These are decision prototypes. Nothing here ships as written. They exist so the owner can look at several answers side by side and pick one.\u2026 Four readers compared. Built 2026-08-24 to answer one question: a card can attach what it produced, but reading it means leaving the board, either for a compiled page under\u2026 Where card pages publish. Written 2026-08-24, beside four prototypes of reading a card's artifacts from the board canvas. The prototypes answer how a human reads a card's\u2026 board-data.js. board-data.js. board-reader-page.css. board-reader-page.css. Board reader page. board/cards/artifacts-read-from-the-canvas/board-reader-page.html. board-reader-page.js. board-reader-page.js. dialog-reader.css. dialog-reader.css. Dialog reader. board/cards/artifacts-read-from-the-canvas/dialog-reader.html. dialog-reader.js. dialog-reader.js. reader-data.js. reader-data.js. reading-overlay.css. reading-overlay.css. Reading overlay (recommended). board/cards/artifacts-read-from-the-canvas/reading-overlay.html. reading-overlay.js. reading-overlay.js. reading-rail.css. reading-rail.css. Reading rail. board/cards/artifacts-read-from-the-canvas/reading-rail.html. reading-rail.js. reading-rail.js. The card belongs to the board."},{"url":"docs/components/method_accordion/index.html","title":"MethodAccordion","content":"Ask AI. MethodAccordion. Expandable accordion panels for documenting class methods. Each method shows its name and signature in the header, with the full description revealed on click. Async methods display an \u201casync\u201d badge. This component is typically auto-generated for classes with public methods. API. MethodAccordion. PropTypeDefaultDescription methods. Method[] \u2014 Array of method objects. Method object. FieldTypeDefaultDescription name. string. \u2014 Method name. signature. string. \u2014 Parameter signature (e.g. (self, x: int) -> bool). description. string. \u2014 Description of what the method does. isAsync. boolean. false. If true, displays an \u201casync\u201d badge next to the name. children. ReactNode. \u2014 Additional content (parameter tables, examples, etc.) rendered inside the accordion. Example. asyncconnect(self, address: str, timeout: float = 30.0) -> None. disconnect(self) -> None. asyncsend(self, data: bytes, compress: bool = True) -> int."},{"url":"docs/kanban/cards/the-board-reads-as-a-tree/prototypes-compared/index.html","title":"Decomposition views for the board \u2014 five prototypes compared","content":"Ask AI. Decomposition views for the board \u2014 five prototypes compared. Built 2026-08-20 to answer one question: the cards carry a parent field that nothing renders, so what should render it. Five layouts were built in full, against the real 35-card board, and compared side by side rather than argued on paper. Each was then verified adversarially in a headless browser. The prototypes are here, beside this document and beside the card both belong to. None of it is product code and none of it will become product code; it is the evidence a decision was made on, kept where the decision is recorded. Open any .html in this directory to see the layout it argues for. The question. parent has been in the cardfile format since it shipped: one card id, validated against the board, settable from the CLI, already a filter field. The guide states the gap plainly \u2014 \u201cparent is a validated pointer, not a workflow\u201d. On a board of 35 cards, none used it. The pointer existed and the shape it pointed at was invisible, so nobody reached for it. Because no real hierarchy existed, the prototypes ran on an invented one, chosen to force the cases that break naive implementations: three levels deep, and one child sitting in a different column than its parent. The five. Tree table. One table for the whole board, the first column carrying the tree by indentation, every other column a field read straight down. The literal reading of the request and the baseline for the rest. Epic swimlanes. The kanban columns kept; rows become lanes, one per epic, with children placed in the column their own status names. Decomposition and status as two axes of one grid. Board with inline expansion. The incumbent column board, minimally changed: a card with children grows a disclosure and lists them inside itself. Tree rail and detail. A persistent outline rail on the left, the selected node\u2019s children as a table on the right. Master-detail, the shape a product tool with deep decomposition converges on. Document outline. The board as one nested document: no rules, no cells, indentation and typography carrying depth. The argument is that these cards are markdown files and a plan should read like a plan. What was chosen. Tree table, with the column board kept untouched beside it. Columns are the right reading for what is in flight and the wrong reading for what something breaks into. The two readings want different shapes, and the board already works \u2014 so the answer is a second view over the same files, not a replacement. Taken from the losing variants: the DIVERGED marker and its toolbar count from the tree table\u2019s own answer to the cross-column child, and the subtree rollup header from tree rail and detail. Rejected, with the reason worth keeping: Swimlanes resolve the cross-column child for free, which is the strongest single argument any variant made. They also encode exactly one level of decomposition \u2014 a grandchild cannot get a lane without shredding the grid \u2014 and they are mostly air: six lanes by four columns is twenty-four cells, of which this board fills nine, measuring 4,457px tall at 1440px. Inline expansion costs nothing to learn, and a deep tree stretches a column without end. Tree rail and detail scales furthest and shows the least at once. Document outline reads best and scans worst. Comparing one field across 35 rows is a saccade per line where a table is one glance down a column. What five independent implementations all got wrong. This is the part that justifies building five instead of one. A defect that every implementation reaches independently is a property of the problem, not of the implementation, and the real one will reach it too. \u201dDone\u201d has no meaning on this board. All five defined a parent\u2019s progress as how many descendants reached the last column. Released is empty, and In progress is empty. Every parent therefore printed 0 of N while its distribution bar rendered full \u2014 truthful, and it reads as a contradiction. Three separate verification passes reached the same conclusion independently: this is not an execution defect and cannot be fixed by adjusting the widget. Redefining done to include In review would be a lie about the board. The open question, which is a product decision: what counts as done for a parent \u2014 the last column, the ratio of checked acceptance criteria across the subtree, or nothing at all. Until it is answered, any rollup shipped will read as a constant zero on this board. Collapsing or filtering throws the focus out of the tree. Three of the five lost keyboard focus when the tree re-rendered, each for a different reason: reading document.activeElement after innerHTML had already been replaced; leaving the only tabindex=\"0\" on a node that collapsing had set to display: none; and dropping the roving tab stop when a filter removed the active row. Three independent implementations hitting the same class of bug means a re-rendering tree must decide explicitly where focus lands when the focused row stops existing. The rule that worked: move to the nearest still-rendered ancestor, and fall back to the first row. A roving tab stop leaks. One implementation promoted rows to tabindex=\"0\" without demoting the previous holder; after four arrow presses a single column had five tab stops while its own comment claimed one. Filter highlighting falls back to the browser default. Two variants styled <mark> in some contexts and not others, so matching text in an unstyled context painted #ff0 into an oklch palette, most visible in dark mode. A match can hide inside a collapsed branch. One filter marked ancestors as context only when the ancestor was not itself a match, so a match nested under another match never rendered while the toolbar counted it. A filtered tree must open every ancestor of every match, and must restore the hand-set collapse state when the filter clears. Cycles are not validated. Found while designing, not while prototyping. _validate_relations checks that parent names an existing card and is not the card itself. It does not check that following the chain terminates, so a \u2192 b \u2192 a passes the build today. This is harmless only because nothing walks the chain. Every prototype had to carry its own visited set, and each was tested by injecting a two-cycle, a three-cycle, a self-parent and a dangling parent. The fix belongs in the loader, before anything renders a tree. A note on the NUL byte. template/components/kanban-board.tsx wrote a cache-key separator as a literal NUL byte inside a template string. It worked, and it made the file binary to grep and diff. The brief warned the prototype agents about it. One of them, while fixing an unrelated markdown bug, wrote four raw NUL bytes into its own source \u2014 and caught it. The failure mode is easy to reach by accident, which is the argument for fixing the original rather than documenting it. The branch now writes an escaped separator, so the component is ordinary UTF-8 text again. Where this went. the-board-reads-as-a-tree \u2014 the epic, and the choice recorded above. a-parent-cycle-fails-the-build \u2014 the loader gap. the-board-component-splits-into-modules \u2014 the 4312-line file, and the NUL byte. the-table-is-a-second-view \u2014 the view switch and the URL. the-table-draws-one-row-per-card \u2014 the row grammar. the-tree-filters-without-re-rooting \u2014 filter semantics. a-parent-says-what-it-breaks-into \u2014 the rollup and the divergence marker. the-cli-prints-the-tree \u2014 the agent-facing surface."},{"url":"docs/api-reference/folio_docs/sources/index.html","title":"folio_docs.sources source","content":"Ask AI. folio_docs.sources source. Classes. @dataclass. class ParsedPythonSources. source @dataclass. class ParsedDocSources. source Functions. parse_python_sources source. NameTypeDefaultDescription config. Config. - Returns: ParsedPythonSources - parse_doc_sources source. NameTypeDefaultDescription config. Config. - Returns: ParsedDocSources -"},{"url":"docs/api-reference/folio_docs/docs/static_rewriter/index.html","title":"folio_docs.docs.static_rewriter source","content":"Ask AI. folio_docs.docs.static_rewriter source. Classes. class StaticAssetRewriter. source __init__ source. NameTypeDefaultDescription output_dir. str | Path. - Returns: None - fix_asset_paths source. Rewrite local URLs so static files work when opened via file://. Returns: None - _copy_opengraph_images_with_png_extension source. Returns: None - @staticmethod. _rewrite_opengraph_image_urls source. NameTypeDefaultDescription content. str. - Returns: str - rewrite_file_urls source. NameTypeDefaultDescription content. str. - fpath. Path. - Returns: str - rewrite_file_url source. NameTypeDefaultDescription url. str. - fpath. Path. - route_index. bool. False. Returns: str - _is_next_asset source. NameTypeDefaultDescription fpath. Path. - Returns: bool - _patch_next_runtime_asset_prefix source. NameTypeDefaultDescription fpath. Path. - Returns: None - @staticmethod. _stash_script_bodies source. NameTypeDefaultDescription content. str. - Returns: tuple[str, list[str]] - @staticmethod. _restore_script_bodies source. NameTypeDefaultDescription content. str. - script_bodies. list[str] - Returns: str - _write_file_search_fallback source. Returns: bool - @staticmethod. _read_pagefind_fragment source. NameTypeDefaultDescription fragment_path. Path. - Returns: dict | None - _pagefind_file_url source. NameTypeDefaultDescription url. str. - Returns: str - @staticmethod. _inject_file_search_script source. NameTypeDefaultDescription content. str. - Returns: str - @staticmethod. _file_search_script source. NameTypeDefaultDescription index_json. str. - Returns: str - _file_url_target_under_root source. NameTypeDefaultDescription target_path. Path. - root. Path. - Returns: Path | None - @staticmethod. _file_url_target source. NameTypeDefaultDescription target_path. Path. - Returns: Path | None -"},{"url":"docs/kanban/cards/the-board-takes-the-name-and-absorbs-the-roadmap/panel-verdict/index.html","title":"Panel verdict \u2014 the board takes the name and absorbs the roadmap","content":"Ask AI. Panel verdict \u2014 the board takes the name and absorbs the roadmap. Five agents, 2026-08-28: one merge assessor, three name generators (plain-literal, market, prior-art lenses), one adversary with kill authority. The owner confirmed the verdict the same day: folio board, roadmap included. Question one: does the roadmap plugin fold into the board plugin? Four options were weighed. The decisive facts came from the code, not from taste: The two plugins already cannot render their own pages without peeking at each other\u2019s raw config in three places (kanban.py:174 reads roadmap:, roadmap.py reads config.extra['kanban'], kanban.py reads config.extra['roadmap'] back). Phases are the only board-adjacent data with no git history, on a product that sells on git history. Phase 0.4\u2019s shipped public text says \u201cthe roadmap reads from the board\u201d. Under the current dependency direction (the board reads FROM the roadmap) that sentence is unshippable without either this merge or a two-sources-of-truth sync mechanism of the kind this project just spent a milestone eliminating for cards. Rejected \u2014 status quo: defers a decision the 0.4 text already made in public, and the cheap-breakage window (pre-beta) is exactly the window that closes. Renaming around a boundary you intend to erase pays the ~1350-mention rename cost twice. Rejected \u2014 one data model, two plugins: takes the full migration cost of the merge while keeping the coordination cost of the seam; the \u201cindependent\u201d renderer would have a hard load-order dependency on the board plugin anyway. Acceptable only as a one-commit intermediate step. Chosen \u2014 merge with preserved surfaces: Milestones/phases move out of docs.yaml into board data as a first-class registry; a phase moving next \u2192 active is a commit, same as a card move. /roadmap stays, as a view of that registry, with its own routes toggle independent of the board\u2019s visibility \u2014 the landing\u2019s public roadmap can outlive a private board. folio roadmap survives as a top-level command, printing the same registry folio board check validates. A milestone registry with zero cards is legal: the timeline-without- a-board user never has to adopt the full board. Strongest counterargument, recorded: the roadmap is a calm public surface (239 lines that essentially cannot fail) and the board is a volatile operational one mid-rewrite; merging chains the calm surface to the volatile one. Conditions 2 and 4 above are the answer, and they are not optional. Question two: what is the name? 25 candidates from three lenses. Killed with cause: project (collides with the project: site-metadata key on line 1 of docs.yaml), work (collides with the workspace view; ungreppable), backlog (names the whole with one of its columns), tickets, cardfile, cards (overloads the vocabulary\u2019s cleanest term). Top survivors, ranked: board, tracker, tasks, agenda, docket. The bet: board. It is unification, not invention \u2014 the data directory is already board/, the config already says source: board, the commit log already says \u201cboard:\u201d, the landing says \u201cRelease board\u201d, and 0.4 says \u201cthe roadmap reads from the board\u201d verbatim. Plugin name, config key, CLI word, route, directory, and roadmap promise collapse into one word already in use. \u201cThe board\u2019s kanban view\u201d and \u201cthe board\u2019s roadmap view\u201d read as natural English; kanban demotes to the name of the column view. board emerged independently from all three lenses, each placing \u201cProject OS\u201d (the 0.4 phase title) above it as the family umbrella. Runner-up for the record: tracker \u2014 the only candidate that names the function (\u201ca file-based project tracker your agents operate through commits\u201d), held back by bug-tracker heritage and the telemetry smell of a public /tracker route. The honesty rule, applied to the name. The owner asked for a name that says organization and agent orchestration. The panel\u2019s ruling: the product today does not orchestrate agents \u2014 agents operate the board through the CLI and SKILL.md; folio does not schedule or supervise them (that is the agents-claim-and-orchestrate idea, unshipped). A name promising orchestration would violate the project\u2019s own docs-honesty rule for as long as that stays true. The orchestration story rides in the sentence, not the noun: the board \u2014 files your agents operate through ordinary commits. When orchestration ships, board absorbs it without a second rename. Sequencing. Merge and rename happen in the same pre-beta breaking window: the rename touches every one of the ~1350 \u201ckanban\u201d mentions anyway, and the merge is what makes the promoted name true. Both known consumers (folio, pguijas/kanban) migrate trivially today; the same breakage costs an order of magnitude more at 0.7. The open sequencing question \u2014 before or after the write path\u2019s Part One lands \u2014 is recorded on the implementation card, not decided here."},{"url":"docs/kanban/cards/artifacts_read_from_the_canvas/where_card_pages_publish/index.html","title":"Where card pages publish","content":"Ask AI. Where card pages publish. Written 2026-08-24, beside four prototypes of reading a card\u2019s artifacts from the board canvas. The prototypes answer how a human reads a card\u2019s output without leaving the board. This note answers the other half of the owner\u2019s verdict, that card pages indexing into /docs is weird: where should the compiled pages live, and which surfaces should carry them. Where a card page goes today. collect_docs (folio/plugins/kanban.py:402) hands every visible .md/.mdx under board/cards/<id>/ to the core pipeline, routed by _card_page_route (kanban.py:800) under _CARD_PAGE_ROOT = \"kanban\" (kanban.py:105). From there the page is an ordinary document: _collect_plugin_docs (folio/build.py:628) parses it with the same parser as any source.docs file, and _reject_duplicate_doc_routes (build.py:676) fails the build on a collision. Six surfaces then pick it up, and each deserves its own verdict. Route. /docs/kanban/<id>/<stem>/, full documentation chrome, linked from the artifact tile by _resolve_artifact_hrefs (kanban.py:275). The address itself is sound: a decision document with a stable URL is the point of compiling at all. The weirdness lives in what surrounds it. Docs sidebar. Listed, and this is the leak that prompted the verdict. _write_meta_pages (build.py:795) feeds every document, plugin-contributed included (merged at build.py:593), to the sidebar generator, and _generate_doc_meta (folio/generator/sidebar.py:263) writes a _meta.ts entry for each. The current build shows the result: a \u201cKanban\u201d folder sits between \u201cMigrating from Sphinx\u201d and \u201cSource Code\u201d (.build/content/_meta.ts), holding a title-cased folder per card, \u201cThe Board Reads As A Tree\u201d (.build/content/kanban/_meta.ts). The sidebar is the documentation\u2019s table of contents; board working papers have no place in its teaching order. Wrong surface. Search. Indexed. The Pagefind postbuild covers {index.html,docs/**/*.html} (template/package.json:9): everything under /docs and nothing else \u2014 the standalone /kanban board page is not indexed. Right surface. Search is how a reader who does not know the board\u2019s layout finds \u201cswimlanes\u201d in the comparison document; the canvas filter searches card fields, not artifact bodies. Sitemap. Listed. template/app/sitemap.ts walks content/*.mdx (lines 15-24) and emits each page at priority 0.6 plus its Markdown mirror at 0.3 (lines 41-48, 87-92). Right, or at worst harmless: the sitemap lists what is published, and hiding a live page from crawlers would not make it less published. llms.txt. Present in both files: generate_llms_txt links every document (folio/generator/llm_output.py:229) and generate_llms_full_txt embeds the whole body (llm_output.py:269); both take the merged docs list (build.py:1096 and 1101). Right surface, and explicitly promised \u2014 the owning card\u2019s checked criterion reads \u201ca .md sibling renders as a page \u2026 in the search index and in llms.txt\u201d (artifacts-live-beside-their-card.md:129). Markdown mirrors. Written. _write_page_markdown (folio/generator/site_builder.py:540) puts public/_folio/markdown/kanban/<id>/<stem>.md beside every compiled page, and the docs head declares it as a text/markdown alternate (template/app/docs/[[\u2026mdxPath]]/page.jsx:43-81). Right surface; this is the copy an agent actually fetches. The raw bundle at /_folio/kanban/<id>/ (_publish_card_assets, kanban.py:724) sits on none of these surfaces, which is correct for emitted output. So the problem is one surface out of six. Route, search, sitemap, llms output and mirrors behave exactly as a published decision document should. The sidebar listing is the part that presents board output as documentation. The options. (a) Keep compiling under /docs; delist deliberately. The sidebar generator cannot currently tell a plugin document from an authored one: MarkdownResult carries no origin, so _generate_doc_meta lists everything it is given. Delisting means carrying an unlisted flag from PluginDocument through _collect_plugin_docs into the meta generator and emitting {\"display\": \"hidden\"} for the subtree. The generator already hides nested index pages with exactly that value (sidebar.py:316); omission alone is not enough, because Nextra lists content pages by default and _meta.ts only orders and hides. Everything else stays. Per surface: sidebar out, search keep, sitemap keep, llms.txt keep, mirrors keep. (b) Compile under the board\u2019s namespace, /kanban/<id>/<stem>/, in board chrome. This costs most of the machinery that makes the pages cheap. There is no second content tree: SiteBuilder._page_path (site_builder.py:527) roots every compiled page in content/, and the one catch-all route serving that tree lives under the docs base, so board-chrome pages need a second catch-all route and layout in the template. The standalone view mechanism cannot carry them: _write_view (folio/generator/extension_emitter.py:174) renders registered components with JSON props into a single app route and has no MDX pipeline, so each card page would become a component call holding pre-rendered HTML, a second Markdown path beside the real one, nested under the already-emitted /kanban view (kanban.py:394). Search silently empties, because the Pagefind glob (template/package.json:9) covers docs/** only; the sitemap stops seeing them, because sitemap.ts walks content/*.mdx; mirrors are written per content route (site_builder.py:540); and link validation accepts non-docs routes only as view routes (_check_generated_links, build.py:957), so a tree of board pages is a new class of valid target. Five inherited surfaces rebuilt, to change a breadcrumb and a frame that the page\u2019s remaining visitors barely experience, since they arrive from a pasted link, a search hit, or an agent fetch. (c) Stop compiling; the canvas renders from the raw bundle. Delete or gate collect_docs (kanban.py:402) and the page disappears together with its search entry, sitemap line, llms.txt line, full-text section and mirror, un-checking the owning card\u2019s shipped criterion (artifacts-live-beside-their-card.md:129). Reading moves client-side: the prototypes here read pre-rendered HTML out of reader-data.js precisely because nothing in the browser parses Markdown, so the product would need a runtime renderer or a build step that pre-renders into the bundle \u2014 which is the compile step again, minus every surface it fed. A doc: artifact href (kanban.py:275) reverts to the raw file under /_folio/kanban/, which the code itself calls \u201can unstyled download of something the site already renders\u201d (kanban.py:234). And a deep link has nowhere durable to land except a canvas-state URL that must boot the whole board app before a document is readable. Recommendation. Option (a). Once the canvas is the human reading path, the compiled page has three jobs left: be the durable address a link from outside lands on; be findable, by Pagefind for people and by llms.txt and the mirror for agents; and stay out of the way. Option (a) is the only one that serves all three. Option (c) deletes the first two. Option (b) rebuilds five surfaces to improve the third by a margin nobody arriving at the page will notice. Concretely: card pages stay at /docs/kanban/<id>/<stem>/ and keep search, sitemap, llms output and Markdown mirrors; they leave the docs sidebar, which was the weirdness. The canvas reader and the compiled page then divide the work the way the board and the roadmap already do. The canvas is where a card\u2019s output is read in context; the page is the address that output answers to."},{"url":"docs/components/page_feedback/index.html","title":"PageFeedback","content":"Ask AI. PageFeedback. A \u201cWas this page helpful?\u201d widget with thumbs up/down buttons. After clicking, the widget displays a confirmation message and stores the response in the browser\u2019s localStorage under the key page-feedback, keyed by page path. This component is used internally by the theme layout and is typically placed at the bottom of documentation pages. It takes no props. API. PageFeedback. This component takes no props. Behavior: Displays thumbs up/down buttons. On click, shows \u201cGlad it helped!\u201d or \u201cThanks, we\u2019ll improve this page.\u201d Feedback is stored per-page in localStorage and cannot be resubmitted (until localStorage is cleared). Example. Layout Component. PageFeedback is rendered at the bottom of every documentation page by the theme layout. Scroll to the bottom of this page to see the \u201cWas this helpful?\u201d widget in action."},{"url":"docs/kanban/cards/how_a_published_board_accepts_a_change/index.html","title":"How a published board accepts a change","content":"Ask AI. How a published board accepts a change. Ideas \u00b7 0.2 \u00b7 plan. A published board is read-only until you give it a write path. The dialog exports git commands, but those land nowhere: a GitHub Pages site has no repository to receive them. Three candidate paths exist, each with its cost and what it does to PRODUCT.md\u2019s \u201cstatic forever\u201d commitment. Panel verdict. Three architectures were designed independently and attacked by four adversarial judges (simplicity, data integrity, security and deployment, product\u2026 The card belongs to the board."},{"url":"docs/components/code_group/index.html","title":"CodeGroup","content":"Ask AI. CodeGroup. Tabbed code blocks for showing the same concept in multiple languages or multiple variants of a command. Tabs are automatically labeled from the language of each code block, or you can provide explicit labels. API. CodeGroup. PropTypeDefaultDescription labels. string[] \u2014 Optional explicit tab labels. If omitted, labels are auto-detected from each code block\u2019s language (e.g. python, bash). children. ReactNode. \u2014 Two or more fenced code blocks to display as tabs. Label auto-detection tries, in order: a title prop, a data-language attribute, a language-* CSS class, and falls back to Tab N. Example. Auto-detected labels from language: Tab 1 Tab 2. With explicit labels: pip uv conda."},{"url":"docs/api-reference/folio_docs/signatures/index.html","title":"folio_docs.signatures source","content":"Ask AI. folio_docs.signatures source. Shared rendering for source signatures consumed by both products. Functions. render_signature source. Render a Python function signature without product-specific markup. NameTypeDefaultDescription func. FunctionIR. - show_parens. bool. True. Returns: str -"},{"url":"docs/api-reference/folio_docs/config/index.html","title":"folio_docs.config source","content":"Ask AI. folio_docs.config source. Classes. @dataclass. class Config. source __post_init__ source. Returns: None - resolve_paths source. NameTypeDefaultDescription base. Path. - Returns: Config - Functions. _normalize_project_repo_ref source. NameTypeDefaultDescription value. Any. - Returns: str - resolve_output_dir source. Resolve output: and refuse anything the build would destroy. A successful export ends in shutil.rmtree(output_dir) followed by a copy, and folio clean removes the same path. Containment inside the project is therefore not enough: an output that is a source directory, or that contains one, deletes the very files the build just read. Callers that know the configured sources should pass them as source_paths. NameTypeDefaultDescription base. Path. - output_dir. str. - source_paths. Sequence[str] (). Returns: Path - resolve_theme_package_path source. Resolve theme.package against the project root with containment checks. Delegates to :func:folio_docs.paths.resolve_contained_dir (the same guard used for template.path): the path is resolved relative to the project root and rejected if it escapes the project directory (absolute paths or .. traversal) or lands inside the .build or output directories. Existence is not required here because folio_docs.build validates it later. NameTypeDefaultDescription base. Path. - theme_package_path. str. - output_dir. str. - Returns: str - _load_config_plugins source. Load first-party default plugins, then the project\u2019s plugins: entries. Default plugins are loaded for every build via load_default_plugins (imported directly, never through entry-point lookup; a broken default degrades to a warning instead of failing the build); a project listing one of them explicitly does not register it twice. A non-list plugins: value raises ValueError so the misconfiguration fails loudly. NameTypeDefaultDescription plugin_names. Any. - base_dir. Path. - Returns: PluginManager - plugin_config_keys source. Collect the extra config keys declared by loaded plugins. Shared by config loading and the CLI (version-matrix sync) so both treat an invalid config_keys() result the same way: warn and skip. NameTypeDefaultDescription pm. PluginManager. - Returns: set[str] - _split_components source. NameTypeDefaultDescription raw_components. Any. - Returns: tuple[list[str], list[dict[str, Any]]] - _normalize_component_spec source. NameTypeDefaultDescription entry. dict[str, Any] - Returns: dict[str, Any] - _resolve_component_specs source. NameTypeDefaultDescription specs. list[dict[str, Any]] - base. Path. - Returns: list[dict[str, Any]] - _disabled_known_config_keys source. NameTypeDefaultDescription raw. dict[str, Any] - Returns: set[str] - _theme_string source. NameTypeDefaultDescription raw_value. Any. - Returns: str - _theme_preview source. NameTypeDefaultDescription raw_preview. Any. - Returns: dict[str, str] - _theme_swatch source. NameTypeDefaultDescription raw_value. Any. - path. str. - Returns: str - _theme_text source. NameTypeDefaultDescription raw_value. Any. - path. str. - Returns: str - _theme_href source. Sanitize a URL/path value, rejecting unsafe schemes (e.g. javascript:). Builds on :func:_theme_text (unsafe-character rejection), then requires either an http(s)/mailto URL or a scheme-less relative path. Any other scheme (javascript:, data:, vbscript:, \u2026) is rejected. NameTypeDefaultDescription raw_value. Any. - path. str. - Returns: str - _repo_url source. Validate a repository URL without restricting it to web schemes. Unlike :func:_theme_href (which guards values rendered as <a href>), repository URLs commonly use ssh://, git://, git+https://, the scp-style git@host:path form, or plain strings, all of which are accepted verbatim. Only schemes that could execute script or read local files (javascript:, data:, vbscript:, file:) are rejected. NameTypeDefaultDescription raw_value. Any. - path. str. - Returns: str - _theme_radius source. Validate theme.radius against the fixed radius scale. The template maps the configured radius onto a fixed index scale; any value outside the scale would silently render as the 0.5rem default, so unknown values are rejected up front. Legacy named values (none, sm, md, lg, full) are accepted as aliases for the scale values so configs written against older docs keep building. NameTypeDefaultDescription raw_value. Any. - Returns: str - _theme_bool source. NameTypeDefaultDescription raw_value. Any. - path. str. - Returns: bool | None - _validate_theme_css_value source. NameTypeDefaultDescription value. Any. - path. str. - Returns: str - _theme_css_vars source. NameTypeDefaultDescription raw_vars. Any. - path. str. - Returns: dict[str, str] - _theme_tokens source. NameTypeDefaultDescription raw_tokens. Any. - Returns: dict[str, dict[str, str]] - _theme_header source. NameTypeDefaultDescription raw_header. Any. - Returns: dict[str, Any] - _theme_variant_id source. NameTypeDefaultDescription raw_id. Any. - path. str. - Returns: str - _theme_variant_option source. NameTypeDefaultDescription raw_option. Any. - path. str. - Returns: dict[str, Any] - _theme_variants source. NameTypeDefaultDescription raw_variants. Any. - Returns: dict[str, dict[str, Any]] - _theme_tune source. NameTypeDefaultDescription raw_tune. Any. - Returns: dict[str, str] - _theme_package_path source. NameTypeDefaultDescription raw_theme. Any. - Returns: str - _template_path source. NameTypeDefaultDescription raw_template. Any. - Returns: str - _template_overlay_path source. Validate the opt-in template.overlay_path partial-override key. template.overlay_path layers user-owned files on top of the bundled template (user wins, missing files fall back to the bundled template). It is mutually exclusive with template.path (a full replacement): configuring both is ambiguous, so the overlay is ignored with a warning and the full replacement wins. See docs/guide/theming/custom-templates.md. NameTypeDefaultDescription raw_template. Any. - Returns: str - _template_params source. Validate template.params against its documented contract. template.params is an arbitrary JSON-serializable mapping that Folio emits verbatim into lib/folio-template.ts as folioTemplateParams; Folio never interprets the values. The contract is: absent (template not a mapping or no params key) -> {} null -> {} a non-mapping value (list, string, number, \u2026) -> {} with a warning, because the template can still build with empty params. a mapping that is not JSON-serializable -> a clear ValueError, because it cannot be emitted into folio-template.ts and would break the build. See docs/guide/theming/custom-templates.md for the full contract. NameTypeDefaultDescription raw_template. Any. - Returns: dict[str, Any] - _template_docs_route_base source. NameTypeDefaultDescription raw_template. Any. - Returns: str - normalize_docs_route_base source. NameTypeDefaultDescription value. Any. - Returns: str - normalize_base_path source. NameTypeDefaultDescription value. Any. - Returns: str - load_config source. NameTypeDefaultDescription path. Path. - Returns: Config - _config_mapping source. NameTypeDefaultDescription raw. dict[str, Any] - key. str. - Returns: dict[str, Any] - _config_string_list source. NameTypeDefaultDescription value. Any. - key. str. - Returns: list[str] - load_config_with_plugins source. NameTypeDefaultDescription path. Path. - plugin_base_dir. Path | None. None. Returns: tuple[Config, PluginManager] -"},{"url":"docs/api-reference/folio_docs/build/index.html","title":"folio_docs.build source","content":"Ask AI. folio_docs.build source. Classes. @dataclass. class BuildSources. source @dataclass. class GenerationResult. source @dataclass. class ExportResult. source class _BuildOutputStream. source __init__ source. Returns: None - __enter__ source. Returns: '_BuildOutputStream' - __exit__ source. NameTypeDefaultDescription exc_type. Any. - exc. Any. - traceback. Any. - Returns: None - record source. NameTypeDefaultDescription line. str. - Returns: None - Functions. _find_template_dir source. Returns: Path - _validate_template_contract source. Validate a resolved template directory against the Folio contract. Applies the required-files, MDX-component, and injection-marker checks to a directory that Folio will copy into .build/. label names the config key (template.path or the merged template.overlay_path result) so errors point at the right surface. NameTypeDefaultDescription template_dir. Path. - label. str. - Returns: None - _check_bundled_template_drift source. Fail the build when the bundled template drifted from the manifest. Bundled-template builds inject builtin components from. NameTypeDefaultDescription template_dir. Path. - Returns: None - _resolve_overlay_dir source. Resolve template.overlay_path against the project root. Applies the same containment guards as template.path via. NameTypeDefaultDescription project_dir. Path. - config. Config. - Returns: Path - _materialize_overlay_template source. Build a merged template = bundled template + overlay files on top. Copies the bundled template into staging_dir, then copies the user\u2019s overlay files over it (user wins, missing files fall back to the bundled template). The overlay is scanned for symlinks first \u2014 shutil.copytree dereferences them, so an untrusted overlay could otherwise pull files from outside the tree into the published site. The build directories that are never published are excluded from both the scan and the copy (so a symlink under e.g. the overlay\u2019s content/ is never copied either). The staging dir is recreated on every build, so two guards protect user data: the overlay may not live inside the staging dir, and an existing staging dir is only removed when it carries the Folio staging marker file (proving a previous Folio build created it). NameTypeDefaultDescription bundled_dir. Path. - overlay_dir. Path. - staging_dir. Path. - Returns: Path - _resolve_template_dir source. NameTypeDefaultDescription project_dir. Path. - config. Config. - build_dir. Path | None. None. Returns: Path - _hash_file source. NameTypeDefaultDescription path. Path. - Returns: str - _hash_tree source. NameTypeDefaultDescription root. Path. - Returns: str - _theme_package_signature source. Signature of the resolved theme.package tree (empty when unset). Folded into the manifest build context and compared against the previous build so that changing/removing theme.package triggers a scoped prune of the overlaid files in .build/ (the theme is copied with copytree(dirs_exist_ok=True) and would otherwise leave orphans). Collects the copyable files and rejects symlinks in a single traversal (collect_copyable_files), then feeds the hash from that file list instead of re-walking the tree. NameTypeDefaultDescription resolved. Config. - Returns: str - _prune_stale_build_overlay source. Prune orphaned overlay files when template/theme/route inputs changed. prepare() overlays the template (and any theme.package) with copytree(dirs_exist_ok=True) and never removes files that vanished from the source. When a warm .build/ was produced from a different template or theme, delete everything under it except the preserved entries before prepare re-copies, so stale files cannot survive into the published site. When only template.docs_route_base changed, remove the docs route dir that the previous build relocated to \u2014 nothing else re-copies over it, so it would otherwise stay published at the old URL. NameTypeDefaultDescription prev_manifest. dict. - build_dir. Path. - build_context. dict[str, str] - Returns: None - _remove_relocated_docs_route source. Remove the docs route dir relocated for a previous docs_route_base. Template preparation moves app/docs to app/<route segments>. When the configured route base changes between warm builds, the previously relocated dir is not overwritten by prepare (which only re-copies app/docs), so it must be dropped explicitly \u2014 together with any parent directories the old relocation created that are now empty. NameTypeDefaultDescription build_dir. Path. - route_base. str. - Returns: None - _build_manifest_context source. NameTypeDefaultDescription config_path. Path. - template_dir. Path. - source_ref. str. - theme_package_signature. str. '' docs_route_base. str. '/docs' Returns: dict[str, str] - _print_banner source. NameTypeDefaultDescription config. Config. - include_news. bool. True. Returns: None - _count_phrase source. NameTypeDefaultDescription count. int. - singular. str. - plural. str | None. None. Returns: str - _step_description source. NameTypeDefaultDescription label. str. - detail. str. - Returns: str - _print_step source. NameTypeDefaultDescription label. str. - detail. str. - marker. str. '\u2713' marker_style. str. 'green' label_style. str. 'bold' Returns: None - _print_step_detail source. NameTypeDefaultDescription detail. str. - style. str. 'dim' Returns: None - _build_output_panel source. NameTypeDefaultDescription lines. list[str] - Returns: Panel - _print_build_output source. NameTypeDefaultDescription lines. list[str] - Returns: None - _warn_missing_assets source. NameTypeDefaultDescription missing. list[str] - route. str. - Returns: None - _copy_doc_assets source. Carry a page\u2019s local images into the content tree beside it. An author writes ![The board](board.png) next to their Markdown. MDX turns that into a module import resolved against the generated .mdx, so unless the file travels with the page the build fails outright. Only project-local paths move: an absolute URL, a root-relative path and a data URI are all left for the browser to resolve. NameTypeDefaultDescription builder. SiteBuilder. - doc. MarkdownResult. - Returns: list[str] - _path_traverses_symlink source. Whether target crosses a symlink at or below lexical root. NameTypeDefaultDescription root. Path. - target. Path. - Returns: bool - _parse_project_sources source. NameTypeDefaultDescription resolved. Config. - verbose. bool. - plugin_views. int. 0. plugin_manager. PluginManager | None. None. Returns: BuildSources - _collect_plugin_docs source. Parse plugin-contributed Markdown through the core document parser. NameTypeDefaultDescription plugin_manager. PluginManager | None. - config. Config. - Returns: list[MarkdownResult] - _reject_duplicate_doc_routes source. NameTypeDefaultDescription docs. list[MarkdownResult] - Returns: None - _canonical_doc_route source. Mirror the public docs router\u2019s aliases for collision detection. NameTypeDefaultDescription route. str. - Returns: str - _prepare_builder source. NameTypeDefaultDescription builder. SiteBuilder. - build_dir. Path. - build_context. dict[str, str] - prev_manifest. dict. - clean. bool. - Returns: None - _write_meta_pages source. NameTypeDefaultDescription builder. SiteBuilder. - config. Config. - modules. list[ModuleIR] - docs. list[MarkdownResult] - Returns: None - _published_modules source. NameTypeDefaultDescription modules. list[ModuleIR] - Returns: list[ModuleIR] - _published_docs source. NameTypeDefaultDescription docs. list[MarkdownResult] - Returns: list[MarkdownResult] - _generate_content_pages source. NameTypeDefaultDescription builder. SiteBuilder. - config. Config. - modules. list[ModuleIR] - docs. list[MarkdownResult] - project_dir. Path. - build_context. dict[str, str] - clean. bool. - verbose. bool. - prev_manifest. dict | None. None. Returns: GenerationResult - build_registry source. Assemble the extension registry for a build. Registration order: builtin layouts/components first, then config components:, then plugin hooks. A config or plugin component may shadow a builtin of the same name (the builtin is replaced and a UserWarning is emitted); a duplicate between two non-builtin registrations raises ValueError. Builtins are registered through the same :class:ExtensionRegistry API that plugins use, but via a deterministic direct call (not a pluggy hook) so emission order is stable. NameTypeDefaultDescription pm. object. - resolved. Config. - Returns: ExtensionRegistry - _apply_extensions source. NameTypeDefaultDescription builder. SiteBuilder. - pm. object. - resolved. Config. - registry. ExtensionRegistry | None. None. Returns: None - _check_generated_links source. NameTypeDefaultDescription builder. SiteBuilder. - modules. list[ModuleIR] - docs. list[MarkdownResult] - Returns: list - _build_timestamp source. This build\u2019s UTC start-of-finalize time, for generated metadata. Returns: str - _finalize_generated_files source. NameTypeDefaultDescription builder. SiteBuilder. - pm. object. - resolved. Config. - generation. GenerationResult. - project_dir. Path. - serve. bool. - registry. ExtensionRegistry | None. None. Returns: bool - _check_generated_links_with_progress source. NameTypeDefaultDescription builder. SiteBuilder. - modules. list[ModuleIR] - docs. list[MarkdownResult] - Returns: list - _install_dependencies source. NameTypeDefaultDescription builder. SiteBuilder. - Returns: bool - _export_static_site source. NameTypeDefaultDescription builder. SiteBuilder. - config. Config. - resolved. Config. - modules. list[ModuleIR] - docs. list[MarkdownResult] - pm. object. - Returns: ExportResult - _write_llm_outputs source. Render configured LLM outputs to the active site\u2019s public root. NameTypeDefaultDescription builder. SiteBuilder. - config. Config. - resolved. Config. - modules. list[ModuleIR] - docs. list[MarkdownResult] - serve. bool. False. Returns: int - _start_dev_server source. NameTypeDefaultDescription builder. SiteBuilder. - config. Config. - resolved. Config. - project_dir. Path. - port. int. - open_browser. bool. - verbose. bool. - kill_existing. bool. - plugin_manager. object | None. None. Returns: None - run_build source. NameTypeDefaultDescription project_dir. Path. - serve. bool. False. verbose. bool. False. config_file. str. 'docs.yaml' port. int. 4321. open_browser. bool. False. clean. bool. False. output_override. str. '' current_version_path. str. '' include_versions. bool. False. build_dir_override. str | Path. '' source_ref_override. str. '' quiet. bool. False. kill_existing. bool. False. Returns: None -"},{"url":"docs/kanban/cards/artifact-viewer-becomes-a-workspace/index.html","title":"Artifact viewer becomes a workspace","content":"Ask AI. Artifact viewer becomes a workspace. Released \u00b7 feature \u00b7 L. Opening a card should leave floating layers behind and turn the kanban into one continuous, resizable page. Each selection progresses after its parent: filters lead to the canvas, the card follows the canvas, and the artifact follows the card to the right when space allows or below when it does\u2026 Progressive canvas direction. Replace the current dialog and artifact overlay with one continuous kanban workspace. Each selection adds its next context without obscuring or\u2026 Progressive canvas (revised direction). progressive-canvas.html. The card belongs to the board."},{"url":"docs/kanban/cards/artifacts-read-from-the-canvas/where-card-pages-publish/index.html","title":"Where card pages publish","content":"Ask AI. Where card pages publish. Written 2026-08-24, beside four prototypes of reading a card\u2019s artifacts from the board canvas. The prototypes answer how a human reads a card\u2019s output without leaving the board. This note answers the other half of the owner\u2019s verdict, that card pages indexing into /docs is weird: where should the compiled pages live, and which surfaces should carry them. Where a card page goes today. collect_docs (folio/plugins/kanban.py:402) hands every visible .md/.mdx under board/cards/<id>/ to the core pipeline, routed by _card_page_route (kanban.py:800) under _CARD_PAGE_ROOT = \"kanban\" (kanban.py:105). From there the page is an ordinary document: _collect_plugin_docs (folio/build.py:628) parses it with the same parser as any source.docs file, and _reject_duplicate_doc_routes (build.py:676) fails the build on a collision. Six surfaces then pick it up, and each deserves its own verdict. Route. /docs/kanban/<id>/<stem>/, full documentation chrome, linked from the artifact tile by _resolve_artifact_hrefs (kanban.py:275). The address itself is sound: a decision document with a stable URL is the point of compiling at all. The weirdness lives in what surrounds it. Docs sidebar. Listed, and this is the leak that prompted the verdict. _write_meta_pages (build.py:795) feeds every document, plugin-contributed included (merged at build.py:593), to the sidebar generator, and _generate_doc_meta (folio/generator/sidebar.py:263) writes a _meta.ts entry for each. The current build shows the result: a \u201cKanban\u201d folder sits between \u201cMigrating from Sphinx\u201d and \u201cSource Code\u201d (.build/content/_meta.ts), holding a title-cased folder per card, \u201cThe Board Reads As A Tree\u201d (.build/content/kanban/_meta.ts). The sidebar is the documentation\u2019s table of contents; board working papers have no place in its teaching order. Wrong surface. Search. Indexed. The Pagefind postbuild covers {index.html,docs/**/*.html} (template/package.json:9): everything under /docs and nothing else \u2014 the standalone /kanban board page is not indexed. Right surface. Search is how a reader who does not know the board\u2019s layout finds \u201cswimlanes\u201d in the comparison document; the canvas filter searches card fields, not artifact bodies. Sitemap. Listed. template/app/sitemap.ts walks content/*.mdx (lines 15-24) and emits each page at priority 0.6 plus its Markdown mirror at 0.3 (lines 41-48, 87-92). Right, or at worst harmless: the sitemap lists what is published, and hiding a live page from crawlers would not make it less published. llms.txt. Present in both files: generate_llms_txt links every document (folio/generator/llm_output.py:229) and generate_llms_full_txt embeds the whole body (llm_output.py:269); both take the merged docs list (build.py:1096 and 1101). Right surface, and explicitly promised \u2014 the owning card\u2019s checked criterion reads \u201ca .md sibling renders as a page \u2026 in the search index and in llms.txt\u201d (artifacts-live-beside-their-card.md:129). Markdown mirrors. Written. _write_page_markdown (folio/generator/site_builder.py:540) puts public/_folio/markdown/kanban/<id>/<stem>.md beside every compiled page, and the docs head declares it as a text/markdown alternate (template/app/docs/[[\u2026mdxPath]]/page.jsx:43-81). Right surface; this is the copy an agent actually fetches. The raw bundle at /_folio/kanban/<id>/ (_publish_card_assets, kanban.py:724) sits on none of these surfaces, which is correct for emitted output. So the problem is one surface out of six. Route, search, sitemap, llms output and mirrors behave exactly as a published decision document should. The sidebar listing is the part that presents board output as documentation. The options. (a) Keep compiling under /docs; delist deliberately. The sidebar generator cannot currently tell a plugin document from an authored one: MarkdownResult carries no origin, so _generate_doc_meta lists everything it is given. Delisting means carrying an unlisted flag from PluginDocument through _collect_plugin_docs into the meta generator and emitting {\"display\": \"hidden\"} for the subtree. The generator already hides nested index pages with exactly that value (sidebar.py:316); omission alone is not enough, because Nextra lists content pages by default and _meta.ts only orders and hides. Everything else stays. Per surface: sidebar out, search keep, sitemap keep, llms.txt keep, mirrors keep. (b) Compile under the board\u2019s namespace, /kanban/<id>/<stem>/, in board chrome. This costs most of the machinery that makes the pages cheap. There is no second content tree: SiteBuilder._page_path (site_builder.py:527) roots every compiled page in content/, and the one catch-all route serving that tree lives under the docs base, so board-chrome pages need a second catch-all route and layout in the template. The standalone view mechanism cannot carry them: _write_view (folio/generator/extension_emitter.py:174) renders registered components with JSON props into a single app route and has no MDX pipeline, so each card page would become a component call holding pre-rendered HTML, a second Markdown path beside the real one, nested under the already-emitted /kanban view (kanban.py:394). Search silently empties, because the Pagefind glob (template/package.json:9) covers docs/** only; the sitemap stops seeing them, because sitemap.ts walks content/*.mdx; mirrors are written per content route (site_builder.py:540); and link validation accepts non-docs routes only as view routes (_check_generated_links, build.py:957), so a tree of board pages is a new class of valid target. Five inherited surfaces rebuilt, to change a breadcrumb and a frame that the page\u2019s remaining visitors barely experience, since they arrive from a pasted link, a search hit, or an agent fetch. (c) Stop compiling; the canvas renders from the raw bundle. Delete or gate collect_docs (kanban.py:402) and the page disappears together with its search entry, sitemap line, llms.txt line, full-text section and mirror, un-checking the owning card\u2019s shipped criterion (artifacts-live-beside-their-card.md:129). Reading moves client-side: the prototypes here read pre-rendered HTML out of reader-data.js precisely because nothing in the browser parses Markdown, so the product would need a runtime renderer or a build step that pre-renders into the bundle \u2014 which is the compile step again, minus every surface it fed. A doc: artifact href (kanban.py:275) reverts to the raw file under /_folio/kanban/, which the code itself calls \u201can unstyled download of something the site already renders\u201d (kanban.py:234). And a deep link has nowhere durable to land except a canvas-state URL that must boot the whole board app before a document is readable. Recommendation. Option (a). Once the canvas is the human reading path, the compiled page has three jobs left: be the durable address a link from outside lands on; be findable, by Pagefind for people and by llms.txt and the mirror for agents; and stay out of the way. Option (a) is the only one that serves all three. Option (c) deletes the first two. Option (b) rebuilds five surfaces to improve the third by a margin nobody arriving at the page will notice. Concretely: card pages stay at /docs/kanban/<id>/<stem>/ and keep search, sitemap, llms output and Markdown mirrors; they leave the docs sidebar, which was the weirdness. The canvas reader and the compiled page then divide the work the way the board and the roadmap already do. The canvas is where a card\u2019s output is read in context; the page is the address that output answers to."},{"url":"docs/folio-docs/index.html","title":"Folio Docs","content":"Ask AI. Folio Docs. Folio Docs reads Python source and Markdown guides, then builds the static site people use: API reference, prose pages, navigation, search, theming, and deployable HTML. It never imports the package it documents. folio build and folio serve are the Docs command groups. The package, version, and release process do not depend on Folio for Agents; when Agents is installed, it adds folio board to the same CLI. Code boundary. Documentation generation lives in folio_docs: SiteBuilder owns the generated content workspace and search index. The MDX writer, sidebar, template workspace, theme support, and Next runtime live beside it. Parsing, IR, configuration, plugins, and orchestration are owned by Folio Docs and ship in the same wheel. The old folio namespace is not retained. Integrations use folio_docs and the public plugin contracts. Agent-readable output. Folio Docs itself writes the Markdown mirror for every generated page, the LLM indexes, and the published authoring contract. These are documentation output, not a runtime dependency on Folio for Agents. Continue with the quick start."},{"url":"docs/kanban/cards/the_board_reads_as_a_tree/prototypes_compared/index.html","title":"Decomposition views for the board \u2014 five prototypes compared","content":"Ask AI. Decomposition views for the board \u2014 five prototypes compared. Built 2026-08-20 to answer one question: the cards carry a parent field that nothing renders, so what should render it. Five layouts were built in full, against the real 35-card board, and compared side by side rather than argued on paper. Each was then verified adversarially in a headless browser. The prototypes are here, beside this document and beside the card both belong to. None of it is product code and none of it will become product code; it is the evidence a decision was made on, kept where the decision is recorded. Open any .html in this directory to see the layout it argues for. The question. parent has been in the cardfile format since it shipped: one card id, validated against the board, settable from the CLI, already a filter field. The guide states the gap plainly \u2014 \u201cparent is a validated pointer, not a workflow\u201d. On a board of 35 cards, none used it. The pointer existed and the shape it pointed at was invisible, so nobody reached for it. Because no real hierarchy existed, the prototypes ran on an invented one, chosen to force the cases that break naive implementations: three levels deep, and one child sitting in a different column than its parent. The five. Tree table. One table for the whole board, the first column carrying the tree by indentation, every other column a field read straight down. The literal reading of the request and the baseline for the rest. Epic swimlanes. The kanban columns kept; rows become lanes, one per epic, with children placed in the column their own status names. Decomposition and status as two axes of one grid. Board with inline expansion. The incumbent column board, minimally changed: a card with children grows a disclosure and lists them inside itself. Tree rail and detail. A persistent outline rail on the left, the selected node\u2019s children as a table on the right. Master-detail, the shape a product tool with deep decomposition converges on. Document outline. The board as one nested document: no rules, no cells, indentation and typography carrying depth. The argument is that these cards are markdown files and a plan should read like a plan. What was chosen. Tree table, with the column board kept untouched beside it. Columns are the right reading for what is in flight and the wrong reading for what something breaks into. The two readings want different shapes, and the board already works \u2014 so the answer is a second view over the same files, not a replacement. Taken from the losing variants: the DIVERGED marker and its toolbar count from the tree table\u2019s own answer to the cross-column child, and the subtree rollup header from tree rail and detail. Rejected, with the reason worth keeping: Swimlanes resolve the cross-column child for free, which is the strongest single argument any variant made. They also encode exactly one level of decomposition \u2014 a grandchild cannot get a lane without shredding the grid \u2014 and they are mostly air: six lanes by four columns is twenty-four cells, of which this board fills nine, measuring 4,457px tall at 1440px. Inline expansion costs nothing to learn, and a deep tree stretches a column without end. Tree rail and detail scales furthest and shows the least at once. Document outline reads best and scans worst. Comparing one field across 35 rows is a saccade per line where a table is one glance down a column. What five independent implementations all got wrong. This is the part that justifies building five instead of one. A defect that every implementation reaches independently is a property of the problem, not of the implementation, and the real one will reach it too. \u201dDone\u201d has no meaning on this board. All five defined a parent\u2019s progress as how many descendants reached the last column. Released is empty, and In progress is empty. Every parent therefore printed 0 of N while its distribution bar rendered full \u2014 truthful, and it reads as a contradiction. Three separate verification passes reached the same conclusion independently: this is not an execution defect and cannot be fixed by adjusting the widget. Redefining done to include In review would be a lie about the board. The open question, which is a product decision: what counts as done for a parent \u2014 the last column, the ratio of checked acceptance criteria across the subtree, or nothing at all. Until it is answered, any rollup shipped will read as a constant zero on this board. Collapsing or filtering throws the focus out of the tree. Three of the five lost keyboard focus when the tree re-rendered, each for a different reason: reading document.activeElement after innerHTML had already been replaced; leaving the only tabindex=\"0\" on a node that collapsing had set to display: none; and dropping the roving tab stop when a filter removed the active row. Three independent implementations hitting the same class of bug means a re-rendering tree must decide explicitly where focus lands when the focused row stops existing. The rule that worked: move to the nearest still-rendered ancestor, and fall back to the first row. A roving tab stop leaks. One implementation promoted rows to tabindex=\"0\" without demoting the previous holder; after four arrow presses a single column had five tab stops while its own comment claimed one. Filter highlighting falls back to the browser default. Two variants styled <mark> in some contexts and not others, so matching text in an unstyled context painted #ff0 into an oklch palette, most visible in dark mode. A match can hide inside a collapsed branch. One filter marked ancestors as context only when the ancestor was not itself a match, so a match nested under another match never rendered while the toolbar counted it. A filtered tree must open every ancestor of every match, and must restore the hand-set collapse state when the filter clears. Cycles are not validated. Found while designing, not while prototyping. _validate_relations checks that parent names an existing card and is not the card itself. It does not check that following the chain terminates, so a \u2192 b \u2192 a passes the build today. This is harmless only because nothing walks the chain. Every prototype had to carry its own visited set, and each was tested by injecting a two-cycle, a three-cycle, a self-parent and a dangling parent. The fix belongs in the loader, before anything renders a tree. A note on the NUL byte. template/components/kanban-board.tsx wrote a cache-key separator as a literal NUL byte inside a template string. It worked, and it made the file binary to grep and diff. The brief warned the prototype agents about it. One of them, while fixing an unrelated markdown bug, wrote four raw NUL bytes into its own source \u2014 and caught it. The failure mode is easy to reach by accident, which is the argument for fixing the original rather than documenting it. The branch now writes an escaped separator, so the component is ordinary UTF-8 text again. Where this went. the-board-reads-as-a-tree \u2014 the epic, and the choice recorded above. a-parent-cycle-fails-the-build \u2014 the loader gap. the-board-component-splits-into-modules \u2014 the 4312-line file, and the NUL byte. the-table-is-a-second-view \u2014 the view switch and the URL. the-table-draws-one-row-per-card \u2014 the row grammar. the-tree-filters-without-re-rooting \u2014 filter semantics. a-parent-says-what-it-breaks-into \u2014 the rollup and the divergence marker. the-cli-prints-the-tree \u2014 the agent-facing surface."},{"url":"docs/deployment/ci_cd/index.html","title":"CI/CD","content":"Ask AI. CI/CD. CI/CD is a deployment strategy: the pipeline builds _site/, checks it, and publishes the same static artifact that local builds produce. This page focuses on GitHub Actions because folio init can generate ready-to-use Pages workflows. GitHub Actions Deployment. folio init creates a GitHub Pages workflow at .github/workflows/pages.yml. It builds docs on every push to main, uploads _site/, and deploys with GitHub Pages: If your docs build needs local project dependencies or versioned documentation, edit the build steps in this same file. Folio\u2019s own repository currently uses uv sync --all-groups --locked before uv run --locked folio build --clean to deploy only the current version. The folio github-pages commands are internal deployment helpers shipped with the folio-docs package. They keep the generated workflow readable while still preserving branch previews, updating the preview index, saving Pages state, and verifying the deployed URLs. Branch Preview Deploys. folio init also creates .github/workflows/branch-previews.yml for GitHub Pages branch previews. It runs from pull_request_target so the workflow definition and deploy steps come from the trusted base branch, while PR branch code is built in a separate unprivileged job. GitHub Pages serves one artifact per site, so Folio does not deploy each branch as an independent Pages site. Instead, every deploy publishes a complete static artifact with this layout: The production workflow and preview workflow cooperate through an internal folio-pages-state branch: A main deploy builds production docs into _site/. It copies any existing _site/previews/ folders from folio-pages-state. It regenerates /previews/index.html. It saves the complete artifact back to folio-pages-state. It deploys the combined artifact to GitHub Pages. A branch preview deploy follows the same rule: publish the complete site, not just the branch folder. The preview workflow resolves the open same-repository PR for the branch. An unprivileged build job checks out the PR head with persisted credentials disabled and builds docs with FOLIO_BASE_PATH=/repo/previews/pr-<number>-<branch>/. That build job uploads only the static _site/ preview artifact. A privileged deploy job revalidates that the PR head is still current before doing any write-capable work. The deploy job checks out the trusted base branch, starts the Pages artifact from the saved folio-pages-state root, and uses a trusted production build only as the first-deploy fallback. It downloads the static preview artifact and replaces only /previews/pr-<number>-<branch>/. It regenerates /previews/index.html, saves state, deploys, and verifies the preview URL. It creates or updates a sticky PR comment with the preview URL, preview index, branch, and commit. Production therefore remains at the normal Pages URL, while each open PR gets a stable preview URL such as: The preview folder name includes the PR number plus a URL-safe version of the branch name, which keeps similarly named branches from overwriting each other. If a PR is stale, closed, still a draft, or comes from a fork, the preview deploy exits early and leaves the current Pages site unchanged. The PR comment uses a hidden marker so the workflow updates the same comment on each deploy. Reviewers get the latest preview link directly in the conversation without a new comment on every push. The workflow still writes the same links to the Actions summary for debugging failed runs. Pull Request Checks. Run docs build and coverage checks on every PR: Coverage Gates. Use folio coverage to enforce minimum docstring coverage in CI: This exits with code 1 if any module falls below the threshold, failing the CI pipeline. Adjust the threshold to match your project\u2019s standards. The coverage report shows per-module statistics: Pre-commit Hook. Run a quick coverage check before every commit:"},{"url":"docs/components/type_badge/index.html","title":"TypeBadge","content":"Ask AI. TypeBadge. A small inline badge for displaying Python type annotations. Renders as a styled badge with monospace font. When an href is provided, the badge becomes a link to the type\u2019s documentation page. This component is used throughout the auto-generated API reference. API. TypeBadge. PropTypeDefaultDescription type. string. \u2014 The type annotation text (e.g. \"str\", \"list[int]\", \"Optional[Config]\"). href. string. \u2014 Optional URL to link the badge to a type\u2019s documentation page. Example. Returns a list[Node] containing all connected peers. The timeout parameter accepts float or None. The configuration uses Optional[Config] for optional settings."},{"url":"docs/kanban/cards/landing_for_the_two_product_family/index.html","title":"Landing for the two-product family","content":"Ask AI. Landing for the two-product family. In progress \u00b7 feature \u00b7 L. Compare two ways to introduce the Folio product family while Folio Docs is available and Folio for Agents is not: a compact family pre-landing, or a shared landing that bifurcates below an unchanged hero. Keep their roadmaps, boards, and release state independent in either architecture. Direction brief. Make the split between Folio Docs and Folio for Agents legible from the first viewport without discarding the landing's approved hero, editorial\u2026 Architecture A \u2014 Folio pre-landing. architecture-a-prelanding.html. Architecture B \u2014 Shared landing with bifurcation. architecture-b-shared-landing.html. Candidate A \u2014 family gateway. candidate-a-family-gateway.html. Candidate B \u2014 Docs-led front door. candidate-b-docs-led.html. Hero 01 \u2014 Dual canvas. hero-01-dual-canvas.html. Hero 02 \u2014 Repository orbit. hero-02-repository-orbit.html. Hero 03 \u2014 Stacked surfaces. hero-03-stacked-surfaces.html. Hero 04 \u2014 Split sheet. hero-04-split-sheet.html. Hero 05 \u2014 Signal rail. hero-05-signal-rail.html. Hero 06 \u2014 Living index. hero-06-living-index.html. Hero 07 \u2014 Product portals. hero-07-product-portals.html. Hero 08 \u2014 Editorial totem. hero-08-editorial-totem.html. Hero gallery \u2014 eight alternatives. hero-gallery.html. Hero V2 01 \u2014 Vector specimen. hero-v2-01-vector-specimen.html. Hero V2 02 \u2014 Editorial ink. hero-v2-02-editorial-ink.html. Hero V2 03 \u2014 Modular atlas. hero-v2-03-modular-atlas.html. Hero V2 04 \u2014 Night edition. hero-v2-04-night-edition.html. Hero V2 gallery \u2014 Quiver-informed directions. hero-v2-gallery.html. The card belongs to the board."},{"url":"docs/components/tabs/index.html","title":"Tabs","content":"Ask AI. Tabs. Generic tabbed content panels for organizing alternative content views. Unlike CodeGroup (which is code-only), Tabs supports any content including text, tables, images, and nested components. API. Tabs. PropTypeDefaultDescription children. ReactNode. \u2014 One or more <TabItem> elements. TabItem. PropTypeDefaultDescription label. string. \u2014 The tab label shown in the tab bar. children. ReactNode. \u2014 Content displayed when this tab is active. Example. Python Node.js Go. Install with pip: pip install my-library. Install with npm: npm install my-library. Install with go get: go get github.com/example/my-library."},{"url":"docs/components/config-panel/index.html","title":"ConfigPanel","content":"Ask AI. ConfigPanel. Pair a YAML example with short field notes. Use it when a configuration snippet needs explanation but a full reference table would interrupt the guide. API. PropTypeDefaultDescription title. string. \u2014 Section title. description. string. \u2014 Short explanation below the title. fields. ConfigField[] [] Field notes shown below the example. children. ReactNode. \u2014 Usually a fenced YAML block. ConfigField supports name, type, default, and description. Example. source.python. Tell Folio where your package code lives. paths Python source directories to scan. list[string] docstring_style Parser for Google or NumPy style docstrings. stringdefault: auto."},{"url":"docs/api-reference/folio_agents/edit/index.html","title":"folio_agents.edit source","content":"Ask AI. folio_agents.edit source. Line-surgery editor for cardfile board cards. Card files are hand-authored Markdown; a writer that round-trips them through yaml.safe_dump would destroy comments, key order, and quoting. Every mutation here is therefore a targeted line edit \u2014 replace one scalar line, insert one artifact line, append one trail line \u2014 followed by a re-parse verification. When a file is structurally exotic (block scalars, anchors, a key the surgery cannot find as a plain key: value line) the edit refuses loudly and tells the actor to edit the file by hand; a wrong write never survives, because verification failure restores the original bytes before raising. Classes. class CardEditError(ValueError). source A surgical edit could not be applied safely. Functions. format_trail_entry source. The canonical trail line: strict writer, tolerant reader. NameTypeDefaultDescription date. str. - actor. str. - note. str. - ref. str. '' Returns: str - format_comment_entry source. The canonical comment line \u2014 the trail\u2019s grammar minus the ref. A comment argues; it does not point at a commit. Same strict-writer contract: the tolerant reader upstream renders anything, so the only place to keep the section parseable is here. NameTypeDefaultDescription date. str. - actor. str. - text. str. - Returns: str - set_scalar source. Replace (or add) one plain key: value frontmatter line. NameTypeDefaultDescription path. Path. - key. str. - value. Any. - Returns: None - set_list source. Replace (or add) one inline key: [a, b] frontmatter line. NameTypeDefaultDescription path. Path. - key. str. - values. list[str] - Returns: None - _append_section_line source. Append one bullet at the END of a ## <heading> section. Appending at the tail keeps concurrent-session conflicts predictable: two appends collide at the same place and the resolution is mechanically \u201ckeep both lines\u201d. A missing section is created where before_re points (comments read before the trail, so they are created before it), or at the file\u2019s end. NameTypeDefaultDescription path. Path. - entry. str. - heading_re. re.Pattern[str] - heading. str. - what. str. - before_re. re.Pattern[str] | None. None. Returns: None - append_trail source. Append one trail line at the END of the ## Trail section. NameTypeDefaultDescription path. Path. - entry. str. - Returns: None - append_comment source. Append one comment line at the END of the ## Comments section. NameTypeDefaultDescription path. Path. - entry. str. - Returns: None - insert_artifact source. Insert one artifact item at the end of the artifacts: block. NameTypeDefaultDescription path. Path. - kind. str. - target. Any. - label. str. '' Returns: None - _artifact_count source. NameTypeDefaultDescription front. str. - Returns: int - _split source. NameTypeDefaultDescription text. str. - path. Path. - Returns: tuple[str, str, tuple[int, int]] - _assemble source. NameTypeDefaultDescription original. str. - new_front. str. - span. tuple[int, int] - Returns: str - _continues source. True when the next frontmatter line continues this scalar (multiline). NameTypeDefaultDescription front. str. - line_end. int. - Returns: bool - _render_scalar source. NameTypeDefaultDescription value. Any. - Returns: str - _parses_as_number source. A string that YAML would read as a number must be quoted to stay one. NameTypeDefaultDescription text. str. - Returns: bool - _write_verified source. NameTypeDefaultDescription path. Path. - original. str. - new_text. str. - verify. Any. - what. str. - Returns: None -"},{"url":"docs/components/deprecation_notice/index.html","title":"DeprecationNotice","content":"Ask AI. DeprecationNotice. A styled banner indicating that a class, function, or module is deprecated. Displays a red \u201cDeprecated\u201d badge with version information and guidance on what to use instead. This component is typically auto-generated when a docstring contains a Deprecated: section. API. DeprecationNotice. PropTypeDefaultDescription since. string. \u2014 Version when the deprecation was introduced (e.g. \"1.5.0\"). alternative. string. \u2014 Name of the replacement to use. Rendered in a <code> element with \u201cUse \u2026 instead.\u201d phrasing. message. string. \u2014 Custom deprecation message. The rendered card shows: A red \u201cDeprecated\u201d badge. \u201csince X.X.X\u201d in muted text (if since is provided). The custom message (if provided). \u201cUse alternative instead.\u201d (if alternative is provided). Example. Deprecatedsince 2.0.0. This class will be removed in v3.0. Use FederatedNode instead. Minimal usage with just a version: Deprecatedsince 1.5.0."},{"url":"docs/api-reference/folio_docs/schemas/theme_contract/index.html","title":"folio_docs.schemas.theme_contract source","content":"Ask AI. folio_docs.schemas.theme_contract source. Canonical theme contract for Folio. This module defines the single source of truth for theme field names, aliases, and style properties used throughout Folio\u2019s theming system."},{"url":"docs/kanban/cards/landing-for-the-two-product-family/index.html","title":"Landing for the two-product family","content":"Ask AI. Landing for the two-product family. In progress \u00b7 feature \u00b7 L. Compare two ways to introduce the Folio product family while Folio Docs is available and Folio for Agents is not: a compact family pre-landing, or a shared landing that bifurcates below an unchanged hero. Keep their roadmaps, boards, and release state independent in either architecture. Direction brief. Make the split between Folio Docs and Folio for Agents legible from the first viewport without discarding the landing's approved hero, editorial\u2026 Architecture A \u2014 Folio pre-landing. architecture-a-prelanding.html. Architecture B \u2014 Shared landing with bifurcation. architecture-b-shared-landing.html. Candidate A \u2014 family gateway. candidate-a-family-gateway.html. Candidate B \u2014 Docs-led front door. candidate-b-docs-led.html. Hero 01 \u2014 Dual canvas. hero-01-dual-canvas.html. Hero 02 \u2014 Repository orbit. hero-02-repository-orbit.html. Hero 03 \u2014 Stacked surfaces. hero-03-stacked-surfaces.html. Hero 04 \u2014 Split sheet. hero-04-split-sheet.html. Hero 05 \u2014 Signal rail. hero-05-signal-rail.html. Hero 06 \u2014 Living index. hero-06-living-index.html. Hero 07 \u2014 Product portals. hero-07-product-portals.html. Hero 08 \u2014 Editorial totem. hero-08-editorial-totem.html. Hero gallery \u2014 eight alternatives. hero-gallery.html. Hero V2 01 \u2014 Vector specimen. hero-v2-01-vector-specimen.html. Hero V2 02 \u2014 Editorial ink. hero-v2-02-editorial-ink.html. Hero V2 03 \u2014 Modular atlas. hero-v2-03-modular-atlas.html. Hero V2 04 \u2014 Night edition. hero-v2-04-night-edition.html. Hero V2 gallery \u2014 Quiver-informed directions. hero-v2-gallery.html. The card belongs to the board."},{"url":"docs/api-reference/folio_docs/docs/theme_package_validator/index.html","title":"folio_docs.docs.theme_package_validator source","content":"Ask AI. folio_docs.docs.theme_package_validator source. Validator for theme packages to ensure they don\u2019t contain reserved paths. Functions. _validate_theme_package source. Validate a theme package for common issues. Returns a list of error messages. Empty list means valid. NameTypeDefaultDescription path. Path. - Returns: list[str] - validate_and_raise source. Validate a theme package and raise ValueError if validation fails. NameTypeDefaultDescription path. Path. - Returns: None - Raises: ValueError - If validation fails with all error messages joined."},{"url":"docs/components/preview-code/index.html","title":"PreviewCode","content":"Ask AI. PreviewCode. Pair a rendered component example with the exact MDX source that produced it. Use PreviewCode for component catalog examples so readers can inspect the result first and switch to the code without scrolling through a duplicated section. API. PreviewCode. PropTypeDefaultDescription title. string. \u2014 Optional heading shown above the preview/code switcher. description. string. \u2014 Optional short context for the example. defaultMode. \"preview\" | \"code\" \"preview\" Initial tab. children. ReactNode. \u2014 Include one fenced code block plus the rendered preview content. Example. Callout example. The first fenced block becomes the Code tab. The remaining children become the Preview tab. Use real output. Prefer generated pages, command output, and source snippets over abstract placeholders."},{"url":"docs/components/before_after/index.html","title":"BeforeAfter","content":"Ask AI. BeforeAfter. Show two related code snippets side by side. It works well for migration notes, config changes, and generated output comparisons. API. PropTypeDefaultDescription before. string. \u2014 Left code snippet. after. string. \u2014 Right code snippet. beforeLabel. string. \"Before\" Left label. afterLabel. string. \"After\" Right label. language. string. \u2014 Reserved for future syntax-label use. Example. RST. .. note:: Install Folio first. MDX. <Callout type=\"note\"> Install Folio first. </Callout>"},{"url":"docs/components/math/index.html","title":"Math (LaTeX)","content":"Ask AI. Math (LaTeX). Inline and display math rendering via KaTeX . Use $ for inline expressions and $$ for centered display equations. Math support is enabled by default \u2014 no configuration needed. API. Math uses Markdown syntax, not a JSX component: SyntaxBehaviorDescription $...$. Inline. Renders the expression inline with surrounding text. $$...$$. Display (block). Renders the expression as a centered block equation. Math expressions inside code blocks (` or ```) are not rendered \u2014 they display as literal text, so you can safely document LaTeX syntax. Example. Inline math. The Pythagorean theorem states that a2+b2=c2a^2 + b^2 = c^2a2+b2=c2 for a right triangle. Einstein\u2019s famous equation E=mc2E = mc^2E=mc2 relates energy and mass. Display equations. x=\u2212b\u00b1b2\u22124ac2ax = \\frac{-b \\pm \\sqrt{b^2 - 4ac}}{2a}x=2a\u2212b\u00b1b2\u22124ac Matrices. [a11a12a21a22][x1x2]=[b1b2]\\begin{bmatrix} a_{11} & a_{12} \\\\ a_{21} & a_{22} \\end{bmatrix} \\begin{bmatrix} x_1 \\\\ x_2 \\end{bmatrix} = \\begin{bmatrix} b_1 \\\\ b_2 \\end{bmatrix}[a11a21a12a22][x1x2]=[b1b2] Summations. The binomial theorem: (x+y)n=\u2211k=0n(nk)xn\u2212kyk(x + y)^n = \\sum_{k=0}^{n} \\binom{n}{k} x^{n-k} y^k(x+y)n=k=0\u2211n(kn)xn\u2212kyk."},{"url":"docs/components/stat_strip/index.html","title":"StatStrip","content":"Ask AI. StatStrip. A row of big numbers with labels \u2014 the fastest way to make scale legible. Values are strings, so units and symbols render exactly as written. API. StatStrip. PropTypeDefaultDescription stats. { value, label, detail? }[] \u2014 One entry per number. Example. Project stats. 94. pages. generated per build. 40. components. 867. tests. green before every merge."},{"url":"docs/kanban/cards/landing-for-the-two-product-family/landing-direction/index.html","title":"Landing direction: two products, one repository truth","content":"Ask AI. Landing direction: two products, one repository truth. Objective. Make the split between Folio Docs and Folio for Agents legible from the first viewport without discarding the landing\u2019s approved hero, editorial tone, or artifact-first proof style. What is fixed. The approved kicker, headline, and one-sentence description remain unchanged unless the owner opens a deliberate copy round. Folio Docs is a docs generator. Folio for Agents is a repository-native meta-harness, not an orchestrator and not a replacement for coding tools. Present-tense claims need shipped proof. Future work stays visibly marked as roadmap. The page should show generated output, repository files, or the real board instead of decorative icon grids. The two products are being split into independent installs and releases: folio-docs 0.3 and folio-agents 0.1. Current landing audit. The existing landing has strong raw material: The heartbeat hero demonstrates source becoming a real documentation page. The funnel explains inputs, the build, and static outputs without relying on abstract brand art. The harness section already names both products and avoids orchestration claims. All sections are config-driven, responsive, theme-aware, and covered by reduced-motion rules. The split exposes four structural mismatches: The navbar, hero action, install command, and footer all lead to one undifferentiated Folio surface. The product split appears only after the Docs build funnel, so it reads as a secondary feature rather than the page\u2019s organizing model. The harness diagram assumes a shared core while the active product work now creates separately installed and released packages. Docs outputs, agent work state, and their optional integration are shown in one flow, which blurs which product owns each result. There is also one source-of-truth conflict to resolve before integration: PRODUCT.md says the 0.3 distinction does not require separate packages, while the active split implements independent packages and releases. The landing should follow the confirmed product model after that text and code agree. Candidate A: family gateway. Open the rendered family-gateway prototype. The root URL represents the Folio family. The first viewport keeps the approved Docs promise but places a product map beside it. The next section gives Docs and Agents equal weight, with separate jobs, installs, proofs, and next actions. Choose this when pguijas.github.io/folio/ is the public front door for both products. Strengths: The product split is visible before scrolling. Independent packages and releases are explicit. The optional Docs integration is shown as a boundary, not a shared runtime. Both products can grow without turning one into a feature of the other. Cost: The Docs-specific approved headline still anchors a family page. A later deliberate copy round may decide whether that remains desirable. Navigation and release proof need stable Agents destinations, not placeholder routes. Candidate B: Docs-led front door. Open the rendered Docs-led prototype. The root URL belongs to Folio Docs. The current hero and build story remain dominant. A strong family band introduces Folio for Agents as an independently installed companion product, then returns to Docs proof. Choose this when the current site remains the product site for folio-docs and Folio for Agents will get its own durable destination. Strengths: The approved hero, primary audience, and conversion path remain coherent. The site can ship before a complete Agents marketing/documentation surface exists. Ownership is simple: Folio Docs generates and owns this site. Cost: Folio for Agents remains secondary even though the repository README presents two independent products. The shared Folio family lacks a true home until another surface is created. Decision. QuestionCandidate ACandidate B Root site represents. Folio family. Folio Docs. Product weight. Equal. Docs primary. Hero. Approved copy + family map. Approved copy + Docs artifact. Agents destination required now. Yes. Not beyond one proof/quickstart route. Best fit with independent packages. Strongest. Transitional. Recommendation: Candidate A, if the package split is the confirmed product direction. It gives the repository README, installs, releases, navigation, and landing one model. Candidate B is the safer transition if Folio for Agents does not yet have a public quickstart and stable routes. Stage 2 integration shape. After the owner confirms A or B: Rebase feat/landing-refresh onto the completed product-split branch. Resolve the PRODUCT.md package-model conflict deliberately before changing landing copy. Keep the approved hero component and evolve the existing harness section rather than adding an overlapping section type. Add only the data needed for independent commands, links, and proof labels. Update Folio\u2019s docs.yaml composition and landing navigation. Keep generic landing-plugin defaults product-neutral. Keep Docs navigation and roadmap on the Docs release cycle; link the Agents product without embedding its board state. Pin the public behavior in the existing landing config/site-builder tests, then update the landing guide if the public section contract changes. Validate the complete page at desktop and mobile widths, in light and dark themes, by keyboard, with reduced motion, and through a clean static export. Validation state. Source, product, active split, landing config, component catalog, and board cards audited on 2026-08-30. Both candidates are standalone and leave product files unchanged. Candidate A, Candidate B, the direction brief, and the filtered board URL return HTTP 200 from the local Folio development site. Board page generation and internal link validation pass. The full static export is blocked by an unrelated existing RoadmapPhase/milestone type mismatch on the board branch. Interactive browser review is pending because no in-app browser backend was connected in this session."},{"url":"docs/components/checklist/index.html","title":"Checklist","content":"Ask AI. Checklist. A readiness list with explicit states. Use it for prerequisites, deployment checks, and release gates. API. PropTypeDefaultDescription title. string. \u2014 Optional checklist title. items. ChecklistItem[] \u2014 Rows to render. ChecklistItem supports label, description, and state. State can be done, warn, or todo. Example. Before you build. OK Python 3.10+ Required by the Folio package. TODO docs.yaml exists. Run folio init if it is missing. WARN No stale output committed. Keep .build/ and _site/ out of source control unless your deployment needs them."},{"url":"docs/components/param-table/index.html","title":"ParamTable","content":"Ask AI. ParamTable. A structured table for displaying function or method parameters with name, type, default value, and description columns. This component is typically auto-generated from Python docstrings during the build, but you can also use it manually for hand-crafted documentation. API. ParamTable. PropTypeDefaultDescription args. Param[] \u2014 Array of parameter objects. Param object. FieldTypeDefaultDescription name. string. \u2014 Parameter name. type. string. \u2014 Type annotation string. default. string. \"-\" Default value. Shown as - if omitted. description. string. \u2014 Description of the parameter. href. string. \u2014 Optional URL to link the type badge to its documentation. Example. NameTypeDefaultDescription host. str. \"localhost\" Server hostname. port. int. 8080. Server port number. debug. bool. False. Enable debug mode. workers. int. - Number of worker processes. Required."},{"url":"docs/components/build_artifact/index.html","title":"BuildArtifact","content":"Ask AI. BuildArtifact. Summarize the files, directories, and generated routes produced by a Folio command. API. PropTypeDefaultDescription title. string. \"Build artifacts\" Panel title. description. string. \u2014 Optional summary. items. BuildArtifactItem[] \u2014 Artifact rows. BuildArtifactItem supports path, kind, and description. Example. Generated output. The build creates an intermediate app and final static files. .build/workspace Intermediate Nextra project with generated MDX. _site/static Deployable static site. _site/llms.txtllm Summary file for AI coding assistants."},{"url":"docs/theming/index.html","title":"Theming","content":"Ask AI. Theming. Folio has one theming model with three ownership levels. Start with the bundled template, move to a theme package when a project needs exact visual control, and use a custom template only when the documentation must live inside a fully project-owned frontend. Choose an Ownership Level. LevelConfigure withUse whenWho owns the frontend Theme personalization. theme.preset, theme.tune, theme.tokens, theme.header, theme.variants. The bundled Folio docs shell is right, but the brand, colors, typography, spacing, or header need tuning. Folio owns the template; your project owns safe theme data. Theme package. theme.package. The docs should still use Folio\u2019s bundled template as a base, but a project needs to override files such as layouts, CSS, the configurator, or project theme code. Folio owns generated content; the package overlays selected template files. Custom template. template.path. The docs must run inside a product-specific Next/Nextra frontend with its own routes, dependencies, chrome, search UI, or application layout. The custom template owns the frontend workspace. Personalize a Theme. Set presets, defaults, tokens, variants, header actions, logo, favicon, and dark mode from docs.yaml. Build a Theme Package. Overlay files on the bundled template while Folio still injects generated content and metadata. Use a Custom Template. Bring a full Next/Nextra frontend and consume Folio's generated MDX, metadata, and component contract. How Folio Applies Theme Configuration. flowchart TD Config[\"docs.yaml\"] --> Theme[\"theme.*\"] Config --> Template[\"template.*\"] Theme --> Preset[\"Bundled preset and safe project preset\"] Theme --> Package{\"theme.package?\"} Package -->|No| Bundled[\"Bundled Folio template\"] Package -->|Yes| Overlay[\"Copy package over bundled template\"] Preset --> Bundled Preset --> Overlay Template --> Custom{\"template.path?\"} Custom -->|No| Build[\"Prepare generated docs workspace\"] Custom -->|Yes| TemplateWorkspace[\"Copy project-owned template\"] Bundled --> Build Overlay --> Build TemplateWorkspace --> Build Build --> Site[\"Static documentation site\"] Folio always owns the generated documentation data: parsed API pages, converted Markdown, _meta.ts, search metadata, LLM outputs, and static export. The theming choice controls how much of the presentation layer your project owns. Recommended Path. Start with theme.preset, theme.tune, logo, and favicon. Add project tokens, header configuration, and variants if the bundled shell is still the right product experience. Move to theme.package when the project needs exact control over selected template files but still wants Folio\u2019s bundled template as a base. Move to template.path when the documentation frontend is a product workspace, not just a styled Folio docs site. Related Reference. Configuration documents the complete docs.yaml shape. ThemeConfigurator documents the runtime drawer, built-in preset controls, and the underlying preset TypeScript contract. Components lists the MDX components that generated docs and custom templates may need to expose."},{"url":"docs/kanban/cards/landing_for_the_two_product_family/landing_direction/index.html","title":"Landing direction: two products, one repository truth","content":"Ask AI. Landing direction: two products, one repository truth. Objective. Make the split between Folio Docs and Folio for Agents legible from the first viewport without discarding the landing\u2019s approved hero, editorial tone, or artifact-first proof style. What is fixed. The approved kicker, headline, and one-sentence description remain unchanged unless the owner opens a deliberate copy round. Folio Docs is a docs generator. Folio for Agents is a repository-native meta-harness, not an orchestrator and not a replacement for coding tools. Present-tense claims need shipped proof. Future work stays visibly marked as roadmap. The page should show generated output, repository files, or the real board instead of decorative icon grids. The two products are being split into independent installs and releases: folio-docs 0.3 and folio-agents 0.1. Current landing audit. The existing landing has strong raw material: The heartbeat hero demonstrates source becoming a real documentation page. The funnel explains inputs, the build, and static outputs without relying on abstract brand art. The harness section already names both products and avoids orchestration claims. All sections are config-driven, responsive, theme-aware, and covered by reduced-motion rules. The split exposes four structural mismatches: The navbar, hero action, install command, and footer all lead to one undifferentiated Folio surface. The product split appears only after the Docs build funnel, so it reads as a secondary feature rather than the page\u2019s organizing model. The harness diagram assumes a shared core while the active product work now creates separately installed and released packages. Docs outputs, agent work state, and their optional integration are shown in one flow, which blurs which product owns each result. There is also one source-of-truth conflict to resolve before integration: PRODUCT.md says the 0.3 distinction does not require separate packages, while the active split implements independent packages and releases. The landing should follow the confirmed product model after that text and code agree. Candidate A: family gateway. Open the rendered family-gateway prototype. The root URL represents the Folio family. The first viewport keeps the approved Docs promise but places a product map beside it. The next section gives Docs and Agents equal weight, with separate jobs, installs, proofs, and next actions. Choose this when pguijas.github.io/folio/ is the public front door for both products. Strengths: The product split is visible before scrolling. Independent packages and releases are explicit. The optional Docs integration is shown as a boundary, not a shared runtime. Both products can grow without turning one into a feature of the other. Cost: The Docs-specific approved headline still anchors a family page. A later deliberate copy round may decide whether that remains desirable. Navigation and release proof need stable Agents destinations, not placeholder routes. Candidate B: Docs-led front door. Open the rendered Docs-led prototype. The root URL belongs to Folio Docs. The current hero and build story remain dominant. A strong family band introduces Folio for Agents as an independently installed companion product, then returns to Docs proof. Choose this when the current site remains the product site for folio-docs and Folio for Agents will get its own durable destination. Strengths: The approved hero, primary audience, and conversion path remain coherent. The site can ship before a complete Agents marketing/documentation surface exists. Ownership is simple: Folio Docs generates and owns this site. Cost: Folio for Agents remains secondary even though the repository README presents two independent products. The shared Folio family lacks a true home until another surface is created. Decision. QuestionCandidate ACandidate B Root site represents. Folio family. Folio Docs. Product weight. Equal. Docs primary. Hero. Approved copy + family map. Approved copy + Docs artifact. Agents destination required now. Yes. Not beyond one proof/quickstart route. Best fit with independent packages. Strongest. Transitional. Recommendation: Candidate A, if the package split is the confirmed product direction. It gives the repository README, installs, releases, navigation, and landing one model. Candidate B is the safer transition if Folio for Agents does not yet have a public quickstart and stable routes. Stage 2 integration shape. After the owner confirms A or B: Rebase feat/landing-refresh onto the completed product-split branch. Resolve the PRODUCT.md package-model conflict deliberately before changing landing copy. Keep the approved hero component and evolve the existing harness section rather than adding an overlapping section type. Add only the data needed for independent commands, links, and proof labels. Update Folio\u2019s docs.yaml composition and landing navigation. Keep generic landing-plugin defaults product-neutral. Keep Docs navigation and roadmap on the Docs release cycle; link the Agents product without embedding its board state. Pin the public behavior in the existing landing config/site-builder tests, then update the landing guide if the public section contract changes. Validate the complete page at desktop and mobile widths, in light and dark themes, by keyboard, with reduced motion, and through a clean static export. Validation state. Source, product, active split, landing config, component catalog, and board cards audited on 2026-08-30. Both candidates are standalone and leave product files unchanged. Candidate A, Candidate B, the direction brief, and the filtered board URL return HTTP 200 from the local Folio development site. Board page generation and internal link validation pass. The full static export is blocked by an unrelated existing RoadmapPhase/milestone type mismatch on the board branch. Interactive browser review is pending because no in-app browser backend was connected in this session."},{"url":"docs/architecture/index.html","title":"Architecture","content":"Ask AI. Architecture. Folio Docs has one local build pipeline. The CLI reads configuration and parses project sources once, then emits the human site and its agent-readable mirrors. Runtime Flow. flowchart LR CLI[\"\u25b8 CLI: build / serve\"] --> Config[\"\u25a3 docs.yaml\"] Config --> Sources[\"\u25a4 Python + Markdown\"] Sources --> Parse[\"\u2699 Parse\"] Parse --> IRNode[\"\u25c7 IR objects\"] IRNode --> Docs[\"\u25a5 folio_docs\"] Extensions[\"\u2295 Extensions\"] --> Docs Template[\"\u25a7 Nextra template\"] --> Docs Docs --> Site[\"\u2713 HTML site + search\"] Docs --> Context[\"\u2713 Markdown + llms.txt + contract\"] folio_docs.cli handles command arguments and calls folio_docs.build.run_build. folio_docs.config loads docs.yaml and resolved project paths. folio_docs.sources parses configured Python and Markdown sources. folio_docs.build orchestrates extensions, validation, and export. folio_docs.docs.site_builder.SiteBuilder owns the human site workspace, pages, metadata, themes, runtime, and search index. folio_docs.agent_output.AgentArtifacts owns Markdown mirrors, LLM files, robots discovery, and the published authoring contract. template/ provides the Nextra application. Product boundaries. Folio Docs does not import folio_agents. The optional board publishing plugin lives in Folio for Agents and depends on the public Docs plugin surface. The core Docs build remains usable when that package is absent. Folio Docs internals. SiteBuilder delegates build-environment work to smaller modules: TemplateWorkspace copies the template and removes bundled demo content. TemplateConfigInjector replaces template placeholders with docs.yaml values. ExtensionEmitter writes plugin components, typed data modules, and generated views. NextRuntime installs dependencies, runs Next.js, serves development mode, and copies static output. StaticAssetRewriter rewrites exported links so the static site works from file://. Extension boundary. Folio keeps extension internals isolated from source parsing and theme implementation details. Plugins can contribute documents and components to the site while inheriting both product outputs: a contributed page also receives a Markdown mirror and appears in the configured LLM indexes."},{"url":"docs/kanban/cards/the_board_takes_the_name_and_absorbs_the_roadmap/panel_verdict/index.html","title":"Panel verdict \u2014 the board takes the name and absorbs the roadmap","content":"Ask AI. Panel verdict \u2014 the board takes the name and absorbs the roadmap. Five agents, 2026-08-28: one merge assessor, three name generators (plain-literal, market, prior-art lenses), one adversary with kill authority. The owner confirmed the verdict the same day: folio board, roadmap included. Question one: does the roadmap plugin fold into the board plugin? Four options were weighed. The decisive facts came from the code, not from taste: The two plugins already cannot render their own pages without peeking at each other\u2019s raw config in three places (kanban.py:174 reads roadmap:, roadmap.py reads config.extra['kanban'], kanban.py reads config.extra['roadmap'] back). Phases are the only board-adjacent data with no git history, on a product that sells on git history. Phase 0.4\u2019s shipped public text says \u201cthe roadmap reads from the board\u201d. Under the current dependency direction (the board reads FROM the roadmap) that sentence is unshippable without either this merge or a two-sources-of-truth sync mechanism of the kind this project just spent a milestone eliminating for cards. Rejected \u2014 status quo: defers a decision the 0.4 text already made in public, and the cheap-breakage window (pre-beta) is exactly the window that closes. Renaming around a boundary you intend to erase pays the ~1350-mention rename cost twice. Rejected \u2014 one data model, two plugins: takes the full migration cost of the merge while keeping the coordination cost of the seam; the \u201cindependent\u201d renderer would have a hard load-order dependency on the board plugin anyway. Acceptable only as a one-commit intermediate step. Chosen \u2014 merge with preserved surfaces: Milestones/phases move out of docs.yaml into board data as a first-class registry; a phase moving next \u2192 active is a commit, same as a card move. /roadmap stays, as a view of that registry, with its own routes toggle independent of the board\u2019s visibility \u2014 the landing\u2019s public roadmap can outlive a private board. folio roadmap survives as a top-level command, printing the same registry folio board check validates. A milestone registry with zero cards is legal: the timeline-without- a-board user never has to adopt the full board. Strongest counterargument, recorded: the roadmap is a calm public surface (239 lines that essentially cannot fail) and the board is a volatile operational one mid-rewrite; merging chains the calm surface to the volatile one. Conditions 2 and 4 above are the answer, and they are not optional. Question two: what is the name? 25 candidates from three lenses. Killed with cause: project (collides with the project: site-metadata key on line 1 of docs.yaml), work (collides with the workspace view; ungreppable), backlog (names the whole with one of its columns), tickets, cardfile, cards (overloads the vocabulary\u2019s cleanest term). Top survivors, ranked: board, tracker, tasks, agenda, docket. The bet: board. It is unification, not invention \u2014 the data directory is already board/, the config already says source: board, the commit log already says \u201cboard:\u201d, the landing says \u201cRelease board\u201d, and 0.4 says \u201cthe roadmap reads from the board\u201d verbatim. Plugin name, config key, CLI word, route, directory, and roadmap promise collapse into one word already in use. \u201cThe board\u2019s kanban view\u201d and \u201cthe board\u2019s roadmap view\u201d read as natural English; kanban demotes to the name of the column view. board emerged independently from all three lenses, each placing \u201cProject OS\u201d (the 0.4 phase title) above it as the family umbrella. Runner-up for the record: tracker \u2014 the only candidate that names the function (\u201ca file-based project tracker your agents operate through commits\u201d), held back by bug-tracker heritage and the telemetry smell of a public /tracker route. The honesty rule, applied to the name. The owner asked for a name that says organization and agent orchestration. The panel\u2019s ruling: the product today does not orchestrate agents \u2014 agents operate the board through the CLI and SKILL.md; folio does not schedule or supervise them (that is the agents-claim-and-orchestrate idea, unshipped). A name promising orchestration would violate the project\u2019s own docs-honesty rule for as long as that stays true. The orchestration story rides in the sentence, not the noun: the board \u2014 files your agents operate through ordinary commits. When orchestration ships, board absorbs it without a second rename. Sequencing. Merge and rename happen in the same pre-beta breaking window: the rename touches every one of the ~1350 \u201ckanban\u201d mentions anyway, and the merge is what makes the promoted name true. Both known consumers (folio, pguijas/kanban) migrate trivially today; the same breakage costs an order of magnitude more at 0.7. The open sequencing question \u2014 before or after the write path\u2019s Part One lands \u2014 is recorded on the implementation card, not decided here."},{"url":"docs/api-reference/folio_docs/docs/integrations/openapi/index.html","title":"folio_docs.docs.integrations.openapi source","content":"Ask AI. folio_docs.docs.integrations.openapi source. Functions. config_keys source. Returns: list[str] - configure source. NameTypeDefaultDescription config. Any. - raw_config. dict[str, Any] - Returns: None - register_extensions source. NameTypeDefaultDescription registry. Any. - config. Any. - Returns: None - emit_assets source. NameTypeDefaultDescription builder. Any. - config. Any. - Returns: None - normalize_openapi source. NameTypeDefaultDescription raw_openapi. Any. - project_dir. Path. - Returns: dict[str, Any] - normalize_source source. NameTypeDefaultDescription raw_source. Any. - project_dir. Path. - Returns: dict[str, Any] | None - get_openapi source. NameTypeDefaultDescription config. Any. - Returns: dict[str, Any] - public_source_data source. NameTypeDefaultDescription source. dict[str, Any] - Returns: dict[str, Any] - docs_page_mdx source. NameTypeDefaultDescription source. dict[str, Any] - Returns: str - _existing_page source. Return the page content currently on disk, or None when unknown. Builders are not required to expose read_page; when it is missing the content is treated as unknown so the page gets rewritten, which keeps warm builds from serving stale pages. NameTypeDefaultDescription builder. Any. - route. str. - Returns: str | None - _write_route_meta source. NameTypeDefaultDescription builder. Any. - route. str. - title. str. - Returns: None - _merge_meta_entry source. Insert or replace a single _meta.ts entry non-destructively. Only the plugin\u2019s own entry (and, when requested, a missing hidden index entry) is touched; every other line \u2014 including nested object entries written by the sidebar generator \u2014 is preserved verbatim. NameTypeDefaultDescription builder. Any. - directory. str. - slug. str. - title. str. - hidden_index. bool. False. Returns: None - _meta_entry_lines source. Serialize entries with folio\u2019s exact _meta.ts formatting. NameTypeDefaultDescription meta. dict[str, Any] - Returns: list[str] - _escape_meta_key source. NameTypeDefaultDescription key. str. - Returns: str - _has_top_level_key source. NameTypeDefaultDescription body. list[str] - key. str. - Returns: bool - _brace_delta source. Net brace/bracket depth change, ignoring braces in string literals. NameTypeDefaultDescription line. str. - Returns: int - _load_spec source. NameTypeDefaultDescription raw_source. dict[str, Any] - project_dir. Path. - Returns: Any - _normalize_route source. NameTypeDefaultDescription raw_route. Any. - title. str. - Returns: str - _source_label source. NameTypeDefaultDescription raw_source. dict[str, Any] - Returns: str - _servers source. NameTypeDefaultDescription spec. dict[str, Any] - Returns: list[str] - _operations source. NameTypeDefaultDescription spec. dict[str, Any] - Returns: list[dict[str, Any]] - _schemas source. NameTypeDefaultDescription spec. dict[str, Any] - Returns: list[str] - _string_list source. NameTypeDefaultDescription value. Any. - Returns: list[str] - _escape_mdx_attr source. NameTypeDefaultDescription value. str. - Returns: str - _escape_mdx_text source. Backslash-escape characters MDX treats as syntax in body text. Braces open ES expressions, angle brackets open JSX elements, and backticks open code spans; a raw backslash could form escapes itself. NameTypeDefaultDescription value. str. - Returns: str -"},{"url":"docs/why_folio/index.html","title":"Why Folio","content":"Ask AI. Why Folio. The questions that decide whether Folio is for you \u2014 answered honestly. \u201c Why Folio exists. Documentation rots because it lives apart from the code. Folio generates your docs from your source \u2014 the reference rebuilds from the current code, and the site it ships in is one you\u2019re proud to put your project\u2019s name on. That\u2019s the whole pitch: you keep writing docstrings and markdown, Folio turns them into a modern site with a generated API reference, instant search, and theming down to the last token. One command against a Python codebase produces: 1. docs.yaml. the entire configuration. 0. hand-written API pages. the reference is parsed from source. 40. components. typed vocabulary for rich pages. 0. servers. static output, deploy anywhere. Folio against the field. Feature matrix comparing Folio with pdoc, Sphinx, MkDocs, Mintlify, and GitBook.ToolPython APIGuidesStatic exportLLM friendlyExtensibilityOpen sourceGit + CIFolio Yes. Yes. Yes. Yes. Yes. Yes. Yes. pdoc Yes. Some. Yes. No. Some. Yes. Some. Sphinx Yes. Yes. Yes. No. Yes. Yes. Yes. MkDocs Some. Yes. Yes. No. Yes. Yes. Yes. Mintlify No. Yes. No. Yes. Some. No. Some. GitBook No. Yes. No. Yes. Some. No. Some. And the capabilities the standard matrix doesn\u2019t capture: CapabilityFolioSphinx / MkDocsDocusaurus / NextraMintlify Agent artifacts (llms.txt + machine output)on the roadmap: ir.json, MCP. \u2014 ~ Theming down to full template ownership. ~ \u2014 Versioned docsimplemented, stabilizing behind a flag. ~ Twenty years of ecosystem. \u2014 ~ \u2014 The last row is the honest one: Sphinx\u2019s extension depth is real, and if your docs depend on exotic extensions, evaluate Folio on a branch first. Folio\u2019s structural differentiator is one source model feeding both the human site and the machine artifacts. Choosing a docs tool. Why Folio instead of Sphinx or MkDocs Material? Same job \u2014 parse Python source into reference docs \u2014 with a different output ceiling: a modern Next.js/shadcn site, instant search, theming to full template ownership, and agent-readable artifacts, from one docs.yaml. The trade: Sphinx\u2019s extension ecosystem is twenty years deep, and Folio\u2019s static analysis doesn\u2019t cover everything yet (see the SWOT below \u2014 we put it in writing). Why not Docusaurus, Nextra, or Fumadocs? Excellent site frameworks for hand-written pages. Python API reference needs a separate integration; Folio reads it from source and supplies the site as one workflow (built on Nextra internally). Why not Mintlify or GitBook? Hosted products, per-seat pricing, and they ingest prose, not source. Folio is a build tool: static output you deploy anywhere (GitHub Pages workflow included), no vendor in the serving path, and the reference is generated from code rather than scraped from markdown you maintain by hand. I'm migrating from Sphinx. What doesn't convert yet? Being honest: Folio\u2019s parser is static AST analysis. Inherited members, intersphinx inventories, and runtime-generated APIs are not covered today \u2014 they\u2019re tracked on the public roadmap\u2019s migration phase alongside a conf.py/mkdocs.yml importer with a what-didn\u2019t-convert report. Why not just have an LLM emit the HTML? Because docs have exactly one non-negotiable property \u2014 being true \u2014 and that\u2019s a provenance problem, not a fluency problem. Six concrete reasons, each one structural: 1 \u00b7 Ground truth beats plausible text An LLM writing HTML documents what it believes your API is. Folio reads signatures, types, and defaults from the source AST instead of asking a model to invent them. Static analysis has limits, but provenance stays inspectable. 2 \u00b7 A snapshot vs a function Emitted HTML rots on the first commit that changes your code. Folio is f(source, markdown) \u2192 site: re-run on every push, the reference updates itself, and every internal link is checked, with the broken ones named in the build output. Regenerating HTML re-rolls the hallucination dice on content that didn\u2019t change \u2014 and bills you tokens for markup boilerplate. 3 \u00b7 Reviewable diffs are the safety rail When an agent moves a kanban card here, the diff is a one-line status: frontmatter edit a human reviews in seconds. If agents regenerated HTML, the diff would be megabytes of unreviewable markup. Small semantic sources of truth are the only real safety rail in human\u2013agent collaboration. 4 \u00b7 A site needs a linker A whole site needs shared navigation, one search index, cross-references that resolve, versioning, social cards. LLMs emit locally plausible pages that don\u2019t cohere globally; Folio provides the global invariants: xref resolution, a link checker that names every broken internal link, generated sidebars. 5 \u00b7 Constrained generation appreciates over time In raw HTML every div soup is \u201cvalid\u201d \u2014 infinite ways to be wrong. Writing Folio markdown, an agent picks from a typed component vocabulary and the build rejects what doesn\u2019t exist. And the same markdown renders better over time as the template improves: HTML artifacts depreciate, Folio artifacts appreciate. 6 \u00b7 The next reader is an agent Emitted HTML is the worst format for other agents \u2014 it forces lossy scraping. Folio keeps the semantic source as the artifact and emits every output from one pass: HTML for people, markdown mirrors and machine-readable output for agents. \u201c That question, compressed. LLMs write better assembly every month \u2014 that\u2019s exactly why you want a build step you can trust. Markdown plus typed components is the high-level language; Folio does the generating, the linking, and the checking. The honest SWOT. Folio, mid-2026. internal \u2191 \u00b7 external \u2193 S Strengths. Typed source IR \u2014 signatures come from source. One pass emits the human site AND the agent artifacts. Released plugin platform; git-as-database patterns proven. Theming from tokens to full template ownership. W Weaknesses. Young ecosystem \u2014 no third-party plugins yet. Node + pnpm required at build time. Static parsing: inherited members and intersphinx pending. Python today \u2014 TypeScript, Rust, Go, C#, and Java on the roadmap, same funnel. O Opportunities. Material-for-MkDocs maintenance mode: thousands of projects re-deciding. Agent-readable docs becoming table stakes \u2014 priced at $100+/mo elsewhere. Skills in agent frameworks are markdown: Folio is their natural renderer. T Threats. Zensical consolidating the MkDocs succession first. Hosted platforms moving down-market with free tiers. \u201cGood enough\u201d LLM-generated one-off sites for small projects. Practical. Do I need to know React or Next.js? No. You write markdown and a docs.yaml. Components are tags in markdown (<Callout>, <Swot>, <Roadmap>\u2026) with documented props. React only becomes relevant if you opt into deep customization. Why does a Python tool need Node? Because the output is a Next.js static site \u2014 that\u2019s where search, theming, and interactivity come from. Folio manages the frontend workspace (.build/ is disposable) and checks your toolchain up front, but Node \u2265 20.19 and pnpm must exist on the build machine. The deployed site needs neither. If I drag cards on the kanban board, who sees it? Folio for Agents owns that workflow. Moves become folio board move commands and ordinary commits; Folio Docs only renders the board when its optional integration is installed. Other languages? Versioned docs? i18n? Python today; TypeScript, Rust, Go, C#, and Java are a roadmap phase, and the machine-readable IR schema is language-aware from day one. Versioned docs and i18n are implemented behind FOLIO_EXPERIMENTAL while they stabilize \u2014 graduating versions is part of the active phase. A question this page doesn\u2019t answer? Open an issue \u2014 unanswered questions are documentation bugs."},{"url":"docs/deployment/github-pages/index.html","title":"GitHub Pages","content":"Ask AI. GitHub Pages. GitHub Pages serves static files. Folio\u2019s Pages integration publishes the same _site/ artifact used by other hosts, with extra handling for project-site base paths and branch previews. Base Path Setup. Set project.url in docs.yaml to the final Pages URL before deploying so sitemap and metadata use the public URL. For GitHub Pages builds, set FOLIO_DEPLOY_PROVIDER=github-pages in the build step or add deploy.provider: \"github-pages\" to docs.yaml. Folio then infers /repo-name for project pages from GITHUB_REPOSITORY, while user and organization pages such as owner.github.io stay at /. Use FOLIO_BASE_PATH or deploy.base_path only when you need an explicit override. Production Deploys. Build your docs. Publish the static site. Upload _site/ as the GitHub Pages artifact. Automate with GitHub Actions. Use the workflow in CI/CD when Pages should deploy on push. Branch Previews. folio init creates a second GitHub Actions workflow for branch previews on the same GitHub Pages site. It runs from pull_request_target: PR branch code is built in an unprivileged job, and the privileged deploy job only consumes the static _site/ artifact. The preview appears under /previews/pr-<number>-<branch>/, for example https://owner.github.io/project/previews/pr-17-docs-redesign/. GitHub Pages publishes one artifact for the whole site. Folio keeps production and branch previews together in that single artifact: A main deploy builds production docs at the site root. It restores existing preview folders from the internal folio-pages-state branch. A branch deploy starts from the saved folio-pages-state root, downloads the static preview artifact, replaces only /previews/pr-<number>-<branch>/, and deploys the complete artifact again. That means the production URL stays stable while PR previews are updated independently. The preview index is available at /previews/, and each deploy prints the preview URL and index URL in the Actions summary before verifying both return HTTP 200. Successful preview deploys also create or update one sticky PR comment with the latest preview link, so reviewers do not need to open the Actions run to find it. State Branch. The production workflow and preview workflow cooperate through an internal folio-pages-state branch. This branch stores the last complete Pages artifact so a preview deploy can replace one preview folder without dropping production docs or other previews. Do not edit the state branch manually unless you are intentionally repairing the published Pages artifact."},{"url":"docs/api-reference/folio_docs/docs/xref/index.html","title":"folio_docs.docs.xref source","content":"Ask AI. folio_docs.docs.xref source. Functions. build_symbol_index source. Build a mapping of fully-qualified symbol names to documentation URLs. Returns a dict like: { \u201cfolio_docs.config\u201d: \u201c/docs/api-reference/folio_docs/config\u201d, \u201cfolio_docs.config.Config\u201d: \u201c/docs/api-reference/folio_docs/config#config\u201d, \u201cfolio_docs.config.load_config\u201d: \u201c/docs/api-reference/folio_docs/config#load_config\u201d, } NameTypeDefaultDescription modules. list[ModuleIR] - docs_route_base. str. '/docs' Returns: dict[str, str] - _index_class source. Index a class and its inner classes recursively. NameTypeDefaultDescription index. dict[str, str] - parent_fqn. str. - mod_route. str. - Returns: None - _extract_bare_names source. Extract all potential symbol names from a type string. Handles generics like list[Config], unions like Config | None, and comma-separated types like dict[str, Config]. NameTypeDefaultDescription type_str. str. - Returns: list[str] - _split_respecting_brackets source. Split a string on commas, but not inside brackets. NameTypeDefaultDescription s. str. - Returns: list[str] - resolve_type_link source. Try to resolve a type string to a documentation URL. Handles: Simple names: Config -> look up {current_module}.Config, then all modules. Qualified names: folio_docs.config.Config -> direct lookup. Generic types: list[Config] -> resolve the inner type Config. Union types: Config | None -> resolve Config. Returns the URL if a single resolvable symbol is found, None otherwise. NameTypeDefaultDescription type_str. str. - index. dict[str, str] - current_module. str. - Returns: str | None -"},{"url":"docs/components/param_table/index.html","title":"ParamTable","content":"Ask AI. ParamTable. A structured table for displaying function or method parameters with name, type, default value, and description columns. This component is typically auto-generated from Python docstrings during the build, but you can also use it manually for hand-crafted documentation. API. ParamTable. PropTypeDefaultDescription args. Param[] \u2014 Array of parameter objects. Param object. FieldTypeDefaultDescription name. string. \u2014 Parameter name. type. string. \u2014 Type annotation string. default. string. \"-\" Default value. Shown as - if omitted. description. string. \u2014 Description of the parameter. href. string. \u2014 Optional URL to link the type badge to its documentation. Example. NameTypeDefaultDescription host. str. \"localhost\" Server hostname. port. int. 8080. Server port number. debug. bool. False. Enable debug mode. workers. int. - Number of worker processes. Required."},{"url":"docs/components/hook-map/index.html","title":"HookMap","content":"Ask AI. HookMap. Visualize an extension lifecycle with stage names, hook names, and short descriptions. API. PropTypeDefaultDescription title. string. \"Extension lifecycle\" Panel title. hooks. HookMapItem[] \u2014 Ordered lifecycle rows. HookMapItem supports stage, hook, and description. Example. Extension lifecycle. 1 Lint. run_linters. Check formatting and static analysis before tests. 2 Test. run_tests. Execute the project test suite with coverage enabled. 3 Build. build_artifacts. Build distributable artifacts from the verified sources. 4 Deploy. publish_site. Upload the built output to the hosting provider."},{"url":"docs/components/stat-strip/index.html","title":"StatStrip","content":"Ask AI. StatStrip. A row of big numbers with labels \u2014 the fastest way to make scale legible. Values are strings, so units and symbols render exactly as written. API. StatStrip. PropTypeDefaultDescription stats. { value, label, detail? }[] \u2014 One entry per number. Example. Project stats. 94. pages. generated per build. 40. components. 867. tests. green before every merge."},{"url":"docs/api-reference/folio_docs/ir/index.html","title":"folio_docs.ir source","content":"Ask AI. folio_docs.ir source. Classes. @dataclass. class ArgIR. source @dataclass. class ReturnIR. source @dataclass. class RaiseIR. source @dataclass. class VarIR. source @dataclass. class DocstringIR. source @dataclass. class FunctionIR. source @dataclass. class ClassIR. source @dataclass. class ModuleIR. source."},{"url":"docs/components/class-overview/index.html","title":"ClassOverview","content":"Ask AI. ClassOverview. A card displaying a Python class with its name, base classes, decorators, and description. This component is typically auto-generated from source code during the build, but you can also use it manually to create hand-crafted class documentation. API. ClassOverview. PropTypeDefaultDescription name. string. \u2014 The class name. bases. (string | { name: string, href?: string })[] [] List of base classes. Each entry can be a plain string or an object with name and optional href for linking to the base class docs. decorators. string[] [] List of decorator names (displayed with @ prefix). description. string. \u2014 A short description of the class. Example. Basic usage with string base classes: @dataclass. class FederatedLearner(BaseNode, Serializable). A node that participates in federated learning rounds. With linked base classes: @dataclass@deprecated. class FederatedLearner(BaseNode, Serializable). A node that participates in federated learning rounds."},{"url":"docs/kanban/cards/kanban-single-board-with-filters/kanban-references-teardown/index.html","title":"Kanban reference teardown \u2014 SVAR React Kanban and ReUI Kanban","content":"Ask AI. Kanban reference teardown \u2014 SVAR React Kanban and ReUI Kanban. Researched 2026-08-04 for the board redesign. Both libraries were read at the source, not the marketing page. The question was narrow: what should folio\u2019s board take, and should it take a dependency. What they are. SVAR React Kanban (@svar-ui/react-kanban) \u2014 a widget from XB Software, the shop behind the SVAR suite (Gantt, DataGrid, Calendar). The free edition is MIT; a separately sold PRO build adds undo/redo, export, and lazy column loading. The React port is very new: one published version (2.6.0, June 2026), 14 GitHub stars, no issue history. The battle-tested original is the Svelte package. Note the package name wx-react-kanban does not exist; that was the pre-rename scope. ReUI Kanban (reui.io, Keenthemes) \u2014 not an npm package but a shadcn-style copy-paste registry: npx shadcn@latest add @reui/kanban drops a ~950-line MIT file into your components directory. Built on dnd-kit v6 (@dnd-kit/core + /sortable + /utilities). The kanban is in the free tier; the polished \u201cKanban Board\u201d blocks are paid. Styles. SVAR ships compiled CSS with two entry points (12 KB kanban-only, 108 KB for the full widget suite). Theming is 224 --wx-* custom properties supplied by a <Willow> / <WillowDark> wrapper, plus ~20 kanban-specific tokens (--wx-kanban-column-bg, --wx-kanban-drop-placeholder-bg, --wx-kanban-priority-high-bg\u2026). Semantic class names are documented as stable (wx-card, wx-column, wx-over-limit, wx-drop-placeholder) but every rule sits at (0,2,0) specificity behind build-generated hash classes, so overrides need escalated selectors. The default skin: Open Sans 14px, 3px card radius, sky-blue primary, hairline column headers \u2014 closer to a 2019 Trello than to current Jira or Linear. Dark mode changes the brand hue from blue to violet. ReUI is almost unstyled by design: the component carries layout classes only (grid auto-rows-fr gap-4 sm:grid-cols-3, flex flex-col gap-2, opacity and cursor states). All visual identity comes from the surrounding shadcn Card/Badge/Avatar. Its default \u201cnova\u201d style is rounded-xl, a 1px ring rather than a border (ring-1 ring-foreground/10), padding driven by a --card-spacing variable, and no shadow anywhere. Every structural node carries data-slot, plus data-dragging, data-disabled, data-value \u2014 clean CSS hooks that let a theme restyle the board without touching source. Neither skin is a starting point for folio: proposal.html is sharp corners, flat cards on 1px borders, recessed lanes, no foreign hues. The anatomy underneath is what is worth reading. Functionality worth naming. SVAR has the deeper feature set: named store actions with an intercept() veto seam, a card editor with placement=\"sidebar\" | \"modal\" | \"inline\", filters as a Map<string, predicate> where each entry is removable by tag, regrouping by any field via columnAccessor as a {get, set} pair, WIP limits that tint the column, column collapse to a rail, card and column virtualization, and a REST data provider. ReUI is a drag layer plus layout, and honest about it: no filtering, no search, no selection, no card detail, no WIP limits. What it does carry is the right persistence API \u2014 onValueChange for live preview and onValueCommit(value, meta) firing once per completed drag with meta.previousValue, built for optimistic update and rollback \u2014 plus sensor constraints tuned so cards stay clickable (10px mouse distance, 250ms touch long-press). Two defects in ReUI worth recording because they are patterns to avoid, both verified in its source: card keyboard drag is broken as shipped (the item spreads dnd-kit\u2019s attributes while the handle spreads listeners, so a keydown on the focused item never reaches the activator), and the column drag handle is opacity-0 until hover with no focus-visible counterpart, so a keyboard user can focus an invisible control. Dependency verdict: keep the hand-rolled drag. Do not adopt either as a dependency. The problem people reach for dnd-kit to solve is not a drag problem. Our drag breaks under filtering because a drag source is a {columnIndex, cardIndex} pair resolved against the visible board, not because HTML5 drag-and-drop is inadequate. dnd-kit fixes it as a side effect of being id-keyed; so does refactoring to a card id, which our own export path (folio kanban move <id> <column>) already speaks. Sortable\u2019s core value is unusable here. Intra-column order is computed by the Python loader and the CLI writes only status:; column order lives in a committed board.yaml with no command to rewrite it. Both reorder gestures would be theatre. The keyboard argument does not survive contact \u2014 see the ReUI defect above. Our explicit move buttons are more discoverable for a four-column board; they only lack an announcement. Touch is the one genuine gap (HTML5 drag does not fire on touch), and it does not need a library: SVAR\u2019s pointer-event implementation is a ~200-line recipe we can own. ReUI pins dnd-kit v6, the legacy line; the maintainer\u2019s rewrite is a separate package. Adopting v6 today buys a migration tomorrow, in a file folio redistributes to every user. SVAR as a dependency is worse: 14 runtime dependencies including an 833 KB core, for a 27-card board, in a template every folio user carries. What to take. Ordered by user value. None of these needs a dependency. Address cards by id, not by index. The root cause of our worst limitation: because a drag source is a pair of array indices resolved against the visible board, we disable drag and the move buttons whenever a filter is active. Filtering a board and then moving what you found is the most natural thing a user does, and today it is forbidden. The same defect lets the open dialog re-point at a different card, since the selection is also an index pair and the dialog has no focus trap. Fix: one uid per card, moves by uid, and the localStorage overlay becomes a Record<uid, columnId> \u2014 which also survives a baseline edit that merely adds a card, where today the whole overlay is discarded. A drop placeholder at the actual landing position. Today the only drop feedback tints the whole target column, while the move always appends to the end, so a card teleports to the bottom of a long backlog. SVAR\u2019s in-flow placeholder is honest about the landing slot. ReUI\u2019s live reflow is the other honest option, but we must not copy it: we have no write-back for intra-column position, so a gesture implying \u201cthird from top\u201d would lie. Move buttons visible on coarse pointers, and a real focus ring. Our move buttons are hover-revealed, and HTML5 drag does not fire on touch \u2014 so on a phone the only way to move a card is a control that is invisible and still hit-testable. A pointer-coarse variant and a proper focus-visible outline fix both. An aria-live region announcing moves and filter results. A screen reader user pressing the move button gets no feedback today. The best accessibility value per line available, and the one idea worth taking from dnd-kit without taking dnd-kit. Undo the last move. Our only escape from a mis-drop is \u201cReset to source\u201d, which discards every staged move. One snapshot per mutation turns a destructive control into a recoverable one. Take the idea from ReUI\u2019s previousValue snapshot, not from SVAR, whose undo is PRO. Filters as a facet record with predicates. Our filter function takes three positional arguments and is called from eight sites; every new facet edits all of them. SVAR\u2019s per-tag predicate map extends to assignee, priority, or blocked-by without touching the call sites, and matches the removable token pills the redesign already specifies. data-slot attributes on every structural node. Folio ships this component into users\u2019 repositories, and \u201cTheme contract for plugin surfaces\u201d is a live backlog card whose whole point is that themes must restyle plugin pages without forking them. Right now the only styling seam is editing the component. ReUI gets this right for about eight attributes. Bigger ideas, each needing its own card. Pointer-event drag replacing HTML5 drag, for touch support. SVAR\u2019s implementation is the recipe: 4px threshold, touch-action: none, a fixed ghost preserving the measured size and grab offset, elementFromPoint hit testing, a suppressed synthetic click on drop, Escape to cancel. Group the board by a field other than status. SVAR\u2019s columnAccessor {get, set} pair is the best structural idea in either library: the same cards regroup by milestone or assignee, with set writing the field back on drop. Blocked on a write path \u2014 folio kanban move only writes status:. Column collapse to a rail. Our backlog holds two thirds of the cards while two columns are empty, so density is genuinely lopsided. Named actions with an interceptor seam. Every mutation as a named, typed action with one place to observe and one to veto. It would let WIP limits stop being purely advisory, and give the export path, the undo stack, the announcer, and the overlay a single seam. What to reject outright. In-browser card creation and editing (SVAR\u2019s editor, add-card, context menu): it contradicts the file-backed board by design. Card files are hand-authored Markdown that a YAML round-trip would destroy, which is why every mutation is verified line surgery run by the CLI in the repo. The browser captures intent and exports commands; that boundary is the product, not a limitation. Also rejected: the REST provider and dynamic loading (no server), virtualization (27 cards, and it breaks in-page find), drag-to-reorder within a column or between columns (no honest destination), and either default skin. Licensing. Both are MIT with no copyleft and no attribution banner, so ideas and techniques carry no obligation. Copying code verbatim does carry the notice requirement, and this template is redistributed into users\u2019 repositories where it currently has zero third-party notices \u2014 so for anything taken, write it against our own data model rather than pasting. SVAR PRO is out of bounds: the licence forbids SaaS and permits redistribution only \u201cas long as it doesn\u2019t compete with SVAR\u201d, which is a live risk for a product whose model is redistributing a template."},{"url":"docs/cli/index.html","title":"CLI Reference","content":"Ask AI. CLI Reference. Every command, flag, and option for the shared folio CLI. Running folio with no arguments displays the help text. Installed Folio products can add command groups; Folio for Agents contributes folio board. Initialize. folio init. Create docs.yaml for an existing Python project. --yes. Build. folio build. Generate MDX, search data, LLM files, and static output. --clean--open--verbose. Serve. folio serve. Start a local authoring server with source watching. --port--open--kill-existing. Coverage. folio coverage. Measure docstring coverage and enforce a minimum threshold. --min--verbose. Board. folio board. Operate the repository-native Agents board when Folio for Agents is installed. --project-dir--config. Global Options. FlagShortDescription --version. -V. Print the installed Folio Docs version and exit. --install-completion. Install shell completion for the current shell. --show-completion. Print shell completion code for manual setup. --help. Show the help message and exit. It prints the CLI host version, for example folio 0.3.0. Commands. folio init. Generate a docs.yaml configuration file for an existing Python project. This is the recommended way to get started. Argument / OptionRequiredDefaultDescription DIRECTORY. No. Current working directory. Path to the project directory where docs.yaml will be created. --yes / -y. No. false. Skip all prompts and use detected defaults (useful for CI/scripts). Interactive wizard: When run without --yes, folio init launches an interactive wizard that walks you through configuration: Interactive terminals use compact inline arrow-key menus for list choices and inline (Y/n) hints for short yes/no choices; press y/n to submit yes/no choices directly. The intro banner includes a Folio update line that refreshes in place every second while arrow-key prompts are open. Detected project settings are pre-filled from the project scan. What it does: Checks if docs.yaml already exists. If it does, prints a message and exits without overwriting. Detects the project name and version from pyproject.toml (if present). Detects the GitHub repo URL from git remote origin (if available). Prints created file paths relative to the current shell directory, so folio init tmp/project reports tmp/project/docs.yaml. Detects the Python source directory by checking, in order: src/<package_name>/ (src layout). src/ (generic src directory). <package_name>/ (flat layout). <project_name>/ (flat layout with project name). Displays the detected project settings, pre-fills them, then prompts for docstring style and visual preset (or uses defaults with --yes). Generates a docs.yaml file with the chosen theme preset. Creates a docs/ directory with a starter index.md file if one doesn\u2019t exist. Creates a GitHub Pages workflow at .github/workflows/pages.yml if it does not already exist. Creates a GitHub Pages PR preview workflow at .github/workflows/branch-previews.yml if it does not already exist. Examples: Exit codes: CodeMeaning 0. Success, or docs.yaml already exists (skipped). folio build. Parse Python source code and documentation files, then generate a static documentation site. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --verbose. -v. flag. false. Show detailed output during the build process. --config. -c. TEXT. \"docs.yaml\" Path to the config file, relative to the project directory. --clean. flag. false. Force full rebuild, clearing the .build/ cache directory. --open. -o. flag. false. Serve the built static site locally, open it in your browser, and keep the preview server running until interrupted. What it does: Loads the config file from the project directory. Parses Python source files and extracts docstrings, class hierarchies, and module structure. Converts Markdown documentation files to MDX. Generates the Nextra site in a .build/ directory. Runs the Next.js build to produce the final output. Build output uses the same compact terminal rhythm as folio init: a centered Folio banner followed by short status rows for sources, template prep, page generation, link checks, dependencies, export, completion, and the ready output path. Warnings stay attached to their step instead of appearing as raw Python warning traces. During static export, Folio prints the full export log once in a bordered build output panel and also saves it to .build/.folio-build.log. The incremental cache tracks source file hashes plus config, template, and generator inputs. Changing docs.yaml, Folio\u2019s generator code, or the site template invalidates generated pages even when Python and Markdown sources are unchanged. Examples: folio build --open is intentionally blocking after the build: it starts a static preview server and waits until you press Ctrl+C. Exit codes: CodeMeaning 0. Build succeeded. 1. Build failed. Error details are printed to the console. Common causes: missing config file, parse errors, or Next.js build failure. folio serve. Build the documentation and start a local development server with hot reloading. This is the primary command during development. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --verbose. -v. flag. false. Show detailed output. --config. -c. TEXT. \"docs.yaml\" Path to the config file. --port. -p. INT. 4321. Port for the development server. Fails with a clear message if the port is already occupied. --open. -o. flag. false. Open the documentation in your default browser after the server starts. --clean. flag. false. Force full rebuild, clearing the .build/ cache directory. --kill-existing. flag. false. Stop an existing process on the selected port before serving. What it does: Performs the same build steps as folio build. Starts a Next.js development server on the specified port. File watching: after the dev server starts, Folio watches your Python and Markdown source files for changes. Edits are automatically detected, the affected pages are regenerated, and the browser updates via hot reload. Incremental builds: subsequent serve runs reuse cached dependencies (.build/node_modules/) and skip pnpm install when deps haven\u2019t changed. Keeps the server running until interrupted with Ctrl+C. The --clean flag forces a full rebuild from scratch, clearing all caches. Examples: Exit codes: CodeMeaning 0. Server stopped cleanly (via Ctrl+C). 1. Failed to start. Usually a missing config file or build error. folio coverage. Analyze docstring coverage for configured Python source files. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --config. -c. TEXT. \"docs.yaml\" Path to the config file. --verbose. -v. flag. false. List each undocumented symbol. --min. FLOAT. 0. Minimum coverage percentage. Exits with code 1 if coverage is below this value. Examples: Exit codes: CodeMeaning 0. Coverage check completed and met the configured threshold. 1. Config was missing, no modules were found, or coverage was below --min. folio clean. Remove generated build artifacts and output directories. ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. What it does: Removes two directories: .build/ \u2014 the intermediate Nextra project used during the build process. The output directory \u2014 by default _site/, or whatever output is set to in docs.yaml. The clean command reads this value directly from YAML, so cleanup still works if a configured plugin is missing or broken. If neither directory exists, it prints \u201cNothing to clean.\u201d Examples: Exit codes: CodeMeaning 0. Always succeeds. folio roadmap. Preview the source-defined roadmap phases configured under the roadmap: key in docs.yaml as a table. Provided by the first-party default roadmap plugin (see the Roadmap page). ArgumentTypeDefaultDescription DIRECTORY. PATH. Current working directory. Project directory containing docs.yaml. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Compatibility option for scripts that prefer named arguments. --config. -c. TEXT. \"docs.yaml\" Path to the config file. Examples: Exit codes: CodeMeaning 0. Phases were listed, or no phases are configured. 1. Config file was missing, or conflicting directory arguments were passed. folio board. Operate the repository-native board. This command group appears automatically when the folio-agents distribution is installed in the same environment as the CLI host. OptionShortTypeDefaultDescription --project-dir. PATH. Current working directory. Project containing agents.yaml. --config. -c. TEXT. \"agents.yaml\" Path to the Agents config file. The group includes init, show, check, add, move, update, trail, comment, and attach. The full behavior and write contract live in the Folio for Agents CLI reference . Error Messages. Here are common errors you might encounter and what they mean: Error: Config file not found: docs.yaml No docs.yaml was found in the project directory. Run folio init to create one, or specify the correct path with --config. Build failed: ... The build process encountered an error. Run with --verbose for detailed output. Common causes include syntax errors in Python source files, invalid YAML in the config, or missing dependencies. docs.yaml already exists, skipping. The init command found an existing config file and did not overwrite it. This is informational, not an error. Typical Workflow. A typical documentation workflow looks like this:"},{"url":"docs/migration/index.html","title":"Migrating from Sphinx","content":"Ask AI. Migrating from Sphinx. Step-by-step guide to moving your project from Sphinx to Folio. Why migrate. Simpler configuration. Sphinx uses a Python conf.py file that can grow to hundreds of lines with complex extension configurations. Folio uses a single docs.yaml, and folio init writes one about thirty lines long. Modern UI. Folio generates a responsive, dark-mode-enabled site with a component-based UI built on Next.js and shadcn/ui. No theme hunting or template customization required. Faster iteration. The dev server supports hot reload \u2014 edit your docstrings and see changes instantly without running a full rebuild. Zero-config API docs. Point Folio at your source directories and it automatically generates API reference pages. No autodoc directives, no .. automodule:: boilerplate. Step-by-step migration. 1. Install Folio. 2. Initialize configuration. Run the init command in your project root. It detects your project name, version, and source layout from pyproject.toml: This creates a docs.yaml file. Edit it to point to your Python source directories: 3. Convert existing documentation. If you have hand-written documentation in .rst format, convert it to Markdown before adding it to source.docs. Folio does not ship or invoke an RST converter; use your migration tool of choice, review the generated Markdown, and then let Folio build the converted pages. Folio warns about .rst files only when those files are still present inside a directory listed in source.docs. It does not emit a warning if the .rst files were already removed before running folio build or folio serve. Markdown docs routes use kebab-case URLs. A source file such as common_errors/index.md is published at /docs/common-errors/. Folio also generates underscore aliases for hand-written docs routes, so /docs/common_errors/ keeps working after migration. API reference routes are the exception: Python package and module names keep their original underscores. 4. Update Sphinx-style docstrings. Folio supports Google-style and NumPy-style docstrings natively \u2014 if your project uses NumPy-style docstrings, just set source.python.docstring_style: \"numpy\" (or rely on \"auto\") and no conversion is needed. Only Sphinx-style docstrings (:param name:) need to be converted. Here is a comparison: Sphinx style. def connect(host, port=8080): \"\"\"Connect to a server. :param host: The hostname. :type host: str :param port: The port number. :type port: int :returns: A connection object. :rtype: Connection :raises ConnectionError: If unreachable. \"\"\" Google style. def connect(host: str, port: int = 8080) -> Connection: \"\"\"Connect to a server. Args: host: The hostname. port: The port number. Returns: A connection object. Raises: ConnectionError: If unreachable. \"\"\" 5. Remove Sphinx configuration. Once migration is complete, you can remove Sphinx-related files: conf.py. Makefile (if only used for docs). make.bat. _build/ directory. _static/, _templates/ directories. Sphinx from your dependencies. 6. Build and verify. This starts a dev server at http://localhost:4321 where you can verify your documentation looks correct. Mapping Sphinx concepts to Folio. conf.py to docs.yaml. Sphinx conf.pyFolio docs.yaml project = \"Name\" project.name: \"Name\" version = \"1.0\" project.version: \"1.0\" extensions = [\"autodoc\"] Not needed (automatic). html_theme = \"furo\" Built-in theme (see Theming guide). html_static_path. Custom assets copied by a plugin or referenced from docs. html_logo. theme.logo. html_favicon. theme.favicon. exclude_patterns. source.python.exclude. autodoc_member_order. Not configurable (source order). autodoc to automatic generation. In Sphinx, you write explicit directives to pull in API documentation: In Folio, you list your source directories and everything is documented automatically: No per-module directives needed. Use __all__ in your Python modules to control which symbols appear in the documentation. RST directives. Folio does not translate RST directives during a build. Convert them before adding the page to source.docs, then review common constructs against this mapping: Common manual rewrites: RST DirectiveConverted To .. code-block:: python. Fenced code block (```python). .. note:: Callout (info). .. warning:: Callout (warning). .. tip:: Callout (tip). .. hint:: Callout (tip). .. danger:: Callout (danger). .. error:: Callout (danger). .. important:: Callout (warning). .. caution:: Callout (warning). .. attention:: Callout (warning). .. seealso:: Callout (info). .. deprecated:: Callout (danger) with version. .. versionadded:: Callout (note) with version. .. versionchanged:: Callout (note) with version. .. image:: path. ![path](path). RST headings (underlines). Markdown headings (#, ##, etc.). inline code `inline code` :role:`text` `text` Features that need manual redesign: RST FeatureStatus .. toctree:: Not needed (auto-generated navigation). .. include:: Not supported. .. math:: Not supported. .. table:: Use Markdown tables instead. .. raw:: Not supported. .. tab-set:: / .. tab-item:: Convert manually to <Tabs> and <TabItem> .. only:: Not supported. Cross-references (:ref:, :doc:). Not yet supported. Substitutions (|name|). Not supported. Field lists (:field:). Not supported. Footnotes. Not supported. For Sphinx tab sets, rewrite each block with Folio\u2019s MDX tabs components: For callouts that were written as Markdown blockquotes during migration, use Folio\u2019s Callout component directly. Patterns such as > **Warning:** should become <Callout type=\"warning\">. For example, convert > **Warning:** This changes state. to: Sphinx extensions to Folio features. Many common Sphinx extension use cases are built into Folio. Custom extension authoring is not part of the public MVP surface yet. Sphinx ExtensionFolio Equivalent autodoc. Built-in (automatic). napoleon. Built-in (Google style). viewcode. Source file + line tracking (built-in). intersphinx. Not yet supported. todo. Not yet supported. coverage. Not yet supported. doctest. Not yet supported. sphinx-copybutton. Built-in (code blocks have copy buttons). Custom extensions. Not public in the MVP. make html to folio build. Sphinx CommandFolio Command make html. folio build. make clean. folio clean. sphinx-autobuild. folio serve. sphinx-quickstart. folio init. What works differently. Sphinx-style docstrings require conversion. Folio supports both Google-style and NumPy-style docstrings. If your project uses NumPy-style docstrings, set source.python.docstring_style: \"numpy\" in your docs.yaml and no conversion is needed. If your project uses Sphinx (:param:) style, you need to convert them to either Google or NumPy style. This is the most significant migration effort for projects using Sphinx-style docstrings. Tools like pyment can help automate docstring conversion: Sphinx role cross-references require conversion. Sphinx has an extensive cross-reference system (:class:, :func:, :meth:, etc.) that creates links between documented symbols. Folio generates API links for parsed type annotations, but it does not support Sphinx role syntax yet. Role syntax like :class:\\MyClass`is converted to inline code (``MyClass` \u201c) and should be replaced with Markdown links where needed. No intersphinx. Sphinx\u2019s intersphinx extension lets you link to other projects\u2019 documentation. Folio does not yet support this. If your docs heavily link to external API documentation (e.g., linking to Python stdlib docs), those links will need to be replaced with plain URLs or removed. Source order, not alphabetical. Folio documents members in the order they appear in the source file. Sphinx\u2019s autodoc_member_order option with alphabetical sorting has no equivalent. No RST-only features in docstrings. If your docstrings use RST features like field lists, substitutions, or complex table markup, they will not be converted. Stick to plain text and Google-style sections in docstrings. Common gotchas. Docstring parsing errors. If a docstring does not follow Google style correctly, the parser falls back to treating the entire text as a plain description. Indentation matters \u2014 section content must be indented under the section header. Missing __init__ docs. Folio documents __init__ like any other method. If you were relying on Sphinx\u2019s autoclass_content = \"both\" to merge class and __init__ docstrings, you may need to adjust your docstrings. Private members. By default, Folio documents all top-level definitions. Use __all__ to control what gets included, or rely on the convention that names starting with _ are excluded from __all__. RST in Markdown files. If your Markdown files contain RST directives wrapped in {eval-rst} blocks, these are stripped during conversion. Replace them with Markdown equivalents. There is no folio migrate command yet that automatically rewrites {eval-rst} blocks, tab sets, or blockquote-style callouts into native Folio syntax. Review externally converted Markdown by hand before adding it to source.docs. Raw <iframe> tags are stripped. The Markdown-to-MDX sanitizer removes raw <iframe>, <script>, <style>, <video>, <audio>, <object>, and <embed> tags before writing generated MDX. A build can still succeed, but the embed will not render. Use a normal Markdown link or a reviewed MDX component instead of relying on raw YouTube or HTML embeds in source Markdown. Curly braces in Markdown. Since Folio outputs MDX (Markdown + JSX), bare curly braces { and } in your Markdown files are interpreted as JSX expressions. If your docs contain literal curly braces (e.g., in code explanations outside of code blocks), they will need to be escaped or placed in code blocks. Feature comparison. FeatureSphinxFolio Python API docs. Via autodoc extension. Built-in, automatic. Docstring styles. Google, NumPy, Sphinx. Google, NumPy. Configuration. Python (conf.py). YAML (docs.yaml). Output format. HTML, PDF, ePub, etc. HTML (Next.js). Theme system. Jinja2 templates. React + shadcn/ui. Dark mode. Theme-dependent. Built-in. Hot reload dev server. Via sphinx-autobuild. Built-in. Search. Built-in. Built-in (Pagefind). Cross-references. Full support. Generated API type links; Sphinx roles not yet. Intersphinx. Full support. Not yet. RST support. Native. Convert to Markdown before build. Markdown support. Via MyST. Native. Custom extensions. Extensive plugin ecosystem. Not public in the MVP. PDF output. Built-in. Not supported. i18n. Built-in. Not yet. LLM-friendly output. Not built-in. llms.txt + llms-full.txt. Custom components. Jinja2 macros. React/MDX components. Setup complexity. High. Low. Build speed. Moderate. Fast (Turbopack). Migration checklist. OK Install Folio and run folio init. OK Edit docs.yaml with project details and source paths. WARN Convert Sphinx-style docstrings. Use Google or NumPy style. NumPy-style docstrings are supported with source.python.docstring_style: \"numpy\". WARN Convert .rst files to .md. Use the migration converter for common directives or your preferred tooling. TODO Replace :ref: and :doc: cross-references. Use Markdown links or inline code where links are not available yet. TODO Remove {eval-rst} blocks from Markdown. TODO Move custom static assets into documented source assets or plugin output. TODO Run folio serve and verify every page. TODO Remove Sphinx configuration files and dependencies. TODO Update CI/CD scripts to use folio build."},{"url":"docs/agents/board/formats/index.html","title":"Board formats","content":"Ask AI. Board formats. A board is a directory of cards. The complete card schema is documented below. The write commands and the agent protocol are built for this format. Cardfile board. Point source: at a directory and the board becomes a cardfile board, the format built for boards that agents and humans operate together through git: board. board.yaml. SKILL.md. cards. \u251c\u2500\u2500 _TEMPLATE.md. \u2514\u2500\u2500 ship-browser-canvas.md. The board directory contains the column set in board.yaml (human-owned, changes rarely), an in-repo copy of the session protocol in SKILL.md, and the card files under cards/. Files starting with _ are not cards. Boards on another branch. Set ref when the board has an independent Git history. Folio reads that branch through a managed detached worktree under .worktrees/; it never merges the planning branch into the code checkout. Use source for one board or sources for named, independent canvases: Every source remains a normal cardfile board with its own board.yaml, WIP limits, cards, and release cycle. If sources share a column id, its title and limit must match. Card ids must be unique across the projection. board.yaml. board.yaml holds the column set, and optionally an icon per tag: title is optional; the optional browser adapter\u2019s kanban.title overrides it when both are set. Each column needs a title. id defaults to the slugified title. Duplicate column ids fail the build. limit must be a positive integer; an invalid value is warned about and ignored (no limit). icons maps a tag to an icon (an emoji string); a card wears the icon of its first mapped tag, on its face and in its dialog. A malformed map warns and renders nothing. Column order in board.yaml is column order on the board. Column order is also meaning: the last column is where blockers count as resolved. Unknown keys are silently ignored. There is no index of cards anywhere. Column membership is derived per card from its status frontmatter, which is what makes cross-card merge conflicts structurally impossible. Card files. Every card is one Markdown file in cards/. The filename stem is the card id: a permanent lowercase slug (ship-browser-canvas.md is card ship-browser-canvas, forever). A filename that is not already a slug fails validation. Files whose name starts with _ or . are ignored (so _TEMPLATE.md, editor lock files, and other stray editor files like .goutputstream-* never break a check), as is anything that is not .md. Machine state lives in the frontmatter, prose in the body: Frontmatter keys. The complete set. Required: KeyRule title. Non-empty string. Missing or empty fails the build. status. Non-empty string; must be a column id declared in board.yaml. Anything else fails the build. Optional (a bare key: with no value is YAML null and treated as absent): KeyRule tags. List of strings; non-string entries are filtered, a non-list value becomes an empty list. assignee. One name or a list: assignee: ana and assignee: [ana, bo] both work. Names are stripped; duplicates collapse. size. The one closed vocabulary: S, M, L, or XL (any case in the file; the board shows uppercase). Anything else fails the build naming the file. source. Where the work lives: a branch, repo#branch, or a URL. Free string like type; a URL renders as a link in the card dialog. track. Optional workstream inside one project. Free vocabulary. project. Not authored on a card. When kanban.sources maps several boards into one, each card takes the name of the source it came from, and that is what the board filters and the roadmap matches milestones against. type. Plain string, stripped. Free vocabulary for the kind of work (bug, plan, feature); the types that exist are the types cards use. link. URL string. http(s), mailto, and relative paths are accepted; javascript: and data: fail validation. priority. low, normal, or high (case-insensitive). Any other value warns and is treated as normal. order. Numeric rank, the explicit-ordering exception. A non-numeric value warns and the card is treated as unranked. created. ISO date YYYY-MM-DD, used for sorting. Any other value silently sorts last, as if the card were newest. milestone. Free string naming this product\u2019s release (quote it in YAML), such as 0.2. parent. Must be an existing card id on this board, and not the card itself; otherwise the build fails. blocked_by. Must be a list; every entry must be an existing card id and not the card itself; otherwise the build fails. A blocker is open until its card sits in the last-listed column. artifacts. List of one-line artifact entries (below); anything malformed fails the build. When the config declares a versioned roadmap, a milestone matching a phase milestone (or its version fallback) lets phase pages deep-link a filtered board. A milestone no phase claims draws a warning, never a failure. Artifacts. Five kinds: doc, api, file, pr, url. The committed form is a one-line single-key map, with an optional sibling label: on any kind. A card with a directory does not list its files. What sits at the top level of board/cards/<id>/ is that card\u2019s artifact list, derived at load: one entry per regular file, sorted by name \u2014 .md and .mdx become doc, everything else file. Dotfiles, _-prefixed names, subdirectories, and symlinks are skipped, the same lines publishing draws. The frontmatter block remains for what is not a file, and for labels: A doc: or file: entry naming a top-level sibling \u2014 as the bare name, as ./name, or as the full project-relative path older boards carry \u2014 attaches its label to the derived entry instead of adding a second one. Derived entries come first, name-sorted; the remaining frontmatter entries follow in written order. Targets read the way a markdown link reads. A relative doc:/file: target resolves against the card\u2019s directory first, then the project root. Every artifact records what was written beside where it resolved, and the tile shows what was written: plan.md, not a repetition of the directory the card already stands in. Validation is fail-fast on shape, and warns on reachability: Each entry must be a mapping with exactly one kind key (plus the optional label:); anything malformed fails the build. pr: must be a positive integer. url: targets pass the shared href scheme policy: javascript: and data: fail the build. api: targets only need to be a non-empty string; symbol existence is not validated yet. A doc:/file: target may carry a #L12-style fragment, stripped before checking. An absolute path or one escaping the project directory fails the build. One that resolves to no file warns, naming the card and the written target, and the tile renders unlinked \u2014 a stale path in one card is prose, not board topology. Three kinds of artifact open. A url: opens because it was written as a URL; the href is recomputed at build time and never trusted from input, so it cannot smuggle a javascript: past the scheme policy. A doc: or file: opens when it points inside the card\u2019s own directory, described next. And a doc: naming a file under a source.docs directory opens the documentation page the site builds from it; a doc: whose file exists but whose page nothing publishes warns at build, because doc: promised a page. Everything else renders as an unlinked tile showing its target. The board builds no URLs for whoever happens to host the repository: that assumed one host and produced a dead link for anything not committed there. Attaching a file is also not a license to publish an arbitrary part of the project. A card\u2019s directory. A card may keep a directory beside its markdown file, named for the card (see the index for how published artifacts open): board/cards. redesign-the-reader.md. redesign-the-reader. \u251c\u2500\u2500 prototypes-compared.md. \u251c\u2500\u2500 wide-reader.html. \u2514\u2500\u2500 wide-reader.css. The card file sits beside its directory. The directory holds what the card produced \u2014 and its top level is the card\u2019s artifact list, derived as described above, so putting a file there and attaching it are one act. A label goes on in the frontmatter, by the sibling\u2019s bare name: The build owns every file in that directory. Markdown and MDX become Folio pages under the configured docs route; other files are served verbatim at /_folio/kanban/<card-id>/. A derived artifact therefore is a link the reader can follow. The rules are short: The whole directory is published, not only what artifacts names. The files reference each other, and a page without its stylesheet opens as nothing. Markdown and MDX are compiled by Folio. report.md becomes <docs route>/kanban/cards/<card-id>/report/, with the project theme, search entry, sitemap entry, Markdown mirror, llms.txt entry, link validation, local-image copying, and incremental rebuild tracking. A leading _ opts out of compilation. Compiled pages stay out of the docs sidebar. A card\u2019s output answers to its URL and stays findable through search, the sitemap, llms.txt, and the Markdown mirror; the sidebar is the documentation\u2019s own table of contents, and board working papers are not part of it. Every folder route above a document resolves. Compiled documents sit below <docs route>/kanban/cards/<card-id>/, and readers reach those URLs by trimming a longer one or from a breadcrumb, so none of them 404s. An index.md or README.md in a folder (the card\u2019s directory or any subdirectory) becomes that folder\u2019s page. A folder that ships neither gets a generated index: the card\u2019s status line and first paragraph, then one tile per document and per attached artifact. A published prototype opens; an unpublished target stays an unlinked tile. The generated page carries the plugin\u2019s marker and steps aside: for a card-authored index the moment one appears, for a documentation page of yours that already owns the folder\u2019s URL, and entirely when the documents are gone. <docs route>/kanban/cards/ itself resolves to a directory of the publishing cards while anything publishes. With routes.docs: false the parent resolves too: <docs route>/kanban/ forwards to the public board, query and hash intact. A board published nowhere gets a directory of the cards that publish documents instead of the board page the configuration turned off. Everything else stays a raw bundle. HTML, CSS, JavaScript, images, and the original source files are copied under /_folio/kanban/<card-id>/ without rewriting, so an HTML prototype keeps loading its sibling assets. Dotfiles and dot-directories stay behind. .verify/, .cache/ and friends are session scratch by convention. Ownership is exact. An artifact cannot use ../ to claim another card\u2019s directory. Symlinks are refused, not followed. That includes the card directory itself and files below it, so a link out of the project cannot publish a file the project does not contain. The top level is the band on the card. Only the directory\u2019s top level derives artifacts; a subdirectory still publishes (a prototype keeps its assets) but is not listed, so curation is placement. A directory whose card is missing is reported. The filename stem names the directory; a card renamed or deleted leaves its directory orphaned and unpublished, and the build and folio board check warn, naming it. Nothing else about the card changes. The card is still board/cards/<id>.md, still found by the same glob, and a card without a directory is unaffected. What belongs in a card directory and what does not is a session convention, not a build rule: What a session leaves behind. Body structure. Description \u2014 everything before the first ## heading. ## Acceptance criteria \u2014 bullet lines of the form - [ ] text or - [x] text become checkboxes. Lines that do not match are silently skipped. ## Trail \u2014 one line per work session, oldest first, matching this grammar: The (ref), a commit sha or PR #n, is optional. The reader is tolerant: a line that misses the grammar warns at build time and still renders as an ordinary note (whose inline markdown renders like any other, per the dialog section). The writer (folio board trail) is strict: the date must be YYYY-MM-DD, the actor a single token, the ref free of parentheses and newlines, the note non-empty. A ref renders as the identifier it was written as, in mono, and the board builds no link from it. A sha is already an address and git show takes it; guessing which host would answer for it is how the board ended up printing dead links. This is also the reason artifacts has no commit kind and does not need one. Git is the source of output history, and a ref that has already been written down once should not have to be written twice. ## Comments \u2014 the card\u2019s conversation, one line per comment, the trail\u2019s grammar minus the ref: The trail records what happened; comments argue about it. Same tolerance contract: a line that misses the grammar warns at build time and still joins the thread as prose. The writer (folio board comment) is strict: date, single-token author, non-empty text with whitespace collapsed. The dialog draws the thread as its own band above the artifacts. Ordering inside a column. Cards sort deterministically by (has-rank, rank, priority, created, id): Cards with a numeric order come first, sorted by rank ascending. Unranked cards follow, by priority (high, normal, low), then by created date (older first; missing or malformed dates last), then by id, lexicographically. order is the rare explicit-ordering exception; everything else is computed, so reordering never has to touch another card\u2019s file. Validation. The contract: a typo in a note must never break validation; a typo in board topology must never silently pass. folio board check is the canonical gate. The optional Docs adapter consumes the same loader. Fails the build (and check exits 1): board.yaml missing, unparseable, or without a columns: list. a column without a title. duplicate column ids. a missing cards/ directory. a card filename that is not a slug. missing, unparseable, or non-mapping frontmatter. a missing title or status. a status that is not a column id. a size that is not S, M, L, or XL. a dangling or self-referential parent. a blocked_by that is not a list, or that contains a dangling or self-referential id. an artifacts value that is not a list. a malformed artifact entry. a non-positive pr: number. a doc: or file: path that is absolute or escapes the project directory. a url: artifact or a card link failing the href scheme policy. Warns only (build and check succeed): a column over its WIP limit. an invalid limit value in board.yaml (ignored). a trail line that misses the grammar (kept, rendered as plain text). an unknown priority (treated as normal). a non-numeric order (treated as unranked). a milestone no roadmap phase claims (only when the config declares a versioned roadmap). a doc: or file: target that resolves to no file (the tile renders unlinked). a doc: whose file exists but whose page nothing publishes (build only \u2014 check runs without the site\u2019s source configuration). a card directory whose card is missing (nothing publishes it)."},{"url":"docs/kanban/cards/how-a-published-board-accepts-a-change/panel-verdict/index.html","title":"Panel verdict: the write path lives in folio serve","content":"Ask AI. Panel verdict: the write path lives in folio serve. Three architectures were designed independently and attacked by four adversarial judges (simplicity, data integrity, security and deployment, product doctrine). Every repository claim the designs made was verified against the code before judging. This document is the durable record of what was considered, what won, and what the winner must do differently. The three candidates. serve-integrated. A stdlib HTTP thread inside the folio serve process. The web UI posts small operations; each one runs through the same kanban_edit surgery and the same one-operation-one-commit discipline the CLI already has; the existing watcher turns the file change into a refreshed board in every open tab. Zero new dependencies, zero new processes. The published static export stays byte-identical. standalone-sidecar. One separate stdlib process that serves the built _site and mounts the same write API, deployable on any small machine. The board probes for the API on its own origin and stays read-only when nothing answers. forge-mediated. No server at all: the browser writes through the GitHub Contents API with a fine-grained token, reproducing the CLI\u2019s commits with GitHub\u2019s compare-and-swap on the file sha as the concurrency guard. Verdict. All four judges ranked serve-integrated first (8, 8, 8, 8.5 out of 10) and none found a fatal flaw in it. The deciding fact: the backend already exists as tested pieces \u2014 kanban_edit.py writes cards with re-parse verification, kanban_cli.py holds the operations and the commit discipline, and the serve watcher already turns any card-file change into a refreshed board in about four seconds. The design adds only the missing HTTP surface, inside the process the owner already runs. The sidecar died on state: its server-side board cache could diverge from the files for every non-web writer, and a compare-and-swap against a stale cache blesses exactly the clobber it exists to prevent. The forge path died on duplication and custody: a second surgery implementation in TypeScript policed by corpus infrastructure, a vendor credential in the write path, and no improvement at all to the local flow where the board actually gets used. Two ideas from the losers were adopted: commit-per-write as the default (the forge design was the only one that got this right), and the sidecar\u2019s capability handshake plus expectation guards \u2014 every mutating operation carries what it believes the current state is, and a mismatch is a refusal, never a wrong write. Mandated changes to the winning design. Commit-per-operation defaults to on. Uncommitted web edits erode \u201cgit is the database\u201d at its most casual surface. Server-originated commits use a pathspec narrowed to the touched card file. The CLI\u2019s current whole-directory git add would silently sweep unrelated hand edits into a web gesture\u2019s commit. next dev binds to 127.0.0.1. It listens on all interfaces today, which is a pre-existing exposure the write API would inherit. The discovery file (serve.json) is deleted and rewritten at every serve start and is already deleted on static export; a killed process must not leave a stale token behind. No new hookspec for API routes while exactly one plugin wants them; the dev server calls the kanban plugin directly. A non-writing tab re-seeds its board state when the baked data module changes, so every tab converges after the watcher echo. The write envelope gets adversarial tests (form-encoded posts that skip preflight, absent Origin, rebound Host values), and kanban_edit\u2019s verified write becomes atomic (temp file and rename), which also protects the CLI against torn writes. Accepted limitation: the millisecond race between an editor save and a server write stays unguarded. With commit-per-operation on, a clobber is a visible commit; a cross-process lockfile is real complexity for a race no judge could construct outside a thought experiment. Owner delta, 2026-08-26. The published tier is not deferred indefinitely. The owner\u2019s direction: a deployed board (GitHub Pages) must be able to name a sync backend in the board\u2019s own configuration \u2014 a sync: key in docs.yaml \u2014 and write through it. That is the decision card\u2019s path 3, and it is the same server component grown a front door: folio kanban server running on a checkout, receiving the same operations, committing and pushing, and serving fresh board data back so the board is live even while the static host waits for its rebuild. The two implementation cards carry the plans: folio-serve-accepts-board-edits \u2014 the local write path, first. a-published-board-syncs-through-a-configured-backend \u2014 the deployed tier, on top of the same operations module. Owner delta, second round (2026-08-27). The deployed tier gains a same-origin mount. When sync.url is a path (/api/kanban), the backend is the static host\u2019s own function runtime \u2014 Vercel\u2019s Python functions beside the exported site \u2014 and git happens through the forge\u2019s Contents API with the file sha as compare-and-swap. The real kanban_edit surgery runs inside the function, which recovers the one virtue of the rejected forge design without either of its fatal flaws: no credential in the browser bundle, no second surgery implementation. Credentials are personal, by owner directive: either the deployer\u2019s fine-grained token in the deployment\u2019s own configuration, or each visitor\u2019s own credential passed through per request and never stored, with the repository\u2019s permissions as the access control. The security invariants were designed before the code \u2014 they are a numbered section of the sync card\u2019s plan."},{"url":"docs/api-reference/folio_docs/docs/integrations/roadmap/index.html","title":"folio_docs.docs.integrations.roadmap source","content":"Ask AI. folio_docs.docs.integrations.roadmap source. Functions. config_keys source. Returns: list[str] - configure source. NameTypeDefaultDescription config. Any. - raw_config. dict[str, Any] - Returns: None - register_extensions source. NameTypeDefaultDescription registry. Any. - config. Any. - Returns: None - emit_assets source. Compatibility hook for generated docs pages. Public routes and data modules are emitted through register_extensions(). NameTypeDefaultDescription builder. Any. - config. Any. - Returns: None - board_href source. Href to the public board from a view depth segments below the root. The roadmap sits at /roadmap, so depth is 1 today. It is a parameter rather than a constant because the caller knows the route and this helper does not, and a hard-coded single ../ was already wrong once. NameTypeDefaultDescription board_path. str. - depth. int. - Returns: str - project_keys source. Distinct project values, in the order their first phase appears. A phase without a project belongs to the default one, which is what the Roadmap component groups it under too. NameTypeDefaultDescription phases. Any. - Returns: list[str] - ordered_project_keys source. Project keys that have phases, in the order the page should show them. Project order is visible state \u2014 the order the groups are drawn in \u2014 so projects: declaration order in docs.yaml is the authority. A key that has phases but no projects: entry keeps its first-appearance order, after the declared ones. Declared keys with no phases are left out: there is nothing to draw. NameTypeDefaultDescription phases. Any. - projects. dict[str, dict[str, str]] - Returns: list[str] - project_block source. {key: {label, description}} for the projects the page will draw. A project with any configured field is included; one with none is left out entirely, because the component already falls back to the key. The filter is on \u201chas any field\u201d rather than \u201chas a label\u201d so a project described but not labelled still reaches the page. NameTypeDefaultDescription roadmap. dict[str, Any] - Returns: dict[str, dict[str, str]] - normalize_projects source. NameTypeDefaultDescription raw_projects. Any. - Returns: dict[str, dict[str, str]] - empty_roadmap source. An inert roadmap carrying every key a normalized one has. Callers index the result (roadmap[\"description\"]), so the empty shape and the parsed shape have to agree on their keys; they drifted once already. Returns: dict[str, Any] - normalize_roadmap source. NameTypeDefaultDescription raw_roadmap. Any. - Returns: dict[str, Any] - active_roadmap source. The normalized roadmap config, or None when the plugin is inactive. The plugin loads for every build (it is a default plugin) but only a roadmap: section in docs.yaml \u2014 surfaced as config.extra[\"roadmap\"] by configure() \u2014 activates its output. NameTypeDefaultDescription config. Any. - Returns: dict[str, Any] | None - get_roadmap source. NameTypeDefaultDescription config. Any. - Returns: dict[str, Any] - get_phases source. NameTypeDefaultDescription config. Any. - Returns: list[dict[str, Any]] - docs_page_mdx source. Returns: str - register_cli source. NameTypeDefaultDescription app. Any. - Returns: None -"},{"url":"docs/kanban/cards/kanban_single_board_with_filters/index.html","title":"Kanban is one board with flexible filters","content":"Ask AI. Kanban is one board with flexible filters. Released \u00b7 0.1. The public board renders every card in one instance, but the only filter is a mount-time URL parameter with no visible control: a visitor who lands on a milestone deep link cannot see the filter, change it, or clear it. Cards also carry their full body on the board, which buries the column story.\u2026 Reference teardown: SVAR and ReUI. Researched 2026-08-04 for the board redesign. Both libraries were read at the source, not the marketing page. The question was narrow: what should\u2026 The card belongs to the board."},{"url":"docs/components/timeline/index.html","title":"Timeline","content":"Ask AI. Timeline. Vertical timeline for changelogs, version history, or sequential events. Each item shows a date, title, optional badge, and description. Badges like \"new\" and \"breaking\" have special styling. API. Timeline. PropTypeDefaultDescription children. ReactNode. \u2014 One or more <TimelineItem> elements. TimelineItem. PropTypeDefaultDescription date. string. \u2014 Date or version label. title. string. \u2014 Event or release title. badge. string. \u2014 Optional badge text. \"new\" and \"breaking\" have special styling; any other string displays as a neutral badge. children. ReactNode. \u2014 Description or details. Example. v2.0.0breaking. Major release. Dropped Python 3.8 support. Migrated to Pydantic v2. New plugin API. v1.5.0new. Mermaid diagrams. Added support for Mermaid diagram rendering in documentation pages. v1.4.2. Bug fixes. Fixed sidebar ordering issue with nested modules. Improved build performance. v1.4.0new. Search customization. Added configurable search placeholder and the ability to disable search."},{"url":"docs/agents/index.html","title":"Folio for Agents","content":"Ask AI. Folio for Agents. Folio for Agents is a meta-harness: a harness over the coding harnesses already working in a repository. It gives Codex, Claude Code, and other tools one shared work surface without replacing or orchestrating them. What ships today. A cardfile board stored as board.yaml plus one Markdown file per card. A repository-owned SKILL.md that teaches any agent how to operate it. Validated commands for adding, moving, updating, commenting on, and attaching artifacts to cards. Narrow git commits that keep planning history separate from code history. The installed Agents product contributes folio board to the shared CLI. Board commands require no server or Node.js runtime and never invoke a Docs build. Optional Docs integration. List folio_agents.integrations.kanban in docs.yaml when a Folio Docs site should publish the board. This build integration remains explicit even though the installed Agents product automatically contributes its CLI command. The integration can read a dedicated board branch through a managed worktree, including multiple product-owned boards as separate canvases. Continue with the board guide or its agent protocol."},{"url":"docs/components/code_blocks/index.html","title":"Code Blocks","content":"Ask AI. Code Blocks. Fenced code blocks with syntax highlighting, line highlighting, line numbers, filenames, and word highlighting \u2014 powered by Shiki and rehype-pretty-code . Code blocks use standard Markdown fenced code block syntax with meta strings for additional features. Copy controls appear when readers hover over or focus a code block. API. All features are controlled via the meta string after the language identifier: FeatureSyntaxDescription Line highlighting. {2,4-6} Highlight specific lines. Commas separate individual lines, dashes define ranges. Line numbers. showLineNumbers. Display line numbers in the gutter. Filename. filename=\"app.py\" Display a filename header above the code block. Word highlighting. \"print\" Highlight all occurrences of a specific word or string. Disable copy. copy=false. Hide the hover and focus copy control for a specific block. Features can be combined in any order on the same code block. Example. Line highlighting. Highlight specific lines with {lines} after the language: Line numbers. Add showLineNumbers to display line numbers: Filename. Add filename=\"...\" to show a file header: Word highlighting. Highlight specific words with quoted strings: Combined features. All meta features work together:"},{"url":"docs/components/before-after/index.html","title":"BeforeAfter","content":"Ask AI. BeforeAfter. Show two related code snippets side by side. It works well for migration notes, config changes, and generated output comparisons. API. PropTypeDefaultDescription before. string. \u2014 Left code snippet. after. string. \u2014 Right code snippet. beforeLabel. string. \"Before\" Left label. afterLabel. string. \"After\" Right label. language. string. \u2014 Reserved for future syntax-label use. Example. RST. .. note:: Install Folio first. MDX. <Callout type=\"note\"> Install Folio first. </Callout>"},{"url":"docs/kanban/cards/artifacts_read_from_the_canvas/canvas_reading_compared/index.html","title":"Reading an artifact from the canvas \u2014 four prototypes compared","content":"Ask AI. Reading an artifact from the canvas \u2014 four prototypes compared. Built 2026-08-24 to answer one question: a card can attach what it produced, but reading it means leaving the board, either for a compiled page under /docs wearing documentation chrome or for a raw file in a new tab. Four layouts were built in full against the real 44-card board and verified adversarially in a headless browser. The sibling note where-card-pages-publish.md answers the other half of the owner\u2019s verdict; this document takes its conclusion as given and says so where a prototype argued against it. The demo path every variant had to survive is the card the-board-reads-as-a-tree: one long markdown document and five HTML prototypes built for a full 1440px window. That card decided more of this comparison than any principle did. Reading overlay. What it argues. Reading is a mode you enter deliberately. The artifact takes the viewport over a dimmed rim of board; two native dialogs stack in the top layer, the mail below and the reader above, and Esc unwinds exactly one level with focus returned to the element that opened what just closed. Strongest. It is the only in-place variant that gives the demo card\u2019s artifacts the room they were made for: the document reads at a 66ch measure and the prototypes embed near the width they were designed at. The implementation leans on the platform \u2014 showModal() supplies the backdrop, the inert canvas and per-level Esc, and the script\u2019s own work is bookkeeping. It is also the only in-place variant that applies the URL on hash change, not only at load; the board reader page\u2019s URL is alive too, but reading there has left the canvas. Where it fails. It is modal. You cannot read and scan the columns at once, and comparing two artifacts means closing one to open the other. Focus inside an embedded prototype takes the keys with it, an honest limit of opaque-origin file:// iframes. The URL keys artifacts by target basename, which collides the day a card attaches two files with the same name. Wheel input over the dimmed rim still scrolls the board behind, and card descriptions render as plain text, markdown punctuation and all. Verification. Passed. The pass found nothing the notes panel had not already admitted, which is worth stating as credit; the one issue logged is the shared data gap covered below. Reading rail. What it argues. Reading and the board share the screen. The artifact opens as a right rail, clamp(500px, 44vw, 680px), beside columns that stay fully live; the card dialog collapses into the rail\u2019s crumb so only one reading surface exists at a time. Strongest. It is the only variant where \u201creadable from the canvas\u201d is literally true while reading: the columns stay interactive, the card being read is marked on the board, the filter keeps working, and when the filter hides the very card whose artifact is open, closing the rail still lands focus somewhere sensible. The housekeeping is careful: the closed rail is inert and its iframe is parked on about:blank. Where it fails. Half a screen is a poor page, and the builder\u2019s own numbers say so: the comparison document reads in roughly a 45%-viewport measure, and prototypes built for 1440px embed at about 600px, so the rail under-sells exactly the artifacts the demo card compares. The compressed board keeps two or three columns in view and grows a horizontal scrollbar; two scroll regions share one wheel. Board, dialog and rail together have more state corners than one modal, and the corners are reachable: the dialog can sit beside the rail showing a different card, with Tab trapped in the dialog while the rail stays click-reachable. The [ and ] shortcuts die inside the embedded iframe, and Open full for the document points at the compiled /docs page, served site only. Verification. Passed on fresh loads. It found what the notes do not say: there is no hashchange listener, so the URL is honoured only at load, and a deep link restores rail and crumb but not the dialog, by design but undeclared. Dialog reader. What it argues. Everything about a card, including what it produced, reads in one surface. The mail dialog widens into letter and reading pane; the band stays on the left with the open attachment marked, so there is no second place to learn and no second layer to unwind. Strongest. The least new geometry of the four: one dialog, no extra layer, no change to the page behind it. Band position stays visible while reading. It is the only variant that renders card prose with a small markdown pass, and it handles the closed-door rule most exactly: Open full is absent for the markdown artifact rather than pointing anywhere dead. Where it fails. The reading pane never gets more than about two thirds of a 1440px screen because the compressed letter keeps 340px, so the five embedded prototypes render at a width nobody uses them at; Open full is the admission. The modal blocks the canvas it claims to preserve, and the letter reshapes the moment a reader opens, so the surface stays one but does not stay still. Focus inside the iframe eats Esc and the arrows; ArrowLeft and ArrowRight always walk the band while reading, stealing horizontal scrolling from wide code blocks; and Chrome\u2019s close-watcher grouping can let a rapid second Esc close the whole dialog. Verification. Passed on fresh loads, with the same undeclared load-only URL as the rail. One thing no notes panel carries: the URL names the artifact by band index (&a=1), so a pasted link silently changes meaning if the card ever reorders its artifacts; the overlay\u2019s basename and the page\u2019s slug both survive a reorder. Board reader page. What it argues. An artifact\u2019s URL is a board page: /kanban/<card>/<artifact>, board chrome, a reading column, the card\u2019s other artifacts in a rail. Opening is a real navigation; Back unwinds perfectly. The route model in the script\u2019s header comment is the most considered of the four: pushState per level, replaceState per band step, a depth counter in history.state so Esc never backs out of the app, and a slug grammar that tries the target\u2019s stem, then its filename, then its index on collision. What happened. The builder\u2019s session died mid-file: the script stopped at line 329, the IIFE never closed, and nothing ran. The back half was written afterwards: the dialog, the reader page and the navigation, built against the completed HTML and CSS and the route model in the script\u2019s own header, then verified like the others. Verification. Passed, and the route model holds up. One press of Esc is one history entry when the history is in-app, and a written parent state when it is not, so a deep link never backs out of the app. Band steps and rail switches replace in place, so Back never replays a reading session; a fresh load of the route restores the page; the closed door is a div marked \u201cpath only\u201d. It adopts the dialog reader\u2019s rule and hides Open full for the markdown artifact rather than pointing at a page file:// cannot serve. Where it fails. Reading leaves the canvas entirely, which its own notes admit, and the route it wants is the one option (b) of where-card-pages-publish.md already costed out and rejected: a second catch-all outside /docs, with search, sitemap, mirrors and link validation rebuilt around it. Complete and verified, it still argues against the sibling note\u2019s conclusion. The cuts. What the reader keeps seeing. Only the rail keeps a live board. The overlay keeps a dimmed inert rim, the dialog reader the same canvas behind a backdrop, the page nothing. What the residual board is worth is the real question: with the rail it buys real work and costs half the reading surface; with the overlay it buys only the sense of not having left, and costs nothing. The Esc stack. The overlay has two levels enforced by the platform: Esc arrives as cancel on the topmost dialog only, so one press is always one level. The dialog reader has two levels enforced by hand inside one dialog, and Chrome\u2019s Esc grouping can eat one. The rail has up to three presses from its deepest corner. The page\u2019s Esc is Back when the history is in-app and a written parent state when it is not, one press per level, verified. What the URL means. All four write the reading position; they disagree on what the words are. The overlay writes card id plus artifact basename, applied on load and on hash change. The rail writes the artifact target plus the filter, load only. The dialog reader writes card id plus band index, load only, and an index is the weakest name of the three. The page writes a route with real history, the only variant where Back means something and the only grammar that survives both a duplicate filename and a reorder. What is left for the compiled page. Every notes panel overreached here. The overlay says a deep link no longer needs the compiled page; the rail demotes it to the Open full target, \u201cindexed nowhere\u201d; the dialog reader says it can go entirely; the page variant retires it into a redirect. All four contradict where-card-pages-publish.md, and this document sides with the note. The compiled page keeps the jobs a canvas reader cannot do: the durable address, the search hit, the llms.txt line, the markdown mirror. It loses only the sidebar seat and the human reading. The rail\u2019s own Open full is the demonstration: a long document eventually wants a full window with a stable address, and that address already exists. Cost in the real component. The board is one client component, template/components/kanban-board.tsx, and the card dialog already exists in it. The overlay is a second dialog element inside that component; the dialog reader is a width state and a second pane inside the existing dialog; the rail is layout, the component\u2019s root becoming a two-pane stage that touches the board\u2019s own scrolling and width logic. None of the three needs a route. The page needs a real route under /kanban, which is the option (b) bill; it is a build-system decision disguised as a layout. A shared gap, not a variant defect. The brief requires that tables read well, and the document every variant demos contains none: verified identically in the source markdown, the compiled page and reader-data.js. Every variant\u2019s table CSS was proven only by injecting a probe table. Whichever variant ships should be re-checked against a document that actually has tables. Recommendation. Reading overlay, as the primary, paired with the compiled page exactly as the sibling note leaves it: the overlay is where a human reads from the canvas, and the delisted /docs/kanban/<id>/<stem>/ page stays the durable full-window address that deep links, search and agents land on. That pairing is a combination, but not of two prototypes; the second half already exists. The reason is the demo card. This board attaches long documents and full-width prototypes, and both want the whole viewport. The rail preserved the canvas and under-sold the artifacts; the dialog reader preserved the surface and cramped the reading; the page preserved the URL and lost the canvas. The overlay preserved the thing being read, and reading was the point. It is also the cheapest implementation in the real component and the only in-place variant whose URL is alive rather than load-only; the page\u2019s URL is alive too, but it is not on the canvas. Reading two artifacts side by side is the one need it cannot meet, and the answer to that is the compiled page in a second window, not a rail. Taken from the losing variants: the board reader page\u2019s slug grammar replaces the overlay\u2019s bare basename in the URL; the dialog reader\u2019s rule of hiding Open full when there is nothing full to open, and its small markdown pass for card prose; the rail\u2019s focus rule for when the card that owns the reading has been filtered off the board. Left for the builders: the load-only URLs of the rail and the dialog reader are worth a line in whichever card carries the product work, because the real board already treats its URL as live."},{"url":"docs/api-reference/folio_agents/loader/index.html","title":"folio_agents.loader source","content":"Ask AI. folio_agents.loader source. Cardfile board loader for board.source: <dir>/. One card, one Markdown file. board.yaml holds the column set (and only the column set); every card lives in cards/<id>.md where the filename stem is the card\u2019s immutable id, frontmatter carries machine state (status, tags, relations, artifacts) and the Markdown body carries human/agent prose (description, acceptance criteria, trail). Column membership is the status: field, so moving a card is a one-line diff and two concurrent sessions editing different cards can never produce a merge conflict. Validation contract (mirrors the plugin\u2019s fail-fast configure dispatch): board topology errors \u2014 unparseable frontmatter, missing title/status, unknown status, dangling parent/blocked_by, malformed artifacts, escaping doc/file artifact targets \u2014 raise ValueError and stop the build loudly. Prose-grammar problems (a trail bullet that doesn\u2019t parse, an unknown priority, a doc/file target that resolves to no file, a card directory whose card is gone) degrade with a warning: a typo in a note must never break a build, a typo in board topology must never silently ship a wrong board. Functions. is_board_dir source. True when board.source points at a cardfile board directory. NameTypeDefaultDescription path. Path. - Returns: bool - load_board_dir source. Load a cardfile board into {\"title\": ..., \"columns\": [...]}. Columns come back in board.yaml order with their cards already grouped (by status:) and sorted; card dicts are raw-but-validated and carry the extended fields that kanban._normalize_card normalizes into the emitted TS contract. NameTypeDefaultDescription board_dir. Path. - project_dir. Path. - Returns: dict[str, Any] - _load_board_meta source. NameTypeDefaultDescription board_file. Path. - Returns: dict[str, Any] - _load_cards source. NameTypeDefaultDescription cards_dir. Path. - column_ids. list[str] - Returns: list[dict[str, Any]] - _load_card source. NameTypeDefaultDescription path. Path. - column_ids. list[str] - Returns: dict[str, Any] - _parse_body source. NameTypeDefaultDescription body. str. - path. Path. - Returns: tuple[str, list[dict[str, Any]], list[dict[str, Any]], list[dict[str, Any]]] - _validate_relations source. NameTypeDefaultDescription cards. list[dict[str, Any]] - Returns: None - _resolve_artifacts source. Derive each card\u2019s artifacts from its directory, then merge the block. The directory is the record: one artifact per regular file at its top level, name-sorted \u2014 doc for Markdown and MDX, file for the rest. Dotfiles, _-prefixed names, subdirectories, and symlinks stay behind, the same lines publishing already draws. The frontmatter block survives for what is not a file \u2014 pr:, url:, api: \u2014 and for labelling a sibling: a doc:/file: entry naming one (bare name, ./ form, or the full project-relative path) lands its label on the derived entry instead of appearing twice. Every entry carries display, the target as the author wrote it (the bare name for a derived sibling nothing labels), and target, always a project-relative path. A doc:/file: target resolves against the card\u2019s directory first, then the project root \u2014 the order a relative markdown link already implies. One that resolves to no file warns and stays: a stale path in one card\u2019s frontmatter is not board topology. Only an absolute path or one escaping the project still raises. NameTypeDefaultDescription cards. list[dict[str, Any]] - cards_dir. Path. - project_dir. Path. - Returns: None - _derived_sibling_artifacts source. One artifact per visible regular file at the directory\u2019s top level. NameTypeDefaultDescription sibling_dir. Path. - cards_prefix. str. - card_id. str. - Returns: list[dict[str, Any]] - _sibling_name source. The top-level sibling a written target names, or None. Three spellings reach a sibling: the bare name, the ./ form a markdown link would use, and the legacy full project-relative path existing boards carry. NameTypeDefaultDescription path_part. str. - cards_prefix. str | None. - card_id. str. - Returns: str | None - _resolve_file_target source. A doc:/file: target as a project-relative path, or a warning. The card\u2019s directory is tried first, then the project root. A target the card resolves is recorded at its project-relative address, so everything downstream keeps one path grammar; one the project resolves is already written in it and stays as written. NameTypeDefaultDescription path_part. str. - kind. str. - written. str. - fragment. str. - card_id. str. - card_dir. Path | None. - project_dir. Path. - project_root. Path. - Returns: str - _warn_orphan_directories source. Name every directory whose card is gone \u2014 shape B\u2019s one drift risk. The filename stem names the directory, so a card renamed or deleted leaves its directory behind and nothing publishes an unclaimed one. Dot and _ prefixes keep their existing meanings \u2014 editor scratch and \u201cnot the board\u2019s business\u201d \u2014 and stay quiet. NameTypeDefaultDescription cards_dir. Path. - card_ids. set[str] - Returns: None - parse_artifact source. One artifact entry -> (kind, target, label). The committed form is a one-line single-key map \u2014 - doc: research/x.md \u2014 with an optional label: sibling key. NameTypeDefaultDescription raw. Any. - card_id. str. - Returns: tuple[str, str, str] - _sort_key source. Deterministic intra-column order: rank, then priority, created, id. order: is the rare escape hatch \u2014 ranked cards sort first among themselves; everything else falls back to computed order so reordering never has to touch another card\u2019s file. NameTypeDefaultDescription card. dict[str, Any] - Returns: tuple -"},{"url":"docs/api-reference/folio_docs/docs/mdx_writer/index.html","title":"folio_docs.docs.mdx_writer source","content":"Ask AI. folio_docs.docs.mdx_writer source. Functions. _escape_mdx_text source. NameTypeDefaultDescription text. str. - Returns: str - _escape_mdx_outside_inline_code source. NameTypeDefaultDescription text. str. - Returns: str - _code_fence_flags source. Mark which lines belong to a fenced code block, fence lines included. Only a run of the opener\u2019s own character, at least as long and carrying no info string, closes a block. Toggling on any ``` prefix instead would let a 3-tick fence nested inside a 4-tick one flip the tracker back to \u201coutside code\u201d and escape real code content. NameTypeDefaultDescription lines. list[str] - Returns: list[bool] - _escape_mdx source. Escape MDX syntax in docstring prose, leaving code alone. MDX parses { and < as expression and JSX delimiters, so prose has to escape them. Code is different: CommonMark treats the content of a fenced block or a backtick span as literal text and decodes neither backslash escapes nor HTML entities there, so escaping inside code renders the escape itself - /\\{repo\\} instead of /{repo}. NameTypeDefaultDescription text. str. - Returns: str - _escape_jsx_attr source. NameTypeDefaultDescription text. str. - Returns: str - _frontmatter source. Generate a YAML frontmatter block. NameTypeDefaultDescription data. dict[str, str] - Returns: str - _render_param_table source. Render a <ParamTable> JSX component for a function\u2019s arguments. NameTypeDefaultDescription func. FunctionIR. - symbol_index. dict[str, str] | None. None. current_module. str. '' Returns: str - _effective_source_ref source. NameTypeDefaultDescription repo_url. str. - source_ref. str | None. None. Returns: str - _source_link source. NameTypeDefaultDescription repo_url. str. - source_file. str. - line_number. int. - source_root. str. '' source_ref. str | None. None. Returns: str - _render_function source. Render a full function section in MDX. NameTypeDefaultDescription func. FunctionIR. - heading_level. int. 3. repo_url. str. '' source_root. str. '' source_ref. str | None. None. symbol_index. dict[str, str] | None. None. current_module. str. '' Returns: str - _render_class source. Render a full class section in MDX. NameTypeDefaultDescription heading_level. int. 3. repo_url. str. '' source_root. str. '' source_ref. str | None. None. symbol_index. dict[str, str] | None. None. current_module. str. '' Returns: str - api_reference_index_to_mdx source. Render the generated source code overview page. NameTypeDefaultDescription modules. list[ModuleIR] - Returns: str - module_to_mdx source. Convert a ModuleIR into an MDX string. NameTypeDefaultDescription module. ModuleIR. - repo_url. str. '' source_root. str. '' source_ref. str | None. None. symbol_index. dict[str, str] | None. None. Returns: str - _escape_curly_outside_math source. Escape bare curly braces, preserving inline math and inline code. MDX leaves both alone, and CommonMark decodes no backslash escape inside a backtick span, so escaping there renders the backslashes to the reader. NameTypeDefaultDescription line. str. - Returns: str - _strip_relative_images source. NameTypeDefaultDescription content. str. - Returns: str - _convert_mermaid_blocks source. Convert fenced ```mermaid code blocks into <Mermaid chart=\u201d\u2026\u201d /> JSX. Only converts top-level mermaid blocks. Blocks nested inside an outer fenced code block (4+ backticks, e.g. ````md) are left untouched so that documentation examples are preserved as-is. NameTypeDefaultDescription content. str. - Returns: str - _sanitize_for_mdx source. NameTypeDefaultDescription content. str. - Returns: str - markdown_to_mdx source. Wrap markdown content with frontmatter to produce MDX. NameTypeDefaultDescription result. MarkdownResult. - Returns: str -"},{"url":"docs/components/command-grid/index.html","title":"CommandGrid","content":"Ask AI. CommandGrid. A compact command overview for CLI-heavy pages. Use CommandGrid with one or more CommandCard children. API. CommandGrid. PropTypeDefaultDescription children. ReactNode. \u2014 CommandCard elements. CommandCard. PropTypeDefaultDescription command. string. \u2014 CLI command shown in monospace. title. string. \u2014 Human-readable command name. description. string. \u2014 Short task description. href. string. \u2014 Optional link target. flags. string[] [] Common flags shown as badges. Example. Initialize. folio init. Create docs.yaml from project metadata. Build. folio build. Generate MDX, search, and static output. --clean--verbose."},{"url":"docs/components/type-badge/index.html","title":"TypeBadge","content":"Ask AI. TypeBadge. A small inline badge for displaying Python type annotations. Renders as a styled badge with monospace font. When an href is provided, the badge becomes a link to the type\u2019s documentation page. This component is used throughout the auto-generated API reference. API. TypeBadge. PropTypeDefaultDescription type. string. \u2014 The type annotation text (e.g. \"str\", \"list[int]\", \"Optional[Config]\"). href. string. \u2014 Optional URL to link the badge to a type\u2019s documentation page. Example. Returns a list[Node] containing all connected peers. The timeout parameter accepts float or None. The configuration uses Optional[Config] for optional settings."},{"url":"docs/components/code-group/index.html","title":"CodeGroup","content":"Ask AI. CodeGroup. Tabbed code blocks for showing the same concept in multiple languages or multiple variants of a command. Tabs are automatically labeled from the language of each code block, or you can provide explicit labels. API. CodeGroup. PropTypeDefaultDescription labels. string[] \u2014 Optional explicit tab labels. If omitted, labels are auto-detected from each code block\u2019s language (e.g. python, bash). children. ReactNode. \u2014 Two or more fenced code blocks to display as tabs. Label auto-detection tries, in order: a title prop, a data-language attribute, a language-* CSS class, and falls back to Tab N. Example. Auto-detected labels from language: Tab 1 Tab 2. With explicit labels: pip uv conda."},{"url":"docs/api-reference/folio_agents/cli_commands/index.html","title":"folio_agents.cli_commands source","content":"Ask AI. folio_agents.cli_commands source. The folio board command group. Bare folio board keeps the read-only table view. The write subcommands (add / move / update / trail / attach) operate on cardfile boards only \u2014 one logical operation, one targeted file edit, optionally one conventional commit (--commit). check runs the same fail-fast validation as the build, exposed as a pre-commit/CI gate. Functions. register source. NameTypeDefaultDescription app. Any. - Returns: None -"},{"url":"docs/api-reference/folio_docs/agent_output/index.html","title":"folio_docs.agent_output source","content":"Ask AI. folio_docs.agent_output source. Agent-readable documentation emitted by Folio Docs."},{"url":"docs/api-reference/folio_docs/agent_output/markdown/index.html","title":"folio_docs.agent_output.markdown source","content":"Ask AI. folio_docs.agent_output.markdown source. Markdown mirrors for Folio for Agents. The human site may use MDX components. Agent mirrors deliberately keep the authored prose and code while reducing that component shell to plain Markdown. Functions. mdx_to_markdown source. Convert generated MDX into the lossy, agent-readable mirror format. NameTypeDefaultDescription content. str. - Returns: str - _strip_mdx_component_tags source. Remove PascalCase JSX tags while retaining useful child prose. NameTypeDefaultDescription content. str. - Returns: str - _mdx_tag_end source. NameTypeDefaultDescription content. str. - cursor. int. - Returns: int | None - _mdx_string_prop source. NameTypeDefaultDescription tag. str. - name. str. - Returns: str - _mdx_tag_markdown source. NameTypeDefaultDescription tag. str. - name. str. - closing. bool. - Returns: str -"},{"url":"docs/components/file_tree/index.html","title":"FileTree","content":"Ask AI. FileTree. A visual file and folder tree for showing project structures, directory layouts, and file hierarchies. Parses an indented text string into a tree with folder/file icons, connectors, and proper nesting. API. FileTree. PropTypeDefaultDescription tree. string. \u2014 An indented text string representing the file tree. Lines ending with / are rendered as folders; all others are rendered as files. Nesting is determined by indentation (spaces). Formatting rules: Lines ending with / are rendered as folders (accent-colored icon). All other lines are rendered as files (muted icon). Nesting is determined by indentation level. Multiple root entries are supported at the top level. Example. my-project. pyproject.toml. README.md. src. \u2514\u2500\u2500 my_lib. \u251c\u2500\u2500 __init__.py. \u2514\u2500\u2500 core.py. tests. \u2514\u2500\u2500 test_core.py. docs. \u251c\u2500\u2500 index.md. \u2514\u2500\u2500 guide. \u2514\u2500\u2500 installation.md."},{"url":"docs/components/feature_cards/index.html","title":"FeatureCard and CardGrid","content":"Ask AI. FeatureCard and CardGrid. Cards for feature overviews, landing pages, and link grids. FeatureCard renders a single card with a named icon, title, description, and optional link. CardGrid arranges multiple cards in a responsive grid layout. API. FeatureCard. PropTypeDefaultDescription title. string. \u2014 The card heading. description. string. \u2014 Short description text displayed below the title. icon. string. \u2014 Optional named icon displayed above the title. Common tokens include install, run, quickstart, workflow, network, topology, analytics, settings, monitor, api, code, server, warning, and community. Unknown values fall back to text rendering for backwards compatibility. href. string. \u2014 Optional URL. When set, the card becomes a clickable link with a hover effect. CardGrid. PropTypeDefaultDescription columns. 2 | 3 | 4. 3. Number of columns on large screens. The grid is always 1 column on mobile and 2 on tablet. children. ReactNode. \u2014 One or more <FeatureCard> elements (or any content). Example. Three-column grid with named icons: Auto API Reference. Extracts documentation from your Python source code automatically. Modern UI. Dark mode, search, and responsive layout out of the box. LLM Output. Generate llms.txt and llms-full.txt for AI assistants. Two-column grid without icons: Getting Started. Install Folio and build your first documentation site. Configuration. Customize your site with docs.yaml options."},{"url":"docs/kanban/cards/the-board-reads-as-a-tree/index.html","title":"The board reads as a tree","content":"Ask AI. The board reads as a tree. Backlog \u00b7 0.2 \u00b7 feature \u00b7 XL. parent has been a real field since the cardfile format shipped: one card id, validated against the board, settable from the CLI, and already a filter field. Nothing renders it. The docs admit the gap in as many words \u2014 \u201cparent is a validated pointer, not a workflow\u201d \u2014 and when this card was\u2026 Prototype brief \u2014 nested/decomposition views for the Folio kanban board. These are throwaway prototypes. Nothing here ships. They exist so the owner can look at several answers side by side and pick one. Do not modify\u2026 Five layouts compared. Built 2026-08-20 to answer one question: the cards carry a parent field that nothing renders, so what should render it. Five layouts were built in\u2026 board-data.js. board-data.js. board-inline-expansion.css. board-inline-expansion.css. Inline expansion (rejected). board/cards/the-board-reads-as-a-tree/board-inline-expansion.html. board-inline-expansion.js. board-inline-expansion.js. document-outline.css. document-outline.css. Document outline (rejected). board/cards/the-board-reads-as-a-tree/document-outline.html. document-outline.js. document-outline.js. epic-swimlanes.css. epic-swimlanes.css. Epic swimlanes (rejected). board/cards/the-board-reads-as-a-tree/epic-swimlanes.html. epic-swimlanes.js. epic-swimlanes.js. tree-rail-detail.css. tree-rail-detail.css. Tree rail and detail (rejected). board/cards/the-board-reads-as-a-tree/tree-rail-detail.html. tree-rail-detail.js. tree-rail-detail.js. tree-table.css. tree-table.css. Tree table (chosen). board/cards/the-board-reads-as-a-tree/tree-table.html. tree-table.js. tree-table.js. The card belongs to the board."},{"url":"docs/api-reference/folio_docs/slugs/index.html","title":"folio_docs.slugs source","content":"Ask AI. folio_docs.slugs source. Shared URL and identifier slug normalization. Functions. slugify source. Convert human-readable text to a stable URL-friendly slug. NameTypeDefaultDescription text. str. - Returns: str -"},{"url":"docs/components/build-artifact/index.html","title":"BuildArtifact","content":"Ask AI. BuildArtifact. Summarize the files, directories, and generated routes produced by a Folio command. API. PropTypeDefaultDescription title. string. \"Build artifacts\" Panel title. description. string. \u2014 Optional summary. items. BuildArtifactItem[] \u2014 Artifact rows. BuildArtifactItem supports path, kind, and description. Example. Generated output. The build creates an intermediate app and final static files. .build/workspace Intermediate Nextra project with generated MDX. _site/static Deployable static site. _site/llms.txtllm Summary file for AI coding assistants."},{"url":"docs/components/copy-page-button/index.html","title":"Page Actions","content":"Ask AI. Page Actions. Folio renders a compact Ask AI page-actions button above every documentation page. It is meant for source-heavy docs: copy the current page, or send the current page context to ChatGPT without adding extra setup. PageActionsButton. A single outline menu button that includes Copy page, View Markdown, a ChatGPT-only assistant action, and MCP JSON actions. The ChatGPT row uses the bundled brand icon and opens ChatGPT with a short Read from ... prompt using the current page\u2019s Markdown URL, including configured deploy base paths. MCP copies a JSON page-context payload for local tooling. PageActionsButton. This component takes no props. CopyPageButton. A standalone copy-only button. It remains available for custom MDX pages, but the default docs layout uses PageActionsButton. CopyPageButton. This component takes no props. Layout. Layout Component. PageActionsButton is rendered by the theme layout above every documentation page."},{"url":"docs/plugins/catalog/index.html","title":"Plugin Catalog","content":"Ask AI. Plugin Catalog. Every plugin in the Folio package and on this site, in one place. Plugins arrive in three ways, and this page lists all of them: default plugins ship inside Folio and wake up when their config key appears in docs.yaml, bundled opt-in plugins ship in the same package but load only when listed under plugins:, and project plugins are plain Python files in your repository, loaded by path. All three go through the same hooks. Default plugins. Loaded on every build, completely inert without their key. Add the key and the plugin builds its pages. Remove it and the plugin goes inert again. A clean build clears any route they had already written. Roadmap. Product phases from docs.yaml rendered as a release timeline, with an optional standalone /roadmap page and a CLI table. Activated by the roadmap: key. Landing Page. A public homepage in front of your docs: hero variants, CTAs, install commands, and a section catalog. Activated by the landing: key. Bundled, opt-in. In the package, off by default. Add the module name under plugins: and it shares the exact lifecycle of the default plugins. OpenAPI. Add folio_docs.docs.integrations.openapi to plugins:, point openapi.sources at a spec file, and get a typed API reference page wired into the sidebar and search. Project plugins on this site. This site loads two plugins by file path. Both are small examples of the emit_assets hook: they publish plain files without adding a docs page. Their sources live in this repository under docs/plugins/. Agent Guide. Publishes agent-guide.md at the site root: a briefing you point a coding agent at, so it explains, installs, and troubleshoots Folio from this repository's facts. Source: docs/plugins/agent_guide.py. Install Script. Publishes install.sh at the site root, so the installer is one curl away instead of a raw.githubusercontent.com one-liner. Source: docs/plugins/install_script.py. Loading. To put your own plugin in this list, start at Writing Plugins. The bundled plugins double as reference implementations, and the two project plugins above are the short form: one file, one hook, one published artifact. The board publisher is owned and versioned by Folio for Agents. Install folio-agents[docs] and list folio_agents.integrations.kanban explicitly when a Docs site should render that product\u2019s board."},{"url":"docs/quickstart/index.html","title":"Quick Start","content":"Ask AI. Quick Start. Go from an existing repository to searchable documentation in a few minutes. Folio Docs reads your source and guides without importing the package. It needs Python 3.10+, Node.js 20.19+, and pnpm 10. See Installation for alternatives and troubleshooting. Install Folio. Install the CLI once as a uv tool. Initialize your repository. Let Folio detect the package and create a small docs.yaml. Preview the result. Open the generated site and inspect its HTML and agent-readable outputs. Build the static site. Export plain files that can be deployed anywhere. 1. Install Folio. 2. Initialize your repository. Run the wizard from the project you want to document: folio init detects project metadata and Python packages, then writes docs.yaml. A minimal configuration looks like this: docs.yaml. Folio reads these paths; it never imports or executes the package. project.name Name shown in the generated site. string. source.python.paths Python packages scanned for public symbols. list[string] source.docs Markdown directories published as guides. list[string] Only keep source paths that exist. The configuration reference covers exclusions, docstring formats, plugins, and theming. 3. Preview the result. Open http://localhost:4321. The development server watches Python and Markdown sources and rebuilds changed pages. Its public root also exposes: /llms.txt, the compact project index; /llms-full.txt, the expanded text export; /_folio/markdown/, the per-page Markdown mirrors. Use folio serve --verbose when a module or page is missing. 4. Build the static site. Generated output. The workspace is disposable; the static output is the artifact you deploy. .build/workspace Generated frontend workspace and development cache. _site/static Deployable HTML, assets, search, and agent outputs. _site/llms.txtllm Compact project context for agents. _site/_folio/markdown/llm One discoverable Markdown mirror per page. Deploy _site/ with the deployment guide. Next steps. Writing docstrings. Components. Plugins."},{"url":"docs/api-reference/folio_docs/builtins/index.html","title":"folio_docs.builtins source","content":"Ask AI. folio_docs.builtins source. Single source of truth for Folio\u2019s builtin MDX components. These components ship inside the bundled Next/Nextra template (template/components/) and are registered into the. Functions. _component source. NameTypeDefaultDescription name. str. - module. str. - props. dict[str, str] | None. None. required. bool. False. category. str. 'component-catalog' contract. bool. False. source_label. str. '' Returns: ComponentDefinition - register_builtin_components source. Register every builtin component into registry in manifest order. NameTypeDefaultDescription registry. ExtensionRegistry. - Returns: None -"}];
let options={};
const script=document.currentScript;
const rootHref=new URL('./',script&&script.src||document.baseURI).href;
function normalize(value){return String(value||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();}
function escapeHtml(value){return String(value).replace(/[&<>"']/g,function(ch){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch];});}
function escapeRegex(value){return String(value).replace(/[.*+?^${}()|[\]\\]/g,'\\$&');}
function splitPath(path){return path.split('/').filter(Boolean);}
function currentDir(){const current=new URL(location.href);const root=new URL(rootHref);let rel=decodeURIComponent(current.pathname).slice(decodeURIComponent(root.pathname).length);if(!rel||rel.endsWith('/'))return rel;return rel.split('/').slice(0,-1).join('/')+'/';}
function relativePath(target){if(/^(https?:)?\/\//.test(target))return target;const hashIndex=target.indexOf('#');const hash=hashIndex>=0?target.slice(hashIndex):'';const noHash=hashIndex>=0?target.slice(0,hashIndex):target;const queryIndex=noHash.indexOf('?');const query=queryIndex>=0?noHash.slice(queryIndex):'';const path=queryIndex>=0?noHash.slice(0,queryIndex):noHash;const from=splitPath(currentDir());const to=splitPath(path);while(from.length&&to.length&&from[0]===to[0]){from.shift();to.shift();}let rel=[...from.map(function(){return '..';}),...to].join('/')||'index.html';if(location.protocol==='file:'&&/\.html$/.test(rel))rel+='?folio-search=1';return rel+query+hash;}
function excerpt(content,terms){const normalized=normalize(content);let index=-1;for(const term of terms){index=normalized.indexOf(term);if(index!==-1)break;}const start=Math.max(0,index-80);const raw=String(content||'').slice(start,start+220).trim();let html=escapeHtml((start>0?'... ':'')+raw+(start+220<String(content||'').length?' ...':''));for(const term of terms){if(!term)continue;html=html.replace(new RegExp('('+escapeRegex(escapeHtml(term))+')','ig'),'<mark>$1</mark>');}return html;}
function search(term){const terms=normalize(term).split(/\s+/).filter(Boolean);if(!terms.length)return [];return documents.map(function(doc){const title=normalize(doc.title);const content=normalize(doc.content);const haystack=title+' '+content;if(!terms.every(function(token){return haystack.includes(token);})){return null;}const titleHits=terms.filter(function(token){return title.includes(token);}).length;const score=titleHits*10+terms.reduce(function(total,token){return total+(content.includes(token)?1:0);},0);const href=relativePath(doc.url);return {id:doc.url,score:score,words:[],data:async function(){return {url:href,meta:{title:doc.title},sub_results:[{title:doc.title,url:href,excerpt:excerpt(doc.content,terms)}]};}};}).filter(Boolean).sort(function(a,b){return b.score-a.score;});}
const api={options:async function(nextOptions){options=nextOptions||options;},preload:async function(){return null;},search:async function(term){const results=search(term);return {results:results,unfilteredResultCount:results.length,filters:{},totalFilters:{},timings:{preload:0,search:0,total:0}};},debouncedSearch:async function(term){return api.search(term,options);}};
window.__folioStaticSearch=api;
if(location.protocol==='file:'&&!window.pagefind){window.pagefind=api;}
})();
