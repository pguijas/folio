def add(left: int, right: int) -> int:
    """Add two integers.

    Args:
        left: First number.
        right: Second number.

    Returns:
        The sum of both numbers.
    """
    return left + right
